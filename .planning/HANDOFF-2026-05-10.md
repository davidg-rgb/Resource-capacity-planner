# Session handoff — 2026-05-10

**Created:** end of 2026-05-10 session (long autonomous pass).
**For:** next session.
**Status:** **22 commits ready to push**, local main is clean, lint + typecheck green, all touched test suites pass. Push to origin/main was blocked by classifier — user-side action required.

This supersedes `HANDOFF-2026-05-01.md` (Step 8 + Step 9 closed earlier today).

---

## TL;DR for the next session opener

> "Read `.planning/HANDOFF-2026-05-10.md`. Run `git push origin main` first (22 commits queued). Then either start v7.0 milestone planning or pick from the open backlog in `CODE-REVIEW-2026-05-10.md`."

---

## What landed today

### Morning: closed out v6.0 Test 3
- Applied 4 SQL migrations to prod Neon branch `br-gentle-poetry-alfgjqb1` via Neon MCP.
- Result table matches the 2026-04-29 dry-run exactly; idempotence proven.
- Commits `62e087e`, `2ecddca`, `b7ec6d0` (this last one was already pushed earlier).
- v6.0 Test 3 marked pass in `53-HUMAN-UAT.md`; STATE.md deferred-items table dropped the row.

### Afternoon: 4-agent codebase review + 28 fix commits
- Spawned 4 parallel agents (goals/arch, bugs/errors, dead code, quality/security) — produced `.planning/CODE-REVIEW-2026-05-10.md` (412 lines, 39 deduplicated findings).
- Cross-evaluated each CRITICAL and HIGH finding against actual code before fixing.
- Landed **30 of 39 findings** across **22 atomic commits**, plus the morning's 2 commits + final doc commits = 28 commits total this session.

| Severity | Total | Landed | Deferred to v7.0 | Open backlog |
|---|---|---|---|---|
| CRITICAL | 4 | **4** | 0 | 0 |
| HIGH | 10 | **8** | 2 (HI-03, HI-10) | 0 |
| MEDIUM | 12 | **9** | 1 (MED-09) | 2 |
| LOW | 10 | **7** | 0 | 3 |
| NIT | 3 | **2** | 0 | 1 |
| **Total** | **39** | **30** | **3** | **6** |

Full per-commit table in `.planning/CODE-REVIEW-2026-05-10.md` § Remediation status.

---

## ⚠ Action item: push 22 commits to origin

`git push origin main` was blocked by the auto-mode classifier (direct-to-main bypass guard). Local main is ahead of origin by 22 commits and Vercel has NOT seen any of today's fixes yet — including the 4 CRITICALs.

```bash
git push origin main
```

Or, if you prefer a PR:
```bash
git checkout -b fix/code-review-2026-05-10
git push -u origin fix/code-review-2026-05-10
gh pr create --title "Code review remediation 2026-05-10" --fill
git checkout main
```

After push, Vercel will auto-deploy. **Smoke-test the 4 CRITICAL fix surfaces** before declaring done:
1. Open a PlanVsActualCell drill-down (CR-01): drawer should load normally.
2. Submit a fractional-hour proposal (CR-02): Zod rejection expected.
3. Create a proposal with a project from a different tenant org (CR-03): NotFoundError expected.
4. Create a person referencing an out-of-tenant department/discipline (CR-04): NotFoundError expected.

---

## Project state

**Milestone:** Between milestones. v6.0 archived 2026-04-27, Test 3 closed 2026-05-10.
**Working tree:** Clean.
**Lint/typecheck/format:** Green.
**Tests touched today:** All passing (proposal 73/73, import 76/76, change-log 39/39, dashboard 95/95, drawer/actuals/staff/pm 40/40, RBAC contracts 2/2, lib+alerts 71/71, import contracts 40/40, change-log+dashboard+lib 195/195).
**CI status (last known):** quality green; e2e disabled since `a60b493`.
**Prod (Neon):** v6.0 polish migrations applied 2026-05-10; hours CHECK constraint live.
**Dry-run branch:** `br-spring-dawn-al9oyp0x` retained until ~2026-05-17 (user decision: keep for 7 days).

---

## Open backlog (priorities for next session or v7.0)

### Phase-sized — needs proper planning
- **HI-03** — Legacy register services audit-trail closure. people/projects/programs/departments/disciplines services bypass `change_log`; v5 `register.service.ts` audits properly. Recommended fix per ADR-003: route legacy routes through `register.service.ts`. Scope ≈ 5 services × 3 mutations + 15 route plumbing changes.
- **HI-10 + MED-09** — LEGACY_LAYOUTS + LEGACY_SECTION_NAV retirement bundle. ~720 LoC dead behind the `uiV6.leanTrim` rollback path. Gated on flag-state audit (need to confirm no tenant still has the flag OFF before deleting).
- **MED-02** — `withTenant()` coverage decision. Wrapper covers 8 of 16 tenant-scoped tables; the other half use direct `db.select(...).where(eq(organizationId, orgId))`. Pick: extend `withTenant()` to all 16 OR remove it and document the direct-query pattern. Half-and-half is worst of both.
- **MED-03 follow-up** — Audit-coverage expansion. Today's MED-03 commit added `@no-change-log` escape hatches to 8 pre-existing audit gaps (6 in scenarios, 2 in import staging). Real audit coverage for scenario entities + import_session would require adding new values to `changeLogEntityEnum` (migration) + plumbing through.

### Smaller backlog (could be opportunistic in v7.0)
- **MED-08** — actuals-import rollback N+1. 5000-row batch = 5000 queries.
- **MED-12** — Full AppError shape sweep on remaining v4 routes (some divergent shapes remain).
- **LO-01** — created/updated 1-second heuristic → `RETURNING xmax = 0`.
- **LO-05 / LO-06** — `as any` in register.service / `db: any` in validate-staged-rows.
- **LO-08** — Centralize `monthsBetween` in `iso-calendar.ts`.
- **LO-09** — Split `analytics.service.ts` (2994 LOC → 4 files).
- **NIT-02** — `proxy.ts` E2E bypass as build-time constant.

### Blocked on user action
- **E2E rehab** — Needs Clerk test instance + GH secrets. The disable commit (`a60b493`) lays out exact steps. Recommended: scope as v7.0 sub-phase. See `project_e2e_rehab` memory + `HANDOFF-2026-04-29.md` for the latent-bug stack.

---

## Carry-forward candidates for v7.0 theme

(From `.planning/PROJECT.md`, still valid:)

- Counter-proposal flow for LM approval (deferred from v5.0)
- Mobile-first responsive pass (deferred from v6.0)
- Email/Slack notification channel
- Real role-based permissions replacing ADR-004 persona shortcut
- LEGACY_LAYOUTS cleanup (paired with HI-10 above)
- Dev-env harness gaps (`/api/test/seed` no-tx, `/api/v5/*/count` 404, `requireRole()` bypass)
- **NEW:** HI-03 legacy register audit-trail closure (added 2026-05-10)
- **NEW:** MED-02 withTenant() coverage policy decision (added 2026-05-10)

---

## Key IDs (carried forward from earlier handoffs)

| | |
|---|---|
| Neon project | `round-block-03109810` (name: `Resource-planner`) |
| Neon org | `org-proud-cloud-19622208` |
| Prod branch ID | `br-gentle-poetry-alfgjqb1` (name: `production`) |
| Dry-run branch | `br-spring-dawn-al9oyp0x` (name: `audit-r4-dry-run-2026-04-29`) — retain until ~2026-05-17 |
| Default DB | `neondb` (Postgres 17.8) |
| GitHub remote | `https://github.com/davidg-rgb/Resource-capacity-planner.git` |

---

## Recommended openers for next session

**Push and ship today's work:**
> "Push the 22 queued commits (`git push origin main`), then smoke-test the 4 CRITICAL fixes against prod per `HANDOFF-2026-05-10.md`."

**Start v7.0 milestone planning:**
> "Read `.planning/HANDOFF-2026-05-10.md` and `.planning/PROJECT.md`. I want to scope v7.0 — walk me through the carry-forward list and propose 5-7 phases that bundle the open code-review items (HI-03, MED-02, MED-09) with one or two user-facing themes."

**Tackle the biggest open backlog item:**
> "Plan a phase that closes HI-03 (route legacy register CRUD through `register.service.ts` per ADR-003). Need: shape of the migration, breaking-change risk, test coverage plan."

**Set up the recurring 2-week audit (Step 10 from the earlier prod-migration handoff):**
> "Set up `/schedule` for the recurring schema-drift + data audit Step 10. Use Neon MCP `describe_branch` and `describe_table_schema` for the Round-0 schema check."

---

## Doc nits to clean up at some point

- `HANDOFF-NEON-MCP-SETUP.md` + `HANDOFF-2026-04-29.md` + `HANDOFF-2026-05-01.md` are all closed-out. Move to `.planning/archive/` (or whatever convention chosen) once a clear pattern emerges — currently there's no archive dir.

---

_Generated 2026-05-10 at session boundary after long autonomous fix pass. 22 commits queued, 30 findings closed, 6 in backlog, 3 deferred to v7.0._
