---
phase: 38-excel-import-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/import/parsers/parser.types.ts
  - src/features/import/parsers/actuals-excel.parser.ts
  - src/features/import/parsers/__tests__/actuals-excel.parser.test.ts
  - src/features/import/matching/name-matcher.ts
  - src/features/import/matching/__tests__/name-matcher.test.ts
  - public/templates/template_row_per_entry.xlsx
  - scripts/generate-import-template.ts
autonomous: true
requirements: [IMP-02, IMP-06, IMP-07]
must_haves:
  truths:
    - "parseActualsWorkbook returns ParsedRow[] for row-per-entry layout"
    - "parseActualsWorkbook returns ParsedRow[] for pivoted layout with ISO date/week/month headers"
    - "Any US WEEKNUM() / Sunday-start pivoted header raises ValidationError code ERR_US_WEEK_HEADERS and aborts the parse"
    - "matchPersonName returns exact | fuzzy (>0.85) | ambiguous | none"
    - "template_row_per_entry.xlsx exists under public/templates and is parseable by the row-per-entry parser"
  artifacts:
    - path: "src/features/import/parsers/actuals-excel.parser.ts"
      provides: "parseActualsWorkbook, detectLayout, parseRowPerEntry, parsePivoted"
    - path: "src/features/import/parsers/parser.types.ts"
      provides: "ParsedRow, ParseWarning, ParseResult types"
    - path: "src/features/import/matching/name-matcher.ts"
      provides: "matchPersonName, matchProjectName (string-similarity based)"
    - path: "public/templates/template_row_per_entry.xlsx"
      provides: "Downloadable import template (static asset)"
  key_links:
    - from: "actuals-excel.parser.ts"
      to: "@/lib/time iso-calendar helpers"
      via: "ISO date/week header classification (no date-fns, no Date.getDay)"
      pattern: "from '@/lib/time'"
    - from: "actuals-excel.parser.ts"
      to: "SheetJS xlsx package"
      via: "XLSX.read + sheet_to_json"
      pattern: "from 'xlsx'"
---

<objective>
Build the pure-function Excel parsing + fuzzy name matching layer for the v5.0 actuals import pipeline, plus generate the static `template_row_per_entry.xlsx` asset. Zero DB writes — this plan is 100% in-memory parsing so it can be exhaustively unit tested with SheetJS workbooks constructed inline.

Purpose: IMP-02 (two-layout parser with US_WEEK hard-stop), IMP-06 (downloadable template), IMP-07 (fuzzy name match primitive). Provides the `parseActualsWorkbook` + `matchPersonName` building blocks that Plan 38-02 composes into the parse→preview→commit service.

Output: Parser module, name matcher module, generated static template, full TC-EX-001..012 + matcher unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v5.0-ARCHITECTURE.md
@.planning/REQUIREMENTS.md
@src/db/schema.ts
@src/lib/time/iso-calendar.ts
@src/features/actuals/actuals.service.ts

<interfaces>
Existing schema (Phase 36, src/db/schema.ts):
```ts
// import_batches — columns already present:
//   id, organizationId, importSessionId, fileName, committedBy, committedAt,
//   overrideManualEdits, rowsInserted, rowsUpdated, rowsSkippedManual,
//   (Plan 38-02 adds: rowsSkippedPriorBatch, reversalPayload jsonb, rolledBackAt, supersededAt)
// actual_entries — keyed by (organizationId, personId, projectId, date) unique
// import_sessions — has jsonb parsedData column already; we reuse for staging
```

Existing ISO time helpers (src/lib/time/iso-calendar.ts):
```ts
export function parseIsoDate(s: string): Date; // rejects non-YYYY-MM-DD
export function getIsoWeek(d: Date): { isoYear: number; isoWeek: number };
export function workDaysInIsoWeek(isoYear: number, isoWeek: number): string[];
export function workDaysInMonth(year: number, monthIndex: number): string[];
```

Existing `xlsx` (SheetJS) is already in package.json dependencies — DO NOT add a new import dependency.

Already in deps: `string-similarity` (ARCHITECTURE ADR list line 296).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ParsedRow types + name-matcher module (TDD)</name>
  <files>
    src/features/import/parsers/parser.types.ts,
    src/features/import/matching/name-matcher.ts,
    src/features/import/matching/__tests__/name-matcher.test.ts
  </files>
  <behavior>
    parser.types.ts exports:
      - `ParsedRow = { personName: string; projectName: string; date: string; hours: number; sourceRow: number }`
      - `ParseWarning = { code: string; message: string; sourceRow?: number; cell?: string }`
      - `ParseResult = { layout: 'row-per-entry' | 'pivoted'; rows: ParsedRow[]; warnings: ParseWarning[] }`
      - Error code constants: `ERR_US_WEEK_HEADERS`, `ERR_UNKNOWN_LAYOUT`, `ERR_BAD_HOURS`, `ERR_BAD_DATE`

    name-matcher.ts exports `matchPersonName(input, candidates)` and `matchProjectName(input, candidates)`:
      - Exact case-insensitive match → `{ kind: 'exact', id, name }`
      - Single candidate with string-similarity > 0.85 → `{ kind: 'fuzzy', id, name, confidence }`
      - Multiple candidates > 0.70 → `{ kind: 'ambiguous', candidates: Array<{id,name,confidence}> }` (top 3)
      - None above 0.70 → `{ kind: 'none' }`
      - Normalizes "Svensson, Erik" ↔ "Erik Svensson" (comma-swap fallback before similarity)

    Test cases (TC-IMP matcher slice):
      - T1: exact "Anna Andersson" → exact
      - T2: "anna andersson" (lowercase) → exact
      - T3: "Svensson, Erik" against ["Erik Svensson"] → exact after comma-swap
      - T4: "Eric Svensson" against ["Erik Svensson"] → fuzzy ~0.90
      - T5: "Jon" against ["Jon Smith", "Jon Doe"] → ambiguous (two candidates)
      - T6: "Totally Unknown" against ["Erik Svensson"] → none
      - T7: empty candidate list → none
      - T8: matchProjectName mirrors matchPersonName semantics
  </behavior>
  <action>
    Create `src/features/import/parsers/parser.types.ts` with the exact shapes above. Export constants as `as const` string literal unions (used by parser + service).

    Create `src/features/import/matching/name-matcher.ts`:
    - Import `compareTwoStrings` from `string-similarity`.
    - Helper `normalize(s)`: trim, collapse whitespace, lowercase.
    - Helper `commaSwap(s)`: if matches `/^(.+),\s*(.+)$/`, return `"$2 $1"`, else return input.
    - `matchPersonName(input: string, candidates: Array<{id:string; name:string}>): MatchResult`:
      1. Normalize input. For each candidate compute normalized name and normalized comma-swap name. If any exact → return `{kind:'exact', id, name}`.
      2. Compute similarity of normalized input vs each normalized candidate (max of direct + swap). Sort desc.
      3. If top > 0.85 AND second < 0.85 → `{kind:'fuzzy', ...}`. If top > 0.70 AND at least one other > 0.70 → `{kind:'ambiguous', candidates: top3}`. If top ≤ 0.70 → `{kind:'none'}`. Edge: top > 0.85 AND second ALSO > 0.85 → ambiguous (the fuzzy is not unique).
    - `matchProjectName` is a thin wrapper that calls the same internal with candidate `name` (no comma-swap needed for project names).

    Create the test file first and watch it fail, then implement, per TDD. Use vitest. Candidates are plain arrays — no DB.

    Per IMP-07: ambiguous/fuzzy/none results drive the preview UI's unmatched-name suggestion list in Plan 38-03.
  </action>
  <verify>
    <automated>pnpm test src/features/import/matching/__tests__/name-matcher.test.ts</automated>
  </verify>
  <done>Types file + matcher module + 8/8 tests pass. Typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parseActualsWorkbook — both layouts + US_WEEK hard-stop (TDD, TC-EX-001..012)</name>
  <files>
    src/features/import/parsers/actuals-excel.parser.ts,
    src/features/import/parsers/__tests__/actuals-excel.parser.test.ts
  </files>
  <behavior>
    `parseActualsWorkbook(buffer: ArrayBuffer): Promise<ParseResult>` must satisfy TC-EX-001..012:

    - TC-EX-001 `detectLayout` row-per-entry: headers `[person_name, project_name, date, hours]` (case-insensitive, accepts `person|namn|name` aliases) → returns `'row-per-entry'`.
    - TC-EX-002 `detectLayout` pivoted: leftmost `[person_name, project_name]` + ≥2 date-parseable column headers → returns `'pivoted'`.
    - TC-EX-003 `detectLayout` unknown: throws `ValidationError` code `ERR_UNKNOWN_LAYOUT`.
    - TC-EX-004 row-per-entry happy path: 3 rows → 3 `ParsedRow` with trimmed strings, `parseIsoDate`-validated dates, numeric hours.
    - TC-EX-005 row-per-entry hours as string "7,5" (Swedish decimal) → parsed to 7.5.
    - TC-EX-006 row-per-entry bad date "2026/04/07" → warning `ERR_BAD_DATE` with `sourceRow`, row skipped.
    - TC-EX-007 row-per-entry hours = 0 or negative → warning `ERR_BAD_HOURS`, row skipped.
    - TC-EX-008 pivoted ISO date headers (e.g. "2026-04-06") → one ParsedRow per (person, project, date-col) where cell > 0.
    - TC-EX-009 pivoted ISO week headers (e.g. "2026-W15") → one ParsedRow per working day of that ISO week using `workDaysInIsoWeek` + largest-remainder hint stored as-is; Plan 38-02 will distribute on commit. For this task emit ONE ParsedRow per week column with date = ISO Monday of that week and a warning code `WEEK_GRAIN_PENDING_DISTRIBUTION` (so preview still counts the row).
    - TC-EX-010 pivoted ISO month headers ("2026-04") → one ParsedRow per month col with date = first working day; warning `MONTH_GRAIN_PENDING_DISTRIBUTION`.
    - TC-EX-011 pivoted US WEEKNUM header detection: any header matching `/^(w(eek)?)?\s*\d+$/i` WITHOUT an ISO year prefix, OR any cell comment / raw value containing `WEEKNUM(`, OR a Sunday-start sequence detected by checking two consecutive headers are 7 days apart with first day-of-week = Sunday → throws `ValidationError` code `ERR_US_WEEK_HEADERS`. DO NOT emit warnings, DO NOT partial-parse. Hard-stop.
    - TC-EX-012 empty workbook (no rows after header) → `ParseResult` with `rows: []` and one warning `EMPTY_SHEET`.

    NO `date-fns` imports. NO `Date.getDay()`. All date + ISO-week logic via `@/lib/time`.
  </behavior>
  <action>
    Implement `src/features/import/parsers/actuals-excel.parser.ts` exporting:
    - `parseActualsWorkbook(buffer)` — top-level entry. Uses `XLSX.read(buffer, { type: 'array' })`, picks first sheet, calls `XLSX.utils.sheet_to_json(sheet, { header: 1, raw: true, defval: null })` to get a `unknown[][]`.
    - `detectLayout(rows: unknown[][]): 'row-per-entry' | 'pivoted'` — inspect row 0 headers.
    - `classifyHeader(label: string): 'date' | 'iso-week' | 'iso-month' | 'us-week' | 'other'` — regex + `parseIsoDate` tryCatch. ISO week: `/^\d{4}-W\d{2}$/`. ISO month: `/^\d{4}-\d{2}$/`. US week: header is bare `W12` / `Week 12` / `12` with no year prefix, or explicit WEEKNUM formula marker.
    - `parseRowPerEntry(rows)` and `parsePivoted(rows)` — pure functions returning `{rows, warnings}`.
    - Swedish decimal parse: `Number(String(cell).replace(',', '.'))`; reject NaN or ≤0.
    - Column alias map for row-per-entry: `person_name | person | namn | name`, `project_name | project | projekt`, `date | datum`, `hours | timmar | tid`.

    **Hard-stop rule (TC-EX-011)**: before parsing a pivoted sheet, run `assertNoUsWeekHeaders(headers, rawCellValues)`. If ANY header classifies as `us-week` OR any header string contains `"WEEKNUM"` (raw formula leakage), throw `ValidationError` with `code: 'ERR_US_WEEK_HEADERS'` and a message listing offending columns. Do NOT swallow and continue.

    Also run the "Sunday-start sequence" check: if all headers classify as `date` AND the first two Dates differ by 7 days AND the first Date is a Sunday (use `parseIsoDate` then check day index via a helper added to `@/lib/time` if not present — OR inline using the ISO week function: if `getIsoWeek(firstDate).isoWeek !== getIsoWeek(secondDate).isoWeek - 1` when headers are weekly and first falls on Sunday → US week).

    Test file: build workbooks in-memory via `XLSX.utils.aoa_to_sheet` + `XLSX.utils.book_new` + `XLSX.write({type:'array'})` → ArrayBuffer. One `describe` block per TC-EX-NNN. Use `expect(...).rejects.toMatchObject({code:'ERR_US_WEEK_HEADERS'})` for hard-stops.

    Write tests first (RED), then implement (GREEN), then refactor if needed.
  </action>
  <verify>
    <automated>pnpm test src/features/import/parsers/__tests__/actuals-excel.parser.test.ts</automated>
  </verify>
  <done>All 12 TC-EX-NNN test cases pass. No date-fns import. No Date.getDay calls (grep confirms). pnpm typecheck + pnpm lint green for the new files.</done>
</task>

<task type="auto">
  <name>Task 3: Generate template_row_per_entry.xlsx static asset</name>
  <files>
    public/templates/template_row_per_entry.xlsx,
    scripts/generate-import-template.ts,
    package.json
  </files>
  <action>
    Create `scripts/generate-import-template.ts` — a deterministic Node script that uses `xlsx` to build the canonical IMP-06 template:

    Sheet 1 "Utfall":
      Row 1 headers: `person_name | project_name | date | hours`
      Rows 2–6 example data (Swedish names that match the seeded demo dataset so Line Mgr can test round-trip):
        Anna Andersson | Atlas | 2026-04-07 | 8
        Anna Andersson | Atlas | 2026-04-08 | 7.5
        Erik Svensson  | Nova  | 2026-04-07 | 6
        Erik Svensson  | Nova  | 2026-04-08 | 8
        Sara Berg      | Atlas | 2026-04-07 | 4

    Sheet 2 "Instruktioner" (Swedish):
      One column with text lines:
        - "Fyll i en rad per person/projekt/datum."
        - "Datum ska vara i formatet ÅÅÅÅ-MM-DD (ISO)."
        - "Timmar kan vara decimaltal (t.ex. 7,5 eller 7.5)."
        - "US WEEKNUM() stöds inte. Använd ISO-veckor (t.ex. 2026-W15) om du använder pivoterad layout."
        - "Hitta en pivoterad mall på hjälpsidan."

    Write the xlsx buffer to `public/templates/template_row_per_entry.xlsx`. `mkdir -p public/templates` first.

    Add npm script `"generate:import-template": "tsx scripts/generate-import-template.ts"` to `package.json` (insert alphabetically near other generate scripts). Run it once to produce the file.

    After generation, write a tiny round-trip assertion inline in the script (exit 1 on failure): re-read the buffer via `parseActualsWorkbook` — must return `layout: 'row-per-entry'`, `rows.length === 5`, `warnings.length === 0`.

    Commit the `.xlsx` binary (it's small, <10 KB, and is a build output under source control per IMP-06 "served as a static asset").
  </action>
  <verify>
    <automated>pnpm generate:import-template &amp;&amp; test -f public/templates/template_row_per_entry.xlsx</automated>
  </verify>
  <done>Template exists under public/templates, round-trip parse in the script passes, file committed.</done>
</task>

</tasks>

<verification>
- pnpm test src/features/import/ — all new tests green (matcher 8/8 + parser 12/12)
- pnpm typecheck — green
- pnpm lint — green (no date-fns imports in parser file)
- public/templates/template_row_per_entry.xlsx exists and is >0 bytes
- pnpm generate:import-template re-runs deterministically (byte-identical output)
</verification>

<success_criteria>
- [ ] TC-EX-001..012 all pass as automated tests
- [ ] ERR_US_WEEK_HEADERS thrown on every US-week variant (bare `W12`, `Week 12`, WEEKNUM formula, Sunday-start sequence)
- [ ] matchPersonName handles exact / lowercase / comma-swap / fuzzy / ambiguous / none
- [ ] template_row_per_entry.xlsx parseable by parseActualsWorkbook with zero warnings
- [ ] No new runtime dependency added (xlsx + string-similarity already present)
</success_criteria>

<output>
After completion, create `.planning/phases/38-excel-import-pipeline/38-01-SUMMARY.md` using the template.
</output>
