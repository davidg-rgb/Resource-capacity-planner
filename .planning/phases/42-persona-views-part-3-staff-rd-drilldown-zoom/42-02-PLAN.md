---
phase: 42-persona-views-part-3-staff-rd-drilldown-zoom
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - src/features/planning/planning.read.ts
  - src/features/planning/__tests__/getStaffSchedule.test.ts
  - src/app/api/v5/planning/allocations/route.ts
  - src/app/api/v5/planning/allocations/__tests__/scope.contract.test.ts
  - src/app/(app)/staff/page.tsx
  - src/components/timeline/staff-timeline-cell.tsx
  - src/app/(app)/staff/__tests__/staff-schedule.test.tsx
  - src/features/personas/__tests__/persona.scope.test.ts
  - src/features/planning/__tests__/staff.e2e.test.ts
  - src/messages/sv.json
  - src/messages/en.json
  - src/messages/keys.ts
autonomous: true
requirements: [UX-V5-07, UX-V5-09]
must_haves:
  truths:
    - "Staff persona on /staff sees a read-only month grid of their projects with summary strip"
    - "Cells render via PlanVsActualCell in read-only mode (no edit handlers fired)"
    - "Clicking a Staff cell opens PlanVsActualDrawer in 'daily' mode for that person/project/month"
    - "PersonaGate redirects non-staff/admin/rd personas with a switch-persona hint"
    - "API GET /api/v5/planning/allocations?scope=staff&personId=...&startMonth=...&endMonth=... returns getStaffSchedule shape"
  artifacts:
    - path: "src/features/planning/planning.read.ts"
      provides: "getStaffSchedule helper"
      contains: "export async function getStaffSchedule"
    - path: "src/app/api/v5/planning/allocations/route.ts"
      provides: "scope=staff branch"
      contains: "scope === 'staff'"
    - path: "src/app/(app)/staff/page.tsx"
      provides: "Staff My Schedule page"
      contains: "PersonaGate"
    - path: "src/components/timeline/staff-timeline-cell.tsx"
      provides: "Read-only cell wrapper"
      contains: "PlanVsActualCell"
  key_links:
    - from: "src/app/(app)/staff/page.tsx"
      to: "/api/v5/planning/allocations?scope=staff"
      via: "TanStack Query fetch"
      pattern: "scope=staff"
    - from: "src/app/(app)/staff/page.tsx"
      to: "src/components/drawer/PlanVsActualDrawer.tsx"
      via: "shared drawer import + onCellClick"
      pattern: "from.*components/drawer/PlanVsActualDrawer"
---

<objective>
Wave 1: Ship Staff "My Schedule" — the smallest persona surface — to de-risk the PersonaGate + drawer wiring + read-model + API extension pattern before Waves 2 and 3 build on the same scaffolding.

Purpose: UX-V5-07 (Staff read-only My Schedule) + first call-site of UX-V5-09 (shared drawer).
Output: /staff page, getStaffSchedule helper, scope=staff API branch, Staff cell wrapper, drawer wired, e2e test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-CONTEXT.md
@.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-RESEARCH.md
@.planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md
@src/features/planning/planning.read.ts
@src/app/api/v5/planning/allocations/route.ts
@src/components/drawer/PlanVsActualDrawer.tsx
@src/components/timeline/PlanVsActualCell.tsx
@src/features/capacity/capacity.read.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: planning.read.getStaffSchedule + API scope=staff branch</name>
  <files>src/features/planning/planning.read.ts, src/features/planning/__tests__/getStaffSchedule.test.ts, src/app/api/v5/planning/allocations/route.ts, src/app/api/v5/planning/allocations/__tests__/scope.contract.test.ts, src/features/personas/__tests__/persona.scope.test.ts</files>
  <read_first>
    - src/features/planning/planning.read.ts (existing getPmProjectGrid + getLineManagerTimeline patterns)
    - src/app/api/v5/planning/allocations/route.ts (existing scope=pm / scope=line-manager branches — exact pattern to mirror per D-08)
    - src/features/capacity/capacity.read.ts getPersonMonthUtilization (D-06: reuse for summaryStrip)
    - .planning/phases/42-.../42-CONTEXT.md D-04, D-05, D-06, D-08
  </read_first>
  <behavior>
    - getStaffSchedule({ orgId, personId, monthRange }) returns { projects: [{projectId, projectName, months: Record<monthKey, CellView>}], summaryStrip: Record<monthKey, {plannedHours, actualHours, utilizationPct}> }
    - Sums APPROVED allocations only (per D-05 / Phase 41 D-07)
    - Empty months still appear as zero CellViews
    - Route returns 400 if personId missing in scope=staff
    - TC-PSN staff scope: staff persona allowed; pm persona forbidden (403)
    - TC-API-001 contract: shape matches type
  </behavior>
  <action>
    1. Add to planning.read.ts (mirror getPmProjectGrid signature):
       ```ts
       export async function getStaffSchedule(args: {
         orgId: string;
         personId: string;
         monthRange: string[];
       }): Promise<StaffScheduleResult>
       ```
       Internally: query allocations table joined to projects WHERE person_id = personId AND status = 'approved' AND month IN monthRange. Group by projectId. For each project build months Record. Then call `getPersonMonthUtilization({orgId, personId, monthRange})` ONCE for the summaryStrip. Export the StaffScheduleResult type.
    2. Add scope=staff branch to GET handler in allocations/route.ts mirroring scope=pm branch exactly. Validate personId required. Return JSON via existing `respondJson` helper.
    3. Extend (or create) src/app/api/v5/planning/allocations/__tests__/scope.contract.test.ts with scope=staff request → asserts shape (PGlite).
    4. Extend src/features/planning/__tests__/getStaffSchedule.test.ts (PGlite) — happy path + empty range + multiple projects.
    5. Extend src/features/personas/__tests__/persona.scope.test.ts with TC-PSN staff scope: staff persona allowed; pm forbidden → 403.
  </action>
  <verify>
    <automated>pnpm vitest run src/features/planning/__tests__/getStaffSchedule.test.ts src/app/api/v5/planning/allocations/__tests__/scope.contract.test.ts src/features/personas/__tests__/persona.scope.test.ts</automated>
  </verify>
  <done>All three test files green; getStaffSchedule + scope=staff branch on disk.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /staff page + StaffTimelineCell + drawer wiring + RTL test + i18n</name>
  <files>src/app/(app)/staff/page.tsx, src/components/timeline/staff-timeline-cell.tsx, src/app/(app)/staff/__tests__/staff-schedule.test.tsx, src/features/planning/__tests__/staff.e2e.test.ts, src/messages/sv.json, src/messages/en.json, src/messages/keys.ts</files>
  <read_first>
    - src/components/timeline/PlanVsActualCell.tsx (read-only mode props)
    - src/components/drawer/PlanVsActualDrawer.tsx + usePlanVsActualDrawer.tsx (mode discriminant from Wave 0)
    - src/app/(app)/line-manager/timeline/page.tsx (Phase 41 PersonaGate + TimelineGrid mount pattern — mirror this)
    - src/features/personas/persona.context.tsx + PersonaGate
    - .planning/phases/42-.../42-CONTEXT.md D-01, D-03, D-19
  </read_first>
  <behavior>
    - /staff renders projects × months grid via TimelineGrid + StaffTimelineCell
    - PersonaGate allows ['staff','admin','rd']; others see hint card
    - Summary strip row shows monthly planned/actual/utilization%
    - Cell click opens PlanVsActualDrawer in mode='daily' with correct context
    - Cells are read-only — no edit handler invoked
    - TC-UI read-only gating: cell does NOT render edit affordances
    - TC-E2E-3A: full Staff happy path with PGlite seed
  </behavior>
  <action>
    1. Create src/components/timeline/staff-timeline-cell.tsx — thin wrapper that takes a CellView and renders `<PlanVsActualCell view={view} readOnly />`. No edit handlers.
    2. Create src/app/(app)/staff/page.tsx as a `'use client'` page:
       - Wrap with `<PersonaGate allowed={['staff','admin','rd']}>`
       - useTranslations('v5.staff')
       - Resolve personId from current persona context (or PersonaSwitcher staff stub)
       - useQuery → fetch `/api/v5/planning/allocations?scope=staff&personId=...&startMonth=...&endMonth=...`
       - Render `<TimelineGrid rows={projects} cellComponent={StaffTimelineCell} onCellClick={openDrawer} zoom={zoom} />`
       - Render summary strip across the top (planned/actual/utilization% per month)
       - Drawer: import `PlanVsActualDrawer` from `src/components/drawer/PlanVsActualDrawer` (EXACT path — load-bearing for Wave 3 TC-UI shared drawer test) and mount with `mode: 'daily'`
    3. Add i18n keys under `v5.staff.*` to sv.json + en.json + keys.ts: `title`, `summaryStrip.planned`, `summaryStrip.actual`, `summaryStrip.utilization`, `empty`, `noPersonaHint`.
    4. RTL test src/app/(app)/staff/__tests__/staff-schedule.test.tsx — render with mocked query, assert grid + summary strip + read-only cells (no edit input rendered) + click opens drawer.
    5. E2E test src/features/planning/__tests__/staff.e2e.test.ts — PGlite seed person + 2 projects + approved allocations across 3 months → mount /staff → assert visible totals + click cell → drawer renders.
  </action>
  <verify>
    <automated>pnpm vitest run src/app/(app)/staff/__tests__/staff-schedule.test.tsx src/features/planning/__tests__/staff.e2e.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>/staff renders, read-only confirmed, drawer opens, e2e green, i18n keys present in both locales.</done>
</task>

</tasks>

<verification>
- pnpm tsc --noEmit green
- All vitest test files in this plan green
- Manual: visit /staff → grid + summary strip + clicking cell opens drawer
</verification>

<success_criteria>
Staff My Schedule live with read-only grid, summary strip, drawer wired in daily mode. UX-V5-07 satisfied; first of four UX-V5-09 call sites in place.
</success_criteria>

<output>
Create `.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-02-SUMMARY.md`
</output>
</content>
</invoke>