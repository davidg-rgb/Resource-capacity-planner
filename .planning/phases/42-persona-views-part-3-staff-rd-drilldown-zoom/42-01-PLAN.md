---
phase: 42-persona-views-part-3-staff-rd-drilldown-zoom
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/time/iso-calendar.ts
  - src/lib/time/formatters.ts
  - src/lib/time/__tests__/iso-calendar.zoom.test.ts
  - src/components/drawer/usePlanVsActualDrawer.tsx
  - src/components/timeline/timeline-columns.ts
autonomous: true
requirements: [UX-V5-08, UX-V5-09, UX-V5-12]
must_haves:
  truths:
    - "rangeQuarters and rangeYears enumerate ISO-year aligned buckets for any monthRange"
    - "TimelineZoom type accepts 'month' | 'quarter' | 'year'"
    - "DrawerContext.personId can be string | null with mode discriminant"
    - "TC-CAL-003 and TC-CAL-006 tests exist and fail RED (no implementation yet) before Wave 2"
  artifacts:
    - path: "src/lib/time/iso-calendar.ts"
      provides: "rangeQuarters + rangeYears helpers"
      contains: "export function rangeQuarters"
    - path: "src/lib/time/formatters.ts"
      provides: "formatQuarter + formatYear locale-aware"
      contains: "export function formatQuarter"
    - path: "src/components/timeline/timeline-columns.ts"
      provides: "TimelineZoom widened type"
      contains: "'month' | 'quarter' | 'year'"
    - path: "src/components/drawer/usePlanVsActualDrawer.tsx"
      provides: "DrawerContext with personId nullable + mode"
      contains: "mode: 'daily' | 'project-person-breakdown'"
    - path: "src/lib/time/__tests__/iso-calendar.zoom.test.ts"
      provides: "TC-CAL-003 + TC-CAL-006 red scaffolds"
      contains: "TC-CAL-003"
  key_links:
    - from: "src/components/timeline/timeline-columns.ts"
      to: "src/lib/time/iso-calendar.ts"
      via: "import { rangeQuarters, rangeYears }"
      pattern: "rangeQuarters|rangeYears"
---

<objective>
Wave 0 foundation: widen types and add calendar helpers + RED test scaffolds so Waves 1-3 can build against stable contracts without scavenger hunts.

Purpose: De-risk the load-bearing 53-week edge case (TC-CAL-006) and the drawer dual-mode contract (UX-V5-09) before any persona pages are built.
Output: Calendar helpers, widened TimelineZoom, DrawerContext discriminant, failing TC-CAL tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v5.0-ARCHITECTURE.md
@.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-CONTEXT.md
@.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-RESEARCH.md
@src/lib/time/iso-calendar.ts
@src/components/timeline/timeline-columns.ts
@src/components/drawer/usePlanVsActualDrawer.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rangeQuarters + rangeYears + TC-CAL RED tests</name>
  <files>src/lib/time/iso-calendar.ts, src/lib/time/__tests__/iso-calendar.zoom.test.ts</files>
  <read_first>
    - src/lib/time/iso-calendar.ts (existing helpers: getISOWeeksInYear, isISO53WeekYear, getISOWeekYear, workDaysInMonth, workingDaysInRange)
    - .planning/phases/42-.../42-RESEARCH.md (Wave 0 calendar helper signatures + property-based sum-consistency check)
    - .planning/v5.0-ARCHITECTURE.md §112 (53-week ISO-year boundary rules)
  </read_first>
  <behavior>
    - rangeQuarters(['2026-01', ..., '2026-12']) returns ['2026-Q1','2026-Q2','2026-Q3','2026-Q4']
    - rangeYears(['2026-01', ..., '2027-06']) returns ['2026','2027']
    - TC-CAL-003: quarter buckets snap on ISO year boundary; Dec 2026 maps to 2026-Q4
    - TC-CAL-006: week 53 of 2026 (Dec 28–31, 4 working days excluding Jan 1 holiday) is included in Q4-2026 and year-2026 totals, NOT 2027
    - Property-based: for any 20-month range, sum(monthHours) === sum(quarterHours) === sum(yearHours)
  </behavior>
  <action>
    1. Append to src/lib/time/iso-calendar.ts:
       ```ts
       export function rangeQuarters(monthRange: string[]): string[]
       export function rangeYears(monthRange: string[]): string[]
       export function quarterKeyForMonth(monthKey: string): string  // '2026-01' -> '2026-Q1'
       export function yearKeyForMonth(monthKey: string): string     // '2026-12' -> '2026'
       ```
       Use existing `getISOWeekYear` for ISO-year semantics. Month → quarter mapping is calendar-quarter (Jan-Mar = Q1) but the YEAR portion uses the ISO year of the majority of the month's working days. For Dec 2026 the majority lives in ISO 2026 → '2026-Q4'. Reuse `workingDaysInRange` to compute majorities.
    2. Create src/lib/time/__tests__/iso-calendar.zoom.test.ts with cases:
       - TC-CAL-003: rangeQuarters for 2026-01..2026-12 length === 4
       - TC-CAL-003: quarterKeyForMonth('2026-12') === '2026-Q4'
       - TC-CAL-006: Q4-2026 working-day total includes Dec 28–31 2026 (week 53)
       - Property test: random 20-month range, sum identity across grains
    3. Tests MUST fail RED until helpers land in this same task (then GREEN).
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/time/__tests__/iso-calendar.zoom.test.ts</automated>
  </verify>
  <done>All TC-CAL tests pass green; helpers exported; pnpm tsc --noEmit clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add formatQuarter + formatYear locale formatters</name>
  <files>src/lib/time/formatters.ts, src/lib/time/__tests__/formatters.zoom.test.ts</files>
  <read_first>
    - src/lib/time/formatters.ts (existing month/week formatters)
    - .planning/phases/42-.../42-CONTEXT.md D-20 (KV1 2026 / Q1 2026)
  </read_first>
  <behavior>
    - formatQuarter('2026-Q1', 'sv') === 'KV1 2026'
    - formatQuarter('2026-Q1', 'en') === 'Q1 2026'
    - formatYear('2026', 'sv'|'en') === '2026'
  </behavior>
  <action>
    Add two exported functions to src/lib/time/formatters.ts:
    ```ts
    export function formatQuarter(quarterKey: string, locale: 'sv' | 'en'): string
    export function formatYear(yearKey: string, locale: 'sv' | 'en'): string
    ```
    Swedish prefix 'KV', English prefix 'Q'. Parse '2026-Q1' → number + year.
    Add formatters.zoom.test.ts asserting both locales for Q1..Q4 and a year.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/time/__tests__/formatters.zoom.test.ts</automated>
  </verify>
  <done>Tests green, exports present.</done>
</task>

<task type="auto">
  <name>Task 3: Widen TimelineZoom type + DrawerContext discriminant</name>
  <files>src/components/timeline/timeline-columns.ts, src/components/drawer/usePlanVsActualDrawer.tsx</files>
  <read_first>
    - src/components/timeline/timeline-columns.ts (line 13 `void zoom; // future: 'quarter' | 'year' land in Phase 42`)
    - src/components/drawer/usePlanVsActualDrawer.tsx (current DrawerContext shape)
    - .planning/phases/42-.../42-CONTEXT.md D-10, D-17
  </read_first>
  <action>
    1. In timeline-columns.ts widen the type:
       ```ts
       export type TimelineZoom = 'month' | 'quarter' | 'year';
       ```
       Keep the body branching on `zoom` with explicit `if (zoom !== 'month') throw new Error('not yet implemented — Wave 2')` so callers compile but runtime guards. Remove the `void zoom` line.
    2. In usePlanVsActualDrawer.tsx widen DrawerContext:
       ```ts
       export type DrawerMode = 'daily' | 'project-person-breakdown';
       export interface DrawerContext {
         personId: string | null;  // null in project-person-breakdown mode
         projectId: string;
         monthKey: string;
         mode: DrawerMode;
       }
       ```
       Default mode in existing call sites stays 'daily' with personId asserted non-null at use site (add a runtime invariant).
    3. Run pnpm tsc --noEmit and fix any callers broken by the personId widening (existing PM/LM call sites — add `mode: 'daily'`).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>Types compile; existing PM drawer call sites still work; new fields exported.</done>
</task>

</tasks>

<verification>
- pnpm tsc --noEmit clean
- pnpm vitest run src/lib/time/__tests__/iso-calendar.zoom.test.ts src/lib/time/__tests__/formatters.zoom.test.ts green
</verification>

<success_criteria>
Wave 0 contracts ready: rangeQuarters/rangeYears/formatters available, TimelineZoom widened, DrawerContext supports project-person-breakdown mode. Waves 1-3 can build without scavenger hunts.
</success_criteria>

<output>
Create `.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-01-SUMMARY.md`
</output>
</content>
</invoke>