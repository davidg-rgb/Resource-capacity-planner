---
phase: 42-persona-views-part-3-staff-rd-drilldown-zoom
plan: 04
type: execute
wave: 3
depends_on: ["42-01", "42-02", "42-03"]
files_modified:
  - src/features/planning/planning.read.ts
  - src/features/planning/__tests__/getPortfolioGrid.test.ts
  - src/app/api/v5/planning/allocations/route.ts
  - src/app/api/v5/planning/allocations/__tests__/scope.contract.test.ts
  - src/app/(app)/rd/page.tsx
  - src/components/timeline/rd-portfolio-cell.tsx
  - src/components/drawer/PlanVsActualDrawer.tsx
  - src/components/drawer/__tests__/shared-import.test.ts
  - src/features/personas/__tests__/persona.scope.test.ts
  - src/features/planning/__tests__/rd.e2e.test.ts
  - src/messages/sv.json
  - src/messages/en.json
  - src/messages/keys.ts
autonomous: true
requirements: [UX-V5-08, UX-V5-09]
must_haves:
  truths:
    - "/rd renders portfolio grid (projects × months OR departments × months toggle) read-only"
    - "PersonaGate allows ['rd','admin'] only"
    - "Clicking an R&D portfolio cell opens PlanVsActualDrawer in mode='project-person-breakdown' (personId=null) showing per-person rows for that project-month"
    - "ZoomControls work on /rd (month/quarter/year)"
    - "All four timeline pages (/pm/projects/[id], /line-manager/timeline, /staff, /rd) import PlanVsActualDrawer from the EXACT SAME path — TC-UI shared drawer test enforces this (LOAD-BEARING for UX-V5-09)"
    - "R&D overcommit drill available (modal folded into /rd)"
  artifacts:
    - path: "src/features/planning/planning.read.ts"
      provides: "getPortfolioGrid helper"
      contains: "export async function getPortfolioGrid"
    - path: "src/app/(app)/rd/page.tsx"
      provides: "R&D Portfolio page"
      contains: "PersonaGate"
    - path: "src/components/drawer/__tests__/shared-import.test.ts"
      provides: "TC-UI shared drawer single-source-of-truth gate"
      contains: "PlanVsActualDrawer"
    - path: "src/components/drawer/PlanVsActualDrawer.tsx"
      provides: "project-person-breakdown mode"
      contains: "project-person-breakdown"
  key_links:
    - from: "src/app/(app)/rd/page.tsx"
      to: "src/components/drawer/PlanVsActualDrawer.tsx"
      via: "shared drawer import"
      pattern: "from.*components/drawer/PlanVsActualDrawer"
    - from: "src/app/(app)/rd/page.tsx"
      to: "/api/v5/planning/allocations?scope=rd"
      via: "TanStack Query"
      pattern: "scope=rd"
---

<objective>
Wave 3: Ship the R&D Portfolio surface, extend the shared drawer with project-person-breakdown mode, mount ZoomControls on /rd, and lock in the LOAD-BEARING TC-UI shared drawer test that enforces UX-V5-09's "single drawer, four call sites" guarantee.

Purpose: UX-V5-08 (R&D Portfolio + zoom + 53-week) and the closing of UX-V5-09 (shared drawer across all 4 personas).
Output: getPortfolioGrid, scope=rd API, /rd page, drawer dual-mode, shared-import grep test, PM project page also wired to drawer if not already.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-CONTEXT.md
@.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-RESEARCH.md
@.planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md
@src/features/planning/planning.read.ts
@src/components/drawer/PlanVsActualDrawer.tsx
@src/components/drawer/usePlanVsActualDrawer.tsx
@src/components/timeline/PlanVsActualCell.tsx
@src/features/actuals/actuals.read.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getPortfolioGrid + scope=rd API branch + persona scope test + drawer dual-mode</name>
  <files>src/features/planning/planning.read.ts, src/features/planning/__tests__/getPortfolioGrid.test.ts, src/app/api/v5/planning/allocations/route.ts, src/app/api/v5/planning/allocations/__tests__/scope.contract.test.ts, src/features/personas/__tests__/persona.scope.test.ts, src/components/drawer/PlanVsActualDrawer.tsx</files>
  <read_first>
    - src/features/planning/planning.read.ts (getStaffSchedule from plan 42-02 — same module/style)
    - src/features/actuals/actuals.read.ts aggregateByMonth (D-07)
    - src/app/api/v5/planning/allocations/route.ts (scope=staff branch from 42-02 — mirror)
    - src/components/drawer/PlanVsActualDrawer.tsx + usePlanVsActualDrawer.tsx (DrawerMode discriminant from Wave 0)
    - .planning/phases/42-.../42-CONTEXT.md D-04, D-08, D-17
  </read_first>
  <behavior>
    - getPortfolioGrid({ orgId, monthRange, groupBy: 'project'|'department' }) returns { rows: [{id, label, meta, months: Record<monthKey, {plannedHours, actualHours}>}] }
    - Sums APPROVED allocations only across the entire org (no person filter)
    - groupBy='project' → one row per project; groupBy='department' → one row per department aggregating its projects
    - GET /api/v5/planning/allocations?scope=rd&groupBy=...&startMonth=...&endMonth=... returns the shape; defaults groupBy='project'
    - TC-PSN-006: rd persona allowed; staff/pm forbidden (403)
    - TC-API-001 contract for scope=rd
    - PlanVsActualDrawer in mode='project-person-breakdown' renders per-person rows for the {projectId, monthKey} pair (personId=null) instead of per-day rows
  </behavior>
  <action>
    1. Add to planning.read.ts:
       ```ts
       export async function getPortfolioGrid(args: {
         orgId: string;
         monthRange: string[];
         groupBy: 'project' | 'department';
       }): Promise<PortfolioGridResult>
       ```
       Use actuals.read.aggregateByMonth for actual totals; query approved allocations grouped by project (or department via project→department join). Export PortfolioGridResult type.
    2. Add scope=rd branch to allocations/route.ts mirroring scope=staff. Validate groupBy in {'project','department'}, default 'project'.
    3. Extend scope.contract.test.ts with scope=rd cases (both groupBy values).
    4. Create getPortfolioGrid.test.ts (PGlite): seed 2 departments × 2 projects × 3 months → assert both groupBy modes.
    5. Extend persona.scope.test.ts with TC-PSN-006: rd allowed, pm/staff forbidden.
    6. Extend src/components/drawer/PlanVsActualDrawer.tsx: branch on `context.mode`. When `mode === 'project-person-breakdown'` and `personId === null`, fetch per-person rows for that project-month via a new server action `getProjectPersonBreakdown({ projectId, monthKey })` (add this function alongside the existing `getDailyCellBreakdown` in the drawer's data module). Render rows: personName | plannedHours | actualHours | delta. Reuse drawer chrome/i18n. Add a unit/RTL test asserting the project-person-breakdown branch renders person rows, NOT day rows.
  </action>
  <verify>
    <automated>pnpm vitest run src/features/planning/__tests__/getPortfolioGrid.test.ts src/app/api/v5/planning/allocations/__tests__/scope.contract.test.ts src/features/personas/__tests__/persona.scope.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>API/read/persona/drawer-mode green and on disk.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /rd page + RdPortfolioCell + ZoomControls + TC-UI shared drawer + e2e + PM page wiring + i18n</name>
  <files>src/app/(app)/rd/page.tsx, src/components/timeline/rd-portfolio-cell.tsx, src/components/drawer/__tests__/shared-import.test.ts, src/features/planning/__tests__/rd.e2e.test.ts, src/app/(app)/pm/projects/[projectId]/page.tsx, src/messages/sv.json, src/messages/en.json, src/messages/keys.ts</files>
  <read_first>
    - src/app/(app)/staff/page.tsx (just-built parallel pattern from 42-02)
    - src/app/(app)/line-manager/timeline/page.tsx (Phase 41 flat-row toggle pattern for groupBy)
    - src/components/timeline/zoom-controls.tsx + useZoom.ts (from 42-03)
    - src/components/drawer/PlanVsActualDrawer.tsx (with new mode from Task 1)
    - src/app/(app)/pm/projects/[projectId]/page.tsx (verify drawer is wired; if not, wire it)
    - .planning/phases/42-.../42-CONTEXT.md D-01, D-03, D-14, D-16, D-18, D-19
  </read_first>
  <behavior>
    - /rd renders flat-row portfolio grid with project/department toggle
    - PersonaGate allows ['rd','admin']; others see hint card
    - ZoomControls present and re-aggregate via Wave 2 plumbing
    - Cell click opens PlanVsActualDrawer in mode='project-person-breakdown' with personId=null
    - Overcommit drill modal triggered from a header button (folds /rd/drilldown into /rd per Claude's Discretion default)
    - TC-UI shared drawer test (LOAD-BEARING for UX-V5-09): grep all four timeline pages and assert PlanVsActualDrawer is imported from the EXACT SAME path
    - PM project page also imports drawer from that path (wire if missing)
    - TC-E2E-4A: R&D portfolio happy path with PGlite seed
  </behavior>
  <action>
    1. Create src/components/timeline/rd-portfolio-cell.tsx — thin wrapper around `<PlanVsActualCell view={view} readOnly />` handling aggregated CellView.
    2. Create src/app/(app)/rd/page.tsx as `'use client'`:
       - `<PersonaGate allowed={['rd','admin']}>`
       - useTranslations('v5.rd')
       - useZoom() from 42-03
       - State: groupBy toggle ('project'|'department'); useQuery → `/api/v5/planning/allocations?scope=rd&groupBy=...&startMonth=...&endMonth=...`
       - Render `<TimelineGrid rows={rows} cellComponent={RdPortfolioCell} zoom={zoom} onCellClick={(row, monthKey) => openDrawer({ projectId: row.id, monthKey, personId: null, mode: 'project-person-breakdown' })} />`
       - `<ZoomControls />` in header
       - "Overcommit drill" button → opens a modal listing overcommitted persons in current range using existing capacity.read.getCapacityBreakdown
       - Import PlanVsActualDrawer from `'@/components/drawer/PlanVsActualDrawer'` (or relative path — but the EXACT SAME path string used in Staff/PM/LM pages)
    3. Audit the PM project page src/app/(app)/pm/projects/[projectId]/page.tsx — confirm it imports PlanVsActualDrawer from the same canonical path. If not wired, wire it now (mode='daily').
    4. Create src/components/drawer/__tests__/shared-import.test.ts — TC-UI shared drawer (LOAD-BEARING):
       ```ts
       // Greps the four timeline page files; asserts each contains exactly one
       // import line for PlanVsActualDrawer AND that all four use the identical
       // import specifier string.
       const FILES = [
         'src/app/(app)/pm/projects/[projectId]/page.tsx',
         'src/app/(app)/line-manager/timeline/page.tsx',
         'src/app/(app)/staff/page.tsx',
         'src/app/(app)/rd/page.tsx',
       ];
       // Read each file, extract /from ['"]([^'"]*PlanVsActualDrawer[^'"]*)['"]/
       // Assert: every file has 1 match AND all matches are === to first.
       ```
       This test FAILS if anyone copies/forks the drawer.
    5. Add i18n keys `v5.rd.title`, `v5.rd.groupBy.project`, `v5.rd.groupBy.department`, `v5.rd.overcommitDrill`, `v5.rd.noPersonaHint` to sv/en/keys.
    6. Create src/features/planning/__tests__/rd.e2e.test.ts (PGlite + RTL) — TC-E2E-4A: seed org + 3 projects across 2 departments + approved allocations → mount /rd → toggle groupBy → click cell → drawer renders person breakdown.
  </action>
  <verify>
    <automated>pnpm vitest run src/components/drawer/__tests__/shared-import.test.ts src/features/planning/__tests__/rd.e2e.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>/rd live with grid + zoom + drawer dual-mode; TC-UI shared drawer test green; PM page audited; e2e green.</done>
</task>

</tasks>

<verification>
- pnpm tsc --noEmit clean
- All Wave 3 vitest files green, especially TC-UI shared-import.test.ts (LOAD-BEARING for UX-V5-09)
- pnpm vitest run (full suite) green — no regressions in PM/LM/Staff
- Manual: /rd → toggle groupBy → switch zoom to Kvartal → click Q4-2026 cell → drawer shows per-person rows for the project; week 53 working days included in Q4 totals
</verification>

<success_criteria>
- UX-V5-07 (Staff) ✓ via 42-02
- UX-V5-08 (R&D Portfolio + zoom + 53-week) ✓ via 42-03 + 42-04
- UX-V5-09 (Shared drawer across 4 pages) ✓ enforced by TC-UI shared-import test
- UX-V5-12 (Zoom levels + ISO 53-week) ✓ via 42-03 with TC-CAL-003/006 from 42-01
- All roadmap test codes (TC-PSN staff scope, TC-PSN-006, TC-API-001 scope=staff/rd, TC-ZOOM-*, TC-CAL-003, TC-CAL-006, TC-UI read-only gating, TC-UI shared drawer, TC-E2E-3A, TC-E2E-4A) created or extended
</success_criteria>

<output>
Create `.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-04-SUMMARY.md`
</output>
</content>
</invoke>