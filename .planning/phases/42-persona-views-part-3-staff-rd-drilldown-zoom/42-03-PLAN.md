---
phase: 42-persona-views-part-3-staff-rd-drilldown-zoom
plan: 03
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - src/components/timeline/timeline-columns.ts
  - src/components/timeline/__tests__/timeline-columns.zoom.test.ts
  - src/components/timeline/zoom-controls.tsx
  - src/components/timeline/useZoom.ts
  - src/components/timeline/__tests__/TimelineGrid.zoom.test.tsx
  - src/components/timeline/timeline-grid.tsx
  - src/components/timeline/PlanVsActualCell.tsx
  - src/app/(app)/pm/projects/[projectId]/page.tsx
  - src/app/(app)/line-manager/timeline/page.tsx
  - src/app/(app)/staff/page.tsx
  - src/messages/sv.json
  - src/messages/en.json
  - src/messages/keys.ts
autonomous: true
requirements: [UX-V5-12, UX-V5-08]
must_haves:
  truths:
    - "buildTimelineColumns(range, 'quarter') returns ISO-year aligned quarter columns"
    - "buildTimelineColumns(range, 'year') returns ISO-year columns"
    - "ZoomControls toggle (Månad/Kvartal/År) updates URL ?zoom= and re-renders the grid"
    - "Switching zoom re-aggregates cells client-side from month-grain data"
    - "Zoom controls present on PM project, LM, and Staff timeline pages"
    - "Week 53 of 2026 working days roll into Q4-2026 / year-2026, never 2027"
  artifacts:
    - path: "src/components/timeline/timeline-columns.ts"
      provides: "Quarter + year branches implemented"
      contains: "case 'quarter'"
    - path: "src/components/timeline/zoom-controls.tsx"
      provides: "3-button zoom toggle"
      contains: "month.*quarter.*year"
    - path: "src/components/timeline/useZoom.ts"
      provides: "URL-synced zoom hook"
      contains: "useSearchParams"
  key_links:
    - from: "src/components/timeline/timeline-columns.ts"
      to: "src/lib/time/iso-calendar.ts"
      via: "rangeQuarters/rangeYears"
      pattern: "rangeQuarters|rangeYears"
    - from: "src/components/timeline/zoom-controls.tsx"
      to: "URL ?zoom="
      via: "router.replace"
      pattern: "router\\.replace"
---

<objective>
Wave 2: Implement long-horizon zoom — quarter and year branches in buildTimelineColumns, ZoomControls toggle with URL sync, and mount controls on the three already-built timeline pages (PM project, LM, Staff). R&D timeline plug-in happens in Wave 3.

Purpose: UX-V5-12 (zoom levels + 53-week handling) + UX-V5-08 prerequisite (zoom available on R&D in Wave 3).
Output: Quarter/year aggregation, ZoomControls + useZoom, CellView extended, three pages wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-CONTEXT.md
@.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-RESEARCH.md
@.planning/v5.0-ARCHITECTURE.md
@src/components/timeline/timeline-columns.ts
@src/components/timeline/timeline-grid.tsx
@src/components/timeline/PlanVsActualCell.tsx
@src/lib/time/iso-calendar.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildTimelineColumns quarter+year branches + CellView aggregate flag</name>
  <files>src/components/timeline/timeline-columns.ts, src/components/timeline/__tests__/timeline-columns.zoom.test.ts, src/components/timeline/PlanVsActualCell.tsx</files>
  <read_first>
    - src/components/timeline/timeline-columns.ts (current month-only impl, line ~14 hook)
    - src/lib/time/iso-calendar.ts (rangeQuarters/rangeYears from Wave 0)
    - src/lib/time/formatters.ts (formatQuarter/formatYear from Wave 0)
    - src/components/timeline/PlanVsActualCell.tsx (current CellView shape)
    - .planning/phases/42-.../42-CONTEXT.md D-10, D-11, D-12, D-15
  </read_first>
  <behavior>
    - buildTimelineColumns(['2026-01'..'2026-12'], 'month') length === 12 (unchanged)
    - buildTimelineColumns(['2026-01'..'2026-12'], 'quarter') length === 4
    - buildTimelineColumns(['2026-01'..'2026-12'], 'year') length === 1
    - Aggregated columns expose underlyingMonths metadata
    - PlanVsActualCell gracefully renders aggregated CellView (aggregate=true) by summing plannedHours/actualHours from underlyingMonths
    - TC-CAL-006 indirect: Q4-2026 column's underlyingMonths === ['2026-10','2026-11','2026-12']
    - TC-ZOOM-001/002/003 unit: month, quarter, year column counts and labels
  </behavior>
  <action>
    1. Replace the `void zoom` runtime guard in timeline-columns.ts with real branching:
       ```ts
       switch (zoom) {
         case 'month': return monthColumns(monthRange);
         case 'quarter': return quarterColumns(monthRange);  // uses rangeQuarters
         case 'year': return yearColumns(monthRange);        // uses rangeYears
       }
       ```
       Quarter and year column factories return ColDef with `field` = quarterKey/yearKey, `headerName` via formatQuarter/formatYear, and `cellRendererParams: { underlyingMonths: string[] }`.
    2. Extend CellView in PlanVsActualCell.tsx (or its types file):
       ```ts
       export interface CellView {
         plannedHours: number;
         actualHours: number;
         // ... existing fields
         aggregate?: boolean;
         underlyingMonths?: string[];
       }
       ```
       Update the cell renderer: when `aggregate === true`, sum underlying month values from row.months and render a "Σ" badge alongside the number for visual distinction.
    3. Create timeline-columns.zoom.test.ts with TC-ZOOM-001 (month count=12), TC-ZOOM-002 (quarter count=4 + labels via formatQuarter), TC-ZOOM-003 (year count=1), and TC-ZOOM-004 (Q4-2026 underlyingMonths === ['2026-10','2026-11','2026-12']).
  </action>
  <verify>
    <automated>pnpm vitest run src/components/timeline/__tests__/timeline-columns.zoom.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>Quarter/year branches return correct columns; CellView extended; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ZoomControls + useZoom hook + RTL re-aggregation test + mount on 3 pages + i18n</name>
  <files>src/components/timeline/zoom-controls.tsx, src/components/timeline/useZoom.ts, src/components/timeline/__tests__/TimelineGrid.zoom.test.tsx, src/components/timeline/timeline-grid.tsx, src/app/(app)/pm/projects/[projectId]/page.tsx, src/app/(app)/line-manager/timeline/page.tsx, src/app/(app)/staff/page.tsx, src/messages/sv.json, src/messages/en.json, src/messages/keys.ts</files>
  <read_first>
    - src/components/timeline/timeline-grid.tsx (existing zoom prop slot)
    - src/app/(app)/pm/projects/[projectId]/page.tsx (PM page mount pattern)
    - src/app/(app)/line-manager/timeline/page.tsx
    - src/app/(app)/staff/page.tsx (just built in plan 42-02)
    - .planning/phases/42-.../42-CONTEXT.md D-13, D-14, D-15
  </read_first>
  <behavior>
    - useZoom() returns [zoom, setZoom]; reads ?zoom=, defaults 'month'
    - setZoom calls router.replace with new ?zoom= preserving other params
    - ZoomControls renders 3 buttons; aria-pressed on active
    - TimelineGrid receives zoom prop and re-renders columns
    - TC-ZOOM RTL: switch from month to quarter → grid column count changes from N to ceil(N/3)
    - Controls visible on /pm/projects/[id], /line-manager/timeline, /staff
  </behavior>
  <action>
    1. Create src/components/timeline/useZoom.ts:
       ```ts
       'use client';
       import { useSearchParams, useRouter, usePathname } from 'next/navigation';
       export function useZoom(): [TimelineZoom, (z: TimelineZoom) => void]
       ```
       Mirror change-log-feed URL-sync pattern from Phase 41.
    2. Create src/components/timeline/zoom-controls.tsx — 3-button group using existing UI button primitives. Labels via `useTranslations('v5.timeline.zoom')` keys `month|quarter|year`. aria-pressed on active.
    3. Confirm timeline-grid.tsx accepts `zoom` prop and forwards to buildTimelineColumns. If the slot exists from Phase 40 but is unused, wire it now.
    4. Mount `<ZoomControls />` in a small header area on /pm/projects/[projectId]/page.tsx, /line-manager/timeline/page.tsx, /staff/page.tsx. Each page passes `zoom` (from useZoom) to TimelineGrid. NO rewrites of those pages — just add the controls + thread the prop.
    5. Add i18n keys `v5.timeline.zoom.month` ("Månad"/"Month"), `.quarter` ("Kvartal"/"Quarter"), `.year` ("År"/"Year") to sv/en/keys.
    6. Create TimelineGrid.zoom.test.tsx (RTL): render TimelineGrid with month-grain rows, click "Kvartal" button, assert column count drops and aggregated cells show summed plan/actual.
  </action>
  <verify>
    <automated>pnpm vitest run src/components/timeline/__tests__/TimelineGrid.zoom.test.tsx && pnpm tsc --noEmit</automated>
  </verify>
  <done>ZoomControls live on 3 pages, URL-synced, RTL test green, i18n complete.</done>
</task>

</tasks>

<verification>
- pnpm tsc --noEmit green
- All zoom test files green
- Manual: /staff?zoom=quarter renders 4 columns for a year range
</verification>

<success_criteria>
Long-horizon zoom shipped with month/quarter/year levels, ISO 53-week safe, URL-synced, mounted on PM project + LM + Staff timelines. R&D will plug into the same controls in Wave 3.
</success_criteria>

<output>
Create `.planning/phases/42-persona-views-part-3-staff-rd-drilldown-zoom/42-03-SUMMARY.md`
</output>
</content>
</invoke>