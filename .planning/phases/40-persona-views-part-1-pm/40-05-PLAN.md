---
phase: 40-persona-views-part-1-pm
plan: 05
type: execute
wave: 4
depends_on: ["40-04"]
files_modified:
  - src/app/(app)/pm/__tests__/pm-home.test.tsx
  - src/app/(app)/pm/projects/[projectId]/__tests__/pm-timeline.test.tsx
  - src/app/(app)/pm/__tests__/pm-wishes.test.tsx
  - src/components/timeline/__tests__/PlanVsActualCell.test.tsx
  - src/components/dialogs/__tests__/historic-edit-dialog.test.tsx
  - src/features/personas/__tests__/persona.context.test.tsx
  - src/components/timeline/__tests__/pm-timeline-cell.test.tsx
  - src/features/planning/__tests__/pm.e2e.test.ts
autonomous: true
requirements:
  - UX-V5-01
  - UX-V5-02
  - UX-V5-03
  - UX-V5-11
  - HIST-01
must_haves:
  truths:
    - "TC-UI-001 passes: PM Home renders overview card + project list with loading/empty/loaded states"
    - "TC-UI-002 passes: PM project timeline renders 13-month grid with PlanVsActualCell per row×col"
    - "UX-V5-03 passes: /pm/wishes mounts MyWishesPanel with proposed/approved/rejected filter tabs and a resubmit affordance on rejected cards"
    - "TC-UI debounce passes: rapid cell edits coalesce into a single onCellEdit call after 600ms"
    - "TC-PSN-003 passes: switching persona to PM navigates to /pm and invalidates ['pm-home']"
    - "TC-PR-001 passes: cell edit on out-of-dept person submits a proposal round-trip"
    - "TC-PS-005 passes: historic direct-edit opens dialog and confirm triggers PATCH with confirmHistoric:true"
    - "D-22 e2e passes: Anna /pm → click project → type 60 in Sara's June cell → wish appears in /pm/wishes"
  artifacts:
    - path: "src/features/planning/__tests__/pm.e2e.test.ts"
      provides: "D-22 PGlite happy path"
    - path: "src/components/dialogs/__tests__/historic-edit-dialog.test.tsx"
      provides: "TC-PS-005 component test"
    - path: "src/app/(app)/pm/__tests__/pm-wishes.test.tsx"
      provides: "UX-V5-03 MyWishesPanel mount + filter tabs + resubmit affordance"
  key_links:
    - from: "pm.e2e.test.ts"
      to: "proposal.service.e2e.test.ts"
      via: "shared PGlite harness pattern"
      pattern: "pglite"
---

<objective>
Wave 4: Close out validation coverage. Every test code from the roadmap + VALIDATION.md (TC-UI-001, TC-UI-002, UX-V5-03 My Wishes, TC-UI-debounce, TC-PSN-003, TC-PR-001, TC-PS-005, plus the D-22 happy-path e2e) gets a task. TC-API-004 and TC-PS-006 were already closed in Plan 40-01.

Purpose: Nyquist compliance for Phase 40 — every observable truth has an automated assertion.
Output: 8 test files covering all UI-level roadmap test codes + the /pm/wishes page test + the D-22 PGlite e2e.

**Task structure (3 tasks — Task 1 split into 1a page-level and 1b component-level per checker W1):**
- Task 1a: page-level RTL tests (3 files) — pm-home, pm-project-timeline, pm-wishes
- Task 1b: component/context RTL tests (4 files) — PlanVsActualCell debounce, historic-edit-dialog, persona.context, pm-timeline-cell
- Task 2: D-22 PGlite e2e
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-persona-views-part-1-pm/40-CONTEXT.md
@.planning/phases/40-persona-views-part-1-pm/40-RESEARCH.md
@.planning/phases/40-persona-views-part-1-pm/40-VALIDATION.md
@src/features/proposals/__tests__/proposal.service.e2e.test.ts
@src/components/timeline/PlanVsActualCell.tsx
@src/components/timeline/pm-timeline-cell.tsx
@src/components/dialogs/historic-edit-dialog.tsx
@src/features/personas/persona.context.tsx
@src/features/proposals/ui/my-wishes-panel.tsx
@src/app/(app)/pm/page.tsx
@src/app/(app)/pm/wishes/page.tsx
@src/app/(app)/pm/projects/[projectId]/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: Page-level RTL tests — TC-UI-001 (pm-home), TC-UI-002 (pm-project-timeline), UX-V5-03 (pm-wishes)</name>
  <files>src/app/(app)/pm/__tests__/pm-home.test.tsx, src/app/(app)/pm/projects/[projectId]/__tests__/pm-timeline.test.tsx, src/app/(app)/pm/__tests__/pm-wishes.test.tsx</files>
  <read_first>
    - .planning/phases/40-persona-views-part-1-pm/40-VALIDATION.md "Per-Task Verification Map" (authoritative file paths for each TC)
    - src/app/(app)/pm/page.tsx (from Plan 40-03)
    - src/app/(app)/pm/wishes/page.tsx (from Plan 40-03 — thin wrapper around MyWishesPanel)
    - src/app/(app)/pm/projects/[projectId]/page.tsx (from Plan 40-04 — TimelineGrid wired)
    - src/features/proposals/ui/my-wishes-panel.tsx (full file — confirm filter tab markup + resubmit button markup, so the test asserts against real DOM selectors)
  </read_first>
  <behavior>
    - TC-UI-001: renders loading state, then loaded overview card with project link + pending wishes count + link to /pm/wishes; empty state when projects=[]
    - TC-UI-002: renders 13 columns (or equivalent assertion) + PlanVsActualCell per row×col combination
    - UX-V5-03: /pm/wishes page mounts `MyWishesPanel`, exposes three filter tabs labelled proposed / approved / rejected (or i18n-equivalent keys `v5.proposals.tabs.proposed|approved|rejected`), and a rejected wish card exposes a resubmit affordance (button or `data-testid="resubmit"`)
  </behavior>
  <action>
    All tests use `vitest` + `@testing-library/react` + `vi.mock` for fetch/router/query-client as appropriate.

    **1. `src/app/(app)/pm/__tests__/pm-home.test.tsx` (TC-UI-001) — NEW**
       - Mock `usePersona` to return `{ persona: { kind: 'pm', personId: 'p-anna' } }`.
       - Mock `useAuth` to return `{ isLoaded: true, userId: 'clerk-anna' }`.
       - Wrap in `QueryClientProvider` + `NextIntlClientProvider` with a stub `v5.pm.*` message bundle.
       - Mock `fetch` to return `{ projects: [{ project: { id: 'p1', name: 'Alpha', code: null }, burn: { plannedTotalHours: 100, actualTotalHours: 80, deltaHours: -20 }, pendingWishes: 2 }], defaultProjectId: 'p1' }`.
       - Assert: renders loading, then renders "Alpha", renders "2 pending wishes" text, renders link to `/pm/projects/p1`, renders link to `/pm/wishes`.
       - Second test: fetch returns `{ projects: [], defaultProjectId: null }` → assert empty-state message.

    **2. `src/app/(app)/pm/projects/[projectId]/__tests__/pm-timeline.test.tsx` (TC-UI-002) — NEW**
       - Mock `useParams` to return `{ projectId: 'p1' }`.
       - Mock fetch to return a `PmTimelineView` with 2 people and 13 months of cells (seed via helper).
       - Render the page inside QueryClientProvider + IntlProvider.
       - Assert: `screen.getAllByRole('columnheader').length === 14` (1 pinned person column + 13 months) — note ag-grid may render headers differently in tests; if the ag-grid DOM is awkward, fall back to asserting `view.monthRange.length === 13` via a `data-testid` hook the grid exposes.
       - Alternative lighter assertion: `screen.getAllByTestId('pm-cell').length === 2 * 13` if cell component carries that testid.
       - Document whichever approach works after first test run.

    **3. `src/app/(app)/pm/__tests__/pm-wishes.test.tsx` (UX-V5-03) — NEW (B1 gap closure)**
       - Mock `useAuth` to return `{ isLoaded: true, userId: 'clerk-anna' }`.
       - Mock `usePersona` to return PM persona.
       - Mock fetch for `/api/v5/proposals?proposerId=clerk-anna` (or whatever MyWishesPanel queries — verify by reading `my-wishes-panel.tsx`) to return a fixture with 3 wishes:
         - 1 proposed (`{ id: 'w1', status: 'proposed', projectId: 'p1', personId: 's1', monthKey: '2026-06', proposedHours: 40 }`)
         - 1 approved (`{ id: 'w2', status: 'approved', ... }`)
         - 1 rejected (`{ id: 'w3', status: 'rejected', rejectionReason: 'no', ... }`)
       - Wrap in `QueryClientProvider` + `NextIntlClientProvider` with the real `v5.proposals.*` key bundle (load `src/messages/en.json` or stub just the keys MyWishesPanel consumes).
       - Render the `/pm/wishes` page component.
       - **Assert MyWishesPanel mounts:** query for its root element — either `screen.getByTestId('my-wishes-panel')` (add the testid to `my-wishes-panel.tsx` in this task if missing) OR a unique heading from the panel.
       - **Assert three filter tabs exist:** query for elements with role `tab` (or whatever `my-wishes-panel.tsx` uses — buttons with text). Assert the DOM contains text/labels for all three states: "proposed" / "approved" / "rejected" (accept either i18n-resolved labels in Swedish/English or raw status strings, whichever the panel renders). Use `screen.getByRole('tab', { name: /proposed|väntande/i })` etc.
       - **Assert resubmit affordance on rejected card:** click/activate the "rejected" tab, then assert at least one element with `data-testid="resubmit"` OR a button whose accessible name matches `/resubmit|edit.*resubmit|återinsänd|redigera.*igen/i`. If `my-wishes-panel.tsx` does not yet carry a `data-testid="resubmit"` attribute on the rejected-card action button, add it in this task (surgical one-line edit) and note the change in the task output.
       - Document the selected selectors in a leading comment block so the test is self-describing.
  </action>
  <verify>
    <automated>pnpm vitest run src/app/\(app\)/pm/__tests__ src/app/\(app\)/pm/projects</automated>
  </verify>
  <done>
    - All 3 test files exist and pass
    - grep `pending wishes` OR `pendingWishes` src/app/(app)/pm/__tests__/pm-home.test.tsx returns a hit
    - grep `columnheader` OR `pm-cell` src/app/(app)/pm/projects/[projectId]/__tests__/pm-timeline.test.tsx returns a hit
    - `src/app/(app)/pm/__tests__/pm-wishes.test.tsx` file exists
    - grep `proposed` src/app/(app)/pm/__tests__/pm-wishes.test.tsx returns a hit
    - grep `approved` src/app/(app)/pm/__tests__/pm-wishes.test.tsx returns a hit
    - grep `rejected` src/app/(app)/pm/__tests__/pm-wishes.test.tsx returns a hit
    - grep `resubmit` src/app/(app)/pm/__tests__/pm-wishes.test.tsx returns a hit
    - grep `MyWishesPanel` src/app/(app)/pm/__tests__/pm-wishes.test.tsx returns a hit (mount assertion)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: Component/context RTL tests — TC-UI-debounce, TC-PS-005, TC-PSN-003, TC-PR-001</name>
  <files>src/components/timeline/__tests__/PlanVsActualCell.test.tsx, src/components/dialogs/__tests__/historic-edit-dialog.test.tsx, src/features/personas/__tests__/persona.context.test.tsx, src/components/timeline/__tests__/pm-timeline-cell.test.tsx</files>
  <read_first>
    - src/components/timeline/PlanVsActualCell.tsx (internal debounce at lines 67/85)
    - src/components/timeline/pm-timeline-cell.tsx (from Plan 40-04 Task 1)
    - src/components/dialogs/historic-edit-dialog.tsx (from Plan 40-04 Task 1)
    - src/features/personas/persona.context.tsx (from Plan 40-03 with invalidateQueries)
    - src/features/proposals/ui/proposal-cell.tsx
    - src/features/proposals/use-proposals.ts
  </read_first>
  <behavior>
    - TC-UI debounce: rapid input events within 600ms coalesce to one onCellEdit call
    - TC-PS-005: historic direct-edit path opens HistoricEditDialog, Confirm triggers fetch PATCH with `confirmHistoric: true`
    - TC-PSN-003: setPersona('pm', ...) calls router.push('/pm') and queryClient.invalidateQueries({ queryKey: ['pm-home'] })
    - TC-PR-001: out-of-dept cell edit opens ProposalCell popover, submit triggers POST /api/v5/proposals
  </behavior>
  <action>
    **1. Extend `src/components/timeline/__tests__/PlanVsActualCell.test.tsx` (TC-UI debounce)**
       - If the file does not exist, create it.
       - `vi.useFakeTimers()`, render `<PlanVsActualCell editable onCellEdit={spy} planned={0} actual={null} delta={null} ... />`.
       - Fire 3 input events with values 10, 20, 30 within 100ms of each other.
       - `vi.advanceTimersByTime(599)` → expect spy NOT called.
       - `vi.advanceTimersByTime(2)` → expect spy called exactly once with `30`.
       - `vi.useRealTimers()` in afterEach.

    **2. `src/components/dialogs/__tests__/historic-edit-dialog.test.tsx` (TC-PS-005) — NEW**
       - Render `<HistoricEditDialog open targetMonthKey="2026-03" onConfirm={onConfirm} onCancel={onCancel} />` inside IntlProvider with stub `v5.historicEdit.*` keys.
       - Assert: dialog has `role="dialog"` + `aria-modal="true"`, body text contains "2026-03".
       - Press Enter → `onConfirm` called.
       - Press Escape → `onCancel` called.
       - Click Cancel button → `onCancel` called.
       - Click Confirm button → `onConfirm` called.

    **3. Extend `src/features/personas/__tests__/persona.context.test.tsx` (TC-PSN-003)**
       - If file does not exist, create it.
       - Render `<PersonaProvider>` wrapped in a fresh `QueryClientProvider`. Mock `useRouter` from `next/navigation`.
       - Call `setPersona({ kind: 'pm', personId: 'p-anna' })` inside a test component.
       - Assert: `router.push` called with `/pm`.
       - Assert: `queryClient.invalidateQueries` was called (spy via `vi.spyOn(queryClient, 'invalidateQueries')` before the call).

    **4. `src/components/timeline/__tests__/pm-timeline-cell.test.tsx` (TC-PR-001) — NEW**
       - Mock `usePersona` → PM persona with `homeDepartmentId: 'dept-A'`.
       - Mock target person with `departmentId: 'dept-B'` (out of dept).
       - Mock `useCreateProposal` to return `{ mutateAsync: vi.fn().mockResolvedValue({ id: 'prop-1' }) }`.
       - Render `<PmTimelineCell />` with a future-month cell.
       - Simulate typing 60 + blur; assert ProposalCell popover opens (`getByText('Submit wish')` or equivalent).
       - Click Submit; assert `useCreateProposal().mutateAsync` called with `{ projectId, personId, monthKey, proposedHours: 60, note: '' }`.
       - Assert: `fetch` to `/api/v5/planning/allocations/*` PATCH was NOT called (direct path did not fire).
  </action>
  <verify>
    <automated>pnpm vitest run src/components/timeline src/components/dialogs src/features/personas</automated>
  </verify>
  <done>
    - All 4 test files exist and pass
    - grep `advanceTimersByTime` src/components/timeline/__tests__/PlanVsActualCell.test.tsx returns a hit
    - grep `role="dialog"` OR `aria-modal` in src/components/dialogs/__tests__/historic-edit-dialog.test.tsx returns a hit
    - grep `invalidateQueries` src/features/personas/__tests__/persona.context.test.tsx returns a hit
    - grep `mutateAsync` src/components/timeline/__tests__/pm-timeline-cell.test.tsx returns a hit
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-22 PGlite e2e happy path — Anna /pm → click project → type 60 in Sara's June cell → wish in /pm/wishes</name>
  <files>src/features/planning/__tests__/pm.e2e.test.ts</files>
  <read_first>
    - src/features/proposals/__tests__/proposal.service.e2e.test.ts (full file — mirror the PGlite harness + seed helpers + drizzle assertions)
    - src/features/planning/planning.read.ts (getPmOverview + getPmTimeline from Plan 40-02)
    - src/features/allocations/allocation.service.ts (patchAllocation from Plan 40-01)
    - src/features/proposals/proposal.service.ts (createProposal)
    - .planning/phases/40-persona-views-part-1-pm/40-CONTEXT.md D-22
  </read_first>
  <behavior>
    Single `describe('D-22 Anna submits a wish happy path')` with ordered `it()` blocks that share a PGlite instance:
    1. Seed org + Anna (PM) + Sara (staff, different dept) + project with lead_pm_person_id=Anna
    2. `getPmOverview({ leadPmPersonId: anna.id })` returns the project card
    3. `getPmTimeline({ projectId })` returns Sara's row with a June cell
    4. Resolve edit gate for Anna editing Sara's June cell → 'proposal'
    5. `createProposal({ projectId, personId: sara.id, monthKey: '2026-06', proposedHours: 60, proposerId: anna.id })` succeeds
    6. `listProposals({ proposerId: anna.id, status: 'proposed' })` returns the new wish
    7. Re-query `getPmTimeline` → cell's `pendingProposal.proposedHours === 60`
  </behavior>
  <action>
    1. Copy the PGlite bootstrap block from `proposal.service.e2e.test.ts` (imports, `beforeAll` CREATE TABLE for organizations, people, projects, allocations, change_log, allocation_proposals, plus any enums).
    2. Seed fixtures inside a `beforeAll`:
       - org `org-1`
       - Anna: `people` row `{ id: 'p-anna', orgId, name: 'Anna', departmentId: 'dept-A', externalUserId: 'clerk-anna' }`
       - Sara: `people` row `{ id: 'p-sara', orgId, name: 'Sara', departmentId: 'dept-B' }`
       - Project: `{ id: 'proj-1', orgId, name: 'Alpha', leadPmPersonId: 'p-anna' }`
       - Allocation: `{ id: 'alloc-1', orgId, projectId: 'proj-1', personId: 'p-sara', monthKey: '2026-06', hours: 40 }`
       - Mock `getServerNowMonthKey` to return '2026-05' (so 2026-06 is non-historic).
    3. Define the Anna persona object literal: `{ kind: 'pm', personId: 'p-anna', homeDepartmentId: 'dept-A' }`.
    4. Write ordered tests per the `behavior` block. Use `resolveEditGate` directly (it is pure) to assert step 4 returns 'proposal'.
    5. Use `proposal.service.createProposal` for step 5 (not an HTTP call — this is a service-level e2e mirroring the Phase 39 style).
    6. Step 7: re-run `getPmTimeline` and assert `cells.find(c => c.personId === 'p-sara' && c.monthKey === '2026-06').pendingProposal?.proposedHours === 60`.
  </action>
  <verify>
    <automated>pnpm vitest run src/features/planning/__tests__/pm.e2e.test.ts</automated>
  </verify>
  <done>
    - Test file exists and passes
    - grep `D-22` OR `Anna submits a wish` in src/features/planning/__tests__/pm.e2e.test.ts returns a hit
    - grep `p-sara` + `2026-06` in the test returns hits for both
    - grep `pendingProposal` returns a hit asserting proposedHours === 60
  </done>
</task>

</tasks>

<verification>
- `pnpm vitest run` full suite green
- Every test code in VALIDATION.md "Per-Task Verification Map" has a corresponding test file (including the new UX-V5-03 pm-wishes row)
- D-22 happy path green
</verification>

<success_criteria>
- Phase 40 nyquist_compliant flag can flip to true in VALIDATION.md
- Full vitest suite passes
- Every roadmap TC-* code + UX-V5-03 in Phase 40 success criteria has a green test
</success_criteria>

<output>
After completion, create `.planning/phases/40-persona-views-part-1-pm/40-05-SUMMARY.md`
</output>
