---
phase: 40-persona-views-part-1-pm
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/features/allocations/allocation.service.ts
  - src/features/allocations/allocation.errors.ts
  - src/app/api/v5/planning/allocations/[id]/route.ts
  - src/features/allocations/__tests__/patch-allocation.contract.test.ts
  - src/app/api/v5/planning/allocations/__tests__/patch.contract.test.ts
autonomous: true
requirements:
  - UX-V5-11
  - HIST-01
must_haves:
  truths:
    - "PATCH /api/v5/planning/allocations/[id] exists and accepts { hours, confirmHistoric? }"
    - "Calling PATCH on a historic month without confirmHistoric:true returns 409 HistoricEditNotConfirmedError"
    - "Calling PATCH on a historic month with confirmHistoric:true writes a change_log row with action=ALLOCATION_HISTORIC_EDITED"
    - "Calling PATCH on a non-historic month writes change_log action=ALLOCATION_EDITED (not HISTORIC)"
  artifacts:
    - path: "src/app/api/v5/planning/allocations/[id]/route.ts"
      provides: "PATCH handler for single allocation row"
      exports: ["PATCH"]
    - path: "src/features/allocations/allocation.service.ts"
      provides: "patchAllocation function honoring confirmHistoric"
      contains: "patchAllocation"
    - path: "src/features/allocations/__tests__/patch-allocation.contract.test.ts"
      provides: "PGlite contract test TC-API-004 + TC-PS-006"
  key_links:
    - from: "allocation.service.patchAllocation"
      to: "getServerNowMonthKey + isHistoricPeriod"
      via: "shared drizzle tx"
      pattern: "getServerNowMonthKey"
    - from: "allocation.service.patchAllocation"
      to: "recordChange(ALLOCATION_HISTORIC_EDITED)"
      via: "change_log write inside tx"
      pattern: "ALLOCATION_HISTORIC_EDITED"
---

<objective>
Wave 0 backend gap fill. Research (40-RESEARCH.md Pitfalls 5 & 6, Open Questions 2) proved that `PATCH /api/v5/planning/allocations/[id]` does NOT exist, `confirmHistoric` is not plumbed anywhere, and `ALLOCATION_HISTORIC_EDITED` is enum-only and never written. Every Wave 1-4 test depends on this existing. Build it end-to-end with a PGlite contract test before any UI work.

Purpose: Unblock UX-V5-11 (historic edit dialog) and HIST-01 (soft-warn) at the API layer so the Wave 3 cell orchestrator has a real endpoint to hit.
Output: New PATCH route + service branch + contract test covering TC-API-004 and TC-PS-006.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-persona-views-part-1-pm/40-CONTEXT.md
@.planning/phases/40-persona-views-part-1-pm/40-RESEARCH.md
@.planning/phases/40-persona-views-part-1-pm/40-VALIDATION.md
@src/features/allocations/allocation.service.ts
@src/features/proposals/proposal.service.ts
@src/features/proposals/__tests__/proposal.service.e2e.test.ts
@src/lib/server/get-server-now-month-key.ts
@src/db/schema.ts

<interfaces>
<!-- The patchAllocation contract this plan introduces. Downstream Wave 1/3 consume this. -->

```ts
// src/features/allocations/allocation.service.ts — NEW export
export interface PatchAllocationArgs {
  orgId: string;
  actorPersonId: string;
  allocationId: string;
  hours: number;                 // 0..<person capacity>; server does not cap, only stores
  confirmHistoric?: boolean;     // must be true to edit a month < getServerNowMonthKey()
}

export interface PatchAllocationResult {
  allocation: { id: string; personId: string; projectId: string; monthKey: string; hours: number };
  changeLogAction: 'ALLOCATION_EDITED' | 'ALLOCATION_HISTORIC_EDITED';
}

export async function patchAllocation(args: PatchAllocationArgs): Promise<PatchAllocationResult>;
```

```ts
// src/features/allocations/allocation.errors.ts — NEW or EXTEND existing AppError hierarchy
export class HistoricEditNotConfirmedError extends AppError {
  readonly statusCode = 409;
  readonly code = 'HISTORIC_EDIT_NOT_CONFIRMED';
  constructor(public targetMonthKey: string, public nowMonthKey: string) {
    super(`Historic edit of ${targetMonthKey} requires confirmHistoric:true (server now=${nowMonthKey})`);
  }
}
```

```ts
// Existing helper this plan consumes — do NOT modify
// src/lib/server/get-server-now-month-key.ts
export function getServerNowMonthKey(tx?: DrizzleTx): Promise<string>; // 'YYYY-MM'

// src/features/proposals/edit-gate.ts — existing, shows the historic predicate
// isHistoricPeriod(month, nowMonthKey) => month < nowMonthKey
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add patchAllocation service branch + HistoricEditNotConfirmedError</name>
  <files>src/features/allocations/allocation.service.ts, src/features/allocations/allocation.errors.ts</files>
  <read_first>
    - src/features/allocations/allocation.service.ts (full file — understand _applyAllocationUpsertsInTx shared-tx pattern)
    - src/features/proposals/proposal.service.ts (approveProposal — canonical shared-tx + recordChange pattern from Phase 39)
    - src/lib/server/get-server-now-month-key.ts
    - src/db/schema.ts lines 200-300 (allocations + change_log enum)
    - .planning/phases/40-persona-views-part-1-pm/40-RESEARCH.md "Architecture Patterns" Pattern 3 + "Pitfall 5/6"
    - .planning/phases/40-persona-views-part-1-pm/40-CONTEXT.md D-15
  </read_first>
  <behavior>
    - Non-historic edit: patchAllocation({ hours: 40 }) succeeds, updates allocations.hours, writes change_log with action='ALLOCATION_EDITED' via='direct'
    - Historic edit without flag: patchAllocation({ hours: 40 }) on a month < server now throws HistoricEditNotConfirmedError; no allocation mutation; no change_log row
    - Historic edit with flag: patchAllocation({ hours: 40, confirmHistoric: true }) on a historic month succeeds and writes change_log with action='ALLOCATION_HISTORIC_EDITED'
    - All three branches share a single drizzle tx with the recordChange call (ADR-003 invariant)
  </behavior>
  <action>
    1. Create `src/features/allocations/allocation.errors.ts` with `HistoricEditNotConfirmedError` class extending the existing `AppError` base (import from wherever proposal.service.ts imports AppError — grep `proposal.service.ts` for `AppError`).
    2. In `src/features/allocations/allocation.service.ts` add `patchAllocation(args: PatchAllocationArgs): Promise<PatchAllocationResult>`:
       - Open a drizzle transaction via `await db.transaction(async (tx) => { ... })`.
       - Load the existing allocation row by id scoped to `orgId`; throw `NotFoundError` if missing.
       - Compute `const nowMonthKey = await getServerNowMonthKey(tx);`
       - Compute `const isHistoric = allocation.monthKey < nowMonthKey;` (string compare works for 'YYYY-MM').
       - If `isHistoric && !args.confirmHistoric` throw `new HistoricEditNotConfirmedError(allocation.monthKey, nowMonthKey)`.
       - Update `allocations.hours` to `args.hours` (use existing drizzle update pattern from `_applyAllocationUpsertsInTx`).
       - Call `recordChange(tx, { orgId, actorPersonId, action: isHistoric ? 'ALLOCATION_HISTORIC_EDITED' : 'ALLOCATION_EDITED', via: 'direct', entityType: 'allocation', entityId: args.allocationId, before: { hours: prev.hours }, after: { hours: args.hours } })` — mirror the exact `recordChange` call used by `approveProposal` in proposal.service.ts.
       - Return `{ allocation: { id, personId, projectId, monthKey, hours }, changeLogAction }`.
    3. Export `patchAllocation`, `HistoricEditNotConfirmedError`, `PatchAllocationArgs`, `PatchAllocationResult` from the service module barrel if one exists.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>
    - grep `patchAllocation` src/features/allocations/allocation.service.ts returns the new function signature
    - grep `HistoricEditNotConfirmedError` src/features/allocations/allocation.errors.ts returns the class
    - grep `ALLOCATION_HISTORIC_EDITED` src/features/allocations/allocation.service.ts returns at least one hit
    - `pnpm tsc --noEmit` passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH /api/v5/planning/allocations/[id] route</name>
  <files>src/app/api/v5/planning/allocations/[id]/route.ts</files>
  <read_first>
    - src/app/api/allocations/route.ts (existing GET pattern for allocations namespace)
    - src/app/api/v5/proposals/route.ts (canonical v5 route shape — zod body validation + AppError → HTTP mapping, if present; else src/app/api/v5/proposals/[id]/approve/route.ts)
    - src/features/allocations/allocation.service.ts (the patchAllocation function from Task 1)
    - .planning/phases/40-persona-views-part-1-pm/40-CONTEXT.md D-11 step 3, D-15
    - .planning/phases/40-persona-views-part-1-pm/40-RESEARCH.md "Architecture Patterns" + Pitfall 5
  </read_first>
  <behavior>
    - PATCH with body { hours: 40 } on a future month returns 200 with JSON `{ allocation, changeLogAction: 'ALLOCATION_EDITED' }`
    - PATCH with body { hours: 40 } on a historic month returns 409 with JSON `{ error: { code: 'HISTORIC_EDIT_NOT_CONFIRMED', targetMonthKey, nowMonthKey } }`
    - PATCH with body { hours: 40, confirmHistoric: true } on a historic month returns 200 with `changeLogAction: 'ALLOCATION_HISTORIC_EDITED'`
    - Malformed body (negative hours, missing hours) returns 400 ValidationError
  </behavior>
  <action>
    Create `src/app/api/v5/planning/allocations/[id]/route.ts` exporting a `PATCH` handler:
    1. `import { NextRequest, NextResponse } from 'next/server';`
    2. `import { z } from 'zod';`
    3. `import { auth } from '@clerk/nextjs/server';` (or whatever auth helper v5 routes already use — check an existing v5 route first).
    4. Define `const PatchBody = z.object({ hours: z.number().min(0), confirmHistoric: z.boolean().optional() });`
    5. Export `export async function PATCH(req: NextRequest, ctx: { params: Promise<{ id: string }> })`:
       - Resolve `{ id } = await ctx.params;`
       - Authenticate via existing auth helper; derive `orgId` + `actorPersonId` using the same pattern as an existing v5 route (grep `src/app/api/v5/proposals/**/route.ts` for the exact shape — mirror it).
       - Parse body with zod; on error return `NextResponse.json({ error: { code: 'VALIDATION_ERROR', issues: … } }, { status: 400 })`.
       - Try/catch around `patchAllocation({ orgId, actorPersonId, allocationId: id, hours, confirmHistoric })`:
         - On `HistoricEditNotConfirmedError` return `NextResponse.json({ error: { code: 'HISTORIC_EDIT_NOT_CONFIRMED', targetMonthKey: e.targetMonthKey, nowMonthKey: e.nowMonthKey } }, { status: 409 })`.
         - On `NotFoundError` return 404.
         - On unknown return 500.
       - On success return `NextResponse.json(result, { status: 200 })`.
    6. Do NOT put business logic in the route handler — all historic-period logic lives in the service from Task 1.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>
    - File exists with `export async function PATCH`
    - grep `HISTORIC_EDIT_NOT_CONFIRMED` src/app/api/v5/planning/allocations/[id]/route.ts returns a hit
    - grep `PatchBody` or equivalent zod schema returns a hit
    - `pnpm tsc --noEmit` passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PGlite contract test — TC-API-004 + TC-PS-006 (ALLOCATION_HISTORIC_EDITED write path)</name>
  <files>src/features/allocations/__tests__/patch-allocation.contract.test.ts</files>
  <read_first>
    - src/features/proposals/__tests__/proposal.service.e2e.test.ts (full file — canonical PGlite + drizzle test harness used across Phase 39)
    - src/features/allocations/allocation.service.ts (the patchAllocation added in Task 1)
    - src/db/schema.ts (allocations + change_log tables + enum)
    - .planning/phases/40-persona-views-part-1-pm/40-VALIDATION.md "Per-Task Verification Map"
    - .planning/phases/40-persona-views-part-1-pm/40-RESEARCH.md "Validation Architecture" table rows TC-API-004 + TC-PS-006
  </read_first>
  <behavior>
    Test file exports 4 `it()` blocks:
    1. `it('non-historic edit writes ALLOCATION_EDITED change_log row')`
    2. `it('historic edit without confirmHistoric throws HistoricEditNotConfirmedError and does NOT mutate allocation')`
    3. `it('historic edit with confirmHistoric:true succeeds and writes ALLOCATION_HISTORIC_EDITED change_log row with before/after hours')`
    4. `it('getServerNowMonthKey determines the cutoff - month equal to now is NOT historic')`
  </behavior>
  <action>
    Create `src/features/allocations/__tests__/patch-allocation.contract.test.ts`:
    1. Copy the PGlite bootstrap block from `proposal.service.e2e.test.ts` (imports, `beforeAll` with `CREATE TABLE` statements for `organizations`, `people`, `projects`, `allocations`, `change_log`, any enum DDL).
    2. Use `vi.mock('@/lib/server/get-server-now-month-key', () => ({ getServerNowMonthKey: vi.fn() }))` and in each test set `(getServerNowMonthKey as Mock).mockResolvedValue('2026-06')`.
    3. Seed one org, one person, one project, one allocation row at monthKey='2026-03' (historic vs '2026-06') and another at '2026-09' (future).
    4. Test 1: `patchAllocation({ allocationId: futureAllocId, hours: 80 })` → expect result.changeLogAction === 'ALLOCATION_EDITED' AND query `change_log` for a row with action='ALLOCATION_EDITED', entityId=futureAllocId, after.hours===80.
    5. Test 2: `await expect(patchAllocation({ allocationId: historicAllocId, hours: 80 })).rejects.toThrow(HistoricEditNotConfirmedError)`; then query allocations table and assert hours is still the original; query change_log and assert zero rows for that allocation.
    6. Test 3: `patchAllocation({ allocationId: historicAllocId, hours: 80, confirmHistoric: true })` → expect result.changeLogAction === 'ALLOCATION_HISTORIC_EDITED'; query change_log for a row with action='ALLOCATION_HISTORIC_EDITED', entityId=historicAllocId, before.hours === original, after.hours === 80.
    7. Test 4: Seed an allocation at monthKey='2026-06' (== server now) and assert patchAllocation without confirmHistoric succeeds (isHistoric uses strict `<`).
  </action>
  <verify>
    <automated>pnpm vitest run src/features/allocations/__tests__/patch-allocation.contract.test.ts</automated>
  </verify>
  <done>
    - All 4 `it()` blocks pass
    - grep `ALLOCATION_HISTORIC_EDITED` src/features/allocations/__tests__/patch-allocation.contract.test.ts returns at least 2 hits (assertion + seed expectation)
    - grep `HistoricEditNotConfirmedError` in the test file returns a hit
  </done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes
- `pnpm vitest run src/features/allocations/__tests__/patch-allocation.contract.test.ts` passes (4/4)
- grep `ALLOCATION_HISTORIC_EDITED` src/features/allocations/allocation.service.ts returns at least one hit (production write path now exists)
</verification>

<success_criteria>
- PATCH /api/v5/planning/allocations/[id] route live and zod-validated
- patchAllocation service honors confirmHistoric per ARCHITECTURE §616-627
- TC-API-004 + TC-PS-006 contract test green
- Wave 1-4 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/40-persona-views-part-1-pm/40-01-SUMMARY.md`
</output>
