---
phase: 39-proposal-approval-workflow
plan: 10
type: execute
wave: 6
depends_on: ["39-03", "39-04", "39-05", "39-06", "39-07", "39-08", "39-09"]
files_modified:
  - src/features/proposals/__tests__/proposal.service.e2e.test.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "End-to-end test covers: create wish → appears in line manager queue via dept filter → approve → allocation row created + dual change_log emitted → queue no longer shows the row"
    - "End-to-end test covers: create wish → reject with reason → resubmit → new proposed row in proposer's list with parent_proposal_id"
    - "End-to-end test covers: PROP-07 routing — create wish targeting person in dept A, move person to dept B, list by dept B returns the wish, approve by dept A user → ForbiddenError"
    - "Manual human verification: flip through the two pages (/line-manager/approval-queue, /wishes) in dev server with seed data"
  artifacts:
    - path: src/features/proposals/__tests__/proposal.service.e2e.test.ts
      provides: "Integration tests stitching all five service methods"
  key_links:
    - from: "e2e test"
      to: "all proposal service methods"
      via: "sequential calls in one test"
      pattern: "createProposal|approveProposal|rejectProposal|resubmitProposal|listProposals"
---

<objective>
Stitch the full proposal lifecycle into an end-to-end PGlite test, then human-verify the UI on a dev server. This is the final gate for Phase 39 before verification.

Purpose: prove the five service methods + routes + UI compose correctly in sequence; catch any integration regression across plans 39-02..39-08.
Output: e2e test file + human sign-off on both pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-proposal-approval-workflow/39-CONTEXT.md
@src/features/proposals/proposal.service.ts
@src/features/proposals/__tests__/proposal.service.approve.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end service lifecycle test</name>
  <files>src/features/proposals/__tests__/proposal.service.e2e.test.ts</files>
  <read_first>
    - src/features/proposals/__tests__/proposal.service.approve.test.ts (PGlite setup)
    - src/features/change-log/__tests__/change-log.service.test.ts (change_log query helpers)
  </read_first>
  <behavior>
    - Test 1 "full approve lifecycle": createProposal → listProposals({departmentId}) returns it → approveProposal → listProposals({departmentId, status:'proposed'}) no longer returns it → assert allocations row exists with proposed hours → assert change_log contains PROPOSAL_SUBMITTED + PROPOSAL_APPROVED + ALLOCATION_EDITED (context.via='proposal')
    - Test 2 "reject + resubmit lifecycle": createProposal → rejectProposal(reason='too many hours') → rejected row exists → resubmitProposal(newHours) → new proposed row with parent_proposal_id set → original rejected row unchanged → approve new row → allocations updated
    - Test 3 "PROP-07 department move": seed person in dept A → createProposal → move person to dept B (direct UPDATE on people.departmentId) → listProposals({departmentId: dept B, status:'proposed'}) returns the wish → approveProposal with callerClaimedDepartmentId=dept A → ForbiddenError → approveProposal with callerClaimedDepartmentId=dept B → success
    - Test 4 "concurrent create siblings, approve one": two createProposal calls for same (person, project, month) with different hours → approveProposal on first → second row marked 'superseded' → change_log has PROPOSAL_WITHDRAWN row with context.reason='superseded_by'
  </behavior>
  <action>
    1. Build the test file using the shared PGlite seeding helpers from existing proposal tests
    2. Each test is a single `test()` block that runs the full sequence of service calls
    3. Assertions query the DB directly via `db` to verify downstream state (allocations, change_log rows)
    4. For Test 3, directly UPDATE people.departmentId via tx to simulate the move (no service method for that yet — it's Phase 43)
  </action>
  <verify>
    <automated>npm test -- src/features/proposals/__tests__/proposal.service.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass
    - `grep -c "test\('" src/features/proposals/__tests__/proposal.service.e2e.test.ts` returns >= 4
    - `grep -n "PROP-07\|superseded_by\|via: 'proposal'" src/features/proposals/__tests__/proposal.service.e2e.test.ts` returns at least 3 matches
  </acceptance_criteria>
  <done>Lifecycle proven end to end at the service layer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — walk both pages in dev server</name>
  <what-built>
    - /line-manager/approval-queue with pending wishes, impact preview, approve/reject modal
    - /wishes page with tabs and resubmit flow
    - ag-grid proposal-mode editor on out-of-department cells
    - All strings in Swedish and English via persona switcher (if i18n switcher exists in header)
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Seed dummy data if not already present (use existing seed script under scripts/seed-demo.ts or similar)
    3. Switch persona to "PM" in the role header
    4. Navigate to a project/person grid for an out-of-department person
    5. Click a cell — a dashed-border editor should appear with Hours + Note + Submit wish + Cancel
    6. Type 40, note "Need this for Nordic delivery", click Submit wish
    7. Verify the cell now shows a "Pending" badge
    8. Switch persona to "Line Manager" (with the matching department)
    9. Navigate to /line-manager/approval-queue
    10. Verify the wish appears with an impact phrase "<name>'s <month> utilization Nh → Nh"
    11. Click Approve → row disappears from the queue
    12. Create another wish (repeat 4-7), this time go to the queue and click Reject
    13. Verify the modal has "A reason is required" + Confirm button disabled until text entered
    14. Type "Over allocated in that month" and click Confirm reject
    15. Switch back to PM, go to /wishes
    16. Verify three tabs visible (Proposed / Approved / Rejected)
    17. Click Rejected tab → see the rejected wish with "Edit & resubmit" button
    18. Click Edit & resubmit, modify hours, click Resubmit
    19. Switch to Proposed tab → new wish appears
    20. Visual check: dashed border is visible, Pending badge is visible, reject modal is accessible via keyboard (Tab navigates, Esc closes)
  </how-to-verify>
  <action>Manual human verification — walk through all 20 steps in the dev server. No automated action; this task gates the phase on visual + interactive correctness.</action>
  <verify><automated>echo "manual-checkpoint"</automated></verify>
  <done>User confirms all 20 steps pass with "approved".</done>
  <resume-signal>Type "approved" after verifying all 20 steps, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- e2e test file green
- Human verified all 20 steps
</verification>

<success_criteria>
- Full proposal lifecycle works end-to-end in one test
- Both pages function in the dev server as described
</success_criteria>

<output>
After completion, create `.planning/phases/39-proposal-approval-workflow/39-10-SUMMARY.md`.
</output>
