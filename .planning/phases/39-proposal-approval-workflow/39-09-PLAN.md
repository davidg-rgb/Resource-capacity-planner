---
phase: 39-proposal-approval-workflow
plan: 09
type: execute
wave: 4
depends_on: []
files_modified:
  - src/messages/sv/v5/proposals.json
  - src/messages/en/v5/proposals.json
  - src/features/proposals/ui/wish-card.tsx
  - src/features/proposals/ui/reject-modal.tsx
  - src/features/proposals/ui/approval-queue.tsx
  - src/features/proposals/ui/my-wishes-panel.tsx
  - src/components/grid/cell-editors/proposal-cell-editor.tsx
  - src/app/(app)/line-manager/approval-queue/page.tsx
  - src/app/(app)/wishes/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "All user-visible strings in proposal-flow UI are read via next-intl t('v5.proposals.<key>') — no hardcoded user-facing text"
    - "Swedish and English message files exist under src/messages/{sv,en}/v5/proposals.json with the same key set"
    - "Impact preview phrase is parameterized: t('v5.proposals.impactPhrase', { name, month, before, after })"
  artifacts:
    - path: src/messages/sv/v5/proposals.json
      provides: "Swedish proposal UI strings (primary)"
    - path: src/messages/en/v5/proposals.json
      provides: "English proposal UI strings (fallback)"
  key_links:
    - from: "All proposal UI components"
      to: "v5.proposals.* keys"
      via: "useTranslations('v5.proposals') from next-intl"
      pattern: "useTranslations"
---

<objective>
Extract all hardcoded English strings from the Phase 39 UI into next-intl message catalog entries under `v5.proposals.*`. Match the Phase 34 i18n infrastructure and Phase 38's `v5.import.*` key layout.

Purpose: D-18 locked decision — all user-visible strings keyed. This plan runs AFTER Plans 39-06/07/08 build the components with placeholder English, and sweeps through them to replace literals with `t('…')`.

**Note:** This plan can run in parallel with 39-10 (tests) because it only touches UI string literals, not logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/messages/sv/v5
@src/messages/en/v5
@src/messages/sv/v5/import.json
@src/features/proposals/ui
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sv + en catalog files with complete key set</name>
  <files>
    src/messages/sv/v5/proposals.json,
    src/messages/en/v5/proposals.json
  </files>
  <read_first>
    - src/messages/sv/v5/import.json (Phase 38 reference for nesting + parameterization style)
    - src/messages/en/v5/import.json
    - src/features/proposals/ui/wish-card.tsx, reject-modal.tsx, approval-queue.tsx, my-wishes-panel.tsx, src/components/grid/cell-editors/proposal-cell-editor.tsx (inventory all hardcoded English strings)
  </read_first>
  <action>
    Inventory ALL user-visible strings across the 5 UI files. Build a key set. Write both files. Example:

    `src/messages/en/v5/proposals.json`:
    ```json
    {
      "v5": {
        "proposals": {
          "page": {
            "approvalQueue": "Approval queue",
            "myWishes": "My wishes",
            "switchToLineManager": "Switch to the Line Manager persona to view the queue.",
            "notAuthenticated": "Not authenticated"
          },
          "tabs": {
            "proposed": "Proposed",
            "approved": "Approved",
            "rejected": "Rejected"
          },
          "card": {
            "statusLabel": "Status: {status}",
            "rejectedLabel": "Rejected: {reason}",
            "hoursMonth": "{hours}h · {month}"
          },
          "actions": {
            "approve": "Approve",
            "reject": "Reject",
            "editResubmit": "Edit & resubmit",
            "submit": "Submit wish",
            "cancel": "Cancel",
            "confirmReject": "Confirm reject",
            "resubmit": "Resubmit"
          },
          "editor": {
            "title": "Submit wish (out-of-department)",
            "notePlaceholder": "Optional note",
            "hoursLabel": "Hours",
            "noteLabel": "Note",
            "charCount": "{count}/1000"
          },
          "rejectModal": {
            "title": "Reject proposal",
            "reasonRequired": "A reason is required and visible to the proposer.",
            "reasonLabel": "Rejection reason"
          },
          "queue": {
            "loading": "Loading…",
            "empty": "No pending wishes.",
            "loadFailed": "Failed to load: {error}",
            "impactPhrase": "{name}'s {monthName} utilization {before}h → {after}h"
          },
          "wishes": {
            "emptyForTab": "No {tab} wishes."
          },
          "errors": {
            "proposalNotActive": "This proposal was already decided by someone else.",
            "forbidden": "You cannot approve this proposal — target department mismatch.",
            "reasonRequired": "A rejection reason is required.",
            "submitFailed": "Failed to submit wish."
          }
        }
      }
    }
    ```

    Then write the Swedish version `src/messages/sv/v5/proposals.json` with idiomatic Swedish translations. Month names stay in English for now (Phase 40/42 handles the full i18n calendar). Key examples:
    - page.approvalQueue → "Godkännandekö"
    - page.myWishes → "Mina önskemål"
    - tabs.proposed/approved/rejected → "Föreslagna" / "Godkända" / "Avvisade"
    - actions.approve → "Godkänn", actions.reject → "Avvisa", actions.submit → "Skicka önskemål"
    - actions.editResubmit → "Redigera & skicka igen"
    - queue.impactPhrase → "{name}s {monthName}-beläggning {before}h → {after}h"
    - queue.empty → "Inga väntande önskemål."
    - rejectModal.reasonRequired → "En anledning krävs och visas för den som skickade önskemålet."
    - errors.proposalNotActive → "Detta önskemål har redan hanterats av någon annan."

    Ensure both files have IDENTICAL nested key structure.
  </action>
  <verify>
    <automated>node -e "const en=require('./src/messages/en/v5/proposals.json'); const sv=require('./src/messages/sv/v5/proposals.json'); function keys(o,p=''){return Object.entries(o).flatMap(([k,v])=>typeof v==='object'?keys(v,p+k+'.'):[p+k])} const ek=keys(en).sort(); const sk=keys(sv).sort(); if(JSON.stringify(ek)!==JSON.stringify(sk)){console.error('KEY MISMATCH', ek.filter(x=>!sk.includes(x)), sk.filter(x=>!ek.includes(x))); process.exit(1)} console.log('OK', ek.length, 'keys')"</automated>
  </verify>
  <acceptance_criteria>
    - Both JSON files exist and are valid JSON (`node -e "JSON.parse(require('fs').readFileSync('...'))"` succeeds)
    - Key parity verified by the inline script (exits 0 and prints count)
    - At least 25 keys total per file
  </acceptance_criteria>
  <done>Catalog files in place.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded strings in all Phase 39 UI files with t('v5.proposals.*')</name>
  <files>
    src/features/proposals/ui/wish-card.tsx,
    src/features/proposals/ui/reject-modal.tsx,
    src/features/proposals/ui/approval-queue.tsx,
    src/features/proposals/ui/my-wishes-panel.tsx,
    src/components/grid/cell-editors/proposal-cell-editor.tsx,
    src/app/(app)/line-manager/approval-queue/page.tsx,
    src/app/(app)/wishes/page.tsx
  </files>
  <read_first>
    - An existing Phase 38 UI file that uses next-intl (e.g., the line-manager import wizard) — copy the `useTranslations('v5.proposals')` import and usage style
  </read_first>
  <action>
    For each file:
    1. Add `import { useTranslations } from 'next-intl';` at top
    2. Inside the component: `const t = useTranslations('v5.proposals');`
    3. Replace every hardcoded English string with `t('<relative-key>')` or `t('<key>', { …params })` for parameterized strings
    4. For the queue impact phrase, replace the `formatMonthLabel(...)` concatenation with a single `t('queue.impactPhrase', { name, monthName, before, after })` call
    5. Do NOT translate strings inside test files or console.error calls — those stay English

    Verify nothing was missed by grepping for common hardcoded literals after the edits:
    `grep -rn "'Approve'\\|'Reject'\\|'Submit wish'\\|'Cancel'\\|'Loading…'" src/features/proposals/ui/ src/components/grid/cell-editors/proposal-cell-editor.tsx src/app/\\(app\\)/line-manager/approval-queue src/app/\\(app\\)/wishes`
    — should return ZERO matches.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "useTranslations('v5.proposals')" src/features/proposals/ui/ src/components/grid/cell-editors/proposal-cell-editor.tsx src/app/\(app\)/line-manager/approval-queue src/app/\(app\)/wishes` returns at least 7 matches (one per file)
    - `grep -rn "'Approve'\|'Submit wish'\|'Approval queue'" src/features/proposals/ui/ src/app/\(app\)/line-manager/approval-queue src/app/\(app\)/wishes` returns ZERO matches (all strings now go through t())
    - `npm run typecheck` passes
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All Phase 39 UI user-visible strings are i18n-keyed.</done>
</task>

</tasks>

<verification>
- Both catalog files exist with identical key trees
- All Phase 39 UI files import useTranslations and use t()
- Build succeeds
</verification>

<success_criteria>
- D-18 locked decision satisfied
- No hardcoded user-facing English remains in Phase 39 UI
</success_criteria>

<output>
After completion, create `.planning/phases/39-proposal-approval-workflow/39-09-SUMMARY.md`.
</output>
