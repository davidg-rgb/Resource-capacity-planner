---
phase: 23-dashboard-framework
plan: 04
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - src/features/dashboard/widgets/kpi-cards-widget.tsx
  - src/features/dashboard/widgets/utilization-heatmap-widget.tsx
  - src/features/dashboard/widgets/discipline-progress-widget.tsx
  - src/features/dashboard/widgets/project-impact-widget.tsx
  - src/features/dashboard/widgets/strategic-alerts-widget.tsx
  - src/features/dashboard/widgets/department-bar-widget.tsx
  - src/features/dashboard/widgets/discipline-chart-widget.tsx
  - src/features/dashboard/widgets/index.ts
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/dashboard/dashboard-content.tsx
autonomous: false
requirements: [R23-09]

must_haves:
  truths:
    - "All 7 existing widgets are wrapped in WidgetDefinition format and registered"
    - "Dashboard page uses the layout engine instead of hardcoded grid"
    - "Existing widget appearance is preserved (no visual regression)"
    - "Edit mode is accessible from the dashboard page"
  artifacts:
    - path: "src/features/dashboard/widgets/index.ts"
      provides: "Barrel file that imports all widget wrappers and registers them"
      min_lines: 10
    - path: "src/features/dashboard/widgets/kpi-cards-widget.tsx"
      provides: "KPI Cards widget wrapper with WidgetProps adapter"
    - path: "src/features/dashboard/widgets/utilization-heatmap-widget.tsx"
      provides: "Utilization HeatMap widget wrapper"
    - path: "src/features/dashboard/widgets/department-bar-widget.tsx"
      provides: "Department Bar Chart widget wrapper with internal data hook"
    - path: "src/app/(app)/dashboard/dashboard-content.tsx"
      provides: "Refactored to use DashboardGrid layout engine"
  key_links:
    - from: "src/features/dashboard/widgets/index.ts"
      to: "src/features/dashboard/widget-registry.ts"
      via: "calls registerWidget() for each widget"
      pattern: "registerWidget"
    - from: "src/app/(app)/dashboard/dashboard-content.tsx"
      to: "src/features/dashboard/dashboard-layout-engine.tsx"
      via: "renders DashboardGrid component"
      pattern: "DashboardGrid"
    - from: "src/features/dashboard/widgets/department-bar-widget.tsx"
      to: "src/hooks/use-dashboard.ts"
      via: "calls useDepartmentUtilization internally"
      pattern: "useDepartmentUtilization"
---

<objective>
Wrap all 7 existing dashboard widgets in the WidgetDefinition registry format and refactor the dashboard page to use the layout engine.

Purpose: This connects the framework (Plan 01-02) to the existing UI. After this, the dashboard is fully dynamic and editable.
Output: 7 widget wrappers registered in the registry, dashboard page refactored to use DashboardGrid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-V4.md
@.planning/phases/23-dashboard-framework/23-RESEARCH.md
@.planning/phases/23-dashboard-framework/23-01-SUMMARY.md
@.planning/phases/23-dashboard-framework/23-02-SUMMARY.md

@src/features/dashboard/widget-registry.types.ts
@src/features/dashboard/widget-registry.ts
@src/features/dashboard/dashboard-layout-engine.tsx
@src/features/dashboard/dashboard-time-range.tsx
@src/components/charts/kpi-card.tsx
@src/components/charts/utilization-heat-map.tsx
@src/components/charts/discipline-progress.tsx
@src/components/charts/project-impact.tsx
@src/components/charts/strategic-alerts.tsx
@src/components/charts/department-bar-chart.tsx
@src/components/charts/discipline-chart.tsx
@src/app/(app)/dashboard/dashboard-content.tsx
@src/hooks/use-dashboard.ts
</context>

<interfaces>
<!-- Widget props interfaces from existing components -->

From src/components/charts/kpi-card.tsx:
```typescript
interface KPICardProps {
  title: string;
  value: string | number;
  subtitle?: string;
  href?: string;
  badge?: string;
  variant?: 'primary' | 'error' | 'outline';
}
```

From src/components/charts/department-bar-chart.tsx:
```typescript
interface DepartmentBarChartProps {
  data: DepartmentUtilization[];
}
```

From src/features/dashboard/widget-registry.types.ts:
```typescript
interface WidgetProps {
  timeRange: { from: string; to: string };
  config?: Record<string, unknown>;
  isEditMode: boolean;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create widget wrappers and register all 7 existing widgets</name>
  <files>
    src/features/dashboard/widgets/kpi-cards-widget.tsx,
    src/features/dashboard/widgets/utilization-heatmap-widget.tsx,
    src/features/dashboard/widgets/discipline-progress-widget.tsx,
    src/features/dashboard/widgets/project-impact-widget.tsx,
    src/features/dashboard/widgets/strategic-alerts-widget.tsx,
    src/features/dashboard/widgets/department-bar-widget.tsx,
    src/features/dashboard/widgets/discipline-chart-widget.tsx,
    src/features/dashboard/widgets/index.ts
  </files>
  <action>
Create adapter wrapper components that bridge existing chart components to the `WidgetProps` interface. Each wrapper:
1. Receives `WidgetProps` (timeRange, config, isEditMode)
2. Internally calls the appropriate data hook or passes demo data
3. Renders the existing component with its original props
4. Is wrapped in `React.memo` to avoid re-renders from edit mode changes (per research pitfall 6)

**Widget 1: `kpi-cards-widget.tsx`** (id: `'kpi-cards'`)
- Adapter: calls `useDashboardKPIs(timeRange.from, timeRange.to)` internally
- Renders the 4 KPICard components in a grid (reproducing the current layout)
- `defaultColSpan: 12`, `minColSpan: 6`, category: `'health-capacity'`
- Icon: `LayoutDashboard` from lucide-react
- supportedDashboards: `['manager', 'project-leader']`

**Widget 2: `utilization-heatmap-widget.tsx`** (id: `'utilization-heatmap'`)
- Adapter: renders `UtilizationHeatMap` directly (uses DEMO_DATA internally)
- `defaultColSpan: 12`, `minColSpan: 6`, category: `'health-capacity'`
- Icon: `Grid3X3` from lucide-react
- supportedDashboards: `['manager']`

**Widget 3: `discipline-progress-widget.tsx`** (id: `'discipline-progress'`)
- Adapter: renders `DisciplineProgress` directly (uses DEMO_DATA internally)
- `defaultColSpan: 4`, `minColSpan: 4`, category: `'breakdowns'`
- Icon: `PieChart` from lucide-react
- supportedDashboards: `['manager']`

**Widget 4: `project-impact-widget.tsx`** (id: `'project-impact'`)
- Adapter: renders `ProjectImpact` directly (uses DEMO_DATA internally)
- `defaultColSpan: 4`, `minColSpan: 4`, category: `'breakdowns'`
- Icon: `TrendingUp` from lucide-react
- supportedDashboards: `['manager']`

**Widget 5: `strategic-alerts-widget.tsx`** (id: `'strategic-alerts'`)
- Adapter: renders `StrategicAlerts` directly (hardcoded demo)
- `defaultColSpan: 12`, `minColSpan: 6`, category: `'alerts-actions'`
- Icon: `AlertTriangle` from lucide-react
- supportedDashboards: `['manager']`

**Widget 6: `department-bar-widget.tsx`** (id: `'department-bar-chart'`)
- Adapter: calls `useDepartmentUtilization(timeRange.from, timeRange.to)` internally, passes data to `DepartmentBarChart`
- Handles loading state (skeleton) and error state inline
- `defaultColSpan: 6`, `minColSpan: 4`, category: `'breakdowns'`
- Icon: `BarChart3` from lucide-react
- supportedDashboards: `['manager']`

**Widget 7: `discipline-chart-widget.tsx`** (id: `'discipline-chart'`)
- Adapter: calls `useDisciplineBreakdown(timeRange.from, timeRange.to)` internally, passes data to `DisciplineChart`
- Handles loading/error states inline
- `defaultColSpan: 6`, `minColSpan: 4`, category: `'breakdowns'`
- Icon: `PieChart` from lucide-react (or `Donut` if available)
- supportedDashboards: `['manager']`

**Barrel file: `index.ts`:**
- Import all 7 widget wrapper modules (side-effect imports trigger registration)
- Each widget file calls `registerWidget()` at module level with its WidgetDefinition
- Export nothing (side-effect only barrel)
- Add a comment: `// Import this file once in the app to register all built-in widgets`
  </action>
  <verify>
    <automated>cd "D:/Kod Projekt/Resurs & Projektplanering" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - 7 widget wrapper files exist in src/features/dashboard/widgets/
    - Each calls registerWidget() with correct WidgetDefinition
    - index.ts barrel imports all 7 for side-effect registration
    - All widgets use React.memo for their inner component
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor dashboard page to use layout engine</name>
  <files>
    src/app/(app)/dashboard/dashboard-content.tsx,
    src/app/(app)/dashboard/page.tsx
  </files>
  <action>
**Step 1:** Modify `src/app/(app)/dashboard/dashboard-content.tsx`:

Replace the entire hardcoded widget layout with the DashboardGrid component:
- Import widget registrations: `import '@/features/dashboard/widgets'` (side-effect import to ensure registration)
- Import `DashboardGrid` from `@/features/dashboard/dashboard-layout-engine`
- Import `TimeRangeProvider` from `@/features/dashboard/dashboard-time-range`
- Remove all direct widget imports (KPICard, UtilizationHeatMap, etc.)
- Remove direct data hook calls (useDashboardKPIs, useDepartmentUtilization, useDisciplineBreakdown)
- Remove the manual TIME_RANGES selector and manual grid layout
- New component body:

```tsx
export function DashboardContent() {
  return (
    <TimeRangeProvider>
      <DashboardGrid dashboardId="manager" />
    </TimeRangeProvider>
  );
}
```

The TimeRangeProvider handles the time range state (previously managed inline). The DashboardGrid handles everything else via the layout engine + registry.

**Step 2:** Update `src/app/(app)/dashboard/page.tsx`:

Keep existing page structure (Breadcrumbs, h1, Suspense). The DashboardContent component interface hasn't changed so minimal changes needed. However:
- Remove the "Team Overview" link card at the bottom (it will become accessible through the widget drawer or sidebar)
- OR keep it as-is if it serves as navigation (use judgment — if it is useful navigation, keep it below the grid)

Ensure the Suspense fallback still works correctly with the new DashboardContent.
  </action>
  <verify>
    <automated>cd "D:/Kod Projekt/Resurs & Projektplanering" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - dashboard-content.tsx uses DashboardGrid instead of hardcoded widget layout
    - Widget registration barrel import present
    - TimeRangeProvider wraps the grid
    - No direct widget imports remain in dashboard-content.tsx
    - Dashboard page renders the layout engine
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete dashboard framework</name>
  <files>none</files>
  <action>
Human verifies the complete dashboard framework works end-to-end. All implementation is done in Tasks 1-2 and Plans 01-03. This checkpoint confirms visual correctness and interactive behavior.
  </action>
  <verify>
    <automated>cd "D:/Kod Projekt/Resurs & Projektplanering" && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>User approves the dashboard framework after visual inspection.</done>
  <what-built>Complete dashboard framework: widget registry, layout engine with drag-and-drop, edit mode (drawer, resize, remove), layout persistence, Person 360 overlay, and all 7 existing widgets wrapped in registry format. The dashboard page now uses the dynamic layout engine instead of hardcoded widgets.</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000/dashboard
    2. Verify all existing widgets render correctly (KPI cards, heat map, charts, alerts) — no visual regression
    3. Click the pencil/edit icon in top-right — edit mode should activate showing drag handles, resize handles, and remove buttons on each widget
    4. Open the widget drawer — verify it lists all 7 widgets grouped by category
    5. Drag a widget to reorder — verify it moves and the layout updates
    6. Click resize handle on a widget — verify colSpan cycles (e.g., 6 -> 12 -> 4)
    7. Remove a widget — verify it disappears from the grid
    8. Click "Done" to exit edit mode — verify chrome disappears
    9. Refresh the page — verify the layout persists (saved to DB)
    10. If a person name is visible anywhere, click it to test Person 360 slide-out panel — verify it opens from the right with person data, closes on Escape/backdrop click
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes
- `pnpm build` succeeds without errors
- Dashboard renders with all 7 widgets via layout engine
- Edit mode fully functional (drag, resize, remove, drawer)
- Layout persists across page refresh
- Person 360 panel accessible app-wide
</verification>

<success_criteria>
- Zero visual regression: existing dashboard looks identical in view mode
- All 7 widgets registered and renderable via the registry
- Edit mode provides full dashboard customization
- Framework is ready for Phase 25+ to add new widgets (just create component + register)
</success_criteria>

<output>
After completion, create `.planning/phases/23-dashboard-framework/23-04-SUMMARY.md`
</output>
