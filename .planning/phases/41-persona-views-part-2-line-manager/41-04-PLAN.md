---
phase: 41-persona-views-part-2-line-manager
plan: 04
type: execute
wave: 3
depends_on: ["41-01"]
files_modified:
  - src/features/proposals/ui/approval-queue.tsx
  - src/features/proposals/ui/wish-card.tsx
  - src/features/proposals/__tests__/approval-queue-impact.test.tsx
  - src/components/change-log/change-log-feed.tsx
  - src/components/change-log/__tests__/change-log-feed.test.tsx
  - src/app/(app)/admin/change-log/page.tsx
autonomous: true
requirements: [UX-V5-06, UX-V5-10]

must_haves:
  truths:
    - "Approval queue wish card shows `Sara's June utilization 40% → 90%` (literal % symbols)"
    - "Counter-proposal button is absent from the approval queue"
    - "Change log feed at /admin/change-log renders a filterable paginated table"
    - "PM persona default filter pre-populates proposerId; Line Manager default pre-populates departmentId; Staff default pre-populates personId"
    - "Filter bar syncs to URL query params"
  artifacts:
    - path: "src/components/change-log/change-log-feed.tsx"
      provides: "Change log feed table + filter bar + expandable diff rows"
      min_lines: 80
    - path: "src/app/(app)/admin/change-log/page.tsx"
      provides: "Change log feed route"
  key_links:
    - from: "src/features/proposals/ui/wish-card.tsx"
      to: "ProposalImpactDTO.currentUtilizationPct"
      via: "impactText template"
      pattern: "currentUtilizationPct|projectedUtilizationPct|utilization.*%"
    - from: "src/app/(app)/admin/change-log/page.tsx"
      to: "/api/v5/change-log"
      via: "useQuery fetch"
      pattern: "/api/v5/change-log|\\['change-log'"
---

<objective>
Wave 3 — Approval queue impact preview wording update (UX-V5-06) and change log feed UI (UX-V5-10). Both consume Wave 0 outputs (extended ProposalImpactDTO and getFeed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md
@.planning/phases/41-persona-views-part-2-line-manager/41-RESEARCH.md
@.planning/phases/41-persona-views-part-2-line-manager/41-01-PLAN.md
@src/features/proposals/ui/approval-queue.tsx
@src/features/proposals/ui/wish-card.tsx
@src/features/proposals/use-proposals.ts
@src/features/change-log/change-log.types.ts
@src/features/personas/persona.context.tsx
@.planning/REQUIREMENTS.md

<interfaces>
From src/features/proposals/use-proposals.ts (extended in Wave 0):
```typescript
export interface ProposalImpactDTO {
  personMonthPlannedBefore: number;
  personMonthPlannedAfter: number;
  currentUtilizationPct: number;      // NEW from Wave 0
  projectedUtilizationPct: number;    // NEW from Wave 0
}
```

Required wording (REQUIREMENTS L45): `"${personName}'s ${monthLabel} utilization ${currentPct}% → ${projectedPct}%"`
GET /api/v5/change-log returns `{ entries: ChangeLogEntry[], nextCursor: string | null }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update WishCard impact preview wording to use utilization percentages</name>
  <files>
    src/features/proposals/ui/approval-queue.tsx,
    src/features/proposals/ui/wish-card.tsx,
    src/features/proposals/__tests__/approval-queue-impact.test.tsx
  </files>
  <read_first>
    - src/features/proposals/ui/approval-queue.tsx (existing useProposalImpact wiring ~L86-96)
    - src/features/proposals/ui/wish-card.tsx (existing impactText prop)
    - src/features/proposals/use-proposals.ts (extended DTO from Wave 0)
    - .planning/REQUIREMENTS.md L45 (exact wording template)
    - .planning/phases/41-persona-views-part-2-line-manager/41-RESEARCH.md Pitfall 2
  </read_first>
  <behavior>
    - WishCard renders impactText as `Sara's June utilization 40% → 90%` (literal % + arrow)
    - Uses `currentUtilizationPct` + `projectedUtilizationPct` from extended DTO (Wave 0)
    - Counter-proposal button is NOT rendered (per UX-V5-06 — verify it never was)
    - TC-PR-004..009: approve calls mutation, reject opens modal, impact text renders correct format
  </behavior>
  <action>
    1. In `approval-queue.tsx` ensure the existing `useProposalImpact` hook result is passed to `wish-card.tsx` with the new percent fields.

    2. Update `wish-card.tsx`:
       - Replace the existing hours-based impact string construction with the REQUIREMENTS L45 template
       - Prefer i18n: `t('v5.proposals.queue.impactPhrase', { name: personName, month: monthLabel, current: impact.currentUtilizationPct, projected: impact.projectedUtilizationPct })`
       - The actual i18n key value (Swedish/English strings) lands in Wave 4; here, update the template variable interpolation and ensure the keys referenced match what Wave 4 will add
       - If impact is loading, show a small skeleton; if error, hide the line
       - Verify no `CounterProposal`/`counterProposal` references exist; remove if present

    3. RTL test `approval-queue-impact.test.tsx` covering TC-PR-004..009:
       - TC-PR-004: wish card shows `40% → 90%` format when impact resolves
       - TC-PR-005: approve button calls mutation with proposalId
       - TC-PR-006: reject button opens reject modal
       - TC-PR-007: counter-proposal button absent (query by name returns null)
       - TC-PR-008: loading state shows skeleton
       - TC-PR-009: error state hides impact line but keeps approve/reject
  </action>
  <verify>
    <automated>pnpm vitest run src/features/proposals/__tests__/approval-queue-impact.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "currentUtilizationPct\|projectedUtilizationPct" src/features/proposals/ui/wish-card.tsx` matches both
    - `grep "v5.proposals.queue.impactPhrase" src/features/proposals/ui/wish-card.tsx` matches
    - `grep "counterProposal\|CounterProposal" src/features/proposals/ui/wish-card.tsx` returns nothing (UX-V5-06 explicit absence)
    - `grep "TC-PR-004\|TC-PR-005\|TC-PR-006\|TC-PR-007\|TC-PR-008\|TC-PR-009" src/features/proposals/__tests__/approval-queue-impact.test.tsx` matches all six
    - `grep "40%.*90%\|40% → 90%" src/features/proposals/__tests__/approval-queue-impact.test.tsx` matches
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>WishCard renders utilization percent preview per REQUIREMENTS L45; TC-PR-004..009 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: change-log-feed component (table + filter bar + expandable diff rows)</name>
  <files>
    src/components/change-log/change-log-feed.tsx,
    src/components/change-log/__tests__/change-log-feed.test.tsx
  </files>
  <read_first>
    - src/features/change-log/change-log.types.ts (ChangeLogEntry shape)
    - src/features/change-log/change-log.read.ts (Wave 0 getFeed)
    - .planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md D-14
    - .planning/v5.0-ARCHITECTURE.md §417 (component location contract)
  </read_first>
  <behavior>
    - Renders a table with columns: time | actor | entity | action | target | summary
    - Row click expands an inline panel showing `previousValue → newValue` JSON diff
    - Filter bar above: project multi-select, person multi-select, period (from/to date inputs), actor multi-select, entity + action dropdown behind "Advanced"
    - Filters sync to URL query params via useSearchParams + router.replace
    - "Load more" button fetches next page using nextCursor
    - Accepts initial filter via props (used by the page to inject persona-scoped default)
  </behavior>
  <action>
    Create `change-log-feed.tsx` as a client component:
      - Props: `{ initialFilter: FeedFilter; projects: ProjectLite[]; people: PersonLite[] }`
      - Local state: `filter` (seeded from initialFilter + URL params), `expandedId`
      - useInfiniteQuery on `['change-log', filterHash(filter)]` hitting `/api/v5/change-log?...`
      - filterHash is a stable JSON stringify of filter
      - Render filter bar with controlled inputs + onChange updates both state and URL params (router.replace)
      - Render table rows; clicking a row toggles `expandedId`
      - Expanded panel renders `<pre>{JSON.stringify(entry.previousValue, null, 2)}</pre>` → `<pre>{JSON.stringify(entry.newValue, null, 2)}</pre>`

    RTL tests in `__tests__/change-log-feed.test.tsx`:
      - Renders fetched rows
      - Expanding a row shows previousValue and newValue content
      - Changing the entity filter triggers refetch and URL update
      - "Load more" calls next page with nextCursor
      - initialFilter is applied on first render (department pre-selected)
  </action>
  <verify>
    <automated>pnpm vitest run src/components/change-log/__tests__/change-log-feed.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useInfiniteQuery\|nextCursor" src/components/change-log/change-log-feed.tsx` matches
    - `grep "\\['change-log'" src/components/change-log/change-log-feed.tsx` matches (D-19 key)
    - `grep "useSearchParams\|router.replace" src/components/change-log/change-log-feed.tsx` matches (URL sync)
    - `grep "previousValue\|newValue" src/components/change-log/change-log-feed.tsx` matches
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>Change log feed component renders, filters, paginates, and URL-syncs with RTL coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /admin/change-log route with persona-scoped default filter</name>
  <files>
    src/app/(app)/admin/change-log/page.tsx
  </files>
  <read_first>
    - src/features/personas/persona.context.tsx (usePersona)
    - src/components/change-log/change-log-feed.tsx (Task 2)
    - .planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md D-02
  </read_first>
  <behavior>
    - /admin/change-log renders the feed
    - When active persona is PM → initialFilter.proposerId = persona.personId (mapped to actorPersonaIds filter where applicable, or personIds for now)
    - When active persona is line-manager → initialFilter.departmentId (mapped via all people in that dept, or pass as a new filter field — use personIds of all dept members)
    - When active persona is staff → initialFilter.personIds = [persona.personId]
    - When active persona is r&d or admin → no default filter
    - Page is accessible to all personas (read-only history)
  </behavior>
  <action>
    Create `src/app/(app)/admin/change-log/page.tsx`:
      - 'use client'
      - `const persona = usePersona();`
      - Compute `initialFilter` via a small `buildPersonaDefault(persona)` helper inline:
        - `pm` → `{ actorPersonaIds: [\`pm:${persona.personId}\`] }` (or whatever persona-id shape the actor column uses — verify)
        - `line-manager` → fetch dept members once and set `personIds` accordingly, OR use a new top-level `departmentId` filter if getFeed supports it (Wave 0 supports `personIds`; keep it simple — fetch dept members via /api/departments/:id/people if available, else pass empty and document as best-effort)
        - `staff` → `{ personIds: [persona.personId] }`
        - `r&d` / `admin` → `{}`
      - Mount `<ChangeLogFeed initialFilter={initialFilter} projects={[]} people={[]} />` (projects/people lists can be fetched inside the feed component or passed; keep the page minimal)
      - Title from i18n `v5.changeLog.title`
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ChangeLogFeed" src/app/(app)/admin/change-log/page.tsx` matches
    - `grep "usePersona" src/app/(app)/admin/change-log/page.tsx` matches
    - `grep "pm\|line-manager\|staff" src/app/(app)/admin/change-log/page.tsx` matches (persona branches)
    - `grep "initialFilter" src/app/(app)/admin/change-log/page.tsx` matches
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>/admin/change-log route renders feed with persona-scoped default filter.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` green
- All task verify commands pass
- WishCard wording matches REQUIREMENTS L45
</verification>

<success_criteria>
- UX-V5-06 impact preview renders literal `% → %`
- UX-V5-10 change log feed filterable with persona-scoped defaults
- TC-PR-004..009 covered
</success_criteria>

<output>
After completion, create `.planning/phases/41-persona-views-part-2-line-manager/41-04-SUMMARY.md`.
</output>
