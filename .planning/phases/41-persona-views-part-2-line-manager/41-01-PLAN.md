---
phase: 41-persona-views-part-2-line-manager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/capacity/capacity.types.ts
  - src/features/capacity/capacity.read.ts
  - src/features/capacity/__tests__/capacity.read.test.ts
  - src/features/change-log/change-log.types.ts
  - src/features/change-log/change-log.read.ts
  - src/features/change-log/__tests__/change-log.read.test.ts
  - src/app/api/v5/capacity/route.ts
  - src/app/api/v5/capacity/__tests__/capacity.contract.test.ts
  - src/app/api/v5/change-log/route.ts
  - src/app/api/v5/change-log/__tests__/change-log.contract.test.ts
  - src/components/responsive/desktop-only-screen.tsx
  - src/features/proposals/use-proposals.ts
  - src/app/api/v5/proposals/[id]/impact/route.ts
  - src/features/proposals/__tests__/proposal-impact-utilization.test.ts
  - src/app/api/v5/planning/allocations/route.ts
  - src/features/planning/planning.read.ts
  - src/features/planning/__tests__/group-timeline.test.ts
  - src/features/personas/persona.context.tsx
autonomous: true
requirements: [UX-V5-04, UX-V5-05, UX-V5-06, UX-V5-10]

must_haves:
  truths:
    - "capacity.read.getPersonMonthUtilization returns v5 thresholds (over>100, under<60, ok 60-100, absent on targetHours===0)"
    - "change-log.read.getFeed paginates via composite (createdAt, id) cursor with default limit 50 / max 200"
    - "GET /api/v5/capacity returns { cells, people } and 400s when departmentId missing or monthRange > 24"
    - "GET /api/v5/change-log returns { entries, nextCursor } filterable by entity/action/actor/dateRange"
    - "ProposalImpactDTO carries currentUtilizationPct and projectedUtilizationPct computed via capacity.read"
    - "GET /api/v5/planning/allocations?scope=line-manager&departmentId=... returns per-person per-project breakdown"
    - "persona.context.tsx exposes department list fetched from /api/departments for the line-manager persona picker"
  artifacts:
    - path: "src/features/capacity/capacity.read.ts"
      provides: "getPersonMonthUtilization + getCapacityBreakdown"
      exports: ["getPersonMonthUtilization", "getCapacityBreakdown"]
    - path: "src/features/change-log/change-log.read.ts"
      provides: "getFeed with cursor pagination"
      exports: ["getFeed"]
    - path: "src/app/api/v5/capacity/route.ts"
      provides: "GET capacity endpoint"
    - path: "src/app/api/v5/change-log/route.ts"
      provides: "GET change-log feed endpoint"
    - path: "src/components/responsive/desktop-only-screen.tsx"
      provides: "DesktopOnlyScreen interstitial"
  key_links:
    - from: "src/app/api/v5/capacity/route.ts"
      to: "src/features/capacity/capacity.read.ts"
      via: "getPersonMonthUtilization"
      pattern: "getPersonMonthUtilization"
    - from: "src/app/api/v5/proposals/[id]/impact/route.ts"
      to: "src/features/capacity/capacity.read.ts"
      via: "getCapacityBreakdown"
      pattern: "currentUtilizationPct|projectedUtilizationPct"
    - from: "src/app/api/v5/planning/allocations/route.ts"
      to: "src/features/planning/planning.read.ts"
      via: "scope=line-manager branch"
      pattern: "scope.*line-manager|getGroupTimeline"
---

<objective>
Wave 0 backend gap fill for Phase 41. Creates the capacity + change-log read models, new v5 API routes, extends the existing proposal impact endpoint to return utilization percentages (not just hours), extends the planning allocations route with scope=line-manager, adds the getGroupTimeline planning helper, wires the real department dropdown into the persona switcher, and ships the DesktopOnlyScreen primitive. All UI waves (1-4) depend on this.

Purpose: unblock LM Home heatmap, group timeline, approval queue impact preview, and change-log feed.
Output: contract-tested backend + shared primitive ready for Waves 1-4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md
@.planning/phases/41-persona-views-part-2-line-manager/41-RESEARCH.md
@.planning/phases/41-persona-views-part-2-line-manager/41-VALIDATION.md
@.planning/v5.0-ARCHITECTURE.md
@src/features/actuals/actuals.read.ts
@src/features/planning/planning.read.ts
@src/features/change-log/change-log.service.ts
@src/features/change-log/change-log.types.ts
@src/features/proposals/use-proposals.ts
@src/app/api/v5/proposals/[id]/impact/route.ts
@src/app/api/v5/planning/allocations/route.ts
@src/db/schema.ts
@src/features/personas/persona.context.tsx

<override>
**CONTEXT D-12 ag-grid master/detail override (research-validated).**
`ag-grid-community ^35.2.0` does NOT include master/detail. Phase 41 uses a flat row model with synthetic per-project child rows interleaved after each person row, tracked via a `Set<personId>` expand state and dual-namespace `getRowId` (`person:${id}` vs `project:${personId}:${projectId}`). This plan (Wave 0) only needs to produce the DATA shape that supports this — the UI decision is implemented in Wave 2.

**Impact preview is EXTENDED, not duplicated.** `useProposalImpact` + `GET /api/v5/proposals/[id]/impact` + `WishCard.impactText` already exist from Phase 39. This plan extends `ProposalImpactDTO` with `currentUtilizationPct: number` and `projectedUtilizationPct: number` and updates the endpoint to compute them via `getCapacityBreakdown`. Do NOT create a new impact endpoint.
</override>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: capacity.read + v5 types + PGlite tests</name>
  <files>
    src/features/capacity/capacity.types.ts,
    src/features/capacity/capacity.read.ts,
    src/features/capacity/__tests__/capacity.read.test.ts
  </files>
  <read_first>
    - src/features/actuals/actuals.read.ts (mirror query shape + orgId-first signature)
    - src/db/schema.ts (allocations, people.target_hours_per_month, absences? verify absence)
    - src/lib/date-utils.ts (generateMonthRange)
    - .planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md D-04..D-08
    - .planning/phases/41-persona-views-part-2-line-manager/41-RESEARCH.md "Capacity read v5 thresholds" example
  </read_first>
  <behavior>
    - Test TC-CP-001: pct=59 classifies as 'under'
    - Test TC-CP-002: pct=60 and pct=100 classify as 'ok'
    - Test TC-CP-003: pct=101 classifies as 'over'
    - Test TC-CP-004: targetHours=0 classifies as 'absent' regardless of planned
    - Approved-only: pending proposal in same person/month does NOT affect plannedHours (D-07)
    - targetHours fallback 160 flagged as targetIsDefault=true when people.target_hours_per_month is null
    - getCapacityBreakdown({ scope:'person', scopeId, monthKey }) returns per-project rows for that person/month
  </behavior>
  <action>
    Create `capacity.types.ts` exporting:
      export type CapacityStatus = 'under' | 'ok' | 'over' | 'absent';
      export interface UtilizationCell { personId: string; monthKey: string; plannedHours: number; targetHours: number; targetIsDefault: boolean; utilizationPct: number; status: CapacityStatus; }
      export interface BreakdownRow { projectId: string; projectName: string; hours: number; }
      export interface UtilizationMap { cells: UtilizationCell[]; people: Array<{ id: string; name: string; departmentId: string | null }>; }

    Create `capacity.read.ts` exporting:
      - `getPersonMonthUtilization({ orgId, departmentId?, monthRange }: { orgId: string; departmentId?: string; monthRange: { start: string; end: string } }): Promise<UtilizationMap>`
      - `getCapacityBreakdown({ orgId, scope, scopeId, monthKey }: { orgId: string; scope: 'person'; scopeId: string; monthKey: string }): Promise<BreakdownRow[]>`
    Implementation:
      - Pull people (filtered by departmentId when provided) with target_hours_per_month; fallback to 160 and set targetIsDefault=true when null.
      - Sum approved allocations (inner join, no proposals) grouped by (personId, monthKey) across generateMonthRange(start, end).
      - classify() per D-05. Never emit 'absent' unless targetHours===0 (absences table not present per research).
      - Return zero-filled cells for person/month combinations with no allocations (plannedHours=0).
    Tests (PGlite, mirror src/features/actuals/__tests__ harness):
      - TC-CP-001..004 boundary tests as listed above
      - Pending-proposal-ignored test
      - targetIsDefault fallback test
      - getCapacityBreakdown returns rows sorted by hours desc
    Per D-04, D-05, D-06, D-07, D-08.
  </action>
  <verify>
    <automated>pnpm vitest run src/features/capacity/__tests__/capacity.read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "getPersonMonthUtilization" src/features/capacity/capacity.read.ts` matches
    - `grep -r "getCapacityBreakdown" src/features/capacity/capacity.read.ts` matches
    - `grep "TC-CP-001\|TC-CP-002\|TC-CP-003\|TC-CP-004" src/features/capacity/__tests__/capacity.read.test.ts` matches all four
    - `pnpm tsc --noEmit` green
    - No import from `@/features/analytics` or `@/components/heat-map` in capacity.read.ts
  </acceptance_criteria>
  <done>Capacity read model lands with v5 thresholds; PGlite tests pass including approved-only assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: change-log.read.getFeed + cursor pagination + PGlite tests</name>
  <files>
    src/features/change-log/change-log.types.ts,
    src/features/change-log/change-log.read.ts,
    src/features/change-log/__tests__/change-log.read.test.ts
  </files>
  <read_first>
    - src/features/change-log/change-log.service.ts (writer pattern, recordChange)
    - src/features/change-log/change-log.types.ts (extend, do not rewrite)
    - src/features/change-log/change-log.schema.ts
    - src/db/schema.ts lines around `change_log` table (verify columns: entity, entityId, actorPersonaId, previousValue, newValue, context, createdAt)
    - .planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md D-09, D-10
    - .planning/phases/41-persona-views-part-2-line-manager/41-RESEARCH.md "Change-log feed cursor" + Pitfalls 5 & 8
  </read_first>
  <behavior>
    - Composite cursor (createdAt, id) — 3 rows with identical createdAt all visited (no drift, per Pitfall 5)
    - limit default 50, max 200 (request limit=500 clamps to 200)
    - Filters: entity, action, actorPersonaId, dateRange {from,to}
    - Project/person filters implemented via JSONB `context->>'projectId'` / `context->>'personId'` extraction (best-effort, per Pitfall 8)
    - Empty filter = unfiltered feed
    - nextCursor is null when fewer than limit rows remain
  </behavior>
  <action>
    Extend `change-log.types.ts` with:
      export interface FeedFilter { projectIds?: string[]; personIds?: string[]; entity?: ChangeLogEntity[]; actions?: ChangeLogAction[]; actorPersonaIds?: string[]; dateRange?: { from: string; to: string }; }
      export interface FeedPagination { limit?: number; cursor?: string | null; }
      export interface FeedPage { entries: ChangeLogEntry[]; nextCursor: string | null; }

    Create `change-log.read.ts` exporting:
      `getFeed({ orgId, filter, pagination }: { orgId: string; filter: FeedFilter; pagination: FeedPagination }): Promise<FeedPage>`
    Implementation (see RESEARCH code sample):
      - encodeCursor/decodeCursor as base64 JSON { createdAt, id }
      - where: eq(orgId) + composite lt cursor + entity IN + action IN + actorPersonaId IN + between(createdAt, from, to) + JSONB filters for projectIds/personIds via `sql` template: `(${schema.changeLog.context}->>'projectId') IN (...)` OR the same on newValue
      - orderBy(desc(createdAt), desc(id))
      - limit min(limit ?? 50, 200) + 1, slice + compute hasMore
    Tests (PGlite):
      - TC-API-040 basic feed pagination (insert 120 rows, paginate with limit 50 → 3 pages)
      - TC-API-041 filter edge cases (entity filter, action filter, dateRange, empty filter)
      - Equal-createdAt cursor test (3 rows same timestamp all returned)
      - JSONB projectId filter (insert rows with context.projectId=X, filter finds them)
  </action>
  <verify>
    <automated>pnpm vitest run src/features/change-log/__tests__/change-log.read.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export.*getFeed" src/features/change-log/change-log.read.ts` matches
    - `grep "TC-API-040\|TC-API-041" src/features/change-log/__tests__/change-log.read.test.ts` matches both
    - `grep "encodeCursor\|decodeCursor" src/features/change-log/change-log.read.ts` matches
    - `grep "context.*projectId\|->>'projectId'" src/features/change-log/change-log.read.ts` matches (JSONB strategy)
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>Change-log feed read helper lands with cursor pagination + filter support; PGlite tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /api/v5/capacity + /api/v5/change-log route handlers + contract tests + DesktopOnlyScreen</name>
  <files>
    src/app/api/v5/capacity/route.ts,
    src/app/api/v5/capacity/__tests__/capacity.contract.test.ts,
    src/app/api/v5/change-log/route.ts,
    src/app/api/v5/change-log/__tests__/change-log.contract.test.ts,
    src/components/responsive/desktop-only-screen.tsx
  </files>
  <read_first>
    - src/app/api/v5/planning/allocations/route.ts (Phase 40 handler pattern: requireRole, zod parse, handleApiError)
    - src/features/capacity/capacity.read.ts (from Task 1)
    - src/features/change-log/change-log.read.ts (from Task 2)
    - .planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md D-15, D-16
  </read_first>
  <behavior>
    - GET /api/v5/capacity?departmentId&startMonth&endMonth → 200 { cells, people }
    - 400 when departmentId missing OR range > 24 months
    - GET /api/v5/change-log?entity&action&projectIds&personIds&from&to&cursor&limit → 200 { entries, nextCursor }
    - 400 on malformed cursor
    - DesktopOnlyScreen: renders children on >=768px viewport, interstitial message otherwise
  </behavior>
  <action>
    Create `src/app/api/v5/capacity/route.ts`:
      - `export async function GET(req: NextRequest)` — requireRole('planner'), zod parse `{ departmentId: z.string().min(1), startMonth: z.string().regex(/^\d{4}-\d{2}$/), endMonth: z.string().regex(/^\d{4}-\d{2}$/) }`, reject if monthDiff(start,end) > 24, call `getPersonMonthUtilization({ orgId, departmentId, monthRange: { start, end } })`, return NextResponse.json, wrap in handleApiError.

    Create `src/app/api/v5/change-log/route.ts`:
      - `export async function GET(req: NextRequest)` — requireRole('planner'), zod parse filter + pagination (arrays via repeated params or CSV), call `getFeed(...)`, return NextResponse.json.

    Create `src/components/responsive/desktop-only-screen.tsx` (~30 LOC):
      - 'use client' component, uses `useSyncExternalStore` or `window.matchMedia('(min-width: 768px)')`, returns `{children}` on desktop and a centered `<div>` with i18n key `v5.lineManager.desktopOnlyMessage` otherwise. Render nothing during SSR (return `null` until mounted).

    Contract tests (PGlite + createRequest helper used by other v5 route tests):
      - TC-API-050: GET /api/v5/capacity happy path returns cells + people
      - TC-API-051: 400 when range > 24 months, 400 when departmentId empty
      - TC-API-040 (route-level): GET /api/v5/change-log happy path + cursor follow-through
      - TC-API-041 (route-level): filter combinations
  </action>
  <verify>
    <automated>pnpm vitest run src/app/api/v5/capacity/__tests__/capacity.contract.test.ts src/app/api/v5/change-log/__tests__/change-log.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TC-API-050\|TC-API-051" src/app/api/v5/capacity/__tests__/capacity.contract.test.ts` matches both
    - `grep "TC-API-040\|TC-API-041" src/app/api/v5/change-log/__tests__/change-log.contract.test.ts` matches both
    - `grep "requireRole\|handleApiError" src/app/api/v5/capacity/route.ts` matches
    - `grep "desktopOnlyMessage\|min-width: 768" src/components/responsive/desktop-only-screen.tsx` matches
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>Both new v5 GET routes serve happy-path and error cases; DesktopOnlyScreen primitive exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend ProposalImpactDTO + impact endpoint with utilization percentages</name>
  <files>
    src/features/proposals/use-proposals.ts,
    src/app/api/v5/proposals/[id]/impact/route.ts,
    src/features/proposals/__tests__/proposal-impact-utilization.test.ts
  </files>
  <read_first>
    - src/features/proposals/use-proposals.ts (existing ProposalImpactDTO ~L124-130)
    - src/app/api/v5/proposals/[id]/impact/route.ts (existing handler — extend, do NOT duplicate)
    - src/features/capacity/capacity.read.ts (from Task 1 — getCapacityBreakdown + classify)
    - .planning/phases/41-persona-views-part-2-line-manager/41-RESEARCH.md Pitfall 2 (hours vs percentages)
    - .planning/REQUIREMENTS.md L45 (impact phrase template)
  </read_first>
  <behavior>
    - ProposalImpactDTO additionally carries `currentUtilizationPct: number` and `projectedUtilizationPct: number` (rounded to nearest int)
    - Endpoint computes `current` from approved-only plannedHours and `projected` from plannedHours + proposal.hours, both divided by the person's targetHours for that month
    - Existing `personMonthPlannedBefore/After` hour fields remain for backward compat
    - Rounding: Math.round() — matches REQUIREMENTS L45 "40% → 90%"
  </behavior>
  <action>
    1. Extend `ProposalImpactDTO` in `use-proposals.ts`:
         export interface ProposalImpactDTO {
           personMonthPlannedBefore: number;
           personMonthPlannedAfter: number;
           currentUtilizationPct: number;      // NEW
           projectedUtilizationPct: number;    // NEW
           // ... existing fields
         }
    2. Update `src/app/api/v5/proposals/[id]/impact/route.ts`:
         - After computing before/after hours, fetch targetHours via the same people row lookup used by `getPersonMonthUtilization` (share a small helper or inline read of `people.target_hours_per_month`, fallback 160)
         - `currentUtilizationPct = Math.round((before / target) * 100)`
         - `projectedUtilizationPct = Math.round((after / target) * 100)`
         - Include both new fields in the response JSON
    3. Add test `proposal-impact-utilization.test.ts` (PGlite) asserting:
         - Given target=100, before=40, after=90 → payload.currentUtilizationPct === 40, payload.projectedUtilizationPct === 90
         - Rounding case: target=100, before=39.6 → 40
         - Fallback target=160 when column null
  </action>
  <verify>
    <automated>pnpm vitest run src/features/proposals/__tests__/proposal-impact-utilization.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "currentUtilizationPct" src/features/proposals/use-proposals.ts` matches
    - `grep "projectedUtilizationPct" src/features/proposals/use-proposals.ts` matches
    - `grep "currentUtilizationPct\|projectedUtilizationPct" src/app/api/v5/proposals/[id]/impact/route.ts` matches both
    - `grep "40.*90\|currentUtilizationPct.*40" src/features/proposals/__tests__/proposal-impact-utilization.test.ts` matches
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>Impact endpoint returns utilization percentages; backward-compatible hours fields retained; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: planning.read.getGroupTimeline + allocations route scope=line-manager + department dropdown wiring</name>
  <files>
    src/features/planning/planning.read.ts,
    src/features/planning/__tests__/group-timeline.test.ts,
    src/app/api/v5/planning/allocations/route.ts,
    src/features/personas/persona.context.tsx
  </files>
  <read_first>
    - src/features/planning/planning.read.ts (existing getPmTimeline pattern)
    - src/app/api/v5/planning/allocations/route.ts (existing scope=pm branch — mirror it)
    - src/features/personas/persona.context.tsx (existing stub with departmentId: '')
    - src/app/api/departments/route.ts (existing departments list endpoint)
    - .planning/phases/40-persona-views-part-1-pm/40-03-SUMMARY.md (departmentId stub notes)
    - .planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md D-12, D-17, Claude's Discretion
  </read_first>
  <behavior>
    - getGroupTimeline({ orgId, departmentId, monthRange }) returns `{ persons: Array<{ personId: string; personName: string; projects: Array<{ projectId: string; projectName: string; months: Record<string, number> }> }> }`
    - GET /api/v5/planning/allocations?scope=line-manager&departmentId=&startMonth=&endMonth= calls getGroupTimeline and returns its shape
    - Persona context fetches /api/departments on mount and exposes department list to the switcher; line-manager persona objects accept a real departmentId
    - scope=pm branch unchanged (no regression)
  </behavior>
  <action>
    1. Add `getGroupTimeline` to `planning.read.ts` — mirror the approved-only aggregation used by getPmTimeline but group by (personId, projectId, monthKey). Filter persons by department membership.

    2. Extend `src/app/api/v5/planning/allocations/route.ts` GET handler: add an `if (scope === 'line-manager')` branch that parses departmentId + monthRange and calls getGroupTimeline. Keep the existing scope=pm branch untouched. 400 if scope=line-manager and departmentId missing.

    3. Update `src/features/personas/persona.context.tsx`:
         - Add a `useQuery(['departments'], () => fetch('/api/departments').then(r => r.json()))` at the provider level
         - Expose `departments: Department[]` via context value
         - When the user picks a line-manager persona, require a departmentId (use the first department as default if none chosen)
         - Do NOT break existing tests — keep the stub fallback behind `departments.length === 0`

    4. Add PGlite test `src/features/planning/__tests__/group-timeline.test.ts`:
         - Seed 2 departments, 4 people (2 each dept), 3 projects, allocations in both depts
         - Call getGroupTimeline for dept A only — assert only dept-A persons returned, each with correct per-project month totals
         - Approved-only: a pending proposal in the same person/project/month is excluded
  </action>
  <verify>
    <automated>pnpm vitest run src/features/planning/__tests__/group-timeline.test.ts src/features/personas/__tests__/persona.context.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "getGroupTimeline" src/features/planning/planning.read.ts` matches
    - `grep "scope.*=.*'line-manager'\|scope === 'line-manager'" src/app/api/v5/planning/allocations/route.ts` matches
    - `grep "/api/departments\|departments" src/features/personas/persona.context.tsx` matches
    - `grep "getGroupTimeline" src/features/planning/__tests__/group-timeline.test.ts` matches
    - Existing scope=pm tests still pass: `pnpm vitest run src/app/api/v5/planning/allocations/__tests__` green
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>getGroupTimeline + line-manager scope branch + real department dropdown wired; no regression on scope=pm.</done>
</task>

</tasks>

<verification>
- pnpm tsc --noEmit green
- All five task verify commands pass
- No import of `@/features/analytics` or `@/components/heat-map` anywhere in new files
- scope=pm regression tests still pass
</verification>

<success_criteria>
- Wave 0 unblocks all downstream UI waves: capacity, change-log, impact preview (pct), group timeline data, desktop-only primitive, department dropdown
- TC-CP-001..004, TC-API-050..051, TC-API-040..041 created
- ProposalImpactDTO extended (not duplicated)
</success_criteria>

<output>
After completion, create `.planning/phases/41-persona-views-part-2-line-manager/41-01-SUMMARY.md`.
</output>
