---
phase: 41-persona-views-part-2-line-manager
plan: 05
type: execute
wave: 4
depends_on: ["41-02", "41-03", "41-04"]
files_modified:
  - src/messages/sv.json
  - src/messages/en.json
  - src/messages/keys.ts
  - src/features/planning/__tests__/line-manager.e2e.test.ts
  - src/app/(app)/line-manager/timeline/__tests__/desktop-only.test.tsx
  - src/features/personas/__tests__/line-manager-route-guard.negative.test.tsx
autonomous: true
requirements: [UX-V5-04, UX-V5-05, UX-V5-06, UX-V5-10]

must_haves:
  truths:
    - "Swedish + English i18n catalogs carry v5.lineManager.* and v5.changeLog.* keys used by Waves 1-3"
    - "TC-E2E-2A happy path: persona switch to line-manager → /line-manager heatmap renders people → /line-manager/approval-queue count matches fixture"
    - "TC-MOBILE-001: /line-manager/timeline at <768px viewport shows DesktopOnlyScreen interstitial"
    - "TC-NEG-013: PM persona attempting /line-manager/* sees switch-persona hint, not the LM UI"
  artifacts:
    - path: "src/messages/sv.json"
      provides: "Swedish i18n keys for v5.lineManager + v5.changeLog"
      contains: "lineManager"
    - path: "src/messages/en.json"
      provides: "English i18n keys for v5.lineManager + v5.changeLog"
      contains: "lineManager"
    - path: "src/features/planning/__tests__/line-manager.e2e.test.ts"
      provides: "TC-E2E-2A load-bearing gate"
  key_links:
    - from: "src/messages/sv.json"
      to: "v5.proposals.queue.impactPhrase"
      via: "{current}% → {projected}% interpolation"
      pattern: "current.*projected.*%|utilization.*%"
---

<objective>
Wave 4 — i18n catalog sweep, end-to-end happy path test (TC-E2E-2A, load-bearing gate), desktop-only interstitial test (TC-MOBILE-001), and negative persona guard test (TC-NEG-013).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md
@.planning/phases/41-persona-views-part-2-line-manager/41-RESEARCH.md
@.planning/phases/41-persona-views-part-2-line-manager/41-01-PLAN.md
@.planning/phases/41-persona-views-part-2-line-manager/41-02-PLAN.md
@.planning/phases/41-persona-views-part-2-line-manager/41-03-PLAN.md
@.planning/phases/41-persona-views-part-2-line-manager/41-04-PLAN.md
@src/messages/sv.json
@src/messages/en.json
@src/messages/keys.ts
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n catalog sweep — v5.lineManager.* and v5.changeLog.* keys</name>
  <files>
    src/messages/sv.json,
    src/messages/en.json,
    src/messages/keys.ts
  </files>
  <read_first>
    - src/messages/sv.json (current shape of v5.proposals.* and how Phase 39/40 added keys)
    - src/messages/en.json
    - src/messages/keys.ts (central catalog reference)
    - .planning/REQUIREMENTS.md L45 (exact impactPhrase wording)
    - .planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md D-18
  </read_first>
  <behavior>
    - Every key referenced by Waves 1-3 exists in both sv.json and en.json under matching paths
    - v5.proposals.queue.impactPhrase updated to use {current} and {projected} placeholders with literal % symbols
    - Swedish translations provided for all new keys
    - keys.ts updated so typed lookups resolve
  </behavior>
  <action>
    Add to both `sv.json` and `en.json` under `v5`:

    `lineManager`:
      - `home.title` ("Linjechef - Översikt" / "Line Manager home")
      - `heatmap.legend.ok` ("OK 60-100%" / same)
      - `heatmap.legend.under` ("Underbelagd <60%" / "Under-utilized <60%")
      - `heatmap.legend.over` ("Överbelagd >100%" / "Over-utilized >100%")
      - `heatmap.legend.absent` ("Frånvaro" / "Absent")
      - `heatmap.usingDefaultCapacity` ("Använder standardkapacitet 160h" / "Using default capacity 160h")
      - `timeline.title` ("Gruppschema" / "Group timeline")
      - `timeline.expandProjects` ("Visa projekt" / "Show projects")
      - `timeline.collapseProjects` ("Dölj projekt" / "Hide projects")
      - `wrongPersonaHint.title` + `wrongPersonaHint.description` + `wrongPersonaHint.switchCta`
      - `desktopOnlyMessage` ("Den här vyn kräver en större skärm." / "This view requires a larger screen.")

    `changeLog`:
      - `title` ("Ändringslogg" / "Change log")
      - `columns.time` / `columns.actor` / `columns.entity` / `columns.action` / `columns.target` / `columns.summary`
      - `filters.project` / `filters.person` / `filters.period` / `filters.actor` / `filters.advanced` / `filters.clearAll`
      - `loadMore` ("Läs in fler" / "Load more")
      - `noResults` ("Inga träffar" / "No results")

    Update `v5.proposals.queue.impactPhrase`:
      - sv: `"{name}s {month} beläggning {current}% → {projected}%"`
      - en: `"{name}'s {month} utilization {current}% → {projected}%"`

    Update `keys.ts` if it has typed catalogs; otherwise a simple re-export is enough.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && node -e "const sv=require('./src/messages/sv.json'); const en=require('./src/messages/en.json'); if(!sv.v5.lineManager||!en.v5.lineManager||!sv.v5.changeLog||!en.v5.changeLog) process.exit(1); if(!sv.v5.proposals.queue.impactPhrase.includes('{current}')||!sv.v5.proposals.queue.impactPhrase.includes('%')) process.exit(2);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "lineManager" src/messages/sv.json` matches
    - `grep "lineManager" src/messages/en.json` matches
    - `grep "changeLog" src/messages/sv.json` matches
    - `grep "changeLog" src/messages/en.json` matches
    - `grep "{current}.*%" src/messages/sv.json` matches (impactPhrase updated)
    - `grep "{current}.*%" src/messages/en.json` matches
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>Swedish + English i18n catalogs carry all Phase 41 keys; impactPhrase uses percent interpolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TC-MOBILE-001 + TC-NEG-013 tests</name>
  <files>
    src/app/(app)/line-manager/timeline/__tests__/desktop-only.test.tsx,
    src/features/personas/__tests__/line-manager-route-guard.negative.test.tsx
  </files>
  <read_first>
    - src/components/responsive/desktop-only-screen.tsx (Wave 0)
    - src/app/(app)/line-manager/timeline/page.tsx (Wave 2)
    - src/features/personas/persona-route-guard.ts (Wave 1)
    - .planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md D-20
  </read_first>
  <behavior>
    - TC-MOBILE-001: set matchMedia to <768px and assert DesktopOnlyScreen message renders instead of the grid
    - TC-NEG-013: set persona context to pm and assert mounting the line-manager page tree renders the switch-persona hint, not the grid
  </behavior>
  <action>
    Test 1 `desktop-only.test.tsx`:
      - Mock `window.matchMedia` to return `{ matches: false }` for `(min-width: 768px)`
      - Render the `<DesktopOnlyScreen>` wrapping a `<div data-testid="grid">`
      - Assert `queryByTestId('grid')` returns null
      - Assert the desktopOnlyMessage i18n key text is visible
      - Then set matchMedia matches=true, re-render, assert the grid IS visible
      - Label the test with `// TC-MOBILE-001`

    Test 2 `line-manager-route-guard.negative.test.tsx`:
      - Render `<PersonaGate allowed={['line-manager']}><div data-testid="lm-home">home</div></PersonaGate>` inside a PersonaProvider stub with persona={kind:'pm'}
      - Assert `queryByTestId('lm-home')` is null
      - Assert `v5.lineManager.wrongPersonaHint.title` text is visible
      - Label the test with `// TC-NEG-013`
  </action>
  <verify>
    <automated>pnpm vitest run src/app/(app)/line-manager/timeline/__tests__/desktop-only.test.tsx src/features/personas/__tests__/line-manager-route-guard.negative.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TC-MOBILE-001" src/app/(app)/line-manager/timeline/__tests__/desktop-only.test.tsx` matches
    - `grep "TC-NEG-013" src/features/personas/__tests__/line-manager-route-guard.negative.test.tsx` matches
    - `grep "matchMedia" src/app/(app)/line-manager/timeline/__tests__/desktop-only.test.tsx` matches
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>Mobile interstitial + negative persona guard tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TC-E2E-2A load-bearing PGlite e2e — persona switch → LM home heatmap → approval queue count</name>
  <files>
    src/features/planning/__tests__/line-manager.e2e.test.ts
  </files>
  <read_first>
    - src/features/planning/__tests__/group-timeline-edit.test.ts (Wave 2 harness)
    - src/features/capacity/capacity.read.ts (Wave 0)
    - src/features/proposals/ (approval queue listing)
    - .planning/phases/41-persona-views-part-2-line-manager/41-CONTEXT.md D-20, D-21
    - .planning/v5.0-ARCHITECTURE.md §2124 (TC-E2E-2A spec)
  </read_first>
  <behavior>
    - TC-E2E-2A: seed org with 1 department, 3 people in that department, 5 approved allocations across 2 months, 2 pending proposals targeting those people
    - Switch persona to line-manager(departmentId=A)
    - Hit GET /api/v5/capacity?departmentId=A&... → assert payload.cells.length === 3 * 2 === 6 and each cell has a status
    - Hit GET /api/v5/planning/allocations?scope=line-manager&departmentId=A&... → assert 3 persons returned
    - Hit the existing list-proposals-for-approval-queue endpoint filtered by department → assert count === 2
    - Assert pending proposals did NOT affect the capacity cells (approved-only invariant from D-07)
  </behavior>
  <action>
    Create `src/features/planning/__tests__/line-manager.e2e.test.ts` using the PGlite harness already used by other v5 tests:
      - Fixtures: org, dept A, 3 people in dept A, 1 other person in dept B, 5 approved allocations in dept A across 2 months, 2 pending proposals for dept A people
      - Call the three endpoints above via their handler imports (not fetch) — mirror how group-timeline-edit.test.ts invokes handlers
      - Assertions per behavior block
      - Label the test `// TC-E2E-2A — load-bearing gate (D-21)`
      - Invariant check: for any person in dept A where the pending proposal would push over 100%, the capacity cell status must still be based on approved hours only
  </action>
  <verify>
    <automated>pnpm vitest run src/features/planning/__tests__/line-manager.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TC-E2E-2A" src/features/planning/__tests__/line-manager.e2e.test.ts` matches
    - `grep "load-bearing gate" src/features/planning/__tests__/line-manager.e2e.test.ts` matches
    - `grep "approved-only\|pending.*not.*affect\|approved.*only" src/features/planning/__tests__/line-manager.e2e.test.ts` matches
    - Test file invokes all three endpoints (capacity, planning/allocations?scope=line-manager, proposals list)
    - Test passes
    - `pnpm tsc --noEmit` green
  </acceptance_criteria>
  <done>TC-E2E-2A load-bearing gate is green.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` green
- All three task verify commands pass
- TC-E2E-2A is the phase's load-bearing gate per D-21
</verification>

<success_criteria>
- i18n catalogs complete for sv + en
- TC-MOBILE-001, TC-NEG-013, TC-E2E-2A all green
- Phase 41 ready for verification
</success_criteria>

<output>
After completion, create `.planning/phases/41-persona-views-part-2-line-manager/41-05-SUMMARY.md`.
</output>
