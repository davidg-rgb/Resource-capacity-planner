---
phase: 51-lean-cleanup-duplicate-removal
plan: 03
type: execute
wave: 3
depends_on: ["51-02"]
files_modified:
  - src/features/dashboard/pdf-export/__tests__/team-heatmap-regression.test.ts
  - src/features/dashboard/__tests__/lean-trim-integration.test.ts
autonomous: true
requirements:
  - LEAN-10

must_haves:
  truths:
    - "PDF export for team-heatmap does not regress after layout trimming"
    - "All LEAN-01..LEAN-09 requirements verified by integration tests"
  artifacts:
    - path: "src/features/dashboard/pdf-export/__tests__/team-heatmap-regression.test.ts"
      provides: "PDF regression test"
      contains: "team-heatmap"
    - path: "src/features/dashboard/__tests__/lean-trim-integration.test.ts"
      provides: "Integration test covering full lean-trim scope"
      contains: "LEAN"
  key_links:
    - from: "team-heatmap-regression.test.ts"
      to: "src/app/api/reports/team-heatmap/route.tsx"
      via: "test imports or calls the PDF generation"
      pattern: "team-heatmap"
---

<objective>
Validate Phase 51 with a PDF snapshot regression test and an integration test covering the full lean-trim scope.

Purpose: Ensure PDF export did not regress from layout changes and confirm all 10 LEAN requirements are met before phase sign-off.
Output: PDF regression test, integration test, full green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-lean-cleanup-duplicate-removal/51-CONTEXT.md
@.planning/phases/51-lean-cleanup-duplicate-removal/51-RESEARCH.md
@.planning/phases/51-lean-cleanup-duplicate-removal/51-01-SUMMARY.md
@.planning/phases/51-lean-cleanup-duplicate-removal/51-02-SUMMARY.md

<interfaces>
From src/app/api/reports/team-heatmap/route.tsx:
```typescript
// Uses @react-pdf/renderer to generate PDF server-side
// Exports GET handler that returns PDF as Response
```

From src/features/dashboard/default-layouts.ts (after Plan 02):
```typescript
export const DEFAULT_LAYOUTS: Record<string, WidgetPlacement[]>;  // trimmed
export const LEGACY_LAYOUTS: Record<string, WidgetPlacement[]>;   // original
export function getDefaultLayout(dashboardId: string, deviceClass: string, useLegacy?: boolean): WidgetPlacement[];
```

From src/features/dashboard/widget-registry.ts:
```typescript
export function getWidget(id: string): WidgetDefinition | undefined;
export function getAllWidgets(): WidgetDefinition[];
export function clearRegistry(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDF regression test + lean-trim integration test</name>
  <files>
    src/features/dashboard/pdf-export/__tests__/team-heatmap-regression.test.ts,
    src/features/dashboard/__tests__/lean-trim-integration.test.ts
  </files>
  <read_first>
    src/app/api/reports/team-heatmap/route.tsx,
    src/features/dashboard/pdf-export/export-pdf-modal.tsx,
    src/features/dashboard/default-layouts.ts,
    src/features/dashboard/widgets/index.ts,
    src/features/dashboard/dashboard-layout-engine.tsx,
    next.config.ts
  </read_first>
  <action>
1. **team-heatmap-regression.test.ts** -- Per D-06, create a regression test for the `/api/reports/team-heatmap` PDF endpoint. The approach depends on what the route.tsx does:

   - **If the route uses `@react-pdf/renderer` `renderToStream`/`renderToBuffer`:** Import the PDF document component directly, render it with test fixture data, and assert the output buffer is non-empty and has a reasonable byte size (> 1KB, which proves content is being rendered, not a blank PDF). Compare the output structure: the PDF should contain the expected number of pages and text elements.

   - **If direct import is complex (many server-side dependencies):** Create a structural test that:
     a. Imports `DEFAULT_LAYOUTS` and verifies manager:desktop layout exists and has widgets
     b. Imports `getWidget` and verifies every widget ID in the manager:desktop layout is registered (i.e., `getWidget(id)` returns non-undefined)
     c. This ensures no layout references a deleted/unregistered widget, which would cause blank tiles in the PDF

   The key assertion per D-06: "the manager dashboard layout used by the PDF export has no unresolvable widget references after the lean trim." This is a structural regression test, not a pixel comparison.

   Test cases:
   - Every widgetId in `DEFAULT_LAYOUTS['manager:desktop']` resolves via `getWidget()`
   - Every widgetId in `DEFAULT_LAYOUTS['manager:mobile']` resolves via `getWidget()`
   - `heat-map-summary-card` is in the layout AND resolves to a registered widget
   - `utilization-heat-map` is NOT in the trimmed layout
   - The layout has > 0 widgets (not accidentally emptied)

2. **lean-trim-integration.test.ts** -- Create a comprehensive integration test that verifies all LEAN requirements at the code level:

   Test cases grouped by requirement:
   - **LEAN-01..03 (redirects):** Read `next.config.ts` content (or import the config if possible) and assert the redirects array contains:
     - `{ source: '/team', destination: '/admin/people', permanent: true }`
     - `{ source: '/team/:path*', destination: '/admin/people/:path*', permanent: true }`
     - `{ source: '/projects', destination: '/admin/projects', permanent: true }`
     - `{ source: '/wishes', destination: '/pm/wishes', permanent: true }`
     If importing next.config.ts is not feasible in vitest, use `fs.readFileSync` to read the file and assert string patterns.

   - **LEAN-04 (input page):** Import `InputPage` from `@/app/(app)/input/page` and render with `@testing-library/react`. Assert it does NOT contain a `<ul>` element. Assert it contains text matching "Valj en person".

   - **LEAN-06 (dead widgets de-registered):** After importing `widgets/index` (triggers side-effect registration), call `getWidget('discipline-progress')` and assert it returns `undefined`. Same for `discipline-demand` and `project-impact`.

   - **LEAN-07 (PL layout trim):** Assert `DEFAULT_LAYOUTS['project-leader:desktop']` has no placement with widgetId in `['kpi-cards', 'capacity-forecast', 'availability-finder']`. Same for mobile.

   - **LEAN-08 (manager layout trim):** Assert `DEFAULT_LAYOUTS['manager:desktop']` has no `utilization-heat-map` placement. Assert it has a `heat-map-summary-card` placement.

   - **LEAN-09 (defensive fallback):** This is covered by widget-fallback.test.ts from Plan 01; reference it here as a cross-check.
  </action>
  <verify>
    <automated>npx vitest run src/features/dashboard/pdf-export/__tests__/team-heatmap-regression.test.ts src/features/dashboard/__tests__/lean-trim-integration.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/features/dashboard/pdf-export/__tests__/team-heatmap-regression.test.ts exists
    - team-heatmap-regression.test.ts verifies every widget in manager:desktop layout resolves via getWidget()
    - team-heatmap-regression.test.ts asserts `heat-map-summary-card` is registered
    - team-heatmap-regression.test.ts asserts `utilization-heat-map` is NOT in trimmed layout
    - src/features/dashboard/__tests__/lean-trim-integration.test.ts exists
    - lean-trim-integration.test.ts covers LEAN-01..03 (redirect config assertions)
    - lean-trim-integration.test.ts covers LEAN-04 (input page empty-state)
    - lean-trim-integration.test.ts covers LEAN-06 (dead widgets return undefined from getWidget)
    - lean-trim-integration.test.ts covers LEAN-07 (PL layout no kpi-cards/capacity-forecast/availability-finder)
    - lean-trim-integration.test.ts covers LEAN-08 (manager layout has heat-map-summary-card, no utilization-heat-map)
    - Both test files pass: `npx vitest run` exits 0
    - Full test suite `npx vitest run --reporter=verbose` exits 0
  </acceptance_criteria>
  <done>PDF regression test confirms no unresolvable widget references in manager dashboard layouts; integration test covers all 10 LEAN requirements; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assertions -> production code | Tests verify production code contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-07 | Tampering | Test reliability | accept | Tests use direct imports of production modules (DEFAULT_LAYOUTS, getWidget). No mocks of core contracts. Tests verify the actual shipped code, not approximations. |
</threat_model>

<verification>
1. `npx vitest run --reporter=verbose` passes (all existing + new tests)
2. `npx vitest run src/features/dashboard/pdf-export/__tests__/team-heatmap-regression.test.ts` passes
3. `npx vitest run src/features/dashboard/__tests__/lean-trim-integration.test.ts` passes
4. No test uses `--watch` mode
</verification>

<success_criteria>
- PDF regression test committed and passing
- Integration test covers all 10 LEAN-* requirements
- Full vitest suite passes
- Phase 51 ready for /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/51-lean-cleanup-duplicate-removal/51-03-SUMMARY.md`
</output>
