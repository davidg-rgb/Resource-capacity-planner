---
phase: 44-api-hardening-and-test-contract-fill
plan: 10
type: execute
wave: 4
depends_on: [44-06]
files_modified:
  - src/app/api/v5/**/__tests__/*.test.ts
  - src/features/admin/__tests__/*.test.ts
  - src/features/change-log/__tests__/*.test.ts
  - .planning/test-contract/tc-allowlist.json
  - .planning/test-contract/tc-manifest.json
autonomous: true
requirements: [TEST-V5-01, API-V5-01, API-V5-02]
must_haves:
  truths:
    - "Every TC-API-* (21), TC-REG-* (9), TC-CL-* (5) canonical ID is present in the manifest as a passing test"
    - "Allowlist no longer contains TC-API-*, TC-REG-*, or TC-CL-* entries"
    - "TC-API-TENANT-* IDs from 44-05 are counted against the TC-API bucket"
  artifacts:
    - path: src/app/api/v5/**/__tests__
      provides: "API route integration tests (TC-API)"
    - path: src/features/admin/__tests__
      provides: "Admin register tests (TC-REG)"
    - path: src/features/change-log/__tests__
      provides: "Change-log service tests (TC-CL)"
  key_links:
    - from: tests
      to: .planning/test-contract/tc-manifest.json
      via: "test-title token extraction"
      pattern: "TC-(API|REG|CL)-\\d+"
---

<objective>
Wave C group C4: fill TC-API-* (21), TC-REG-* (9), TC-CL-* (5) gaps.
TC-API-* and TC-REG-* are partially covered by Phases 40-43 work — most
of this plan is renaming existing `it()` titles to prepend TC-IDs. TC-CL-*
needs the runtime harness repair (handled in Wave D 44-14 separately for
TC-CL-005 specifically); this plan only adds/renames the other 4 TC-CL-*
IDs that aren't TC-CL-005.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-api-hardening-and-test-contract-fill/44-CONTEXT.md
@.planning/phases/44-api-hardening-and-test-contract-fill/44-RESEARCH.md
@.planning/v5.0-ARCHITECTURE.md
@.planning/test-contract/tc-allowlist.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill TC-API-* + TC-REG-* gaps</name>
  <files>src/app/api/v5/**/__tests__/*.test.ts, src/features/admin/__tests__/*.test.ts</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.10 (TC-API), §15.17 (TC-REG)
    - .planning/test-contract/tc-allowlist.json
    - Existing admin register tests from Phase 43 (grep: `rg "TC-REG" src/`)
    - Existing API contract tests (grep: `rg "TC-API" src/`)
  </read_first>
  <action>
    1. `jq '.stillMissing | map(select(test("^TC-(API|REG)-")))' .planning/test-contract/tc-allowlist.json`
    2. For each ID, find §15 text
    3. Rename or add. Many `it()` blocks already exist and just need the TC-ID prepended to the title
    4. Confirm TC-API-TENANT-* from 44-05 is already picked up
    5. Regenerate manifest, prune allowlist
  </action>
  <verify>
    <automated>pnpm test -- src/app/api/v5 src/features/admin && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.stillMissing | map(select(test("^TC-(API|REG)-"))) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - tc-id-coverage test exits 0
  </acceptance_criteria>
  <done>TC-API + TC-REG groups complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill TC-CL-* gaps (excluding TC-CL-005 runtime harness which is owned by 44-14)</name>
  <files>src/features/change-log/__tests__/*.test.ts</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.3 (TC-CL)
    - .planning/test-contract/tc-allowlist.json
    - Existing change-log tests (grep: `rg "TC-CL" src/features/change-log tests/invariants`)
  </read_first>
  <action>
    Same flow. Explicitly EXCLUDE TC-CL-005 from this plan — it is the
    runtime harness repair handled by 44-14. Do not remove TC-CL-005 from
    the allowlist here.
  </action>
  <verify>
    <automated>pnpm test -- src/features/change-log && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.stillMissing | map(select(startswith("TC-CL-") and . != "TC-CL-005")) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - TC-CL-005 remains in the allowlist (to be removed by 44-14)
    - tc-id-coverage test exits 0
  </acceptance_criteria>
  <done>TC-CL group complete except TC-CL-005.</done>
</task>

</tasks>

<verification>
- TC-API + TC-REG + TC-CL (minus TC-CL-005) removed from allowlist
- tc-id-coverage green
</verification>

<success_criteria>
- 34 TC-IDs filled (21 TC-API + 9 TC-REG + 4 TC-CL)
</success_criteria>

<output>
After completion, create `.planning/phases/44-api-hardening-and-test-contract-fill/44-10-SUMMARY.md`.
</output>
</content>
</invoke>