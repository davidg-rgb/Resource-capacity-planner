---
phase: 44-api-hardening-and-test-contract-fill
plan: 02
type: execute
wave: 1
depends_on: [44-01]
files_modified:
  - eslint.config.mjs
  - src/app/api/v5/**/route.ts
  - src/features/**/*.service.ts
autonomous: true
requirements: [API-V5-01]
must_haves:
  truths:
    - "`throw new Error(...)` is forbidden by ESLint under src/app/api/v5/** and src/features/**/*.service.ts"
    - "No raw `throw new Error(` or `NextResponse.json({ error: ... })` bypass remains in v5 route handlers or services"
    - "All error paths under v5 routes flow through `handleApiError(new <AppError subclass>(...))`"
  artifacts:
    - path: eslint.config.mjs
      provides: "`no-restricted-syntax` guard block scoped to v5 routes + feature services"
    - path: src/app/api/v5/**
      provides: "Swept routes; all throws are typed AppError subclasses"
  key_links:
    - from: "v5 route handlers"
      to: "src/lib/errors.ts subclasses"
      via: "import + throw"
      pattern: "throw new (HistoricConfirmRequired|BadHours|ProposalNotActive|ReasonRequired|BatchAlreadyRolledBack|RollbackWindowExpired|DependentRowsExist|UsWeekHeaders|Validation|NotFound|Conflict)Error"
---

<objective>
Wave A step 2: enforce the AppError taxonomy by (1) adding the ESLint
`no-restricted-syntax` guard targeting `src/app/api/v5/**` and
`src/features/**/*.service.ts`, and (2) doing a one-time sweep replacing
raw `throw new Error(...)` and direct `NextResponse.json({error: ...})`
bypasses with the correct AppError subclass from `src/lib/errors.ts`.

Purpose: API-V5-01 — guard + sweep together eliminate raw errors from v5
API surface. After this plan, the static invariant from 44-01 task 3 is
green and the ESLint guard prevents regression.

Output: Updated ESLint config; all v5 route and service files use typed
AppError subclasses exclusively.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-api-hardening-and-test-contract-fill/44-CONTEXT.md
@.planning/phases/44-api-hardening-and-test-contract-fill/44-RESEARCH.md
@src/lib/errors.ts
@src/lib/api-utils.ts
@eslint.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add no-restricted-syntax guard to eslint.config.mjs</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - eslint.config.mjs (full file — see existing no-restricted-syntax block at lines 62-70 per RESEARCH)
    - .planning/phases/44-api-hardening-and-test-contract-fill/44-CONTEXT.md §3
  </read_first>
  <action>
    Append a new config block at the end of the flat-config array. Use the
    EXACT selector and files glob below — do not weaken either:

    ```js
    {
      files: ['src/app/api/v5/**/*.ts', 'src/features/**/*.service.ts'],
      rules: {
        'no-restricted-syntax': ['error', {
          selector: "ThrowStatement[argument.type='NewExpression'][argument.callee.name='Error']",
          message: "Throw AppError subclasses from '@/lib/errors', not raw Error. v5 API contract requires typed error codes.",
        }],
      },
    },
    ```

    If the existing config uses `defineConfig([...])`, insert inside the
    array. Do NOT merge with the existing `no-restricted-syntax` block at
    lines ~62-70 — that one targets `getDay()` and has different `files` scope.
  </action>
  <verify>
    <automated>pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - grep of `eslint.config.mjs` for `ThrowStatement[argument.type='NewExpression'][argument.callee.name='Error']` returns exactly 1 match
    - grep for `src/app/api/v5/**/*.ts` returns a match in the new block
    - grep for `src/features/**/*.service.ts` returns a match in the new block
    - After the sweep in Task 2 completes, `pnpm lint` exits 0
  </acceptance_criteria>
  <done>ESLint guard block committed with exact selector and exact files glob.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep raw errors in v5 routes and services to typed AppError subclasses</name>
  <files>src/app/api/v5/**/route.ts, src/features/**/*.service.ts</files>
  <read_first>
    - src/lib/errors.ts (full file — know every subclass available after 44-01)
    - src/lib/api-utils.ts (confirm handleApiError wire format)
    - src/app/api/v5/proposals/route.ts (reference pattern: try → parse → service → catch → handleApiError)
    - .planning/phases/44-api-hardening-and-test-contract-fill/44-RESEARCH.md Pitfall 1
  </read_first>
  <action>
    Step 1: Inventory bypasses. Run:
    ```
    rg -n "throw new Error\\(|NextResponse\\.json\\(\\s*\\{\\s*error" src/app/api/v5 src/features
    ```
    Keep a list.

    Step 2: For each hit, replace with the matching AppError subclass:
    - "Historic ... confirm" / planning historic edits → `HistoricConfirmRequiredError`
    - Hours out of range / negative hours / > 24h → `BadHoursError`
    - Proposal state transition on inactive proposal → `ProposalNotActiveError` (already used)
    - Missing reason string (approval/rejection/withdraw) → `ReasonRequiredError`
    - Import batch already rolled back → `BatchAlreadyRolledBackError`
    - Rollback window > N days → `RollbackWindowExpiredError`
    - Archive with dependent rows → `DependentRowsExistError`
    - Excel import with US week headers → `UsWeekHeadersError`
    - Generic bad input → `ValidationError` (existing)
    - Resource not found → `NotFoundError` (existing)
    - Conflict not matching specifics → `ConflictError` (existing)

    Step 3: For `NextResponse.json({ error: ... })` bypasses inside a
    catch or early-return: delete them and `throw new <Specific>Error(...)`
    instead; rely on the outer `try/catch → handleApiError(error)` in the
    route handler (see `src/app/api/v5/proposals/route.ts` lines 29-48
    pattern). If the route lacks the `catch → handleApiError` shell, add it.

    Step 4: Never change `AppError.toJSON()`. Never nest the wire shape.
    Existing contract tests (`capacity.contract.test.ts`,
    `change-log.contract.test.ts`) assert the flat shape — keep them green.

    Step 5: Do NOT refactor routes to `withTenant()` — Wave A tenant audit
    (44-03) accepts the current `requireRole()+orgId` pattern. Out of scope.
  </action>
  <verify>
    <automated>pnpm lint && pnpm test -- tests/invariants/error-taxonomy.static.test.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `rg "throw new Error\\(" src/app/api/v5 src/features` returns 0 matches for files under `src/features/**/*.service.ts`
    - `rg "throw new Error\\(" src/app/api/v5` returns 0 matches
    - `rg "NextResponse\\.json\\(\\s*\\{\\s*error" src/app/api/v5` returns 0 matches (all error returns go through handleApiError)
    - `pnpm lint` exits 0
    - `pnpm test -- tests/invariants/error-taxonomy.static.test.ts` exits 0 (all TC-INV-ERRTAX assertions pass)
    - `pnpm test` full run exits 0 (no contract test regressions)
  </acceptance_criteria>
  <done>Sweep complete; ESLint guard active; invariant static test green; no contract regressions.</done>
</task>

</tasks>

<verification>
- `pnpm lint` green
- `pnpm test` green
- grep proves zero raw `throw new Error(` under v5 routes and service files
- grep proves zero `NextResponse.json({error` under v5 routes
</verification>

<success_criteria>
- ESLint guard enforces AppError usage in v5 routes + services
- Sweep complete: all error paths typed
- No contract test regressions (flat wire format preserved)
- 44-01 static taxonomy invariant now green
</success_criteria>

<output>
After completion, create `.planning/phases/44-api-hardening-and-test-contract-fill/44-02-SUMMARY.md`.
</output>
</content>
</invoke>