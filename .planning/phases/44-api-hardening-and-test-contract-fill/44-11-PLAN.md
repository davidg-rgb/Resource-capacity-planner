---
phase: 44-api-hardening-and-test-contract-fill
plan: 11
type: execute
wave: 4
depends_on: [44-06]
files_modified:
  - src/components/**/__tests__/*.test.tsx
  - src/features/persona/__tests__/*.test.ts
  - .planning/test-contract/tc-allowlist.json
  - .planning/test-contract/tc-manifest.json
autonomous: true
requirements: [TEST-V5-01]
must_haves:
  truths:
    - "Every TC-PSN-* (8), TC-UI-* (30), TC-ZOOM-* (3), TC-MOBILE-* (1), TC-RD-READONLY-* (1) canonical ID is present"
    - "Allowlist no longer contains entries from these 5 prefix groups"
  artifacts:
    - path: src/components/**/__tests__
      provides: "Vitest + React Testing Library component tests"
    - path: src/features/persona/__tests__
      provides: "Persona context tests"
  key_links:
    - from: tests
      to: src/components/**
      via: "@testing-library/react render + assertions"
      pattern: "TC-(PSN|UI|ZOOM|MOBILE|RD-READONLY)-"
---

<objective>
Wave C group C5: fill TC-PSN-* (8), TC-UI-* (30), TC-ZOOM-* (3),
TC-MOBILE-* (1), TC-RD-READONLY-* (1) gaps = up to 43 IDs. These are
Vitest + React Testing Library component tests per CONTEXT tier split —
no browser, no PGlite.

Purpose: TEST-V5-01. TC-UI-* is the largest group and includes range-
expanded IDs like `TC-UI-EMPTY-001..014` and `TC-UI-LOAD-001..014` (see
RESEARCH Pitfall 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-api-hardening-and-test-contract-fill/44-CONTEXT.md
@.planning/phases/44-api-hardening-and-test-contract-fill/44-RESEARCH.md
@.planning/v5.0-ARCHITECTURE.md
@.planning/test-contract/tc-allowlist.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill TC-UI-* + TC-ZOOM-* + TC-MOBILE-* + TC-RD-READONLY-* component tests</name>
  <files>src/components/**/__tests__/*.test.tsx</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.12 (TC-UI), §15.18 (TC-ZOOM, TC-MOBILE, TC-RD-READONLY)
    - .planning/test-contract/tc-allowlist.json
    - Existing component tests (grep: `rg "TC-UI|TC-ZOOM" src/components`)
    - Vitest + RTL config (check vitest.config.ts or vitest.workspace.ts for jsdom environment)
  </read_first>
  <action>
    1. `jq '.stillMissing | map(select(test("^TC-(UI|ZOOM|MOBILE|RD-READONLY)-")))' .planning/test-contract/tc-allowlist.json`
    2. For each ID, find §15.12 / §15.18 text. TC-UI-EMPTY-NNN and TC-UI-LOAD-NNN assertions cover empty-state and loading-skeleton rendering for the v5 component set
    3. Add/rename component tests using `@testing-library/react` `render()` + `getByRole` / `queryByText`. No browser
    4. Regenerate manifest, prune allowlist
  </action>
  <verify>
    <automated>pnpm test -- src/components && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.stillMissing | map(select(test("^TC-(UI|ZOOM|MOBILE|RD-READONLY)-"))) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - tc-id-coverage test exits 0
  </acceptance_criteria>
  <done>TC-UI, TC-ZOOM, TC-MOBILE, TC-RD-READONLY groups complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill TC-PSN-* persona context tests</name>
  <files>src/features/persona/__tests__/*.test.ts, src/app/**/__tests__/*.test.tsx</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.11 (TC-PSN) + §15.18
    - .planning/test-contract/tc-allowlist.json
    - Existing persona tests from Phases 34, 40-43
  </read_first>
  <action>
    Same flow. TC-PSN-001 is the admin-default-landing assertion (already
    landed in Phase 43-04 per STATE.md) — verify it exists in the manifest
    and only rename the title if needed.
  </action>
  <verify>
    <automated>pnpm test -- src/features/persona && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.stillMissing | map(select(startswith("TC-PSN-"))) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - tc-id-coverage test exits 0
  </acceptance_criteria>
  <done>TC-PSN group complete.</done>
</task>

</tasks>

<verification>
- All 5 prefix groups removed from allowlist
</verification>

<success_criteria>
- Up to 43 TC-IDs filled
</success_criteria>

<output>
After completion, create `.planning/phases/44-api-hardening-and-test-contract-fill/44-11-SUMMARY.md`.
</output>
</content>
</invoke>