---
phase: 44-api-hardening-and-test-contract-fill
plan: 07
type: execute
wave: 4
depends_on: [44-06]
files_modified:
  - tests/unit/calendar/**/*.test.ts
  - tests/unit/db/**/*.test.ts
  - .planning/test-contract/tc-allowlist.json
  - .planning/test-contract/tc-manifest.json
autonomous: true
requirements: [TEST-V5-01]
must_haves:
  truths:
    - "Every TC-CAL-* and TC-DB-* ID in tc-canonical.json appears as a passing test with the ID as first token in its title"
    - "tc-allowlist.json has all TC-CAL-* and TC-DB-* entries removed"
  artifacts:
    - path: tests/unit/calendar/
      provides: "Vitest unit tests covering TC-CAL-* (ISO calendar + Swedish holidays + largest-remainder)"
    - path: tests/unit/db/
      provides: "Schema-introspection tests covering TC-DB-*"
  key_links:
    - from: tests/unit/calendar/*.test.ts
      to: src/lib/time/iso-calendar.ts
      via: "imports + assertions"
      pattern: "TC-CAL-\\d+"
---

<objective>
Wave C group C1: fill the TC-CAL-* (31 canonical) and TC-DB-* (10 canonical)
gaps with Vitest unit tests. These are pure-function tests per CONTEXT
test tier split (Vitest unit, no PGlite, no RTL).

Purpose: TEST-V5-01 — shrink the Wave C allowlist by the TC-CAL + TC-DB
subsets.

Output: New/updated test files using the `it('TC-CAL-NNN ...')` naming
convention; updated allowlist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-api-hardening-and-test-contract-fill/44-CONTEXT.md
@.planning/phases/44-api-hardening-and-test-contract-fill/44-RESEARCH.md
@.planning/v5.0-ARCHITECTURE.md
@.planning/test-contract/tc-canonical.json
@.planning/test-contract/tc-manifest.json
@.planning/test-contract/tc-allowlist.json
@src/lib/time/iso-calendar.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill TC-CAL-* gap (ISO calendar + Swedish holidays + largest-remainder)</name>
  <files>tests/unit/calendar/**/*.test.ts</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.1 + §15.18 (full TC-CAL-* assertion list)
    - .planning/test-contract/tc-allowlist.json (the exact TC-CAL-* IDs to fill)
    - src/lib/time/iso-calendar.ts (the module under test)
    - Any existing tests under `tests/unit/calendar/` or wherever ISO calendar tests currently live (grep: `rg "TC-CAL" tests/ src/`)
  </read_first>
  <behavior>
    - Every TC-CAL-* ID currently in `tc-allowlist.json` has a passing `it('TC-CAL-NNN ...', ...)` after this task
    - For TC-CAL-* IDs already present in the manifest but NOT yet prefixed with the TC-ID token, rename the `it()` title to prepend the token (e.g. `it('week numbers use ISO 8601', ...)` → `it('TC-CAL-001 week numbers use ISO 8601', ...)`)
    - 2026 being a 53-week year and Swedish holidays 2026-2030 are asserted where the canonical list says so
  </behavior>
  <action>
    Step 1: List target IDs — `jq '.stillMissing | map(select(startswith("TC-CAL-")))' .planning/test-contract/tc-allowlist.json`.

    Step 2: For each ID, find the matching assertion text in `.planning/v5.0-ARCHITECTURE.md §15.1` (use `rg -n "TC-CAL-NNN" .planning/v5.0-ARCHITECTURE.md`).

    Step 3: Either
      (a) add a new `it('TC-CAL-NNN ...')` in the appropriate file under `tests/unit/calendar/`, OR
      (b) if an existing test already covers the assertion but lacks the token, rename the title to prepend `TC-CAL-NNN`.

    Step 4: Regenerate manifest + prune allowlist:
    ```
    pnpm tsx scripts/generate-tc-manifest.ts
    ```
    Then edit `.planning/test-contract/tc-allowlist.json`, removing every TC-CAL-* ID that is now present in the manifest.

    Step 5: Run `pnpm test -- tests/invariants/tc-id-coverage.test.ts` and confirm green (allowlist not stale, no missing).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/calendar && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -o "TC-CAL-\\d+[a-z]?" .planning/test-contract/tc-manifest.json | sort -u | wc -l` ≥ 31 (all canonical TC-CAL-* now present)
    - `jq '.stillMissing | map(select(startswith("TC-CAL-"))) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - `pnpm test -- tests/invariants/tc-id-coverage.test.ts` exits 0
    - All TC-CAL-* tests pass
  </acceptance_criteria>
  <done>TC-CAL-* group fully covered and manifest/allowlist updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill TC-DB-* gap (schema introspection)</name>
  <files>tests/unit/db/**/*.test.ts</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.2 (full TC-DB-* list)
    - .planning/test-contract/tc-allowlist.json
    - Drizzle schema files under `src/db/schema/` or `drizzle/schema.ts`
  </read_first>
  <behavior>
    - Every TC-DB-* canonical ID has a passing `it('TC-DB-NNN ...', ...)` that introspects the Drizzle schema (column presence, FK, nullable, unique constraint, etc.)
    - Tests are pure unit — they import the schema objects, not a running DB
  </behavior>
  <action>
    Step 1: `jq '.stillMissing | map(select(startswith("TC-DB-")))' .planning/test-contract/tc-allowlist.json` to list targets.
    Step 2: For each, write a schema-introspection assertion. Example:
    ```ts
    import { projects } from '@/db/schema';
    it('TC-DB-001 projects.lead_pm_person_id is a nullable uuid FK to people.id', () => {
      expect(projects.leadPmPersonId).toBeDefined();
      // drizzle column metadata assertions
    });
    ```
    Step 3: Regenerate manifest + prune allowlist (same flow as Task 1).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/db && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.stillMissing | map(select(startswith("TC-DB-"))) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - `rg -o "TC-DB-\\d+[a-z]?" .planning/test-contract/tc-manifest.json | sort -u | wc -l` ≥ 10
    - `pnpm test -- tests/invariants/tc-id-coverage.test.ts` exits 0
  </acceptance_criteria>
  <done>TC-DB-* group covered; allowlist shrunk.</done>
</task>

</tasks>

<verification>
- TC-CAL + TC-DB fully covered
- Allowlist no longer contains any TC-CAL-* or TC-DB-* entries
- tc-id-coverage test still green
</verification>

<success_criteria>
- 41 TC-IDs filled (31 TC-CAL + 10 TC-DB)
- Manifest + allowlist updated and committed
</success_criteria>

<output>
After completion, create `.planning/phases/44-api-hardening-and-test-contract-fill/44-07-SUMMARY.md`.
</output>
</content>
</invoke>