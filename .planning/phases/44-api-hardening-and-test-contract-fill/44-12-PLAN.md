---
phase: 44-api-hardening-and-test-contract-fill
plan: 12
type: execute
wave: 4
depends_on: [44-06]
files_modified:
  - e2e/**/*.spec.ts
  - .planning/test-contract/tc-allowlist.json
  - .planning/test-contract/tc-manifest.json
autonomous: true
requirements: [TEST-V5-01]
must_haves:
  truths:
    - "Every TC-E2E-* canonical ID (~12, e.g. TC-E2E-1A..1D, 2A, 2B-approve, 2B-reject, 2C, 2D, 3A, 4A, 4B) has a passing Playwright test with the ID as first title token"
    - "Allowlist contains no TC-E2E-* entries"
  artifacts:
    - path: e2e/**/*.spec.ts
      provides: "Playwright full-stack browser tests for the 12 TC-E2E flows"
  key_links:
    - from: e2e
      to: the running app
      via: "Playwright browser driver"
      pattern: "test\\('TC-E2E-"
---

<objective>
Wave C group C6: fill TC-E2E-* gaps using Playwright per CONTEXT test
tier split. Per RESEARCH, the TC-E2E-* set is small (~12 flows) but
each is a real end-to-end browser test, so keep scope tight.

Purpose: TEST-V5-01 E2E tier. The TC-E2E token extractor in Wave C
foundation (44-06) should recognize multi-segment IDs like `TC-E2E-1A`,
`TC-E2E-2B-approve`, `TC-E2E-2B-reject` — verify the regex covers these
before starting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-api-hardening-and-test-contract-fill/44-CONTEXT.md
@.planning/phases/44-api-hardening-and-test-contract-fill/44-RESEARCH.md
@.planning/v5.0-ARCHITECTURE.md
@.planning/test-contract/tc-allowlist.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify TC-E2E ID regex + extract canonical list</name>
  <files>.planning/test-contract/tc-canonical.json, scripts/extract-tc-ids-from-architecture.ts</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.13
    - scripts/extract-tc-ids-from-architecture.ts
  </read_first>
  <action>
    Confirm the canonical extractor regex matches TC-E2E IDs with mixed
    alphanumeric sub-segments (e.g. `TC-E2E-1A`, `TC-E2E-2B-approve`).
    If not, update the regex in `scripts/extract-tc-ids-from-architecture.ts`
    to allow `TC-[A-Z0-9]+(?:-[A-Z0-9a-z]+)*` for the trailing token, then
    re-run and re-commit `tc-canonical.json` and `tc-allowlist.json`.
  </action>
  <verify>
    <automated>pnpm tsx scripts/extract-tc-ids-from-architecture.ts && node -e "const c=require('./.planning/test-contract/tc-canonical.json'); const e2e=c.canonical.filter(x=>x.startsWith('TC-E2E-')); if(e2e.length<10) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - At least 10 distinct TC-E2E-* IDs appear in `tc-canonical.json`
    - IDs like `TC-E2E-1A` and `TC-E2E-2B-approve` are both present
  </acceptance_criteria>
  <done>Canonical list covers all TC-E2E IDs including multi-segment forms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Playwright specs for each TC-E2E-* flow</name>
  <files>e2e/**/*.spec.ts</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.13 (full TC-E2E flow descriptions)
    - .planning/test-contract/tc-allowlist.json
    - Existing Playwright config + any existing e2e tests (grep: `rg "TC-E2E|test\\(" e2e/`)
  </read_first>
  <action>
    For each TC-E2E-* ID, write one `test('TC-E2E-XXX ...')` block. Group
    flows into files by feature (e.g. `e2e/proposals.spec.ts`,
    `e2e/imports.spec.ts`, `e2e/admin.spec.ts`). Use the persistent PGlite
    or dev-database bootstrap pattern from existing Playwright infra — do
    NOT invent a new setup.

    Regenerate manifest and prune allowlist. TC-PERF-* tests may remain
    marked pending/skipped in CI per RESEARCH R4 — handled in 44-13.
  </action>
  <verify>
    <automated>pnpm exec playwright test && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.stillMissing | map(select(startswith("TC-E2E-"))) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - `pnpm exec playwright test` exits 0
    - tc-id-coverage test exits 0
  </acceptance_criteria>
  <done>TC-E2E group complete and green in Playwright.</done>
</task>

</tasks>

<verification>
- All TC-E2E-* IDs removed from allowlist
- Playwright suite green
</verification>

<success_criteria>
- ~12 TC-E2E flows passing in Playwright
</success_criteria>

<output>
After completion, create `.planning/phases/44-api-hardening-and-test-contract-fill/44-12-SUMMARY.md`.
</output>
</content>
</invoke>