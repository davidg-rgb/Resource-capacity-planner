---
phase: 44-api-hardening-and-test-contract-fill
plan: 09
type: execute
wave: 4
depends_on: [44-06]
files_modified:
  - src/features/import/__tests__/*.test.ts
  - src/features/export/__tests__/*.test.ts
  - .planning/test-contract/tc-allowlist.json
  - .planning/test-contract/tc-manifest.json
autonomous: true
requirements: [TEST-V5-01]
must_haves:
  truths:
    - "Every TC-IMP-* (18) and TC-EX-* (12) canonical ID has a passing test with TC-ID as first title token"
    - "ERR_US_WEEK_HEADERS wire shape is asserted by a TC-IMP-* test"
    - "Allowlist contains no TC-IMP-* or TC-EX-* entries"
  artifacts:
    - path: src/features/import/__tests__
      provides: "Excel import pipeline tests (TC-IMP)"
    - path: src/features/export/__tests__
      provides: "Export/download tests (TC-EX)"
  key_links:
    - from: src/features/import/__tests__
      to: src/features/import/**/*.ts
      via: "PGlite integration tests"
      pattern: "TC-IMP-\\d+"
---

<objective>
Wave C group C3: fill TC-IMP-* (18 canonical) and TC-EX-* (12 canonical)
gaps. TC-EX-* is noted in RESEARCH as having little existing work — some
or all of these tests may be new.

Purpose: TEST-V5-01. Shrink the allowlist by the import + export groups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-api-hardening-and-test-contract-fill/44-CONTEXT.md
@.planning/phases/44-api-hardening-and-test-contract-fill/44-RESEARCH.md
@.planning/v5.0-ARCHITECTURE.md
@.planning/test-contract/tc-allowlist.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill TC-IMP-* gaps (Excel parser + commit/rollback)</name>
  <files>src/features/import/__tests__/*.test.ts</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.9 + §15.19 (TC-IMP-*)
    - .planning/test-contract/tc-allowlist.json
    - Existing Phase 38 import tests (grep: `rg "TC-IMP" src/features/import tests/`)
  </read_first>
  <action>
    1. `jq '.stillMissing | map(select(startswith("TC-IMP-")))' .planning/test-contract/tc-allowlist.json`
    2. For each ID, locate §15 assertion text
    3. Add/rename tests. TC-IMP-006 specifically must assert the `ERR_US_WEEK_HEADERS` wire format (status 400, code `ERR_US_WEEK_HEADERS`) — reuse `@/lib/errors/codes` constant
    4. Regenerate manifest, prune allowlist
  </action>
  <verify>
    <automated>pnpm test -- src/features/import && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.stillMissing | map(select(startswith("TC-IMP-"))) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - TC-IMP-006 (or the ID tied to ERR_US_WEEK_HEADERS in §15.19) explicitly asserts `{ error: 'ERR_US_WEEK_HEADERS' }`
    - tc-id-coverage test exits 0
  </acceptance_criteria>
  <done>TC-IMP-* group complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill TC-EX-* gaps (export/download)</name>
  <files>src/features/export/__tests__/*.test.ts</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md (grep `rg -n "TC-EX-" .planning/v5.0-ARCHITECTURE.md`)
    - .planning/test-contract/tc-allowlist.json
  </read_first>
  <action>
    Same flow. TC-EX is flagged by RESEARCH as having little existing work — expect to write most of these tests from scratch. Use PGlite integration per CONTEXT tier split. If the export feature directory does not yet exist, colocate tests with the nearest existing export-related service module; do not invent a new feature folder.
  </action>
  <verify>
    <automated>pnpm test -- src/features/export && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.stillMissing | map(select(startswith("TC-EX-"))) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - All added TC-EX-* tests pass
    - tc-id-coverage test exits 0
  </acceptance_criteria>
  <done>TC-EX-* group complete.</done>
</task>

</tasks>

<verification>
- TC-IMP + TC-EX groups removed from allowlist
- Full test suite green
</verification>

<success_criteria>
- 30 TC-IDs filled (18 TC-IMP + 12 TC-EX)
</success_criteria>

<output>
After completion, create `.planning/phases/44-api-hardening-and-test-contract-fill/44-09-SUMMARY.md`.
</output>
</content>
</invoke>