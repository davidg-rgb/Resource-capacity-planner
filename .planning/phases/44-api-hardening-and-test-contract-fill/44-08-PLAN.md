---
phase: 44-api-hardening-and-test-contract-fill
plan: 08
type: execute
wave: 4
depends_on: [44-06]
files_modified:
  - src/app/api/v5/planning/**/__tests__/*.test.ts
  - src/app/api/v5/proposals/**/__tests__/*.test.ts
  - src/features/actuals/__tests__/*.test.ts
  - .planning/test-contract/tc-allowlist.json
  - .planning/test-contract/tc-manifest.json
autonomous: true
requirements: [TEST-V5-01]
must_haves:
  truths:
    - "Every TC-PS-*, TC-PR-*, TC-AC-*, TC-AR-*, TC-CP-* ID is present in the manifest as a passing test"
    - "Allowlist no longer contains entries from these 5 prefix groups"
  artifacts:
    - path: src/app/api/v5/planning/**/__tests__
      provides: "Planning direct-edit (TC-PS) + proposals (TC-PR) PGlite integration tests"
    - path: src/features/actuals/__tests__
      provides: "Actuals (TC-AC) + aggregation (TC-AR) + copy-forward (TC-CP) tests"
  key_links:
    - from: tests
      to: .planning/test-contract/tc-manifest.json
      via: "test-title token extraction"
      pattern: "TC-(PS|PR|AC|AR|CP)-\\d+"
---

<objective>
Wave C group C2: fill TC-PS-* (16), TC-PR-* (14), TC-AC-* (17), TC-AR-* (4),
TC-CP-* (4) gaps = up to 55 IDs. These are Vitest + PGlite integration
tests per CONTEXT test tier split.

Purpose: TEST-V5-01. Many of these IDs are already partially covered by
Phase 37/39/40 work — the task is mostly to rename existing tests to
prepend TC-IDs and add the actual-missing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-api-hardening-and-test-contract-fill/44-CONTEXT.md
@.planning/phases/44-api-hardening-and-test-contract-fill/44-RESEARCH.md
@.planning/v5.0-ARCHITECTURE.md
@.planning/test-contract/tc-canonical.json
@.planning/test-contract/tc-allowlist.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill TC-PS-* + TC-PR-* planning gaps</name>
  <files>src/app/api/v5/planning/**/__tests__/*.test.ts, src/app/api/v5/proposals/**/__tests__/*.test.ts</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.4 (TC-PS), §15.5 (TC-PR), §15.18
    - .planning/test-contract/tc-allowlist.json
    - Existing planning + proposals tests (grep: `rg "TC-PS|TC-PR" src/ tests/`)
  </read_first>
  <action>
    1. List targets: `jq '.stillMissing | map(select(test("^TC-(PS|PR)-")))' .planning/test-contract/tc-allowlist.json`
    2. For each ID, find the §15 assertion text (`rg -n "TC-PS-NNN" .planning/v5.0-ARCHITECTURE.md`)
    3. Either rename existing test title to prepend the ID, or add a new `it('TC-PS-NNN ...', ...)` PGlite test
    4. Regenerate manifest: `pnpm tsx scripts/generate-tc-manifest.ts`
    5. Prune allowlist entries
  </action>
  <verify>
    <automated>pnpm test -- src/app/api/v5/planning src/app/api/v5/proposals && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.stillMissing | map(select(test("^TC-(PS|PR)-"))) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - All TC-PS-* + TC-PR-* tests pass
    - tc-id-coverage test exits 0
  </acceptance_criteria>
  <done>TC-PS + TC-PR groups covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill TC-AC-* + TC-AR-* + TC-CP-* actuals/aggregation/copy-forward gaps</name>
  <files>src/features/actuals/__tests__/*.test.ts, src/features/aggregation/__tests__/*.test.ts</files>
  <read_first>
    - .planning/v5.0-ARCHITECTURE.md §15.6, §15.7, and sections referencing TC-AR-* / TC-CP-*
    - .planning/test-contract/tc-allowlist.json
    - Existing tests (grep: `rg "TC-AC|TC-AR|TC-CP" src/ tests/`)
  </read_first>
  <action>
    Same flow as Task 1: list → add/rename → regenerate manifest → prune allowlist.
    Focus on PGlite integration tests under `src/features/actuals/__tests__` and similar. For TC-CP-* copy-forward and TC-AR-* aggregation, use existing service tests or add new ones colocated with the feature.
  </action>
  <verify>
    <automated>pnpm test -- src/features/actuals src/features/aggregation && pnpm tsx scripts/generate-tc-manifest.ts && pnpm test -- tests/invariants/tc-id-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.stillMissing | map(select(test("^TC-(AC|AR|CP)-"))) | length' .planning/test-contract/tc-allowlist.json` returns 0
    - All added/renamed tests pass
    - tc-id-coverage test exits 0
  </acceptance_criteria>
  <done>TC-AC + TC-AR + TC-CP groups covered.</done>
</task>

</tasks>

<verification>
- TC-PS + TC-PR + TC-AC + TC-AR + TC-CP all removed from allowlist
- tc-id-coverage green
</verification>

<success_criteria>
- Up to 55 TC-IDs filled
- No regressions in existing Phase 37/39 tests
</success_criteria>

<output>
After completion, create `.planning/phases/44-api-hardening-and-test-contract-fill/44-08-SUMMARY.md`.
</output>
</content>
</invoke>