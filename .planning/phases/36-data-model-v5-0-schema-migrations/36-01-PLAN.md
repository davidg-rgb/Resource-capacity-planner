---
phase: 36-data-model-v5-0-schema-migrations
plan: 01
type: execute
wave: 1
depends_on: [35-01]
files_modified:
  - src/db/schema.ts
  - drizzle/migrations/0004_v5_data_model.sql
  - drizzle/migrations/meta/_journal.json
  - drizzle/seed.ts
  - src/features/change-log/__tests__/v5-schema.contract.test.ts
autonomous: true
gap_closure: false
requirements: [ACT-01, IMP-01, PROP-01, PROP-02]
must_haves:
  truths:
    - "`actual_entries` table exists with columns (id, organization_id, person_id, project_id, date, hours numeric(5,2), source enum('import'|'manual'), import_batch_id nullable, created_at, updated_at) — TC-DB-003, TC-DB-004"
    - "`actual_entries` has UNIQUE index `actuals_org_person_project_date_uniq` on (organization_id, person_id, project_id, date) — TC-DB-003"
    - "`actual_entries.hours` PostgreSQL column type is `numeric(5,2)` (NOT integer) — TC-DB-004"
    - "`allocation_proposals` table exists with all columns from ARCHITECTURE §7.1 incl. month, proposed_hours numeric(5,2), status, rejection_reason, requested_by, decided_by/at, parent_proposal_id self-FK, target_department_id FK departments — TC-DB-001"
    - "`proposal_status` enum contains exactly the 5 values: proposed, approved, rejected, withdrawn, superseded (no more, no less) — TC-DB-008"
    - "`actual_source` enum contains exactly: import, manual"
    - "`allocation_proposals.parent_proposal_id` self-FK uses ON DELETE RESTRICT (rejected proposals cannot be deleted)"
    - "`import_batches` table exists with columns from ARCHITECTURE §7.3 including `reversal_payload jsonb` nullable, `superseded_at`, `superseded_by_batch_id` self-FK — TC-DB-006"
    - "`projects.lead_pm_person_id` UUID nullable column added with FK to people.id; partial index `projects_lead_pm_idx` on (organization_id, lead_pm_person_id) WHERE lead_pm_person_id IS NOT NULL — TC-DB-002"
    - "The pre-existing v4.0 unique index `allocations_org_person_project_month_uniq` is still present and unchanged after this migration — TC-DB-002"
    - "No other v4.0 tables (allocations, people, departments, programs, scenarios, etc.) are mutated by migration 0004 — only the additive `lead_pm_person_id` column on projects"
    - "`pnpm db:generate` produces `drizzle/migrations/0004_v5_data_model.sql` deterministically; running it twice produces no schema diff (idempotent vs current schema.ts)"
    - "`pnpm db:migrate` applies cleanly against a fresh Neon branch on top of 0003_busy_black_bird.sql with zero errors"
    - "`pnpm db:seed` still completes successfully (with at least one demo project carrying lead_pm_person_id pointing at a seeded PM person)"
    - "All v4.0 screens still load against the migrated DB — verified by `pnpm typecheck && pnpm build` (no compile breakage from new relations) and a smoke test that imports schema and asserts the union types — TC-DB-001..010"
    - "The change_log table from Phase 35 is NOT re-declared, NOT dropped, NOT altered — schema diff for change_log is empty in 0004"
  artifacts:
    - path: "src/db/schema.ts"
      provides: |
        Adds (strictly additive):
        - Enums: `proposal_status` (proposed/approved/rejected/withdrawn/superseded), `actual_source` (import/manual)
        - `projects.leadPmPersonId` column (uuid, nullable, FK people.id) PLUS partial index `projects_lead_pm_idx`
        - pgTable `import_batches` with all §7.3 fields + 2 indexes (`batches_org_committed_idx` DESC, partial `batches_org_rollback_idx` WHERE rolled_back_at IS NULL) and self-FK `superseded_by_batch_id`
        - pgTable `actual_entries` with all §7.2 fields + UNIQUE `actuals_org_person_project_date_uniq` + 4 secondary indexes
        - pgTable `allocation_proposals` with all §7.1 fields + 6 indexes, `parent_proposal_id` self-FK ON DELETE RESTRICT, `target_department_id` FK departments
        - Drizzle relations entries for the 3 new tables (organization, person, project, importBatch, parentProposal, targetDepartment)
      contains: "pgTable('actual_entries'"
    - path: "drizzle/migrations/0004_v5_data_model.sql"
      provides: "drizzle-kit generated migration. Creates 2 enums, 3 tables, all indexes/constraints, and ALTERs `projects` to add `lead_pm_person_id` + the partial index. Sequential after 0003_busy_black_bird.sql. Touches NO other v4.0 tables and does NOT redefine change_log."
    - path: "drizzle/seed.ts"
      provides: "Updates the existing demo seed so that (a) at least one seeded project has `leadPmPersonId` set to a seeded person (the PM persona Anna if present, else the first person), and (b) the seed still completes idempotently. No new tables are required to be seeded — empty actual_entries / allocation_proposals / import_batches are valid Phase 36 end state. Future phases populate them."
    - path: "src/features/change-log/__tests__/v5-schema.contract.test.ts"
      provides: |
        Lightweight pglite-backed contract test (re-uses Phase 35 pglite harness pattern). Spins up an in-process Postgres, runs the slice of 0004 SQL needed for these assertions, and asserts:
        - actual_entries.hours is numeric(5,2) via `information_schema.columns` query (TC-DB-004)
        - actuals unique index exists with the 4-column key in correct order (TC-DB-003)
        - proposal_status enum has exactly the 5 values via `pg_enum` query (TC-DB-008)
        - import_batches.reversal_payload column exists with jsonb type (TC-DB-006)
        - projects.lead_pm_person_id column exists, is nullable, FK to people (TC-DB-002)
        - The v4.0 `allocations_org_person_project_month_uniq` constraint name is still present after the migration runs (TC-DB-002 preservation clause)
        Test file lives under existing `src/features/change-log/__tests__/` so vitest auto-discovers it; placement is intentional because that directory already has the pglite harness wired up in Phase 35.
      min_lines: 60
  key_links:
    - from: "src/db/schema.ts (projects)"
      to: "src/db/schema.ts (people)"
      via: "leadPmPersonId uuid().references(() => people.id)"
      pattern: "leadPmPersonId.*references"
    - from: "src/db/schema.ts (actualEntries)"
      to: "src/db/schema.ts (importBatches)"
      via: "importBatchId nullable FK"
      pattern: "importBatchId.*references\\(\\(\\) => importBatches"
    - from: "src/db/schema.ts (allocationProposals)"
      to: "src/db/schema.ts (allocationProposals — self)"
      via: "parentProposalId self-FK ON DELETE RESTRICT"
      pattern: "parentProposalId.*onDelete: 'restrict'"
    - from: "src/db/schema.ts (allocationProposals)"
      to: "src/db/schema.ts (departments)"
      via: "targetDepartmentId FK departments — denormalized for approval queue"
      pattern: "targetDepartmentId.*references\\(\\(\\) => departments"
    - from: "drizzle/migrations/0004_v5_data_model.sql"
      to: "drizzle/migrations/0003_busy_black_bird.sql"
      via: "sequential drizzle-kit migration ordering recorded in meta/_journal.json"
      pattern: "0004_v5_data_model"
---

<objective>
Land the v5.0 data-model migration: three new tables (`actual_entries`, `allocation_proposals`, `import_batches`), the only v4.0 table mutation (`projects.lead_pm_person_id` + partial index), and the supporting enums (`proposal_status`, `actual_source`).

Strictly additive on top of Phase 35's `0003_busy_black_bird.sql`. The change_log table from Phase 35 is NOT touched. After this plan, downstream phases (37 actuals layer, 38 import pipeline, 39 proposal workflow) have a stable schema to build services on top of.

Purpose: Unblock all v5.0 service work. Phase 36 is pure SQL/Drizzle plumbing — no business logic, no API routes, no UI.
Output: Migration `0004_v5_data_model.sql`, updated `src/db/schema.ts`, updated seed, schema-contract test asserting TC-DB-001..010 against the live migrated schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v5.0-ARCHITECTURE.md
@.planning/phases/35-foundations-universal-change_log-infrastructure/35-01-SUMMARY.md
@src/db/schema.ts
@drizzle/seed.ts
@drizzle/migrations/0003_busy_black_bird.sql

<interfaces>
<!-- Existing tables this plan references — DO NOT redefine, only ADD relations to. -->

From src/db/schema.ts (existing v4.0 + Phase 35):
- `organizations` (uuid id) — root tenant table
- `people` (uuid id, organizationId, departmentId) — referenced by lead_pm_person_id and by all 3 new tables
- `projects` (uuid id, organizationId) — gains `leadPmPersonId` column
- `departments` (uuid id, organizationId) — referenced by allocation_proposals.target_department_id
- `allocations` — UNTOUCHED. Existing unique index `allocations_org_person_project_month_uniq` MUST survive
- `importSessions` (uuid id) — referenced by import_batches.import_session_id
- `changeLog` + `changeLogEntityEnum` + `changeLogActionEnum` — landed in Phase 35 migration 0003_busy_black_bird.sql, DO NOT re-declare
- Drizzle imports already in scope: `pgTable, pgEnum, uuid, varchar, text, integer, boolean, date, timestamp, jsonb, index, unique, numeric` — `numeric` may need to be added to the `drizzle-orm/pg-core` import line.

ARCHITECTURE source-of-truth sections:
- §7.0 lead_pm_person_id
- §7.1 allocation_proposals (full column list, 6 indexes, parent self-FK ON DELETE RESTRICT, target_department_id semantics)
- §7.2 actual_entries (numeric(5,2) hours, unique idempotency key, 4 secondary indexes)
- §7.3 import_batches (reversal_payload jsonb, superseded_at, self-FK)
- §15 testable contract: TC-DB-001..010
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add v5.0 enums + 3 new tables + projects.leadPmPersonId to schema.ts</name>
  <files>src/db/schema.ts</files>
  <behavior>
    After this task, `pnpm typecheck` and `pnpm db:generate` produce a clean migration containing exactly:
    - 2 new enums: `proposal_status` (5 values), `actual_source` (2 values)
    - 1 ALTER on `projects`: add column `lead_pm_person_id uuid` + FK to people + partial index
    - 3 CREATE TABLE: `import_batches`, `actual_entries`, `allocation_proposals` with all columns/indexes per ARCHITECTURE §7.1–§7.3
    - Zero diffs against existing tables (allocations, people, departments, change_log, etc.)
  </behavior>
  <action>
    Edit `src/db/schema.ts`:

    1. Add `numeric` to the `drizzle-orm/pg-core` import line if not already present.

    2. Add two new enums near the existing v5 change_log enums (after line ~83):
       ```ts
       export const proposalStatusEnum = pgEnum('proposal_status', [
         'proposed', 'approved', 'rejected', 'withdrawn', 'superseded',
       ]);
       export const actualSourceEnum = pgEnum('actual_source', ['import', 'manual']);
       ```

    3. Modify the existing `projects` pgTable definition (lines ~193-215) — add ONE column and ONE index, do NOT touch any existing column or index:
       ```ts
       leadPmPersonId: uuid('lead_pm_person_id').references(() => people.id),
       ```
       In the index list, add:
       ```ts
       index('projects_lead_pm_idx')
         .on(t.organizationId, t.leadPmPersonId)
         .where(sql`${t.leadPmPersonId} IS NOT NULL`),
       ```
       (Add `import { sql } from 'drizzle-orm'` if needed.)

    4. After the existing `changeLog` pgTable block (~line 542), add THREE new pgTables in this order (importBatches first because actualEntries FKs to it):

       a) `importBatches` per ARCHITECTURE §7.3 — all columns including `importSessionId` FK to importSessions, `fileName` text, `committedBy` text, `committedAt` timestamp, `overrideManualEdits` boolean NOT NULL, `rowsInserted/Updated/SkippedManual/SkippedPriorBatch` integers, `reversalPayload` jsonb nullable, `rolledBackAt`/`rolledBackBy` nullable, `supersededAt` timestamp nullable, `supersededByBatchId` self-FK uuid nullable. Indexes: `batches_org_committed_idx` on (organizationId, committedAt.desc()), partial `batches_org_rollback_idx` on (organizationId, rolledBackAt) WHERE rolledBackAt IS NULL.

       b) `actualEntries` per §7.2 — id, organizationId FK orgs, personId FK people, projectId FK projects, date `date('date', { mode: 'string' })` NOT NULL, `hours: numeric('hours', { precision: 5, scale: 2 }).notNull()`, `source: actualSourceEnum('source').notNull()`, `importBatchId: uuid('import_batch_id').references(() => importBatches.id)` (nullable), createdAt, updatedAt. Indexes: UNIQUE `actuals_org_person_project_date_uniq` on (orgId, personId, projectId, date), `actuals_org_date_idx`, `actuals_org_person_date_idx`, `actuals_org_project_date_idx`, `actuals_batch_idx`.

       c) `allocationProposals` per §7.1 — all columns. Critical details:
          - `month: date('month', { mode: 'string' }).notNull()` — MUST match allocations.month exactly
          - `proposedHours: numeric('proposed_hours', { precision: 5, scale: 2 }).notNull()` — REQUIREMENTS PROP-01 specifies numeric(5,2); ARCHITECTURE table says integer but requirements file is the source of truth and downstream actuals/distribution math needs decimals
          - `status: proposalStatusEnum('status').notNull().default('proposed')`
          - `rejectionReason: varchar('rejection_reason', { length: 1000 })` nullable
          - `requestedBy: text('requested_by').notNull()`, `decidedBy: text('decided_by')` nullable, `decidedAt` timestamptz nullable
          - `parentProposalId: uuid('parent_proposal_id').references((): AnyPgColumn => allocationProposals.id, { onDelete: 'restrict' })` — self-FK ON DELETE RESTRICT (use `AnyPgColumn` cast pattern from drizzle docs to handle the forward ref)
          - `targetDepartmentId: uuid('target_department_id').notNull().references(() => departments.id)`
          - createdAt/updatedAt
          - 6 indexes per §7.1: `proposals_org_status_idx`, `proposals_org_dept_status_idx`, `proposals_org_person_status_idx`, `proposals_org_person_project_month_idx`, `proposals_requester_idx`, `proposals_parent_idx`

    5. Add Drizzle `relations(...)` blocks for the 3 new tables at the bottom of the relations section (after `scenarioTempEntitiesRelations`). Each new relation must wire: organization, person (where applicable), project (where applicable), importBatch (actualEntries), parentProposal + targetDepartment (allocationProposals).

    6. Extend `organizationsRelations` and `peopleRelations` and `projectsRelations` and `departmentsRelations` to include the new `many(...)` references where appropriate. Do NOT remove any existing entries.

    Finally run:
    ```bash
    pnpm typecheck
    ```
    Must be green before moving to Task 2. If `numeric` import or `sql` import is missing, fix and re-run.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
    `src/db/schema.ts` exports `proposalStatusEnum`, `actualSourceEnum`, `importBatches`, `actualEntries`, `allocationProposals`. `projects` definition has `leadPmPersonId` column and `projects_lead_pm_idx` partial index. `pnpm typecheck` passes. No edits to any other v4.0 table or to `changeLog`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply migration 0004_v5_data_model.sql</name>
  <files>drizzle/migrations/0004_v5_data_model.sql, drizzle/migrations/meta/_journal.json</files>
  <action>
    1. Run drizzle-kit generator:
       ```bash
       pnpm db:generate
       ```
       This must produce `drizzle/migrations/0004_*.sql` (drizzle picks the suffix automatically; if it differs from `0004_v5_data_model.sql`, accept the generated name and update this plan's frontmatter `files_modified` accordingly — do NOT rename the file by hand, drizzle journals it).

    2. Open the generated SQL and verify by inspection:
       - Creates `proposal_status` and `actual_source` enums (and ONLY those — no recreation of `change_log_entity` / `change_log_action` / `change_log` table — if it tries to drop/recreate change_log, STOP and reconcile schema.ts)
       - `CREATE TABLE import_batches`, `CREATE TABLE actual_entries`, `CREATE TABLE allocation_proposals` with all columns
       - `actual_entries.hours` is `numeric(5, 2)` (not integer)
       - `proposal_status` enum has exactly the 5 values in order
       - `ALTER TABLE projects ADD COLUMN lead_pm_person_id uuid` + FK + the partial index
       - UNIQUE constraint on actual_entries (org, person, project, date)
       - parent_proposal_id has `ON DELETE RESTRICT`
       - NO `DROP` or `ALTER` on allocations, people, departments, change_log, etc.
       - The pre-existing `allocations_org_person_project_month_uniq` constraint name does NOT appear in any DROP statement

    3. Apply against the dev DB:
       ```bash
       pnpm db:migrate
       ```
       Must complete without error. If the migration fails partway, do NOT hand-edit the migration file — fix the schema.ts and re-run `pnpm db:generate` (delete the broken 0004 file first, plus its journal entry).

    4. Sanity-check existing tables still query:
       ```bash
       pnpm typecheck && pnpm build
       ```

    Commit with message: `feat(36): add v5 data model migration (actual_entries, allocation_proposals, import_batches, projects.lead_pm_person_id)`.
  </action>
  <verify>
    <automated>pnpm db:generate && pnpm db:migrate && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>
    `drizzle/migrations/0004_*.sql` exists, `_journal.json` lists it as the 5th entry, all 4 commands above are green, the SQL contains the expected CREATE/ALTER statements and zero unexpected DROPs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Schema contract test for TC-DB-001..010</name>
  <files>src/features/change-log/__tests__/v5-schema.contract.test.ts</files>
  <behavior>
    Test file uses the same pglite pattern Phase 35 established (`@electric-sql/pglite` + `drizzle-orm/pglite`). It boots an in-process Postgres, runs the relevant CREATE statements from migration 0004 (or applies the migration file directly via `fs.readFileSync` + `pglite.exec`), then asserts via `information_schema` / `pg_enum` / `pg_indexes` queries:

    - Test 1 (TC-DB-001): `allocation_proposals` table exists with all 14 expected columns
    - Test 2 (TC-DB-002): `projects.lead_pm_person_id` column exists, is_nullable=YES, data_type=uuid; partial index `projects_lead_pm_idx` exists; pre-existing `allocations_org_person_project_month_uniq` is still present (apply 0001/0002/0003/0004 in order from disk so this is meaningful)
    - Test 3 (TC-DB-003): UNIQUE index `actuals_org_person_project_date_uniq` exists with column list (organization_id, person_id, project_id, date) in that order
    - Test 4 (TC-DB-004): `actual_entries.hours` has data_type=numeric, numeric_precision=5, numeric_scale=2
    - Test 5 (TC-DB-006): `import_batches.reversal_payload` column exists with data_type=jsonb, is_nullable=YES
    - Test 6 (TC-DB-007): `allocation_proposals` FK constraints to people, projects, organizations, departments are all present
    - Test 7 (TC-DB-008): `pg_enum` query returns exactly `['proposed','approved','rejected','withdrawn','superseded']` for proposal_status, AND exactly `['import','manual']` for actual_source
    - Test 8 (TC-DB-010 sanity): `change_log` table from Phase 35 is still present and unchanged (column count matches Phase 35 spec — 9 columns)

    All 8 sub-tests must pass against the migrated pglite instance.
  </behavior>
  <action>
    Create `src/features/change-log/__tests__/v5-schema.contract.test.ts`. Reference the existing Phase 35 pglite test (`change-log.service.test.ts`) for the exact pglite + drizzle bootstrap pattern.

    Structure:
    ```ts
    import { describe, it, expect, beforeAll } from 'vitest';
    import { PGlite } from '@electric-sql/pglite';
    import { drizzle } from 'drizzle-orm/pglite';
    import { sql } from 'drizzle-orm';
    import { readFileSync } from 'node:fs';
    import { join } from 'node:path';

    let pg: PGlite;
    let db: ReturnType<typeof drizzle>;

    beforeAll(async () => {
      pg = new PGlite();
      db = drizzle(pg);
      // Apply all migrations sequentially so we test the real cumulative schema
      const migrationsDir = join(process.cwd(), 'drizzle/migrations');
      for (const file of ['0000_tearful_the_initiative.sql','0001_goofy_doctor_doom.sql','0002_oval_butterfly.sql','0003_busy_black_bird.sql','0004_<actual_filename>.sql']) {
        const sqlText = readFileSync(join(migrationsDir, file), 'utf8');
        // drizzle migrations use --> statement-breakpoint markers
        for (const stmt of sqlText.split('--> statement-breakpoint')) {
          const trimmed = stmt.trim();
          if (trimmed) await pg.exec(trimmed);
        }
      }
    });
    ```

    Replace `<actual_filename>` with the actual generated 0004 filename. If pre-existing migrations 0000–0002 were generated against neon-http and contain non-pglite-compatible syntax, narrow the bootstrap to only 0003 + 0004 plus a minimal CREATE TABLE for organizations/people/projects/departments/import_sessions (mirroring the Phase 35 test approach). Pick whichever is faster to get green.

    Then write the 8 `it(...)` blocks listed in the behavior section, querying `information_schema.columns`, `information_schema.table_constraints`, `pg_indexes`, and `pg_enum` via `pg.query(...)` (raw SQL is fine, we're testing the database schema, not the ORM).

    Run:
    ```bash
    pnpm test src/features/change-log/__tests__/v5-schema.contract.test.ts
    ```

    Commit: `test(36): add v5 schema contract test (TC-DB-001..010)`.
  </action>
  <verify>
    <automated>pnpm test src/features/change-log/__tests__/v5-schema.contract.test.ts</automated>
  </verify>
  <done>
    All 8 sub-tests pass. The test runs in isolation under pglite (no live DB needed) and re-runs deterministically on CI.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update seed.ts to set leadPmPersonId + smoke verify v4 still loads</name>
  <files>drizzle/seed.ts</files>
  <action>
    1. Open `drizzle/seed.ts`. Find the section that inserts demo projects. After projects are inserted and people are inserted, add a single UPDATE that sets `leadPmPersonId` on at least one demo project to the first person's id (or to the seeded "Anna" PM persona if she exists by name match — `firstName='Anna'`).

       Pattern:
       ```ts
       const [pm] = await db.select().from(schema.people)
         .where(eq(schema.people.organizationId, org.id))
         .limit(1);
       if (pm) {
         await db.update(schema.projects)
           .set({ leadPmPersonId: pm.id })
           .where(eq(schema.projects.organizationId, org.id));
         // Or restrict to a single project if you prefer one PM owns one project
       }
       ```

       Do NOT seed `actual_entries`, `allocation_proposals`, or `import_batches` — empty tables are the correct Phase 36 end state. Phase 37+ owns seeding those.

    2. Verify the seed still completes against the dev DB:
       ```bash
       pnpm db:seed
       ```
       Idempotency clause should mean it skips if already seeded — that's fine. To prove the new code path actually runs, you may temporarily wipe the demo org and re-seed (developer judgment; not required for sign-off).

    3. Smoke-test that the app still builds and key v4 routes compile:
       ```bash
       pnpm build
       pnpm lint
       ```

    Commit: `chore(36): seed leadPmPersonId on demo projects`.
  </action>
  <verify>
    <automated>pnpm db:seed && pnpm build && pnpm lint</automated>
  </verify>
  <done>
    `drizzle/seed.ts` sets `leadPmPersonId` on at least one demo project. `pnpm db:seed` completes successfully. `pnpm build` and `pnpm lint` are green. No v4.0 screens regressed (build success is the proxy — full E2E lives in Phase 44).
  </done>
</task>

</tasks>

<verification>
End-to-end Phase 36 verification chain (run in order from a clean tree):

```bash
pnpm typecheck                                                    # Task 1
pnpm db:generate && pnpm db:migrate && pnpm build                  # Task 2
pnpm test src/features/change-log/__tests__/v5-schema.contract.test.ts  # Task 3
pnpm db:seed && pnpm lint                                          # Task 4
```

All four must be green. The schema-contract test is the goal-backward proof point: if it passes, TC-DB-001..010 are objectively satisfied.
</verification>

<success_criteria>
- [ ] `proposal_status` and `actual_source` enums exist in DB with exactly the documented values (TC-DB-008)
- [ ] `actual_entries`, `allocation_proposals`, `import_batches` tables exist with all columns from §7.1–§7.3 (TC-DB-001, TC-DB-005, TC-DB-006)
- [ ] `actual_entries.hours` is numeric(5,2) and `actuals_org_person_project_date_uniq` is the idempotency key (TC-DB-003, TC-DB-004)
- [ ] `projects.lead_pm_person_id` exists with partial index, is the ONLY v4 mutation (TC-DB-002)
- [ ] `allocations_org_person_project_month_uniq` from v4.0 is preserved (TC-DB-002 preservation clause)
- [ ] `change_log` from Phase 35 is untouched
- [ ] `pnpm db:generate && pnpm db:migrate && pnpm db:seed && pnpm build && pnpm test && pnpm lint` all green
- [ ] Schema contract test asserting TC-DB-001..010 lives in the repo and passes
</success_criteria>

<output>
After completion, create `.planning/phases/36-data-model-v5-0-schema-migrations/36-01-SUMMARY.md` documenting:
- Final 0004 migration filename and the exact SQL it produced
- Any deviations (esp. if proposed_hours type was reconciled differently from REQUIREMENTS vs ARCHITECTURE)
- Confirmation that Phase 35 change_log was not touched
- Hand-off note for Phase 37: `actualEntries`, `actualSourceEnum`, `importBatches` are now exported from `src/db/schema.ts` and ready for the actuals service to import
</output>
