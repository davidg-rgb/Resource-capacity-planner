---
phase: 53-chrome-polish
plan: 04
type: execute
wave: 2
depends_on: ["53-01", "53-03"]
files_modified:
  - src/components/alerts/strategic-alerts-banner.tsx
  - src/components/alerts/__tests__/strategic-alerts-banner.test.tsx
  - src/app/(app)/dashboard/dashboard-content.tsx
  - src/features/dashboard/default-layouts.ts
  - src/features/dashboard/__tests__/default-layouts.test.ts
  - src/db/migrations/20260422_polish_strip_widgets.sql
  - src/db/migrations/__tests__/polish-strip-widgets.test.ts
autonomous: true
requirements: [POLISH-04, POLISH-06]
must_haves:
  truths:
    - "`DEFAULT_LAYOUTS['manager:desktop']` no longer contains `bench-report` (removed per POLISH-04); `availability-finder` is the sole availability surface"
    - "`DEFAULT_LAYOUTS['manager:mobile']` no longer contains `strategic-alerts` (removed per POLISH-06 / REQ wording: 'removed from manager:mobile layout')"
    - "`LEGACY_LAYOUTS` retains BOTH `bench-report` (manager:desktop:7) AND `strategic-alerts` (manager:mobile:7) unchanged (flag-off rollback)"
    - "`<StrategicAlertsBanner>` renders on the manager dashboard (dashboard-content.tsx) when `uiV6Polish=true` AND `alerts.length > 0`; renders nothing otherwise"
    - "Banner is a single-line summary with i18n copy (`v6.polish.banner.title` + `v6.polish.banner.cta`); CTA links to `/alerts`"
    - "One-shot SQL migration strips `bench-report` AND `strategic-alerts` widget IDs from tenant custom `dashboard_layouts.layout` rows in a single transaction"
    - "Old widget files `bench-report-widget.tsx` + `strategic-alerts-widget.tsx` remain on disk (D-06 — physical deletion deferred); their registrations in `widgets/index.ts` are UNCHANGED (LEGACY_LAYOUTS rendering depends on them)"
  artifacts:
    - path: "src/components/alerts/strategic-alerts-banner.tsx"
      provides: "Inline banner component: consumes useAlerts(monthFrom, monthTo), renders count + CTA to /alerts when count > 0; returns null when empty"
      exports: ["StrategicAlertsBanner"]
      min_lines: 30
    - path: "src/app/(app)/dashboard/dashboard-content.tsx"
      provides: "Mount point — banner rendered above <DashboardGrid dashboardId='manager'> when flags.uiV6Polish=true"
      contains: "StrategicAlertsBanner"
    - path: "src/features/dashboard/default-layouts.ts"
      provides: "DEFAULT_LAYOUTS: manager:desktop drops bench-report (availability-finder slides 8→7); manager:mobile drops strategic-alerts; LEGACY_LAYOUTS unchanged"
      contains: "bench-report"
    - path: "src/db/migrations/20260422_polish_strip_widgets.sql"
      provides: "One-shot migration: strip bench-report + strategic-alerts widget IDs from dashboard_layouts.layout"
      contains: "bench-report"
  key_links:
    - from: "src/app/(app)/dashboard/dashboard-content.tsx"
      to: "src/components/alerts/strategic-alerts-banner.tsx"
      via: "<StrategicAlertsBanner/> mounted above <DashboardGrid dashboardId='manager'> behind flag"
      pattern: "StrategicAlertsBanner"
    - from: "src/components/alerts/strategic-alerts-banner.tsx"
      to: "useAlerts hook"
      via: "re-uses existing useAlerts(monthFrom, monthTo) — no new endpoint"
      pattern: "useAlerts"
    - from: "src/features/dashboard/default-layouts.ts"
      to: "strategic-alerts + bench-report removed from DEFAULT_LAYOUTS"
      via: "2 removals: manager:desktop position 7 (bench-report), manager:mobile position 7 (strategic-alerts)"
      pattern: "^(?!.*LEGACY).*bench-report|^(?!.*LEGACY).*strategic-alerts"
---

<objective>
Ship Wave 2's second plan (executes sequentially after Plan 03 because both edit `src/features/dashboard/default-layouts.ts`): **POLISH-04 (delete `bench-report` from manager dashboard)** and **POLISH-06 (replace `strategic-alerts` with an inline banner on the manager dashboard)**. Bundling these is correct because (a) they touch the same file, (b) they share the same migration path (both strip widget IDs from `DEFAULT_LAYOUTS` + tenant custom layouts), and (c) they share the same rollback story (LEGACY_LAYOUTS retains both).

Per D-06, this plan DOES NOT physically delete `bench-report-widget.tsx` or `strategic-alerts-widget.tsx`. They stay registered in `widgets/index.ts` because `LEGACY_LAYOUTS` (flag-off) still references them — the Phase 51 defensive fallback is the last-ditch safety, not the primary mechanism. Physical file deletion is a separate post-rollout cleanup phase.

POLISH-06 interpretation (per RESEARCH A6): REQ wording "removed from manager:mobile layout" is followed LITERALLY — we only strip `strategic-alerts` from `manager:mobile` in `DEFAULT_LAYOUTS`. `manager:desktop` does NOT currently have `strategic-alerts` in the post-Phase-51 layout (confirmed by reading `default-layouts.ts:82-92` — only `manager:mobile:7` has it). The inline banner renders above the dashboard grid on ALL device classes when the flag is on; the banner is orthogonal to the widget removal.

Output: 1 new banner component + test, dashboard-content.tsx mount point update, default-layouts.ts updates (2 removals), one-shot SQL migration + integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-chrome-polish/53-RESEARCH.md
@.planning/phases/53-chrome-polish/53-01-PLAN.md
@.planning/phases/53-chrome-polish/53-03-PLAN.md
@.planning/phases/51-lean-cleanup-duplicate-removal/51-02-PLAN.md
@src/features/dashboard/widgets/bench-report-widget.tsx
@src/features/dashboard/widgets/strategic-alerts-widget.tsx
@src/features/dashboard/default-layouts.ts
@src/app/(app)/dashboard/dashboard-content.tsx
@src/hooks/use-alerts.ts
@src/components/charts/strategic-alerts.tsx
@src/components/alerts/alert-badge.tsx
@src/components/alerts/alert-list.tsx

<interfaces>
<!-- Banner target shape (`src/components/alerts/strategic-alerts-banner.tsx`): -->
```tsx
'use client';
import Link from 'next/link';
import { AlertTriangle } from 'lucide-react';
import { useTranslations } from 'next-intl';

import { useAlerts } from '@/hooks/use-alerts';
import { generateMonthRange, getCurrentMonth } from '@/lib/date-utils';

export function StrategicAlertsBanner() {
  const t = useTranslations('v6.polish.banner');
  const monthFrom = getCurrentMonth();
  const monthTo = generateMonthRange(monthFrom, 3).at(-1)!;
  const { data: alerts } = useAlerts(monthFrom, monthTo);

  const count = alerts?.length ?? 0;
  if (count === 0) return null;

  return (
    <div role="alert"
         className="border-warning-container bg-warning-container/40 text-on-warning-container
                    mb-6 flex items-center justify-between gap-3 rounded-md border px-4 py-3">
      <div className="flex items-center gap-2">
        <AlertTriangle size={18} />
        <span className="font-medium">{t('title', { count })}</span>
      </div>
      <Link href="/alerts"
            data-testid="strategic-alerts-banner-cta"
            className="text-primary text-sm font-semibold hover:underline">
        {t('cta')}
      </Link>
    </div>
  );
}
```

(Exact Tailwind classes should match existing banner conventions — if `bg-warning-container` doesn't exist in the project's Tailwind theme, fall back to `bg-yellow-50 text-yellow-900 border-yellow-200`. Verify in `tailwind.config.ts` or by grepping existing `border-warning`.)

**Mount point in `dashboard-content.tsx`** — exact shape depends on current file structure; target is:
```tsx
import { StrategicAlertsBanner } from '@/components/alerts/strategic-alerts-banner';
import { useFlags } from '@/features/flags/flag.context';

// inside the component, ABOVE the first <DashboardGrid dashboardId="manager"> usage
const flags = useFlags();
return (
  <>
    {flags.uiV6Polish && <StrategicAlertsBanner />}
    {/* existing <DashboardGrid ... /> */}
  </>
);
```

**Default-layouts target diff** (`src/features/dashboard/default-layouts.ts`):

Current DEFAULT_LAYOUTS `'manager:desktop'` (post-Phase-51, post-Plan 03):
```
[0] kpi-cards
[1] heat-map-summary-card
[2] capacity-gauges (6-wide)
[3] department-bar-chart (6-wide)
[4] utilization-sparklines (6-wide)
[5] discipline-breakdown (6-wide)  // changed by Plan 03
[6] capacity-forecast (12-wide)
[7] bench-report (12-wide)          // <<< REMOVE (POLISH-04)
[8] availability-finder (12-wide)   // <<< slides to position 7
```

AFTER this plan:
```
[0] kpi-cards
[1] heat-map-summary-card
[2] capacity-gauges
[3] department-bar-chart
[4] utilization-sparklines
[5] discipline-breakdown
[6] capacity-forecast
[7] availability-finder  // was position 8
```

Current DEFAULT_LAYOUTS `'manager:mobile'` (post-Phase-51, post-Plan 03):
```
[0] kpi-cards
[1] heat-map-summary-card
[2] capacity-forecast
[3] capacity-gauges
[4] resource-conflicts
[5] department-bar-chart
[6] discipline-breakdown  // changed by Plan 03
[7] strategic-alerts      // <<< REMOVE (POLISH-06 — REQ wording: "removed from manager:mobile layout")
```

AFTER this plan:
```
[0] kpi-cards
[1] heat-map-summary-card
[2] capacity-forecast
[3] capacity-gauges
[4] resource-conflicts
[5] department-bar-chart
[6] discipline-breakdown
```

LEGACY_LAYOUTS: **UNCHANGED** — `bench-report` stays at `manager:desktop:7`, `strategic-alerts` stays at `manager:mobile:7` (and `manager:desktop:7` of LEGACY, per `default-layouts.ts:24` verified during read).

**One-shot migration** (`src/db/migrations/20260422_polish_strip_widgets.sql`):
```sql
-- v6.0 Phase 53 POLISH-04 + POLISH-06: strip deleted widget IDs from tenant custom layouts
UPDATE dashboard_layouts
SET layout = (
  SELECT jsonb_agg(placement)
  FROM jsonb_array_elements(layout) placement
  WHERE placement->>'widgetId' NOT IN ('bench-report', 'strategic-alerts')
)
WHERE layout::text ~* 'bench-report|strategic-alerts';
```

**Mount-point discovery** — the current `dashboard-content.tsx` may be wrapped with a `DashboardProvider`, may render `<DashboardGrid dashboardId={...}>` conditionally (per persona / viewport), or may use a different mounting pattern. Read the file FIRST and adjust the mount point accordingly. The banner must render ONLY when the dashboard being shown is the manager dashboard (i.e., `dashboardId === 'manager'`); if the file renders per-persona dashboards, gate the banner to the manager branch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship `<StrategicAlertsBanner>` component + unit test + mount into `dashboard-content.tsx` behind flag (POLISH-06 banner half)</name>
  <files>src/components/alerts/strategic-alerts-banner.tsx, src/components/alerts/__tests__/strategic-alerts-banner.test.tsx, src/app/(app)/dashboard/dashboard-content.tsx</files>
  <behavior>
    - Test 1: useAlerts returns [] → banner renders nothing (returns null).
    - Test 2: useAlerts returns 3 alerts → banner renders; title shows "3 kritiska varningar" (sv) or "3 critical warnings" (en); CTA link href is `/alerts`.
    - Test 3: useAlerts returns 1 alert → title pluralization works correctly via ICU `{count}` interpolation.
    - Test 4 (mount): dashboard-content.tsx with uiV6Polish=false → `<StrategicAlertsBanner>` NOT rendered (no DOM presence).
    - Test 5 (mount): dashboard-content.tsx with uiV6Polish=true AND dashboardId='manager' → banner rendered above `<DashboardGrid>`.
  </behavior>
  <read_first>
    - src/hooks/use-alerts.ts (confirm `useAlerts(monthFrom, monthTo)` signature + return type; if it returns `{data, isLoading}` vs just data, adapt)
    - src/lib/date-utils.ts (confirm `getCurrentMonth` + `generateMonthRange` exports — these are already imported by the existing top-nav alert-bell link and useAlertCount usages)
    - src/app/(app)/dashboard/dashboard-content.tsx (FULL FILE — confirm how the manager dashboard is rendered; identify the correct mount point; check if it's the same file that renders project-leader too)
    - src/components/alerts/alert-badge.tsx (existing banner-like primitive — confirms Tailwind class conventions used for alert UI; prefer mirroring those classes)
    - src/messages/sv.json (confirm Plan 01 landed `v6.polish.banner.title` with `{count}` interpolation + `v6.polish.banner.cta`)
    - tailwind.config.ts (or tailwind.config.js) — confirm whether `warning-container` / `on-warning-container` color tokens exist; if not, fall back to yellow-50/yellow-900/yellow-200 as documented in `<interfaces>`
    - .planning/phases/53-chrome-polish/53-RESEARCH.md §POLISH-06 (banner placement; "above <DashboardGrid>")
  </read_first>
  <action>
    1. **Write `src/components/alerts/__tests__/strategic-alerts-banner.test.tsx`** — RTL + Vitest. Mock `useAlerts`, `useTranslations`, `generateMonthRange`/`getCurrentMonth`. Cover tests 1–3 per behavior. **RED first.**

    2. **Create `src/components/alerts/strategic-alerts-banner.tsx`** — use the shape from `<interfaces>` block. Verify the actual Tailwind color tokens during read_first; fall back to yellow-50/yellow-900/yellow-200 if `warning-container` is absent. Use `role="alert"` (a11y). Include `data-testid="strategic-alerts-banner-cta"` on the CTA link so e2e specs can target it.

    3. **Edit `src/app/(app)/dashboard/dashboard-content.tsx`** — import `StrategicAlertsBanner` + `useFlags`. Render `{flags.uiV6Polish && <StrategicAlertsBanner />}` IMMEDIATELY ABOVE the `<DashboardGrid dashboardId="manager">` element. If the file renders multiple dashboard IDs, gate the banner to ONLY the manager rendering path (do NOT render it on project-leader).

    4. **Extend the banner test file** with Tests 4 + 5 — either import and render `dashboard-content.tsx` directly (RTL) or write a minimal harness. If `dashboard-content.tsx` has heavy dependencies that make it hard to render in isolation, keep Tests 4+5 at unit level by asserting the render logic on a trimmed wrapper component; file a note in the summary.

    Do NOT edit `strategic-alerts-widget.tsx` (D-06; kept for LEGACY rendering).
    Do NOT edit `default-layouts.ts` here — that's Task 2.
  </action>
  <verify>
    <automated>pnpm test --run src/components/alerts/__tests__/strategic-alerts-banner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/alerts/strategic-alerts-banner.tsx` exists and exports `StrategicAlertsBanner`
    - `grep -n "return null" src/components/alerts/strategic-alerts-banner.tsx` returns ≥ 1 line (empty-state short-circuit)
    - `grep -n "href=\"/alerts\"" src/components/alerts/strategic-alerts-banner.tsx` returns exactly 1 line
    - `grep -n "v6.polish.banner" src/components/alerts/strategic-alerts-banner.tsx` returns ≥ 1 line (i18n namespace)
    - `grep -n "StrategicAlertsBanner" src/app/(app)/dashboard/dashboard-content.tsx` returns ≥ 2 lines (import + JSX)
    - `grep -n "flags.uiV6Polish" src/app/(app)/dashboard/dashboard-content.tsx` returns ≥ 1 line (flag gate)
    - `pnpm test --run src/components/alerts/__tests__/strategic-alerts-banner.test.tsx` exits 0 with ≥ 3 passing tests (4–5 if achievable)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Banner renders above manager dashboard grid when uiV6Polish=true + alerts > 0; empty-state returns null; flag-off parity preserved. Implements POLISH-06 component + mount point. Widget layout removal happens in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove `bench-report` + `strategic-alerts` from DEFAULT_LAYOUTS + update tests + ship one-shot strip migration (POLISH-04 + POLISH-06 layout halves)</name>
  <files>src/features/dashboard/default-layouts.ts, src/features/dashboard/__tests__/default-layouts.test.ts, src/db/migrations/20260422_polish_strip_widgets.sql, src/db/migrations/__tests__/polish-strip-widgets.test.ts</files>
  <behavior>
    - Test 1 (unit): `DEFAULT_LAYOUTS['manager:desktop'].find(w => w.widgetId === 'bench-report')` is undefined.
    - Test 2: `DEFAULT_LAYOUTS['manager:desktop'].find(w => w.widgetId === 'availability-finder').position === 7` (slid from 8).
    - Test 3: `DEFAULT_LAYOUTS['manager:desktop'].length === 8` (was 9 post-Plan-03).
    - Test 4: `DEFAULT_LAYOUTS['manager:mobile'].find(w => w.widgetId === 'strategic-alerts')` is undefined.
    - Test 5: `DEFAULT_LAYOUTS['manager:mobile'].length === 7` (was 8 post-Plan-03).
    - Test 6: `LEGACY_LAYOUTS['manager:desktop'][7].widgetId === 'bench-report'` (unchanged).
    - Test 7: `LEGACY_LAYOUTS['manager:mobile'][7].widgetId === 'strategic-alerts'` (unchanged).
    - Test 8 (migration): Seed layout with 5 widgets including `bench-report` and `strategic-alerts`; run migration; assert resulting layout has 3 widgets, neither stripped ID present.
    - Test 9 (migration): Seed layout with ONLY `bench-report` and `strategic-alerts`; run migration; assert layout becomes `[]` (valid empty jsonb array — widget-registry fallback ensures dashboard doesn't crash).
    - Test 10 (migration): Seed layout without either ID; run migration; assert row untouched.
    - Test 11 (migration): Migration is idempotent (running twice produces the same result as running once).
  </behavior>
  <read_first>
    - src/features/dashboard/default-layouts.ts (FULL FILE — confirm CURRENT state of DEFAULT_LAYOUTS['manager:desktop'] + ['manager:mobile'] AFTER Plan 03's discipline-breakdown swap; read line 82-106)
    - src/features/dashboard/__tests__/default-layouts.test.ts (extended in Plan 03 — add to it, don't replace)
    - src/db/migrations/20260422_polish_discipline_rename.sql (Plan 03's migration — mirror filename pattern)
    - src/db/migrations/__tests__/polish-discipline-rename.test.ts (Plan 03's migration test — mirror PGlite setup)
    - .planning/phases/51-lean-cleanup-duplicate-removal/51-02-PLAN.md (canonical LEAN-11 migration precedent — STRIP pattern with `NOT IN`)
    - .planning/phases/53-chrome-polish/53-RESEARCH.md §POLISH-04 + §POLISH-06 (locked interpretation: mobile-only removal for strategic-alerts per REQ literal reading; A6 acceptance of potential future reconsideration)
  </read_first>
  <action>
    1. **Edit `src/features/dashboard/default-layouts.ts`** in the `DEFAULT_LAYOUTS` map ONLY:
       - `'manager:desktop'`: delete the `{ widgetId: 'bench-report', position: 7, colSpan: 12 }` line. Change `availability-finder`'s position from 8 to 7. Array length drops from 9 → 8.
       - `'manager:mobile'`: delete the `{ widgetId: 'strategic-alerts', position: 7, colSpan: 12 }` line. Array length drops from 8 → 7.
       - `LEGACY_LAYOUTS`: **DO NOT TOUCH.**
       - Add top comment: `// v6.0 Phase 53 Plan 04 POLISH-04 + POLISH-06 — bench-report removed from manager:desktop; strategic-alerts removed from manager:mobile; LEGACY_LAYOUTS preserved.`

    2. **Extend `src/features/dashboard/__tests__/default-layouts.test.ts`** — add tests 1-7 per behavior spec. Read the actual LEGACY_LAYOUTS lines to ensure tests 6+7 match ground truth.

    3. **Create `src/db/migrations/20260422_polish_strip_widgets.sql`** — verbatim from `<interfaces>` block. Header comment references POLISH-04 + POLISH-06.

    4. **Create `src/db/migrations/__tests__/polish-strip-widgets.test.ts`** — PGlite integration. Mirror `polish-discipline-rename.test.ts` structure. Cover tests 8-11 per behavior. **RED first.**

    5. **Apply to migration runner** — same pattern as Plan 03 Task 3 step 5. If drizzle uses a manifest, register this SQL file.

    Do NOT remove imports from `widgets/index.ts` (D-06 — `bench-report-widget.tsx` + `strategic-alerts-widget.tsx` remain registered so LEGACY layout renders).
    Do NOT touch the widget files themselves.
  </action>
  <verify>
    <automated>pnpm test --run src/features/dashboard/__tests__/default-layouts.test.ts src/db/migrations/__tests__/polish-strip-widgets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "widgetId: 'bench-report'" src/features/dashboard/default-layouts.ts` returns ONLY lines inside the LEGACY_LAYOUTS block (1 line — the existing one)
    - `grep -n "widgetId: 'strategic-alerts'" src/features/dashboard/default-layouts.ts` returns ONLY lines inside LEGACY_LAYOUTS (≥ 1 line; verify it's only inside `LEGACY_LAYOUTS['manager:desktop']` and `LEGACY_LAYOUTS['manager:mobile']`, NOT in DEFAULT_LAYOUTS)
    - In `DEFAULT_LAYOUTS['manager:desktop']` block — `availability-finder` appears exactly once with `position: 7`
    - `src/db/migrations/20260422_polish_strip_widgets.sql` exists and contains `NOT IN ('bench-report', 'strategic-alerts')`
    - `pnpm test --run src/features/dashboard/__tests__/default-layouts.test.ts` exits 0 (≥ 12 tests: Plan 03's + the 7 added here)
    - `pnpm test --run src/db/migrations/__tests__/polish-strip-widgets.test.ts` exits 0 with 4 passing tests
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0
    - `grep -n "'./bench-report-widget'" src/features/dashboard/widgets/index.ts` returns 1 line (unchanged — D-06)
    - `grep -n "'./strategic-alerts-widget'" src/features/dashboard/widgets/index.ts` returns 1 line (unchanged — D-06)
  </acceptance_criteria>
  <done>Bench-report removed from manager:desktop; strategic-alerts removed from manager:mobile; LEGACY_LAYOUTS preserved for flag-off rollback; SQL migration strips both widget IDs from tenant custom layouts; widget files kept on disk and registered. Implements POLISH-04 + POLISH-06 layout halves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db migration → dashboard_layouts.layout (jsonb) | Second migration of the phase (first was Plan 03); same tenant + same transactional safety. |
| banner alerts source | `useAlerts` already tenant-scoped (existing endpoint); banner is a pass-through consumer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-16 | T (Tampering — layout data) | Strip migration | mitigate | `jsonb_agg(... WHERE ...)` filters array elements; re-run produces no change (idempotent — Test 11). Layout-NULL rows untouched (Test 10). |
| T-53-17 | I (Info Disclosure — banner count) | `<StrategicAlertsBanner>` | accept | Only shows count and CTA label; no tenant/PII details. `useAlerts` already performs tenant scoping server-side. |
| T-53-18 | D (Denial — layout empty after strip) | Tenant layout reduced to `[]` | mitigate | Phase 51 LEAN-08 defensive fallback renders "Widget ej tillgänglig" placeholder for missing IDs; empty layouts simply render the dashboard without widgets (users can re-add via edit-mode). Test 9 confirms the data shape is valid. |
| T-53-19 | I (Info Disclosure — cross-tenant) | Strip migration WHERE clause | mitigate | Migration rewrites in place; no cross-tenant row mixing. |
| T-53-20 | S (Spoofing — banner CTA) | `<Link href="/alerts">` | accept | Static href to existing route; no user-controlled URL. |
</threat_model>

<verification>
Wave 2 Plan 04 overall checks:
1. `pnpm typecheck` exits 0
2. `pnpm test --run src/components/alerts src/features/dashboard src/db/migrations` exits 0
3. `pnpm build` exits 0
4. Manual dashboard smoke (dev server, uiV6Polish=ON, seeded alerts > 0):
   - Banner renders above the manager dashboard; clicking CTA goes to `/alerts`.
   - Empty-alerts tenant → no banner.
   - bench-report widget absent from manager:desktop; availability-finder is now at position 7.
   - strategic-alerts absent from manager:mobile.
   - Toggle flag OFF → LEGACY_LAYOUTS kicks in; bench-report + strategic-alerts render as before; banner vanishes.
5. Dry-run migration against dev Neon: `SELECT count(*) FROM dashboard_layouts WHERE layout::text ~* 'bench-report|strategic-alerts'` before > 0, after = 0.
</verification>

<success_criteria>
- POLISH-04 green: `bench-report` no longer in `DEFAULT_LAYOUTS['manager:desktop']`; `availability-finder` slid to position 7; widget file remains on disk (D-06).
- POLISH-06 green: `strategic-alerts` no longer in `DEFAULT_LAYOUTS['manager:mobile']`; `<StrategicAlertsBanner>` renders above manager dashboard grid behind flag.
- LEGACY_LAYOUTS preserves both widgets for flag-off rollback.
- SQL migration strips both IDs from tenant custom layouts in a single idempotent transaction.
- Banner empty-state + flag-off both return null (no empty-frame UI artifacts).
- 11 new tests green (5 layout + 4 migration + 3 banner + ≥ 0 mount-point integration).
</success_criteria>

<output>
After completion, create `.planning/phases/53-chrome-polish/53-04-SUMMARY.md` documenting: banner mount point (which line/branch of `dashboard-content.tsx`), Tailwind color tokens used (warning-container vs yellow fallback), default-layouts diff (copy manager:desktop + manager:mobile before/after), migration SQL + dev Neon affected-row count (before/after), and confirmation that `widgets/index.ts` imports for `bench-report-widget` + `strategic-alerts-widget` were preserved per D-06.
</output>
