---
phase: 47-playwright-e2e-infra
plan: 10
type: execute
wave: 3
depends_on: ["47-09"]
files_modified:
  - .planning/test-contract/tc-allowlist.json
  - .planning/test-contract/tc-manifest.json
autonomous: true
requirements: [PLAY-09]
gap_closure: false
must_haves:
  truths:
    - "`tc-allowlist.json` no longer contains any TC-E2E-* IDs in stillMissing"
    - "`reasons.TC-E2E` block removed from allowlist"
    - "`groups.TC-E2E` block removed from allowlist"
    - "tc-manifest.json regenerated and includes all 12 TC-E2E-* from Playwright spec test names"
    - "tests/invariants/tc-id-coverage.test.ts still green"
  artifacts:
    - path: ".planning/test-contract/tc-allowlist.json"
      provides: "Allowlist with TC-E2E entries removed"
    - path: ".planning/test-contract/tc-manifest.json"
      provides: "Regenerated manifest covering 12 TC-E2E IDs via Playwright specs"
  key_links:
    - from: "scripts/generate-tc-manifest.ts"
      to: "e2e/**/*.spec.ts test names"
      via: "TC-E2E-* prefix parser"
      pattern: "TC-E2E-"
---

<objective>
Close the Test Contract gap that originated in Phase 44-12: remove the 12 TC-E2E-* entries from `tc-allowlist.json`, remove the `reasons.TC-E2E` deferral block, regenerate the TC manifest, and prove `tc-id-coverage` still green.

Purpose: Final cleanup of Phase 47. The TC contract is now whole — 285 canonical IDs, 0 allowlist entries for E2E.
Output: Updated allowlist, regenerated manifest, green invariant.
</objective>

<context>
@.planning/phases/47-playwright-e2e-infra/47-CONTEXT.md
@.planning/test-contract/tc-allowlist.json
@scripts/generate-tc-manifest.ts
@tests/invariants/tc-id-coverage.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify manifest script picks up TC-E2E test names</name>
  <files>scripts/generate-tc-manifest.ts</files>
  <action>
Read `scripts/generate-tc-manifest.ts` end-to-end. Confirm:

1. It scans file globs that include `e2e/**/*.spec.ts`. If it does NOT, extend the globs to include Playwright specs. The Phase 44 version scanned vitest paths; this is the moment to widen.
2. Its TC-ID regex matches `TC-E2E-1A`, `TC-E2E-2B-approve`, `TC-E2E-2B-reject`, etc. The hyphenated `-approve`/`-reject` variants are tricky — ensure the regex is something like `/TC-E2E-[0-9]+[A-Z](?:-[a-z]+)?/g` or broader.
3. It extracts TC-IDs from `test('TC-E2E-XX: ...', ...)` call strings (not function names — Playwright uses the first-string argument).

If the script needs extending to cover Playwright spec globs OR TC-E2E-* regex, update it. Run:

```bash
pnpm tsx scripts/generate-tc-manifest.ts
```

Then inspect `.planning/test-contract/tc-manifest.json` — all 12 TC-E2E-* IDs should appear with their `e2e/**/*.spec.ts` file paths as sources.
  </action>
  <verify>
    <automated>pnpm tsx scripts/generate-tc-manifest.ts &amp;&amp; node -e "const m=require('./.planning/test-contract/tc-manifest.json'); const ids=['TC-E2E-1A','TC-E2E-1B','TC-E2E-1C','TC-E2E-1D','TC-E2E-2A','TC-E2E-2B-approve','TC-E2E-2B-reject','TC-E2E-2C','TC-E2E-2D','TC-E2E-3A','TC-E2E-4A','TC-E2E-4B']; const missing=ids.filter(id=>!JSON.stringify(m).includes(id)); if(missing.length){console.error('missing:',missing);process.exit(1)} console.log('all 12 TC-E2E present')"</automated>
  </verify>
  <done>Manifest regenerated, all 12 TC-E2E-* IDs present with e2e/**/*.spec.ts sources.</done>
</task>

<task type="auto">
  <name>Task 2: Remove TC-E2E from tc-allowlist.json</name>
  <files>.planning/test-contract/tc-allowlist.json</files>
  <action>
Edit `.planning/test-contract/tc-allowlist.json`:

1. Remove all 12 `TC-E2E-*` entries from the `stillMissing` array (keep `TC-NEG-*` entries untouched — those are a separate deferral).
2. Delete the entire `groups.TC-E2E` array (keep `groups.TC-NEG`).
3. Delete the entire `reasons.TC-E2E` block (keep `reasons.TC-NEG`).

The final structure should be:

```json
{
  "note": "Each Wave C plan (44-07..44-13) removes its prefix group from this list as it lands. Final state: empty array, except for groups with a documented deferral reason below.",
  "stillMissing": [
    "TC-NEG-001",
    "TC-NEG-002",
    "TC-NEG-003",
    "TC-NEG-004",
    "TC-NEG-005",
    "TC-NEG-006",
    "TC-NEG-007",
    "TC-NEG-008",
    "TC-NEG-009",
    "TC-NEG-010",
    "TC-NEG-011",
    "TC-NEG-012",
    "TC-NEG-014"
  ],
  "groups": {
    "TC-NEG": [
      "TC-NEG-001",
      "TC-NEG-002",
      "TC-NEG-003",
      "TC-NEG-004",
      "TC-NEG-005",
      "TC-NEG-006",
      "TC-NEG-007",
      "TC-NEG-008",
      "TC-NEG-009",
      "TC-NEG-010",
      "TC-NEG-011",
      "TC-NEG-012",
      "TC-NEG-014"
    ]
  },
  "reasons": {
    "TC-NEG": {
      "status": "deferred",
      "decidedIn": "44-verification",
      "decidedOn": "2026-04-09",
      "rationale": "TC-NEG-* IDs from ARCHITECTURE §15.14/§15.21 are negative-space assertions (things that must NOT exist: no task_id column, no counter-propose endpoint, etc.), not behavioral flow tests. Several are already implicitly covered by existing invariants: TC-DB-005/009 static schema checks cover TC-NEG-003-class schema forbiddings; TC-INV-002/003 cross-cutting invariants cover wire-format and taxonomy forbiddings. A dedicated TC-NEG-*-labelled static audit pass has not been written — phase budget prioritized positive behavioral TCs and real security bugs surfaced in 44-05.",
      "followUp": "Fold TC-NEG-* dedicated labeling into a follow-up hardening pass, or pin TC-NEG-* labels to the existing covering invariants during the next test-contract hygiene sweep. Mitigating coverage is already in place — this is a labeling/traceability gap, not a coverage gap."
    }
  }
}
```
  </action>
  <verify>
    <automated>node -e "const a=require('./.planning/test-contract/tc-allowlist.json'); if(a.stillMissing.some(x=>x.startsWith('TC-E2E'))) process.exit(1); if(a.groups['TC-E2E']) process.exit(1); if(a.reasons['TC-E2E']) process.exit(1); console.log('TC-E2E fully removed')"</automated>
  </verify>
  <done>Allowlist no longer mentions TC-E2E anywhere; TC-NEG deferral preserved intact.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test contract invariants + full suite</name>
  <files>(no new files)</files>
  <action>
Run the full invariant + test suite gate:

1. `pnpm vitest run tests/invariants/tc-id-coverage.test.ts` — green
2. `pnpm vitest run` (full Vitest) — green
3. `pnpm exec playwright test` (full Playwright, all 12 specs) — green, against `nc_e2e`
4. `pnpm typecheck`, `pnpm lint`, `pnpm build` — all green
5. Push, confirm CI `quality` + `e2e` jobs both green on the phase PR

If `tc-id-coverage` fails, it means either (a) the manifest did not pick up the new specs or (b) the allowlist edit missed an entry. Debug by reading the invariant's failure output — it lists which IDs are expected vs present.
  </action>
  <verify>
    <automated>pnpm vitest run tests/invariants/tc-id-coverage.test.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>All gates green. Phase 47 ships.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/invariants/tc-id-coverage.test.ts` green
- `pnpm vitest run` full suite green (includes `no-test-routes-in-prod` + `proxy.e2e-bypass` from earlier plans)
- `pnpm exec playwright test` all 12 TC-E2E green
- CI green on phase PR
</verification>

<success_criteria>
Phase 47 complete. TC contract has 0 TC-E2E allowlist entries. All 12 canonical TC-E2E-* IDs covered by real Playwright specs running in CI. Phase 44-12's remaining deferral is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/47-playwright-e2e-infra/47-10-SUMMARY.md` and update STATE.md marking Phase 47 complete. This is the final plan — run `/gsd:verify-work` to close the phase.
</output>
