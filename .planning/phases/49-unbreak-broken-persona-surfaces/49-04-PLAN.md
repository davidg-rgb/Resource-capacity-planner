---
phase: 49-unbreak-broken-persona-surfaces
plan: 04
type: execute
wave: 2
depends_on:
  - 49-01
  - 49-02
  - 49-03
files_modified:
  - e2e/pm/historic-edit.spec.ts
  - e2e/pm/monday-checkin.spec.ts
  - e2e/pm/rejected-resubmit.spec.ts
  - e2e/pm/submit-wish.spec.ts
  - e2e/line-manager/approve.spec.ts
  - e2e/line-manager/direct-edit.spec.ts
  - e2e/line-manager/heatmap.spec.ts
  - e2e/line-manager/import.spec.ts
  - e2e/line-manager/reject.spec.ts
  - e2e/staff/read-only.spec.ts
  - e2e/rd/overcommit-drill.spec.ts
  - e2e/rd/portfolio.spec.ts
autonomous: true
requirements:
  - UNBREAK-07

must_haves:
  truths:
    - "All 12 Playwright specs in the pre-flight VERIFY-06 inventory pass against the unbroken-but-pre-Phase-50 code path (i.e., after Plans 49-01/02/03 land but before any `uiV6.*` flag work)"
    - "Each LM spec (5 specs) has a setup step that ensures `persona.departmentId` is populated — either by relying on the new auto-select-when-1-department branch or by clicking the new `data-testid=\"persona-switcher-department\"` dropdown after `personaAs(page, 'line-manager')`"
    - "Each PM spec (4 specs) has a setup step that ensures a PM person is selected via the existing person picker before asserting on project list content (UNBREAK-03's empty-state fall-through means specs that previously hung on a spinner now hit the empty branch — they must explicitly select a person)"
    - "No new Playwright spec files are created (CONTEXT D-06 explicit)"
    - "No spec navigates to `/team`, `/projects`, or `/wishes` (those redirects ship in Phase 51 — confirmed by pre-flight §VERIFY-06 deleted-route check returning `<no matches>`)"
    - "Each spec is updated in a single commit per CONTEXT D-06"
  artifacts:
    - path: e2e/pm/historic-edit.spec.ts
      provides: "Setup updated to select PM person after persona switch (so UNBREAK-03 empty-state doesn't hide the project list)"
    - path: e2e/pm/monday-checkin.spec.ts
      provides: "Same as above"
    - path: e2e/pm/rejected-resubmit.spec.ts
      provides: "Same as above"
    - path: e2e/pm/submit-wish.spec.ts
      provides: "Same as above"
    - path: e2e/line-manager/approve.spec.ts
      provides: "Setup updated to wait for / trigger department selection after `personaAs(page, 'line-manager')`"
    - path: e2e/line-manager/direct-edit.spec.ts
      provides: "Same as above"
    - path: e2e/line-manager/heatmap.spec.ts
      provides: "Same as above"
    - path: e2e/line-manager/import.spec.ts
      provides: "Same as above"
    - path: e2e/line-manager/reject.spec.ts
      provides: "Same as above"
    - path: e2e/staff/read-only.spec.ts
      provides: "Setup verified — Staff persona doesn't depend on switcher state changes from Phase 49; smoke pass + commit if no edit needed"
    - path: e2e/rd/overcommit-drill.spec.ts
      provides: "Same — R&D persona unaffected by switcher edits in Phase 49; verified to pass"
    - path: e2e/rd/portfolio.spec.ts
      provides: "Same"
  key_links:
    - from: e2e/line-manager/*.spec.ts
      to: src/components/persona/persona-switcher.tsx (Plan 49-01 Task 2)
      via: "department dropdown `data-testid=\"persona-switcher-department\"` populated by the new picker"
      pattern: "persona-switcher-department"
    - from: e2e/pm/*.spec.ts
      to: src/app/(app)/pm/page.tsx (Plan 49-02 Task 1)
      via: "person picker selection in switcher must precede `expect(page.getByText(...))` assertions on project list"
      pattern: "personaAs|personId"
---

<objective>
Update all 12 existing Playwright specs (per pre-flight §VERIFY-06 inventory: 4 PM, 5 LM, 1 Staff, 2 R&D) so they pass against the post-Plan-49-01/02/03 code path — without adding any new specs (CONTEXT D-06 explicit) and without touching any deleted-route assertions (Phase 51's job).

Purpose: Plans 49-01 and 49-02 change observable behavior in two ways that affect spec setup:
- LM specs previously relied on `persona.departmentId === ''` triggering a fallback copy that the old spec setup possibly clicked through. After Plan 49-01, the dept dropdown is the only entry point — specs must wait for auto-select (1-dept tenant) or explicitly select one.
- PM specs previously hung on the loading spinner when no person was selected (Plan 49-02 surface this as an empty state). Specs that assert on populated project content must explicitly trigger person selection in the switcher.

Output: 12 spec files updated, each in a single commit, all green against the post-Wave-1 code path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-unbreak-broken-persona-surfaces/49-CONTEXT.md
@.planning/phases/49-unbreak-broken-persona-surfaces/49-RESEARCH.md
@.planning/phases/49-unbreak-broken-persona-surfaces/49-VALIDATION.md
@.planning/pre-flight-report.md
@e2e/playwright.config.ts
@e2e/global-setup.ts
@e2e/pm/historic-edit.spec.ts
@e2e/pm/monday-checkin.spec.ts
@e2e/pm/rejected-resubmit.spec.ts
@e2e/pm/submit-wish.spec.ts
@e2e/line-manager/approve.spec.ts
@e2e/line-manager/direct-edit.spec.ts
@e2e/line-manager/heatmap.spec.ts
@e2e/line-manager/import.spec.ts
@e2e/line-manager/reject.spec.ts
@e2e/staff/read-only.spec.ts
@e2e/rd/overcommit-drill.spec.ts
@e2e/rd/portfolio.spec.ts

<interfaces>
<!-- Pre-flight VERIFY-06 inventory (verbatim, all 12 use `await page.goto('/')` post-`personaAs`): -->

| Spec file | Setup signal (current) | Phase 49 follow-up |
|---|---|---|
| e2e/pm/historic-edit.spec.ts | line 14: `await page.goto('/')` | Ensure PM person selected before project-list assertion |
| e2e/pm/monday-checkin.spec.ts | line 19: `await page.goto('/')` | Same |
| e2e/pm/rejected-resubmit.spec.ts | line 16: `await page.goto('/')` | Same |
| e2e/pm/submit-wish.spec.ts | line 17: `await page.goto('/')` | Same |
| e2e/line-manager/approve.spec.ts | line 14: `await page.goto('/')` | Wait for / select department |
| e2e/line-manager/direct-edit.spec.ts | line 14: `await page.goto('/')` | Same |
| e2e/line-manager/heatmap.spec.ts | line 21: `await page.goto('/')` | Same |
| e2e/line-manager/import.spec.ts | lines 15, 29: `await page.goto('/')` (2x) | Same |
| e2e/line-manager/reject.spec.ts | line 11: `await page.goto('/')` | Same |
| e2e/staff/read-only.spec.ts | line 15: `await page.goto('/')` | Smoke-verify; no edit needed if green |
| e2e/rd/overcommit-drill.spec.ts | line 14: `await page.goto('/')` | Same |
| e2e/rd/portfolio.spec.ts | line 15: `await page.goto('/')` | Same |

NOTE per pre-flight VERIFY-06: "All 12 specs hit `/` after `personaAs(page, 'persona')`. The root-path redirect behavior changes in Phase 50 NAV-01 — Phase 49 does NOT change what `/` does." So `await page.goto('/')` stays as-is in this plan; the changes are downstream selector waits, not the goto itself.

NEW selectors introduced by Plan 49-01 Task 2:
- `data-testid="persona-switcher-department"` — the LM department dropdown
- `data-testid="persona-switcher-no-department"` — the no-dept hint (LM tenant with 0 active depts)
- `data-testid="persona-gate-allowed"` (existing — preserved)
- `data-testid="persona-gate-hint"` (existing — preserved)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + update 5 LM specs (heatmap, direct-edit, approve, reject, import) — single commit per spec</name>
  <files>e2e/line-manager/approve.spec.ts, e2e/line-manager/direct-edit.spec.ts, e2e/line-manager/heatmap.spec.ts, e2e/line-manager/import.spec.ts, e2e/line-manager/reject.spec.ts</files>
  <read_first>
    @e2e/line-manager/heatmap.spec.ts (start with this — it's the canonical LM landing test; pattern likely repeats across the other 4)
    @e2e/line-manager/approve.spec.ts
    @e2e/line-manager/direct-edit.spec.ts
    @e2e/line-manager/import.spec.ts
    @e2e/line-manager/reject.spec.ts
    @e2e/lib/ (any persona/setup helpers — `personaAs` helper likely lives here)
    @e2e/fixtures/ (test tenant seed — confirm number of departments seeded; if exactly 1, auto-select branch fires and specs may need NO change)
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-CONTEXT.md (D-06 — single commit per spec)
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-RESEARCH.md (§Per-UNBREAK Implementation Notes — UNBREAK-07)
  </read_first>
  <action>
    1. **Run baseline first** — execute the 5 LM specs against the post-Wave-1 code:
       ```bash
       pnpm playwright test --config=e2e/playwright.config.ts e2e/line-manager/
       ```
       Capture which specs pass without edits (likely the case if the test fixture seeds exactly 1 department — auto-select branch fires; the LM page renders directly).

    2. **For each FAILING spec**, identify the failure root cause:
       - If the failure is "department dropdown not visible" — the test fixture has 0 departments; this is a fixture problem, not a spec problem. Document and proceed without spec edit; the spec's failure reflects a real bug.
       - If the failure is "page rendered no-department hint when one was expected" — same as above.
       - If the failure is "heatmap query never fired" / "skeleton stayed visible" — the test fixture has >1 department and the spec needs to explicitly select one.

    3. **Spec edit pattern** (apply only to specs that fail without edits):
       After the existing `personaAs(page, 'line-manager')` + `await page.goto('/')` lines, add:
       ```typescript
       // Phase 49 UNBREAK-08: department picker is now in the switcher; ensure a dept is selected
       const deptSelect = page.getByTestId('persona-switcher-department');
       if (await deptSelect.isVisible()) {
         await deptSelect.selectOption({ index: 0 }); // pick first available; fixture has known dept
       }
       // Auto-select branch (1-dept tenant) skips the dropdown; no action needed
       ```
       Adjust the `selectOption` argument to match the fixture's seeded department label/value if the spec already knows it (consult `e2e/fixtures/`).

    4. **Commit each spec separately** (CONTEXT D-06):
       ```bash
       git add e2e/line-manager/heatmap.spec.ts
       git commit -m "test(49-04): update LM heatmap spec for department-picker setup"
       # repeat for direct-edit, approve, reject, import
       ```
       For specs that needed NO edit, still confirm they pass — but do NOT create empty commits.

    5. Re-run after edits:
       ```bash
       pnpm playwright test --config=e2e/playwright.config.ts e2e/line-manager/
       ```
       All 5 specs must be green.

    DO NOT add new specs (CONTEXT D-06).
    DO NOT change `await page.goto('/')` to `await page.goto('/line-manager')` — that's Phase 50's NAV-01 work; in Phase 49, root-path semantics are unchanged.
    DO NOT edit specs that already pass — only the failures get edits.
  </action>
  <verify>
    <automated>pnpm playwright test --config=e2e/playwright.config.ts e2e/line-manager/</automated>
  </verify>
  <acceptance_criteria>
    - All 5 LM specs pass: approve, direct-edit, heatmap, import, reject
    - For specs that required edits, each has its own commit message of the form `test(49-04): update LM <name> spec for department-picker setup`
    - For specs that did NOT require edits, no commit was created (no empty commits)
    - `grep -rc "persona-switcher-department" e2e/line-manager/` returns >= `0` (might be 0 if all specs pass via auto-select; that's fine)
    - `grep -rc "describe\|test\|it" e2e/line-manager/*.spec.ts` is unchanged from pre-edit count (no NEW test cases added)
    - No spec navigates to `/team`, `/projects`, or `/wishes`: `grep -cE "(goto|page\.goto)\\(['\\\"](/team|/projects|/wishes)" e2e/line-manager/` returns `0`
  </acceptance_criteria>
  <done>All 5 LM specs green; per-spec commits created where edits were needed.</done>
</task>

<task type="auto">
  <name>Task 2: Audit + update 4 PM specs (historic-edit, monday-checkin, rejected-resubmit, submit-wish) — single commit per spec</name>
  <files>e2e/pm/historic-edit.spec.ts, e2e/pm/monday-checkin.spec.ts, e2e/pm/rejected-resubmit.spec.ts, e2e/pm/submit-wish.spec.ts</files>
  <read_first>
    @e2e/pm/submit-wish.spec.ts (start here — likely the simplest landing flow)
    @e2e/pm/historic-edit.spec.ts
    @e2e/pm/monday-checkin.spec.ts
    @e2e/pm/rejected-resubmit.spec.ts
    @e2e/lib/ (persona helpers)
    @e2e/fixtures/ (confirm fixture seeds at least 1 PM person + 1 project)
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-CONTEXT.md (D-06)
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-RESEARCH.md (§Per-UNBREAK Implementation Notes — UNBREAK-07 PM section)
  </read_first>
  <action>
    1. **Run baseline first**:
       ```bash
       pnpm playwright test --config=e2e/playwright.config.ts e2e/pm/
       ```
       Capture which specs pass. After Plan 49-02 lands, PM specs that didn't explicitly select a person will hit the empty state instead of hanging on a spinner — they fail with "no project list found" rather than timing out.

    2. **For each FAILING spec**, identify root cause:
       - If failure is "page rendered empty-state but spec expected project list" — spec needs to select a PM person via the existing person picker (`useQuery(['personas-people-picker'])` populates the second `<select>` when persona kind is `pm`).
       - If failure is "no projects in fixture" — fixture problem, not spec problem.

    3. **Spec edit pattern** (apply only to failing specs):
       After `personaAs(page, 'pm')` + `await page.goto('/')`, ensure a PM person is selected. The existing person dropdown is the second `<select>` in the persona switcher (rendered when `persona.kind === 'pm'` and `people.length > 0`):
       ```typescript
       // Phase 49 UNBREAK-03: PM Home falls through to empty state when no person selected;
       // explicitly pick the seeded test PM person.
       const personSelect = page.locator('[aria-label="Projektledare"]'); // matches t('kind.pm') aria-label on the person picker
       if (await personSelect.isVisible()) {
         await personSelect.selectOption({ index: 0 }); // first seeded PM
       }
       // Wait for project list to render after selection
       await expect(page.getByRole('list')).toBeVisible({ timeout: 5000 });
       ```
       Adjust the locator if the existing test infra has a more stable selector (e.g., a `data-testid` already on the person picker — check `persona-switcher.tsx:128` `aria-label={t('kind.pm')}` confirms the aria-label approach, but if the spec library has a `selectPersonaPerson(page, name)` helper, use that).

    4. **Commit each spec separately** (CONTEXT D-06).

    5. Re-run:
       ```bash
       pnpm playwright test --config=e2e/playwright.config.ts e2e/pm/
       ```
       All 4 PM specs must be green.

    DO NOT add new specs.
    DO NOT add `data-testid` attributes to the existing person picker in this plan — Plan 49-01 added the department-picker testid only; the PM person picker keeps its current `aria-label` selector strategy.
    DO NOT modify the production code's PM page based on E2E findings — if a fixture issue surfaces, document it but leave the fix for a later phase.
  </action>
  <verify>
    <automated>pnpm playwright test --config=e2e/playwright.config.ts e2e/pm/</automated>
  </verify>
  <acceptance_criteria>
    - All 4 PM specs pass: historic-edit, monday-checkin, rejected-resubmit, submit-wish
    - For specs that required edits, each has its own commit message `test(49-04): update PM <name> spec for empty-state guard fix`
    - `grep -rcE "describe\|test\|it" e2e/pm/*.spec.ts` shows no NEW test cases added (only existing ones modified)
    - No spec navigates to `/team`, `/projects`, or `/wishes`
  </acceptance_criteria>
  <done>All 4 PM specs green; per-spec commits where edits were needed.</done>
</task>

<task type="auto">
  <name>Task 3: Audit Staff + R&D specs (3 specs) — verify pass without edits</name>
  <files>e2e/staff/read-only.spec.ts, e2e/rd/overcommit-drill.spec.ts, e2e/rd/portfolio.spec.ts</files>
  <read_first>
    @e2e/staff/read-only.spec.ts
    @e2e/rd/overcommit-drill.spec.ts
    @e2e/rd/portfolio.spec.ts
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-CONTEXT.md (D-06)
  </read_first>
  <action>
    1. Run the 3 specs:
       ```bash
       pnpm playwright test --config=e2e/playwright.config.ts e2e/staff/ e2e/rd/
       ```

    2. Staff and R&D personas don't depend on the department picker (Staff uses `personId`, R&D has no scoping field). The PersonaGate rewire (Plan 49-01 Task 3) changes hint description copy — only specs that assert on the OLD `linjechefs-personan` text would break. Pre-flight §VERIFY-06 Signal-3 captured `<none>` for all 3 specs, so they shouldn't reference the old copy.

    3. **If all 3 pass**: no edits needed. Proceed to verify acceptance.

    4. **If any fail**: identify root cause. Likely candidates:
       - Old PersonaGate hint copy assertion (would have been caught in Signal 3 — should be impossible per pre-flight)
       - Persona-switcher render order changed (the new department block is conditional on `isLM`; should not affect Staff/RD specs)
       - Genuine test flake — re-run before assuming a real failure

    5. Apply edits only if necessary, single commit per spec.

    6. Re-run all 12 specs to confirm full green:
       ```bash
       pnpm playwright test --config=e2e/playwright.config.ts
       ```

    DO NOT add new specs.
    DO NOT skip running the full suite at the end — Tasks 1+2+3 changes interact via the shared persona switcher.
  </action>
  <verify>
    <automated>pnpm playwright test --config=e2e/playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 12 specs pass when run as a full suite
    - Staff (1) + R&D (2) specs pass — with or without edits as appropriate
    - For any specs that were edited, each has its own commit
    - Total spec count unchanged: `find e2e -name "*.spec.ts" | wc -l` returns `12`
    - No `e2e/admin/` directory created (admin specs are net-new work for a future phase per pre-flight §VERIFY-06)
  </acceptance_criteria>
  <done>Full Playwright suite green; total spec count still 12; admin spec block NOT created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright runner → app | Test bypass via `NODE_ENV=test` Clerk shim (per Phase 47); no production-data exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-10 | Tampering | Test fixture department selection | accept | Tests run against `nc_e2e` database with seeded fixture data; no production tenant impact (Phase 47 hardening). |
| T-49-11 | Information disclosure | Spec failures may print SQL or env in logs | mitigate | Standard Playwright output; no new instrumentation added. CI logs already redact `DATABASE_URL` per existing config. |
</threat_model>

<verification>
- `pnpm playwright test --config=e2e/playwright.config.ts` exits 0 with all 12 specs passing
- Total spec file count is 12 (`find e2e -name "*.spec.ts" -type f | wc -l`)
- Per-spec commits created only where edits were needed (no empty commits)
- No `/team`, `/projects`, `/wishes` navigation in any spec
</verification>

<success_criteria>
1. UNBREAK-07: 12-spec inventory updated where needed; all 12 pass against post-Wave-1 code path; no new specs created; no deleted-route navigation introduced
</success_criteria>

<output>
After completion, create `.planning/phases/49-unbreak-broken-persona-surfaces/49-04-SUMMARY.md`
</output>
