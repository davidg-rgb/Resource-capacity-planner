---
phase: 49-unbreak-broken-persona-surfaces
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md
  - .planning/phases/49-unbreak-broken-persona-surfaces/PROD-NEON-MIGRATION-CHECKLIST.md
autonomous: false
requirements:
  - UNBREAK-04
  - UNBREAK-05

must_haves:
  truths:
    - "Dev Neon branch's `drizzle.__drizzle_migrations` table reports 9 applied migrations (matching `drizzle/migrations/meta/_journal.json`) — verified by direct SELECT post-migrate"
    - "`information_schema.columns` reports `archived_at` present on `departments`, `disciplines`, `programs` tables on the dev Neon branch"
    - "`information_schema.tables` reports `change_log` table EXISTS on the dev Neon branch"
    - "`/admin` (Ändringslogg) and `/admin/people` and the three collateral admin pages (`/admin/departments`, `/admin/disciplines`, `/admin/programs`) all load without rendering any `Kunde inte ladda…` error banner — verified via authenticated browser smoke test (mcp__claude-in-chrome) AND by tailing `.next/dev/logs/next-development.log` for zero new `Unhandled API error` entries during the smoke window"
    - "Production Neon branch deploy checklist `.planning/phases/49-unbreak-broken-persona-surfaces/PROD-NEON-MIGRATION-CHECKLIST.md` exists, documents pre-deploy snapshot procedure, the `pnpm db:migrate` invocation, post-deploy verification queries, and the rollback plan — does NOT execute the prod migration in Phase 49"
  artifacts:
    - path: .planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md
      provides: "Pre/post-migration evidence for the dev Neon branch — pre-snapshot row count, executed command, post-snapshot row count, smoke-test capture (5 admin pages), dev-log tail showing zero new errors"
      contains: "drizzle.__drizzle_migrations"
      min_lines: 50
    - path: .planning/phases/49-unbreak-broken-persona-surfaces/PROD-NEON-MIGRATION-CHECKLIST.md
      provides: "Production deploy checklist for the same migration — Neon branch snapshot via `neon branches create`, dry-run verification, executed command, rollback procedure"
      contains: "neon branches create"
      min_lines: 30
  key_links:
    - from: ".planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md"
      to: "drizzle.__drizzle_migrations row count"
      via: "post-migrate `SELECT COUNT(*) FROM drizzle.__drizzle_migrations` capture (expected: 9)"
      pattern: "COUNT.*drizzle_migrations"
    - from: ".planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md"
      to: "/admin (Ändringslogg) page render"
      via: "mcp__claude-in-chrome navigate + read_console_messages capture"
      pattern: "/admin"
    - from: ".planning/phases/49-unbreak-broken-persona-surfaces/PROD-NEON-MIGRATION-CHECKLIST.md"
      to: "production deploy pipeline"
      via: "checklist consumed during next prod deploy; not executed in Phase 49"
      pattern: "neon branches create|pnpm db:migrate"
---

<objective>
Fix the admin 500s (UNBREAK-04, UNBREAK-05) by running the four missing Drizzle migrations against the dev Neon branch — the root cause is environmental drift, NOT application code (RESEARCH §Admin 500 Research Findings: code is correct, the dev Neon branch's `drizzle.__drizzle_migrations` table has 5 rows but `drizzle/migrations/meta/_journal.json` ships 9; missing migrations 0005-0008 leave `archived_at` columns absent and `change_log` table absent).

Purpose: `/admin`, `/admin/people`, `/admin/departments`, `/admin/disciplines`, `/admin/programs` (5 pages — wider blast radius than VERIFY-04 hypothesized; see RESEARCH §Surprise Finding 1) all crash with `NeonDbError: column "archived_at" does not exist (42703)` or `relation "change_log" does not exist`. The fix is `pnpm db:migrate` against the dev Neon branch in `.env.local`, plus a documented production checklist for the same remediation at next deploy.

Output: Working admin pages on dev (verified by browser smoke), an evidence file at `.planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md` capturing pre/post-migration state, and a production deploy checklist at `.planning/phases/49-unbreak-broken-persona-surfaces/PROD-NEON-MIGRATION-CHECKLIST.md`. NO application source code is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-unbreak-broken-persona-surfaces/49-CONTEXT.md
@.planning/phases/49-unbreak-broken-persona-surfaces/49-RESEARCH.md
@.planning/phases/49-unbreak-broken-persona-surfaces/49-VALIDATION.md
@.planning/pre-flight-report.md
@package.json
@drizzle/migrations/meta/_journal.json

<interfaces>
<!-- Confirmed migration scripts (verified from package.json:22-25): -->
- `pnpm db:generate` → `drizzle-kit generate` (creates new SQL from schema diffs — DO NOT use)
- `pnpm db:migrate`  → `drizzle-kit migrate` (file-driven; applies SQL files via `__drizzle_migrations` tracker — USE THIS)
- `pnpm db:push`     → `drizzle-kit push` (schema-sync; bypasses migration tracker — DO NOT use per RESEARCH Pitfall 1)

Migration journal (drizzle/migrations/meta/_journal.json) ships 9 entries:
- idx 0  0000_tearful_the_initiative
- idx 1  0001_goofy_doctor_doom
- idx 2  0002_oval_butterfly
- idx 3  0003_busy_black_bird               — creates `change_log` table + enums
- idx 4  0004_slippery_epoch
- idx 5  0005_motionless_blob               — `ALTER TYPE change_log_action ADD VALUE 'ACTUAL_UPSERTED'`
- idx 6  0006_import_status_staged_committed
- idx 7  0007_register_archive              — `ALTER TABLE departments/disciplines/programs ADD archived_at`
- idx 8  0008_change_log_entity_program

Captured Postgres error (RESEARCH §Direct Postgres Error):
NeonDbError: column "archived_at" does not exist  (code 42703, errorMissingColumn)

Verified live:
- 62+ Unhandled API error log lines in `.next/dev/logs/next-development.log` for tenant `0b200821-c78c-4717-9099-696c8520d2d3`
- All 5 admin pages (`/admin`, `/admin/people`, `/admin/departments`, `/admin/disciplines`, `/admin/programs`) affected
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-migration snapshot — capture current dev Neon branch state for rollback evidence</name>
  <files>.planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md</files>
  <read_first>
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-RESEARCH.md (§Admin 500 Research Findings — Reproduction + Root Cause)
    @drizzle/migrations/meta/_journal.json (the 9-entry source of truth)
    @package.json (lines 22-25 — confirm migrate script)
  </read_first>
  <action>
    1. Create `.planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md` with a `## Pre-migration snapshot` section.

    2. Capture dev Neon branch pre-state by running these queries against the `DATABASE_URL` in `.env.local` (use a one-shot Node script with `@neondatabase/serverless`, since `psql` may not be on PATH per RESEARCH §Environment Availability). The script template:
       ```javascript
       // tmp/49-03-pre-snapshot.mjs (delete after capture)
       import { neon } from '@neondatabase/serverless';
       const sql = neon(process.env.DATABASE_URL);
       console.log('=== drizzle.__drizzle_migrations ===');
       console.log(JSON.stringify(await sql`SELECT id, hash, created_at FROM drizzle.__drizzle_migrations ORDER BY created_at`, null, 2));
       console.log('=== departments columns ===');
       console.log(JSON.stringify(await sql`SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='departments'`, null, 2));
       console.log('=== change_log table presence ===');
       console.log(JSON.stringify(await sql`SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name='change_log'`, null, 2));
       ```

    3. Run via:
       ```bash
       node --env-file=.env.local tmp/49-03-pre-snapshot.mjs
       ```

    4. Paste the verbatim output into the EVIDENCE.md `## Pre-migration snapshot` section in fenced code blocks.

    5. Capture `git rev-parse HEAD` and record at the top of EVIDENCE.md as the commit SHA the migration is being run against (per CONTEXT specifics line 167).

    6. **Branch snapshot for rollback** (per CONTEXT brief — "capture pre-migration Neon branch snapshot as rollback"): if `neon` CLI is installed (`npx neon --version` or `pnpm exec neon --version`), run:
       ```bash
       neon branches create --name "phase-49-pre-migrate-$(date +%Y%m%d-%H%M)" --from-branch <current-dev-branch-id>
       ```
       Capture the new branch ID into EVIDENCE.md under `## Rollback branch`. If `neon` CLI is NOT available, document in EVIDENCE.md: "Neon CLI not available locally — rollback via Neon dashboard's branch-restore feature using the `created_at` timestamp captured above; manual `psql` revert of 0005-0008 is the fallback."

    7. Delete `tmp/49-03-pre-snapshot.mjs` after capture.
  </action>
  <verify>
    <automated>test -f .planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md && grep -c "Pre-migration snapshot" .planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md` exists
    - Contains a `## Pre-migration snapshot` heading
    - Contains the verbatim output of `SELECT FROM drizzle.__drizzle_migrations` (expected: 5 rows pre-fix per RESEARCH evidence)
    - Contains the verbatim output of the `information_schema.columns` query for `departments` (`archived_at` should be ABSENT pre-fix)
    - Contains the verbatim output of the `information_schema.tables` query for `change_log` (table should be ABSENT pre-fix)
    - Records the git commit SHA at the top
    - Records either a Neon rollback branch ID OR a documented dashboard-restore fallback
  </acceptance_criteria>
  <done>Pre-migration evidence captured; rollback path documented; tmp script deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Verify migration 0003 uses `IF NOT EXISTS` for `change_log` (RESEARCH Pitfall 5 + Surprise Finding 5)</name>
  <files>.planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md</files>
  <read_first>
    @drizzle/migrations/0003_busy_black_bird.sql (or whatever filename matches idx 3 in `_journal.json`)
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-RESEARCH.md (§Surprise Finding 5 — change_log table missing despite 5 migration rows; §Common Pitfalls Pitfall 1 — push vs migrate)
  </read_first>
  <action>
    1. Read `drizzle/migrations/0003_busy_black_bird.sql` (filename pattern; verify exact name from `_journal.json`).

    2. Search for the `CREATE TABLE` statement that creates `change_log`:
       ```bash
       grep -n "CREATE TABLE.*change_log" drizzle/migrations/0003_busy_black_bird.sql
       ```

    3. Determine if the statement uses `CREATE TABLE IF NOT EXISTS` or just `CREATE TABLE`.

    4. Append a `## Migration 0003 idempotency check` section to EVIDENCE.md documenting:
       - The exact CREATE TABLE statement found
       - Whether `IF NOT EXISTS` is present
       - Decision: if `IF NOT EXISTS` is present → safe to run `pnpm db:migrate` directly. If absent → BEFORE running migrate, manually create just the `change_log` table on the dev branch by extracting that one statement and running it via psql/Node (do NOT modify the migration file itself; the file is signed by Drizzle's hash).

    5. If the manual change_log creation is needed, capture the executed SQL and the row count check (`SELECT COUNT(*) FROM change_log;`) into EVIDENCE.md under `## Pre-migrate change_log seed`.

    Per RESEARCH Pitfall 1: do NOT use `pnpm db:push` as a workaround — it bypasses the `__drizzle_migrations` tracker and leaves the dev branch in a state where future runs of `migrate` fail with "relation already exists" on tables push created.
  </action>
  <verify>
    <automated>grep -c "Migration 0003 idempotency check" .planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - EVIDENCE.md `## Migration 0003 idempotency check` section exists
    - Documents whether `0003_busy_black_bird.sql` uses `CREATE TABLE IF NOT EXISTS change_log` or not
    - If NOT present, EVIDENCE.md captures the manual change_log creation SQL that was executed AND the post-creation row count (should be 0 — empty table)
    - Contains explicit go/no-go statement: "Safe to proceed with `pnpm db:migrate`" or "Manual change_log seeded; safe to proceed"
  </acceptance_criteria>
  <done>Idempotency verified or pre-seeded; go/no-go decision recorded in EVIDENCE.md.</done>
</task>

<task type="auto">
  <name>Task 3: Run `pnpm db:migrate` against dev Neon branch and capture post-migration state</name>
  <files>.planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md</files>
  <read_first>
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md (Tasks 1+2 evidence; honor Task 2's go/no-go decision before running)
    @package.json (line 23 — `db:migrate`)
  </read_first>
  <action>
    1. Confirm Task 2's decision was "safe to proceed". If "manual seed needed", confirm Task 2 actually executed the seed.

    2. Execute the migration:
       ```bash
       pnpm db:migrate
       ```
       Capture stdout + stderr verbatim. If the command exits non-zero, STOP, paste the error into EVIDENCE.md, and do NOT proceed to Task 4. Investigate based on the error class:
       - "relation already exists" → Pitfall 1 likely already triggered; consult RESEARCH §Pitfall 1 mitigation (manual SQL apply of just 0005-0008 via psql/Node)
       - "column already exists" → check if a previous attempt half-applied; reconcile via direct DB inspection
       - "duplicate enum value" → 0005's `ALTER TYPE … ADD VALUE 'ACTUAL_UPSERTED'` is not idempotent in Postgres < 12; check if value already present and skip if so

    3. On successful migrate, append `## Migration execution` to EVIDENCE.md with:
       - Verbatim stdout/stderr
       - Re-run the same three queries from Task 1 (`drizzle.__drizzle_migrations`, `information_schema.columns` for departments, `information_schema.tables` for change_log) and paste verbatim output as `## Post-migration snapshot`

    Expected post-state:
    - `drizzle.__drizzle_migrations` has 9 rows
    - `departments` columns include `archived_at`
    - `disciplines` columns include `archived_at`
    - `programs` columns include `archived_at`
    - `change_log` table is present in `information_schema.tables`

    DO NOT touch production Neon branch. The `.env.local` `DATABASE_URL` MUST point at the dev branch — verify the host string contains `ep-raspy-sea-al5kxh7j-pooler` (or the current dev branch identifier per pre-flight report) and is NOT a production identifier.

    DO NOT use `pnpm db:push` (RESEARCH Pitfall 1).

    DO NOT run migrations against production — that's Plan 49-03 Task 5's checklist, executed at next deploy, not in Phase 49.
  </action>
  <verify>
    <automated>grep -c "Post-migration snapshot" .planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - EVIDENCE.md contains `## Migration execution` section with verbatim `pnpm db:migrate` stdout
    - EVIDENCE.md contains `## Post-migration snapshot` section
    - Post-migration `drizzle.__drizzle_migrations` row count is `9` (or documented why it differs)
    - Post-migration `information_schema.columns` for `departments` includes `archived_at`
    - Post-migration `information_schema.tables` for `change_log` shows the table present
    - `pnpm db:migrate` exit code was `0` (recorded in EVIDENCE.md)
  </acceptance_criteria>
  <done>Migrations applied; post-state verified; evidence captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Browser smoke test — verify all 5 admin pages load without 500 (UNBREAK-04 + UNBREAK-05)</name>
  <files>.planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md</files>
  <what-built>
    Migrations 0005-0008 applied to dev Neon branch (Tasks 1-3). Five admin pages should now load without 500 errors:
    - `/admin` (Ändringslogg / change-log feed)
    - `/admin/people`
    - `/admin/departments`
    - `/admin/disciplines`
    - `/admin/programs`
  </what-built>
  <how-to-verify>
    1. Confirm dev server is running (`pnpm dev` — port 3000 or 3001).
    2. Use `mcp__claude-in-chrome` MCP tools (per CONTEXT D-03):
       - `mcp__claude-in-chrome__navigate` to `http://localhost:3000/admin`
       - `mcp__claude-in-chrome__read_console_messages` — capture any errors
       - `mcp__claude-in-chrome__read_network_requests` — capture status of the underlying API call (`GET /api/v5/change-log`)
       - Visually confirm the page renders entries (or empty state) WITHOUT the "Kunde inte ladda ändringsloggen" toast/banner
       - Capture screenshot via `mcp__claude-in-chrome__take_screenshot` (optional)
    3. Repeat the navigate + console + network capture for each of the other 4 admin pages.
    4. Tail `.next/dev/logs/next-development.log` (or stream from the running `pnpm dev` process) for the entire smoke window. Filter for `Unhandled API error` — count should be `0` for the smoke window's timestamp range.
    5. Append all captures to EVIDENCE.md under `## Smoke test evidence` — one subsection per admin page, plus a `## Dev log tail` subsection showing the count of errors during the smoke window.

    Expected outcome: All 5 pages render content (entries or legitimate empty state); zero new `Unhandled API error` entries during the smoke window; zero `Kunde inte ladda…` banners visible.
  </how-to-verify>
  <action>Operator follows the steps in `<how-to-verify>` above using `mcp__claude-in-chrome` MCP tools, signs in as admin via Clerk dev pane if needed, navigates each of the 5 admin pages in sequence, captures console + network + screenshot evidence per page, tails the dev log for the smoke window, and appends all captures to EVIDENCE.md under `## Smoke test evidence` and `## Dev log tail`.</action>
  <verify>
    <automated>grep -cE "Smoke test evidence|Dev log tail" .planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md</automated>
  </verify>
  <done>All 5 admin pages render without error; smoke evidence captured in EVIDENCE.md; operator approves.</done>
  <acceptance_criteria>
    - EVIDENCE.md `## Smoke test evidence` section exists with 5 sub-sections (one per admin page)
    - Each sub-section captures: the navigate URL, console messages (or "no console errors"), the API status code (expected `200`), and a verdict line ("LOADS WITHOUT ERROR" / "STILL FAILS")
    - EVIDENCE.md `## Dev log tail` section shows zero new `Unhandled API error` entries during the smoke window
    - All 5 admin pages report "LOADS WITHOUT ERROR"
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 5 admin pages render without errors and EVIDENCE.md captures the smoke evidence. If any page still fails, paste the new error and pause for diagnosis.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Author production Neon branch deploy checklist (do NOT execute against prod)</name>
  <files>.planning/phases/49-unbreak-broken-persona-surfaces/PROD-NEON-MIGRATION-CHECKLIST.md</files>
  <read_first>
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-03-EVIDENCE.md (Task 1-4 captures form the dev playbook; mirror the structure for prod)
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-RESEARCH.md (§Fix Approach — "Important: this is a dev-branch fix. The same remediation must be re-run against the production Neon branch as a deploy step")
    @.planning/phases/49-unbreak-broken-persona-surfaces/49-CONTEXT.md (Open Question #2 / scope-reduction flag — "Don't fix production Neon drift inside Phase 49 — this is a deploy checklist item, not a phase task")
  </read_first>
  <action>
    1. Create `.planning/phases/49-unbreak-broken-persona-surfaces/PROD-NEON-MIGRATION-CHECKLIST.md` with these sections:

       ## Purpose
       Apply the same migrations that fixed the dev branch in Phase 49 to the production Neon branch. To be executed at the next production deploy — NOT during Phase 49.

       ## Pre-deploy snapshot
       - [ ] Capture production branch ID via Neon dashboard or `neon branches list`
       - [ ] Create rollback branch: `neon branches create --name "pre-phase-49-migrate-$(date +%Y%m%d)" --from-branch <prod-branch-id>` — record new branch ID below
       - [ ] Capture pre-migration `drizzle.__drizzle_migrations` row count via the same Node script template as dev (Task 1) — record output here
       - [ ] Verify no in-flight admin-page traffic by checking Vercel logs for the last 5 minutes

       ## Migration 0003 idempotency (carry-over from dev)
       - Reference: `49-03-EVIDENCE.md ## Migration 0003 idempotency check`
       - If dev required manual change_log seeding, prod likely needs the same — execute the same SQL against prod BEFORE running migrate

       ## Execute
       - [ ] Set DATABASE_URL to point at production branch (NOT dev)
       - [ ] Run `pnpm db:migrate`
       - [ ] Capture stdout/stderr verbatim
       - [ ] Confirm exit code 0

       ## Post-deploy verification
       - [ ] Re-run the three snapshot queries (drizzle_migrations, information_schema.columns for departments, information_schema.tables for change_log)
       - [ ] Confirm 9 migrations applied
       - [ ] Confirm `archived_at` columns present
       - [ ] Confirm `change_log` table present
       - [ ] Sign in as admin in production, navigate `/admin`, `/admin/people`, `/admin/departments`, `/admin/disciplines`, `/admin/programs` — all five render without error
       - [ ] Tail Vercel function logs for 5 minutes, confirm zero new `Unhandled API error` for `archived_at` or `change_log`

       ## Rollback
       If post-deploy verification fails:
       - Restore the rollback branch created in pre-deploy snapshot via Neon dashboard branch-promote
       - Or run targeted reverse SQL (DROP COLUMN archived_at on each table; DROP TABLE change_log) — UNSAFE if any data was written; prefer branch restore
       - Revert any associated application code deploys

       ## Sign-off
       - Operator: ___________
       - Date: ___________
       - Production verified: ☐

    2. DO NOT execute any of the steps in this file. The checklist exists for the next prod deploy; Phase 49 only authors it.

    3. DO NOT touch the production Neon branch in any way during Phase 49 — verify by checking `.env.local` `DATABASE_URL` still points at dev.
  </action>
  <verify>
    <automated>test -f .planning/phases/49-unbreak-broken-persona-surfaces/PROD-NEON-MIGRATION-CHECKLIST.md && grep -cE "Pre-deploy snapshot|Execute|Post-deploy verification|Rollback" .planning/phases/49-unbreak-broken-persona-surfaces/PROD-NEON-MIGRATION-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/49-unbreak-broken-persona-surfaces/PROD-NEON-MIGRATION-CHECKLIST.md` exists
    - File contains all 6 expected sections (`Purpose`, `Pre-deploy snapshot`, `Migration 0003 idempotency`, `Execute`, `Post-deploy verification`, `Rollback`, `Sign-off`)
    - File explicitly states "NOT during Phase 49" / "executed at the next production deploy"
    - File references `neon branches create` for rollback snapshot
    - File references `pnpm db:migrate` (NOT `db:push`)
    - Production Neon branch has NOT been touched (verify via Neon dashboard or by confirming `.env.local` is unchanged)
  </acceptance_criteria>
  <done>Production deploy checklist authored and committed; production branch untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shell → dev Neon branch | Migration commands use the `DATABASE_URL` in `.env.local`; secret stays local-only |
| Dev Neon branch → application | Once schema matches code, all admin queries succeed; no auth changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-06 | Tampering | Manual SQL execution outside the migration tracker | mitigate | Task 2 audits 0003 idempotency BEFORE running migrate; if manual seed needed, evidence captures the exact SQL run. RESEARCH Pitfall 1 codifies "never use db:push" to prevent tracker desync. |
| T-49-07 | Denial of service | Migration could lock admin tables briefly | accept | All four migrations 0005-0008 are ALTER TABLE ADD COLUMN / ADD VALUE / CREATE TABLE — Postgres takes brief AccessExclusiveLock. Dev branch has minimal traffic; impact ~seconds. Production checklist (Task 5) flags the same risk for the deploy operator. |
| T-49-08 | Tampering | Accidental run against prod from dev shell | mitigate | Task 3 explicitly verifies `DATABASE_URL` host matches dev branch identifier (`ep-raspy-sea-al5kxh7j-pooler`) before migrate; Task 5 checklist makes the prod execution a deliberate, separate event. |
| T-49-09 | Elevation of privilege | Schema migration grants no new access | accept | Migrations only add columns/tables; no GRANT/ROLE changes. Existing Clerk auth remains the only access boundary. |
</threat_model>

<verification>
- All four admin pages load without error in dev (Task 4 checkpoint)
- `drizzle.__drizzle_migrations` reports 9 rows post-migrate
- `archived_at` present on departments / disciplines / programs
- `change_log` table present
- Production Neon branch UNTOUCHED (verified by checklist file existence + `.env.local` unchanged)
- Zero new `Unhandled API error` entries in dev log during smoke window
</verification>

<success_criteria>
1. UNBREAK-04: `/admin` (Ändringslogg) loads without "Kunde inte ladda ändringsloggen" — verified in Task 4
2. UNBREAK-05: `/admin/people` loads without "Kunde inte ladda listan" — verified in Task 4
3. Collateral fix (RESEARCH Surprise Finding 1): `/admin/departments`, `/admin/disciplines`, `/admin/programs` also recover
4. Production deploy path documented (PROD-NEON-MIGRATION-CHECKLIST.md exists), production NOT touched during Phase 49
</success_criteria>

<output>
After completion, create `.planning/phases/49-unbreak-broken-persona-surfaces/49-03-SUMMARY.md`
</output>
