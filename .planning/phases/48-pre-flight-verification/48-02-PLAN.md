---
phase: 48-pre-flight-verification
plan: 02
type: execute
wave: 2
depends_on:
  - "48-01"
files_modified:
  - .planning/pre-flight-report.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03
  - VERIFY-04
  - VERIFY-05
  - VERIFY-06
  - VERIFY-07
  - VERIFY-08
  - VERIFY-09

must_haves:
  truths:
    - "`pre-flight-report.md` Reviewer-Agent Sign-Off section contains a signed verdict (`APPROVED` or `REJECTED`) with a justification citing evidence in the 9 detail sections"
    - "`pre-flight-report.md` Scope-Expansion Summary table contains one row per EXPANDS-SCOPE verdict, with the expansion text mirrored in ROADMAP.md and REQUIREMENTS.md"
    - "If VERIFY-03 verdict = EXPANDS-SCOPE: ROADMAP.md Phase 49 section contains an `Expanded by VERIFY-03:` line and REQUIREMENTS.md UNBREAK block contains a new sub-bullet for the department picker build"
    - "If VERIFY-05 verdict = EXPANDS-SCOPE: ROADMAP.md Phase 51 section contains an `Expanded by VERIFY-05:` line and REQUIREMENTS.md LEAN block contains a new sub-bullet for the one-shot dashboard_layouts migration"
    - "If no VERIFY-0N verdict = EXPANDS-SCOPE: ROADMAP.md + REQUIREMENTS.md untouched; that fact is recorded in the Scope-Expansion Summary row set as `(none triggered)`"
  artifacts:
    - path: ".planning/pre-flight-report.md"
      provides: "Reviewer-agent sign-off + final scope-expansion summary"
      contains: "## Reviewer-Agent Sign-Off"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 49 / Phase 51 entries updated with Expanded by VERIFY-0N: line when applicable"
      contains: "## Phase 48: Pre-flight verification"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UNBREAK / LEAN blocks updated with scope-expansion sub-bullets when applicable"
      contains: "### Pre-Flight Verification (Wave −1 → Phase 48)"
  key_links:
    - from: ".planning/pre-flight-report.md Reviewer-Agent Sign-Off"
      to: "each `## VERIFY-0N` detail section"
      via: "reviewer-agent cites command + raw output for every verdict"
      pattern: "APPROVED|REJECTED"
    - from: ".planning/pre-flight-report.md Scope-Expansion Summary"
      to: ".planning/ROADMAP.md Phase 49/51 entries"
      via: "atomic commit per D-13"
      pattern: "Expanded by VERIFY-0"
---

<objective>
Run the reviewer-agent sign-off pass on `.planning/pre-flight-report.md` (per D-03) and atomically record any scope expansions (per D-12/D-13) into ROADMAP.md and REQUIREMENTS.md.

Purpose: Phase 48's ROADMAP success criterion #1 requires "signed-off by one reviewer". In the solo+AI dev model (D-04) the reviewer is a second-pass Claude agent that confirms every verdict is backed by captured evidence — no hand-waved passes. Scope expansions (VERIFY-03 → Phase 49, VERIFY-05 → Phase 51) must propagate to ROADMAP + REQUIREMENTS atomically with the report-finalization commit so downstream `CONTEXT.md` files can ingest them.

Output: `.planning/pre-flight-report.md` finalized (Sign-Off + Scope-Expansion Summary populated), ROADMAP.md + REQUIREMENTS.md updated conditionally, all in a single commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/pre-flight-report.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-pre-flight-verification/48-CONTEXT.md
@.planning/phases/48-pre-flight-verification/48-01-SUMMARY.md
@.planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md

<interfaces>
<!-- Verdict contract — reviewer-agent output: -->
<!-- APPROVED = every verdict in the report is backed by (a) the verbatim command from §Wave −1 AND (b) raw captured output AND (c) a verdict in {PASS, FAIL, EXPANDS-SCOPE}. -->
<!-- REJECTED = at least one verdict fails that contract. Reviewer must list the failing VERIFY-0N IDs. Plan 02 halts and does NOT update ROADMAP/REQUIREMENTS. -->

<!-- Scope-expansion propagation rules (D-12): -->
<!-- VERIFY-03 EXPANDS-SCOPE → -->
<!--   ROADMAP.md Phase 49 "**Depends on:**" line stays as-is; add new line immediately below: `**Expanded by VERIFY-03:** Build the department picker component before UNBREAK-01/02 can wire it.` -->
<!--   REQUIREMENTS.md "### Unbreak Broken Surfaces (Wave 0 → Phase 49)" section: add new checkbox sub-bullet: `- [ ] **UNBREAK-08**: Department-picker component exists at <path-to-be-determined-by-Phase-49-planner> with <props>; `/line-manager` + `/line-manager/timeline` consume it (replaces raw i18n key rendering)` -->

<!-- VERIFY-05 EXPANDS-SCOPE → -->
<!--   ROADMAP.md Phase 51 "**Depends on:**" line stays as-is; add new line: `**Expanded by VERIFY-05:** One-shot `UPDATE dashboard_layouts SET layout = ...` migration runs before `widgets/index.ts` deletion (see UI-RESTRUCTURE-PLAN-v2.md §2.5 Wave 2 for migration draft).` -->
<!--   REQUIREMENTS.md "### Lean Cleanup — Duplicate Removal (Wave 2 → Phase 51)" section: add new checkbox sub-bullet: `- [ ] **LEAN-11**: One-shot migration strips unknown widget IDs from `dashboard_layouts.layout` for the N affected rows identified in VERIFY-05; LEAN-05 widget deletion blocks on this migration completing.` (Replace N with the row count from VERIFY-05 raw output.) -->

<!-- If no EXPANDS-SCOPE verdicts: write a single Scope-Expansion Summary row reading "(none triggered — proceed with Phase 49-53 as scoped)" and skip ROADMAP/REQUIREMENTS edits entirely. Task 2 acceptance criterion covers this. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reviewer-agent pass — confirm every verdict cites evidence</name>
  <files>.planning/pre-flight-report.md</files>
  <read_first>
    - .planning/pre-flight-report.md (complete — all 9 detail sections + summary table + placeholders from Plan 01)
    - .planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md §Wave −1 (to cross-check verbatim commands)
    - .planning/phases/48-pre-flight-verification/48-CONTEXT.md §decisions D-03, D-04 (sign-off mechanism)
  </read_first>
  <action>
Act as a second-pass reviewer. Read the complete `pre-flight-report.md` end-to-end and audit every verdict against the D-03 self-review checklist:

For each `## VERIFY-0N:` section, confirm:

1. **Command verbatim check.** The `**Command` block contains the EXACT command string from `UI-RESTRUCTURE-PLAN-v2.md §Wave −1` (byte-for-byte). Diff them mentally — any paraphrased command fails the check.
2. **Raw output present.** A `**Raw output:**` (or `**Server stack trace:**` for VERIFY-04) fenced code block exists AND is non-empty (if the command genuinely returned nothing, it must say literally `<no matches>` or `<empty>` — not be absent).
3. **Verdict valid.** `**Verdict:**` line contains exactly one of `PASS`, `FAIL`, `EXPANDS-SCOPE` (case-sensitive, in backticks or plain).
4. **Impact cites downstream.** `**Impact:**` line names a specific phase (49/50/51/52/53) and what the verdict means for that phase's scope.
5. **No source-code drift.** Run `git status` — only `.planning/pre-flight-report.md` (and optionally the SUMMARY from Plan 01) should be modified. Any `src/` or `messages/` change means the phase violated its scope guard.

If ALL 9 checks pass every rule: the reviewer verdict is `APPROVED`.
If ANY check fails a rule: the reviewer verdict is `REJECTED` and this task HALTS — it must not proceed to write into ROADMAP/REQUIREMENTS and must not mark itself done. The executor surfaces the failure so the user can run `/gsd-plan-phase 48 --gaps` or direct a manual fix.

**Write the sign-off.** Replace the existing `## Reviewer-Agent Sign-Off` placeholder in `.planning/pre-flight-report.md` with:

```markdown
## Reviewer-Agent Sign-Off

**Reviewer:** Claude (second-pass agent per D-03/D-04)
**Date:** <today's date>
**Verdict:** `APPROVED` | `REJECTED`

### Evidence cited per VERIFY-0N

| ID | Command-verbatim | Raw output present | Verdict valid | Impact cites downstream | Pass? |
|---|:---:|:---:|:---:|:---:|:---:|
| VERIFY-01 | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ |
| VERIFY-02 | ... | ... | ... | ... | ... |
| VERIFY-03 | ... | ... | ... | ... | ... |
| VERIFY-04 | ... | ... | ... | ... | ... |
| VERIFY-05 | ... | ... | ... | ... | ... |
| VERIFY-06 | ... | ... | ... | ... | ... |
| VERIFY-07 | ... | ... | ... | ... | ... |
| VERIFY-08 | ... | ... | ... | ... | ... |
| VERIFY-09 | ... | ... | ... | ... | ... |

### Source-code drift check

`git status` output at sign-off:
```
<paste output — expected: only .planning/ files modified>
```

### Justification

<One paragraph: if APPROVED, state which downstream planners can now consume the report; if REJECTED, list the failing VERIFY-0N IDs and the specific rule(s) they failed.>
```

If `APPROVED`, proceed to Task 2. If `REJECTED`, do NOT proceed — leave the Scope-Expansion Summary empty, do NOT touch ROADMAP/REQUIREMENTS, and fail this task loudly so the executor stops.
  </action>
  <verify>
    <automated>grep -q "\*\*Verdict:\*\* \`APPROVED\`" .planning/pre-flight-report.md && grep -q "### Evidence cited per VERIFY-0N" .planning/pre-flight-report.md && grep -cE "^\| VERIFY-0[1-9] \|" .planning/pre-flight-report.md | awk '$1+0 >= 9 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `## Reviewer-Agent Sign-Off` section contains a `**Verdict:**` line with either `APPROVED` or `REJECTED` in backticks
    - Section contains an evidence table with exactly one row per `VERIFY-0N` (9 rows minimum matching `^\| VERIFY-0[1-9] \|`)
    - Section contains a `### Source-code drift check` block with paste of `git status` output
    - Section contains a `### Justification` paragraph
    - If verdict is `APPROVED`, every row in the evidence table has Pass? = `✓` (no `✗` in the last column)
    - If verdict is `REJECTED`, the justification explicitly lists the VERIFY-0N IDs that failed and which rule each failed
  </acceptance_criteria>
  <done>Reviewer-agent verdict written into the report with per-check evidence table and source-drift check; APPROVED unblocks Task 2, REJECTED halts the plan.</done>
</task>

<task type="auto">
  <name>Task 2: Populate Scope-Expansion Summary + conditionally update ROADMAP.md / REQUIREMENTS.md</name>
  <files>.planning/pre-flight-report.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/pre-flight-report.md (Sign-Off now APPROVED per Task 1; read each VERIFY-0N verdict)
    - .planning/ROADMAP.md (specifically "## Phase 49" and "## Phase 51" blocks — find the exact insertion points)
    - .planning/REQUIREMENTS.md (specifically "### Unbreak Broken Surfaces (Wave 0 → Phase 49)" and "### Lean Cleanup — Duplicate Removal (Wave 2 → Phase 51)" sections)
    - .planning/phases/48-pre-flight-verification/48-CONTEXT.md §decisions D-12, D-13
    - The `<interfaces>` block above (propagation rules)
  </read_first>
  <action>
Precondition: Task 1 wrote `**Verdict:** `APPROVED`` in the Reviewer-Agent Sign-Off section. If the verdict is `REJECTED`, abort this task (the plan is already halted per Task 1 rules).

**Step 1 — Collect EXPANDS-SCOPE verdicts.**
Grep the report for every VERIFY-0N section where the verdict is `EXPANDS-SCOPE`. Build an in-memory list of (VERIFY-ID, expansion text from the Impact line, target phase).

Only VERIFY-03 and VERIFY-05 are designed to expand scope per CONTEXT.md (D-12). If any OTHER VERIFY-0N has verdict EXPANDS-SCOPE, the reviewer should have flagged it as malformed in Task 1 — but if it slipped through, record it in the Scope-Expansion Summary anyway and note in the row that it is an unplanned expansion requiring user decision.

**Step 2 — Write the Scope-Expansion Summary table.**
Replace the `## Scope-Expansion Summary` placeholder in `.planning/pre-flight-report.md` with:

```markdown
## Scope-Expansion Summary

| Source check | Verdict | Downstream phase | Expansion text | Recorded in ROADMAP | Recorded in REQUIREMENTS |
|---|---|---|---|:---:|:---:|
<!-- One row per EXPANDS-SCOPE verdict. If none, use a single row: -->
<!-- | (none triggered) | — | — | No scope expansions; Phases 49-53 ship as originally scoped. | n/a | n/a | -->
```

For each EXPANDS-SCOPE row, fill exactly one of (VERIFY-03 → Phase 49) or (VERIFY-05 → Phase 51), cite the Impact text verbatim as the expansion text, and use `✓` or `✗` in the last two columns (✓ = written in Step 3 below).

**Step 3 — Conditionally edit ROADMAP.md and REQUIREMENTS.md.**

Only edit these two files if the Scope-Expansion Summary has ≥1 data row (i.e., not the "(none triggered)" placeholder row).

**If VERIFY-03 = EXPANDS-SCOPE:**

In `.planning/ROADMAP.md`, locate the `## Phase 49: Unbreak broken persona surfaces` section (around line 332). After the `**Depends on**: Phase 48` line and before `**Requirements**:`, insert:

```
**Expanded by VERIFY-03:** Build the department picker component before UNBREAK-01/02 can wire it. Pre-flight report §VERIFY-03 confirms the component is absent.
```

In `.planning/REQUIREMENTS.md`, locate the `### Unbreak Broken Surfaces (Wave 0 → Phase 49)` section (starts around line 27). At the end of the `UNBREAK-01 … UNBREAK-07` checkbox list, add:

```markdown
- [ ] **UNBREAK-08**: Department-picker component authored (location + props to be decided by Phase 49 planner from VERIFY-03 evidence); `/line-manager` and `/line-manager/timeline` consume it so raw `v5.lineManager.*.selectDepartment` keys stop rendering as primary text. Replaces VERIFY-03's EXPANDS-SCOPE note.
```

Also update REQUIREMENTS.md "Traceability" table (around line 117) — update the row `| UNBREAK-01 … UNBREAK-07 | 49 | TBD | pending |` to `| UNBREAK-01 … UNBREAK-08 | 49 | TBD | pending |` and the total count footer from "7 unbreak" to "8 unbreak" (and update overall total 42 → 43).

**If VERIFY-05 = EXPANDS-SCOPE:**

In `.planning/ROADMAP.md`, locate the `## Phase 51: Lean cleanup — duplicate removal` section. After the `**Depends on**: Phase 50 ...` line and before `**Requirements**:`, insert:

```
**Expanded by VERIFY-05:** One-shot `UPDATE dashboard_layouts SET layout = ...` migration runs before `widgets/index.ts` dead-widget deletion; migration draft in `UI-RESTRUCTURE-PLAN-v2.md §2.5 Wave 2`. Pre-flight report §VERIFY-05 lists the N affected `dashboard_layouts` rows.
```
(Replace N with the actual row count from VERIFY-05 raw output.)

In `.planning/REQUIREMENTS.md`, locate `### Lean Cleanup — Duplicate Removal (Wave 2 → Phase 51)` section. At the end of the `LEAN-01 … LEAN-10` list, add:

```markdown
- [ ] **LEAN-11**: One-shot migration strips dead widget IDs (`discipline-progress`, `discipline-demand`, `project-impact`) from `dashboard_layouts.layout` for the N rows identified in VERIFY-05; LEAN-05 widget file deletion blocks on this migration's successful run. SQL draft: `UI-RESTRUCTURE-PLAN-v2.md §2.5 Wave 2`.
```

Update the Traceability table row `| LEAN-01 … LEAN-10 | 51 | TBD | pending |` → `| LEAN-01 … LEAN-11 | 51 | TBD | pending |` and increment the total count (10 lean → 11 lean; overall total +1).

**If BOTH VERIFY-03 AND VERIFY-05 expanded:** apply both sets of edits; total count goes 42 → 44 (7 unbreak + 10 lean + 5 nav + 12 per-journey + 8 polish + 9 verify = 51 base; unbreak becomes 8, lean becomes 11, so final total = 9 + 8 + 5 + 11 + 12 + 8 = 53 active; update footer accordingly). Double-check the math from the actual REQUIREMENTS.md footer line before editing.

**Step 4 — Update the Scope-Expansion Summary `Recorded` columns** to `✓` for every row whose ROADMAP + REQUIREMENTS edits were written in Step 3. If any edit was skipped (e.g., reviewer rejected mid-task), mark the row `✗` and note why in the expansion text.

**Step 5 — No git commit here.** The phase-closing commit (done by the `/gsd-execute-phase` wrapper) bundles all three files atomically per D-13. Just ensure the working tree has all three files modified when the plan finishes.
  </action>
  <verify>
    <automated>grep -q "^## Scope-Expansion Summary" .planning/pre-flight-report.md && grep -qE "^\| \[VERIFY-(03|05)\]|^\| \(none triggered\)" .planning/pre-flight-report.md && ( grep -q "EXPANDS-SCOPE" .planning/pre-flight-report.md && grep -qE "Expanded by VERIFY-0(3|5):" .planning/ROADMAP.md || ! grep -q "EXPANDS-SCOPE" .planning/pre-flight-report.md )</automated>
  </verify>
  <acceptance_criteria>
    - `## Scope-Expansion Summary` section in the report contains a table with header `| Source check | Verdict | Downstream phase | Expansion text | Recorded in ROADMAP | Recorded in REQUIREMENTS |`
    - If the report has ANY `**Verdict:** `EXPANDS-SCOPE`` line in VERIFY-03 or VERIFY-05: `.planning/ROADMAP.md` contains a matching `**Expanded by VERIFY-0(3|5):**` line inside the corresponding Phase 49 or Phase 51 block
    - If the report has ANY `**Verdict:** `EXPANDS-SCOPE`` line in VERIFY-03 or VERIFY-05: `.planning/REQUIREMENTS.md` contains a new checkbox line starting with either `- [ ] **UNBREAK-08**:` or `- [ ] **LEAN-11**:` in the matching block
    - If the report has ZERO `EXPANDS-SCOPE` verdicts: ROADMAP.md and REQUIREMENTS.md git diff shows no changes (`git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md` returns empty)
    - The Scope-Expansion Summary's `Recorded` columns are ✓ for every row whose edit was actually written
    - NO source-code files under `src/` or `messages/` are modified (git diff --stat shows only `.planning/` files)
  </acceptance_criteria>
  <done>Scope-Expansion Summary populated; ROADMAP + REQUIREMENTS edited only when triggered by an actual EXPANDS-SCOPE verdict; Summary's Recorded columns truthfully reflect what was written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer-agent → report verdicts | reviewer-agent could rubber-stamp without checking; mitigation = structured evidence table per VERIFY-0N |
| planner authoring ROADMAP/REQUIREMENTS edits → downstream phases | wrong insertion point could orphan a requirement; mitigation = exact line anchors in Task 2 action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-07 | R (Repudiation) | Reviewer-agent sign-off | mitigate | Sign-off writes a structured evidence table (per-check ✓/✗ per rule) and a git-status paste — post-hoc audit can re-run the same 5 rules against the report and catch a rubber-stamp |
| T-48-08 | T (Tampering) | ROADMAP/REQUIREMENTS edits when no scope expansion fired | mitigate | Task 2 acceptance criterion explicitly requires `git diff --stat` on those two files to be EMPTY when there are zero EXPANDS-SCOPE verdicts; prevents accidental edits |
| T-48-09 | E (Elevation of privilege) | Unplanned VERIFY-0N expanding scope | mitigate | Task 2 Step 1 explicitly handles the case of an unexpected EXPANDS-SCOPE verdict (not VERIFY-03 or VERIFY-05) — records it but flags for user decision rather than auto-editing ROADMAP/REQUIREMENTS |
</threat_model>

<verification>
- `.planning/pre-flight-report.md` has Reviewer-Agent Sign-Off populated with APPROVED/REJECTED verdict + evidence table
- `.planning/pre-flight-report.md` has Scope-Expansion Summary populated
- If any VERIFY-0N was EXPANDS-SCOPE: the ROADMAP.md + REQUIREMENTS.md edits exist and mirror the expansion text
- If no VERIFY-0N was EXPANDS-SCOPE: ROADMAP.md + REQUIREMENTS.md are untouched
- No source-code file modified in this plan
</verification>

<success_criteria>
1. Reviewer-Agent Sign-Off verdict is either `APPROVED` (phase closes cleanly) or `REJECTED` (phase halts for remediation, no ROADMAP/REQUIREMENTS writes) — never blank
2. Scope-Expansion Summary has one row per EXPANDS-SCOPE verdict in the report (or a single "(none triggered)" row if zero)
3. Every scope expansion in the Summary has `✓` in both Recorded columns when ROADMAP.md + REQUIREMENTS.md were actually updated; `✗` only when the update was deliberately skipped with a noted reason
4. D-13 atomicity: the phase-closing commit includes `pre-flight-report.md`, `ROADMAP.md` (if edited), and `REQUIREMENTS.md` (if edited) together — no partial state
</success_criteria>

<output>
After completion, create `.planning/phases/48-pre-flight-verification/48-02-SUMMARY.md` documenting:
- Reviewer verdict (APPROVED or REJECTED)
- If REJECTED: list of failing VERIFY-0N IDs + rules
- If APPROVED: list of scope expansions written (VERIFY-03 → Phase 49 new req, VERIFY-05 → Phase 51 new req, or none)
- Files modified in the phase-closing commit (pre-flight-report.md + optionally ROADMAP.md + REQUIREMENTS.md)
- Handoff note for Phase 49 planner: "Read `.planning/pre-flight-report.md` §VERIFY-03, §VERIFY-04, §VERIFY-06 appendix before writing Phase 49 PLAN."
</output>
