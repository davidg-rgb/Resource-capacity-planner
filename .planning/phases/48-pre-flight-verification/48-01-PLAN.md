---
phase: 48-pre-flight-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/pre-flight-report.md
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03
  - VERIFY-04
  - VERIFY-05
  - VERIFY-06
  - VERIFY-07
  - VERIFY-08
  - VERIFY-09

must_haves:
  truths:
    - "`.planning/pre-flight-report.md` exists on disk"
    - "Report contains a summary table with one row per VERIFY-01..09 and a verdict cell (PASS | FAIL | EXPANDS-SCOPE) in each row"
    - "Report contains nine `## VERIFY-0N` detail sections, each with (a) check statement, (b) verbatim command text, (c) raw captured output, (d) verdict, (e) impact notes"
    - "Report header records the git commit SHA the evidence was gathered against (`git rev-parse HEAD` output)"
    - "Every Playwright spec under `e2e/` appears in the VERIFY-06 appendix table with a keep/update/retire classification and signal count"
  artifacts:
    - path: ".planning/pre-flight-report.md"
      provides: "Authoritative pass/fail report for all 9 Wave −1 assumptions"
      contains: "## VERIFY-01, ## VERIFY-02, ## VERIFY-03, ## VERIFY-04, ## VERIFY-05, ## VERIFY-06, ## VERIFY-07, ## VERIFY-08, ## VERIFY-09"
      min_lines: 200
  key_links:
    - from: ".planning/pre-flight-report.md#verify-05"
      to: "`dashboard_layouts` Neon dev branch row-count"
      via: "psql output captured verbatim"
      pattern: "SELECT organization_id, clerk_user_id, dashboard_id FROM dashboard_layouts"
    - from: ".planning/pre-flight-report.md#verify-06"
      to: "every `e2e/**/*.spec.ts` file"
      via: "classification table in report appendix"
      pattern: "keep|update|retire"
---

<objective>
Run the 9 Wave −1 evidence checks from `UI-RESTRUCTURE-PLAN-v2.md` verbatim and assemble `.planning/pre-flight-report.md` with raw command output for every verdict.

Purpose: Every downstream v6.0 phase (49-53) depends on the outcomes of these checks — Phase 49 scope may expand if the LM department picker is missing (VERIFY-03); Phase 51 scope may expand if the custom-dashboard SQL returns rows (VERIFY-05); every planner reads the admin API 500 root causes, the Playwright spec classification, and the i18n key presence before they write their plan. Skipping any of these checks leaves the downstream phases planning on speculation.

Output: Single monolithic markdown file at `.planning/pre-flight-report.md` (NOT inside the phase directory — it is a milestone-wide gate). Contains a summary table with pass/fail/expands-scope per VERIFY-0N row and nine detail sections underneath. No source code is modified in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-pre-flight-verification/48-CONTEXT.md
@.planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md
@.planning/ui-reviews/WIDGET-INVENTORY.md
@.planning/v6.0-HANDOFF.md
@.planning/v5.0-USER-JOURNEYS.md

<interfaces>
<!-- Verbatim commands from UI-RESTRUCTURE-PLAN-v2.md §Wave −1 (table rows 1-9). -->
<!-- Executor must use these command strings exactly; do not paraphrase. -->

| VERIFY | Command (verbatim from §Wave −1) | Expected result |
|---|---|---|
| VERIFY-01 | `grep -rn "export function getLandingRoute" src/` | one match in `persona.routes.ts:15` |
| VERIFY-02 | `ls src/app/api/v5/proposals/queue/count` OR `grep -rn "proposals/queue/count" src/app/api/` | if missing, add to Wave 3.2 scope |
| VERIFY-03 | `grep -rn "DepartmentPicker\|selectDepartment" src/features/personas src/components` | verify component ships or scope its build into Wave 0.1 |
| VERIFY-04a | `pnpm dev` + `curl -s http://localhost:3000/api/admin/change-log` once; tail server log | document the actual error |
| VERIFY-04b | same for `/api/admin/people` | document |
| VERIFY-05 | `psql $DATABASE_URL -c "SELECT organization_id, clerk_user_id, dashboard_id FROM dashboard_layouts WHERE layout::text ~* 'discipline-progress\|discipline-demand\|project-impact\|utilization-heat-map\|bench-report\|strategic-alerts\|resource-conflicts'"` | if >0 rows: widget stripping requires a data migration |
| VERIFY-06 | `ls e2e/{pm,line-manager,staff,rd,admin}/*.spec.ts` + per-spec grep | catalog each; classify keep/update/retire per D-09/D-10 rubric |
| VERIFY-07 | `grep -rn "sidebar.staff\|sidebar.projects" src/ messages/` | confirm existing meanings so new keys don't collide |
| VERIFY-08 | `jq '.v5.persona.kinds' messages/sv.json messages/en.json` | both return an object with pm/lineManager/staff/rd/admin |
| VERIFY-09 | grep PM/Staff/R&D pages for shared plan-vs-actual cell + timeline-grid component imports; confirm identical import path on all three personas | single shared component used by all three; no per-persona fork |

<!-- 3-signal Playwright spec rubric (from D-09/D-10) — apply to every e2e/**/*.spec.ts file: -->
<!-- Signal 1 (Route-touched): spec visits any of /, /team, /team/*, /projects, /wishes, or any path whose sidebar/breadcrumb rewrites in Waves 1–4 -->
<!-- Signal 2 (Selector-touched): spec queries persona-switcher, sidebar, breadcrumb, top-nav, notification bell, or any of the 3 dead widget IDs -->
<!-- Signal 3 (Copy-touched): spec asserts literal text for any string whose i18n key moves under sidebar.personaSections.* -->
<!-- Classification: 0 signals → keep. 1–2 → update. 3 OR deleted-route target → retire. -->

<!-- Decision constraints from 48-CONTEXT.md: -->
<!-- D-01: Monolithic report file at .planning/pre-flight-report.md (NOT inside phase dir) -->
<!-- D-02: English body; Swedish citations preserved -->
<!-- D-05: VERIFY-05 SQL runs against the dev Neon branch (DATABASE_URL from .env.local). Production Neon is NOT touched. -->
<!-- D-06: Capture raw SQL output; zero rows → PASS with note; >0 rows → EXPANDS-SCOPE -->
<!-- D-07: VERIFY-04 = static code read → hypothesis → live repro with log tail → capture stack trace verbatim -->
<!-- D-08: Do NOT fix the 500s in this phase -->
<!-- D-12: If VERIFY-03 or VERIFY-05 expand scope, Plan 02 records the expansion — Plan 01 only records the finding in the report -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize report with header + summary table skeleton</name>
  <files>.planning/pre-flight-report.md</files>
  <read_first>
    - .planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md §Wave −1 (the table with the 9 verbatim commands)
    - .planning/phases/48-pre-flight-verification/48-CONTEXT.md §decisions (D-01, D-02, D-03)
    - .planning/REQUIREMENTS.md §v6.0 Pre-Flight Verification (VERIFY-01..09 exact wording)
  </read_first>
  <action>
Create `.planning/pre-flight-report.md` with this exact skeleton. Do not populate verdicts yet — later tasks fill the detail sections.

1. Run `git rev-parse HEAD` and capture the SHA — this goes in the report header so downstream planners know which commit the evidence was gathered against (per CONTEXT.md §specifics).

2. Write the file with this structure (use the Write tool — this is a NEW file):

```markdown
# v6.0 Pre-flight Verification Report

**Date:** 2026-04-15
**Phase:** 48 — Pre-flight verification
**Commit:** `<SHA-from-git-rev-parse>`
**Scope:** Wave −1 assumption verification before any v6.0 code change.
**Source of truth:** `.planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md` §Wave −1

## Self-Review Checklist (D-03)

Before reviewer-agent sign-off, every verdict below must satisfy:

- [ ] Cites the verbatim command from `UI-RESTRUCTURE-PLAN-v2.md §Wave −1`
- [ ] Captures raw command output in a fenced code block (no paraphrasing)
- [ ] Verdict cell reads exactly one of: `PASS` | `FAIL` | `EXPANDS-SCOPE`
- [ ] Impact notes link the verdict to the downstream phase that consumes it

## Summary Table

| ID | Check | Verdict | Scope impact |
|---|---|---|---|
| [VERIFY-01](#verify-01-getlandingroute-exists) | `getLandingRoute(persona)` exists in `persona.routes.ts` | *TBD* | — |
| [VERIFY-02](#verify-02-proposalsqueuecount-endpoint) | `/api/v5/proposals/queue/count` endpoint exists | *TBD* | — |
| [VERIFY-03](#verify-03-phase-41-department-picker) | Phase 41 department-picker component ships | *TBD* | — |
| [VERIFY-04](#verify-04-admin-api-500-root-causes) | `/api/admin/change-log` + `/api/admin/people` 500 root causes | *TBD* | — |
| [VERIFY-05](#verify-05-custom-dashboard-dead-widget-references) | Custom-dashboard layouts reference dead/deletable widget IDs | *TBD* | — |
| [VERIFY-06](#verify-06-playwright-spec-inventory) | Every `e2e/**/*.spec.ts` classified keep/update/retire | *TBD* | — |
| [VERIFY-07](#verify-07-sidebar-i18n-collision-check) | `sidebar.staff` / `sidebar.projects` existing meanings | *TBD* | — |
| [VERIFY-08](#verify-08-v5personakinds-keys-present) | `v5.persona.kinds.*` keys present in both locales | *TBD* | — |
| [VERIFY-09](#verify-09-plan-vs-actual-cell-reuse) | Plan-vs-actual cell + timeline-grid reused across PM/Staff/R&D | *TBD* | — |

<!-- Detail sections follow; Tasks 2-6 fill them. -->
```

3. Commit the skeleton inside the same task-complete commit as the rest of the plan (no separate commit now — the executor's plan-end commit covers it).
  </action>
  <verify>
    <automated>test -f .planning/pre-flight-report.md && grep -c "^| \[VERIFY-0" .planning/pre-flight-report.md | grep -q "^9$"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/pre-flight-report.md` exists
    - File contains 9 rows matching `^| \[VERIFY-0[1-9]\]` in the summary table
    - Header contains a non-empty git SHA (output of `git rev-parse HEAD`)
    - File contains the literal string "Self-Review Checklist (D-03)"
  </acceptance_criteria>
  <done>Report skeleton with summary table and git-commit header committed to the working tree.</done>
</task>

<task type="auto">
  <name>Task 2: Grep cluster — VERIFY-01 / VERIFY-03 / VERIFY-07</name>
  <files>.planning/pre-flight-report.md</files>
  <read_first>
    - .planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md §Wave −1 (rows 1, 3, 8 — exact commands)
    - .planning/pre-flight-report.md (current state after Task 1)
    - src/features/personas/persona.routes.ts (VERIFY-01 target)
    - src/features/personas/ and src/components/ (VERIFY-03 targets — whatever exists)
    - messages/sv.json, messages/en.json (VERIFY-07 — grep hits here)
  </read_first>
  <action>
Run three greps, append three detail sections to `.planning/pre-flight-report.md`, and update the corresponding summary-table verdict cells.

**VERIFY-01** — getLandingRoute existence.
Run this exact command and capture the raw output (stdout + exit code):
```
grep -rn "export function getLandingRoute" src/
```
Expected per UI-RESTRUCTURE-PLAN-v2.md §Wave −1: one match in `persona.routes.ts:15`.
- 1+ matches in `persona.routes.ts` → verdict `PASS`, impact = "NAV-01 (Phase 50) can call it directly."
- 0 matches → verdict `FAIL`, impact = "Phase 50 scope expands to author `getLandingRoute` before NAV-01 can ship." (Note: do NOT expand ROADMAP here; Plan 02 owns that. Just record the finding.)

Append this section to the report (after the summary table, before any existing detail section):

```markdown
## VERIFY-01: `getLandingRoute` exists

**Requirement:** VERIFY-01 — `pre-flight-report.md` documents whether `getLandingRoute(persona)` exists in `src/features/personas/persona.routes.ts`
**Command (verbatim from UI-RESTRUCTURE-PLAN-v2.md §Wave −1):**
```
grep -rn "export function getLandingRoute" src/
```
**Raw output:**
```
<paste literal stdout; if empty, write "<no matches>">
```
**Verdict:** `PASS` | `FAIL`
**Impact:** <per rules above>
```

Also update the summary-table VERIFY-01 row: replace `*TBD*` in column 3 with the verdict and column 4 with a one-line impact note.

**VERIFY-03** — Department picker existence.
Exact command:
```
grep -rn "DepartmentPicker\|selectDepartment" src/features/personas src/components
```
Expected: component ships OR needs build.
- Hit on a real component file (e.g., `DepartmentPicker.tsx` with a default export) → `PASS`, impact = "Phase 49 UNBREAK-01/02 can wire the existing component."
- Only hits on the raw i18n key strings `v5.lineManager.home.selectDepartment` / `v5.lineManager.timeline.selectDepartment` with no component implementation → `EXPANDS-SCOPE`, impact = "Phase 49 scope expands to include building the department picker (per ROADMAP Phase 48 success criterion #3)."
- No hits at all → `EXPANDS-SCOPE`, same impact.

Append a `## VERIFY-03: Phase 41 department picker` section with the same structure (requirement wording from REQUIREMENTS.md, verbatim command, raw output, verdict, impact). Update summary table.

**VERIFY-07** — sidebar i18n collision check.
Exact command:
```
grep -rn "sidebar.staff\|sidebar.projects" src/ messages/
```
- Record every hit. For each distinct key reference (not every hit), note the UI surface it drives (section heading vs leaf item).
- Verdict: `PASS` if existing meanings are clearly section-heading usages (so `sidebar.personaSections.staff` / `sidebar.personaSections.projects` new keys don't collide) — per UI-RESTRUCTURE-PLAN-v2.md §6, the new keys deliberately live under `sidebar.personaSections.*` to avoid collision.
- Verdict: `FAIL` only if an existing `sidebar.staff.*` / `sidebar.projects.*` path already occupies the `personaSections` namespace.

Impact: always include a compact table listing existing `sidebar.staff` + `sidebar.projects` meanings (key → Swedish value → Where used) — Phase 50 planner consumes this to confirm the 18 new keys land safely.

Append `## VERIFY-07: sidebar i18n collision check` with command, raw output (include the grep hits AND the extracted Swedish values from `messages/sv.json` for each hit), verdict, and the meanings table. Update summary.
  </action>
  <verify>
    <automated>grep -q "^## VERIFY-01:" .planning/pre-flight-report.md && grep -q "^## VERIFY-03:" .planning/pre-flight-report.md && grep -q "^## VERIFY-07:" .planning/pre-flight-report.md && grep -cE "^\*\*Verdict:\*\* \`(PASS|FAIL|EXPANDS-SCOPE)\`" .planning/pre-flight-report.md | awk '$1+0 >= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Report contains `## VERIFY-01:`, `## VERIFY-03:`, `## VERIFY-07:` headings
    - Each of those three sections contains a `**Command` line with the verbatim command string from UI-RESTRUCTURE-PLAN-v2.md §Wave −1 (byte-for-byte)
    - Each of those three sections contains a `**Raw output:**` fenced code block
    - Each of those three sections' `**Verdict:**` is one of `PASS` / `FAIL` / `EXPANDS-SCOPE`
    - Summary-table rows for VERIFY-01, 03, 07 no longer contain `*TBD*`
    - VERIFY-07 section includes a meanings table with at least one row per existing `sidebar.staff` / `sidebar.projects` key hit
  </acceptance_criteria>
  <done>Three grep-cluster checks recorded in the report with raw output + verdict + impact; summary table updated for each.</done>
</task>

<task type="auto">
  <name>Task 3: Filesystem + Playwright inventory cluster — VERIFY-02 / VERIFY-06</name>
  <files>.planning/pre-flight-report.md</files>
  <read_first>
    - .planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md §Wave −1 (rows 2, 7)
    - .planning/phases/48-pre-flight-verification/48-CONTEXT.md §decisions D-09, D-10, D-11 (3-signal Playwright rubric)
    - src/app/api/v5/proposals/ (check subtree for queue/count)
    - e2e/ directory listing
    - Any e2e/**/*.spec.ts file that grep below flags as signal-touched (read to confirm rubric scoring)
  </read_first>
  <action>
Two checks in this task. Produce two new sections.

**VERIFY-02** — Queue-count endpoint existence.
Exact command sequence (from §Wave −1 row 2):
```
ls src/app/api/v5/proposals/queue/count
```
AND (fallback if the directory is absent):
```
grep -rn "proposals/queue/count\|/queue/count" src/app/api/
```
Capture both outputs (use the second one regardless, to prove negative).
- `route.ts` present in that directory → `PASS`, impact = "Phase 52 LM-01 can call it directly."
- Directory missing AND no code path defines the endpoint → `EXPANDS-SCOPE`, impact = "Phase 52 LM-01 scope expands to include authoring the endpoint (server route + service + unit test) before the badge can render a real count."

Append `## VERIFY-02: /api/v5/proposals/queue/count endpoint` with: requirement wording, both commands verbatim, raw output for each, verdict, impact. Update summary.

**VERIFY-06** — Playwright spec inventory + classification.
Exact commands from §Wave −1 row 7:
```
ls e2e/
ls e2e/pm/*.spec.ts 2>/dev/null ; ls e2e/line-manager/*.spec.ts 2>/dev/null ; ls e2e/staff/*.spec.ts 2>/dev/null ; ls e2e/rd/*.spec.ts 2>/dev/null ; ls e2e/admin/*.spec.ts 2>/dev/null
```

Then apply the 3-signal rubric from CONTEXT.md D-09/D-10 to every spec file. For each spec:

- **Signal 1 (Route-touched)**: run `grep -nE "'/' |'/team'|'/team/|'/projects'|'/wishes'|'/pm'|'/line-manager'|'/staff'|'/rd'|'/admin'" <spec>`. Count distinct routes hit.
- **Signal 2 (Selector-touched)**: `grep -nE "persona-switcher|side-?nav|breadcrumb|top-?nav|notification-bell|discipline-progress|discipline-demand|project-impact" <spec>`. Presence = signal 2 triggered.
- **Signal 3 (Copy-touched)**: `grep -nE "Projektledare|Linjechef|FoU|Administration|Mina projekt|Mina önskemål|Godkännandekö|Gruppschema|Mitt schema|Ändringslogg|Personer" <spec>` (the Swedish values from UI-RESTRUCTURE-PLAN-v2.md §6 table). Presence = signal 3 triggered.

Classification rule (from D-10):
- 0 signals → **keep**
- 1–2 signals → **update** (follow-up delegated to Wave that owns the change)
- 3 signals OR spec targets `/team` | `/projects` | `/wishes` (routes scheduled for deletion in Phase 51 LEAN-01/02/03) → **retire**

Append `## VERIFY-06: Playwright spec inventory` with:
1. Requirement wording
2. Verbatim `ls` commands + raw output (the file listing)
3. Inventory table (appendix per D-11):

```markdown
| Spec file | Signal 1 (route) | Signal 2 (selector) | Signal 3 (copy) | Classification | Wave that owns follow-up |
|---|:---:|:---:|:---:|---|---|
| e2e/pm/xxx.spec.ts | routes hit | yes/no | yes/no | keep / update / retire | — / 49 / 50 / 51 / 52 / 53 |
| ... | ... | ... | ... | ... | ... |
```

4. Verdict: `PASS` if every spec is classified (no TBD rows). `FAIL` only if a spec cannot be read/classified.
5. Impact: summary counts — "N kept, M updated, K retired" — plus a note that downstream phase CONTEXT.md files ingest the `update` + `retire` rows verbatim per D-11.

Update summary.

Note: if `e2e/` directory is empty or missing entirely (Phase 47 did not ship specs per the inventory count assumed), record `EXPANDS-SCOPE` with impact = "Phase 47 Playwright suite absent — ROADMAP Phase 48 success criterion #4 cannot be met without spec production; the spec-build belongs to the downstream waves that ship affected routes." Do NOT back-propagate into Phase 47 (shipped milestone).
  </action>
  <verify>
    <automated>grep -q "^## VERIFY-02:" .planning/pre-flight-report.md && grep -q "^## VERIFY-06:" .planning/pre-flight-report.md && grep -q "| Spec file | Signal 1" .planning/pre-flight-report.md</automated>
  </verify>
  <acceptance_criteria>
    - Report contains `## VERIFY-02:` and `## VERIFY-06:` headings
    - VERIFY-02 section has both `ls` and `grep` raw output blocks (captures both positive/negative evidence)
    - VERIFY-06 section contains an inventory table with header `| Spec file | Signal 1 (route) | Signal 2 (selector) | Signal 3 (copy) | Classification |`
    - Every row in the VERIFY-06 inventory table has a Classification cell reading exactly one of: `keep`, `update`, `retire`
    - Summary-table rows for VERIFY-02 and VERIFY-06 no longer contain `*TBD*`
  </acceptance_criteria>
  <done>Endpoint existence documented; every Playwright spec classified with signal counts; report updated.</done>
</task>

<task type="auto">
  <name>Task 4: Data + i18n cluster — VERIFY-05 (SQL) / VERIFY-08 (jq)</name>
  <files>.planning/pre-flight-report.md</files>
  <read_first>
    - .planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md §Wave −1 (rows 6, 9) AND §2.5 Wave 2 (corrected SQL + migration draft)
    - .planning/phases/48-pre-flight-verification/48-CONTEXT.md §decisions D-05, D-06 (SQL environment + capture format)
    - .planning/ui-reviews/WIDGET-INVENTORY.md §1 (widget IDs the SQL regex covers)
    - .env.local (to resolve DATABASE_URL — do NOT echo the raw connection string into the report; redact password)
    - messages/sv.json, messages/en.json
  </read_first>
  <action>
Two checks. Produce two new sections.

**VERIFY-05** — Custom-dashboard dead-widget references.
Per D-05: run against the Neon branch `.env.local` DATABASE_URL points to. Production is NOT touched.

Exact SQL (verbatim from UI-RESTRUCTURE-PLAN-v2.md §2.5 Wave 2, already corrected from the K1 review finding):
```sql
SELECT organization_id, clerk_user_id, dashboard_id
FROM dashboard_layouts
WHERE layout::text ~* 'discipline-progress|discipline-demand|project-impact|utilization-heat-map|bench-report|strategic-alerts|resource-conflicts';
```

Execute with the project's usual SQL path. Preferred:
```
psql "$DATABASE_URL" -c "SELECT organization_id, clerk_user_id, dashboard_id FROM dashboard_layouts WHERE layout::text ~* 'discipline-progress|discipline-demand|project-impact|utilization-heat-map|bench-report|strategic-alerts|resource-conflicts';"
```
Fallback if `psql` is not on PATH: use the project's existing DB script runner (check `package.json` scripts for anything like `db:query` or `db:psql`); if none, install `psql` via `pnpm dlx` or call the Neon branch through an ad-hoc Node `pg` snippet written to a scratch file under `tmp/` (delete after) — capture the command AND the output verbatim.

**Before executing:**
- Redact secrets: if the DATABASE_URL must be shown, replace password with `***` when recording the command in the report.
- Do NOT run the `UPDATE` migration from §2.5. Only SELECT.

**Verdict rules (per D-06):**
- Zero rows returned (`0 rows` in output) → `PASS`. Impact = "Phase 51 LEAN-05 ships as a straight code delete — no migration needed."
- ≥1 rows → `EXPANDS-SCOPE`. Impact = "Phase 51 LEAN-05 scope expands: ship the one-shot `UPDATE dashboard_layouts SET layout = ...` migration drafted in UI-RESTRUCTURE-PLAN-v2.md §2.5 BEFORE deleting the widget files. Plan 02 records this in ROADMAP.md + REQUIREMENTS.md."
- Query errors (table missing, connection refused, etc.) → `FAIL` with the error captured. Impact = "Dev Neon branch not representative; defer authoritative run to Phase 51 kick-off, still behind `uiV6.leanTrim` — see D-05."

Append `## VERIFY-05: Custom-dashboard dead-widget references` with: requirement wording, the exact SQL (verbatim), the command you actually invoked (redacted), raw output (fenced block), verdict, impact.

**VERIFY-08** — `v5.persona.kinds.*` key presence.
Exact command from §Wave −1 row 9:
```
jq '.v5.persona.kinds' messages/sv.json messages/en.json
```
Expected: both outputs return an object with at least `pm`, `lineManager`, `staff`, `rd`, `admin` keys.

**Verdict rules:**
- Both files return an object containing all 5 expected keys → `PASS`. Impact = "Phase 49 UNBREAK-06 can rely on the keys; PersonaGate error copy wires to real strings."
- Either file returns `null` or is missing one or more of the 5 keys → `FAIL`. Impact = "Phase 49 UNBREAK-06 scope expands to add the missing key(s) to both locale files before the error copy can read from `allowed` prop."

Append `## VERIFY-08: v5.persona.kinds keys present` with: requirement wording, verbatim command, raw output for BOTH files (keep them in separate fenced blocks labelled `sv.json` / `en.json`), verdict, impact. Include a small table listing which of `pm | lineManager | staff | rd | admin` is present in each file.

Update summary table rows for VERIFY-05 and VERIFY-08.
  </action>
  <verify>
    <automated>grep -q "^## VERIFY-05:" .planning/pre-flight-report.md && grep -q "^## VERIFY-08:" .planning/pre-flight-report.md && grep -q "SELECT organization_id, clerk_user_id, dashboard_id" .planning/pre-flight-report.md</automated>
  </verify>
  <acceptance_criteria>
    - Report contains `## VERIFY-05:` and `## VERIFY-08:` headings
    - VERIFY-05 section contains the verbatim SELECT SQL from UI-RESTRUCTURE-PLAN-v2.md §2.5 Wave 2 (regex `discipline-progress\|...\|resource-conflicts` covering all 7 widget IDs)
    - VERIFY-05 verdict is one of: `PASS` (zero rows) | `EXPANDS-SCOPE` (≥1 row) | `FAIL` (error)
    - VERIFY-05 section does NOT contain a raw database password in plaintext (grep for `postgres://.*:.*@` must not match a real password pattern; `***` redaction is OK)
    - VERIFY-08 section contains two separately labelled raw-output blocks (one for sv.json, one for en.json) and a presence table listing all 5 expected kinds
    - Summary-table rows for VERIFY-05 and VERIFY-08 no longer contain `*TBD*`
  </acceptance_criteria>
  <done>SQL audit and i18n key presence recorded with raw output; verdict rules applied; report updated.</done>
</task>

<task type="auto">
  <name>Task 5: Live-repro — VERIFY-04 (admin API 500 root causes)</name>
  <files>.planning/pre-flight-report.md</files>
  <read_first>
    - .planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md §Wave −1 (rows 4, 5)
    - .planning/phases/48-pre-flight-verification/48-CONTEXT.md §decisions D-07, D-08 (combined static+live method; NO fix ships here)
    - src/app/api/admin/change-log/route.ts (and any service file it imports)
    - src/app/api/admin/people/route.ts (and any service file it imports)
    - Any files under src/features/* referenced by those two routes
  </read_first>
  <action>
Per D-07: combined static + live repro method. Per D-08: do NOT ship a fix. Record the root causes for Phase 49 UNBREAK-04/05 to consume.

**Static phase (hypothesis formation).**
1. Read `src/app/api/admin/change-log/route.ts` end-to-end. Note: imports, handler signature, service calls, any `throw` sites, any implicit assumptions (auth context, table names, column shapes).
2. Read `src/app/api/admin/people/route.ts` similarly.
3. For each route, write a one-paragraph *hypothesis* of where the 500 likely originates (e.g., "Route queries a service that joins `persons` × `memberships`; `memberships` column `deleted_at` may be missing on the dev branch, producing a Postgres error").

**Live phase (confirmation).**
1. Start the dev server in the background:
   ```
   pnpm dev
   ```
   Wait for "Ready on http://localhost:3000" before proceeding. Authenticate via Clerk is already handled by the dev environment cookie — if not, prepend the hit with any required auth step documented in the project (check existing README / `.env.local`).
2. Hit each endpoint once and capture the HTTP status + body:
   ```
   curl -s -o /tmp/changelog-body.txt -w "HTTP %{http_code}\n" http://localhost:3000/api/admin/change-log
   curl -s -o /tmp/people-body.txt    -w "HTTP %{http_code}\n" http://localhost:3000/api/admin/people
   ```
3. Tail the dev-server stdout for the stack trace that fired between the two curl calls (scroll the terminal or use the background process output). Capture the FULL stack trace verbatim — not just the error message line.
4. Stop the dev server (Ctrl-C or `kill` on the background PID).

**Recording.**
Append `## VERIFY-04: Admin API 500 root causes` with:
- Requirement wording (from REQUIREMENTS.md VERIFY-04)
- Two sub-sections: `### /api/admin/change-log` and `### /api/admin/people`
- Each sub-section contains:
  - **Static hypothesis:** (your paragraph)
  - **Live commands:** (verbatim curl from step 2)
  - **HTTP response:** status code + body (from the captured file)
  - **Server stack trace:** fenced code block with the full trace verbatim
  - **Confirmed root cause:** one sentence tying trace to hypothesis (or replacing the hypothesis if the trace contradicts it)
  - **Phase 49 handoff:** one sentence of what UNBREAK-04/05 must do to fix (code location + fix direction; no code changes ship here)

**Verdict rules:**
- Both routes captured a stack trace with a diagnosable root cause → `PASS`. Impact = "Phase 49 UNBREAK-04/05 plan can land with targeted fixes; root cause documented."
- Either route cannot be repro'd (returns 200, or dev server crashed before the hit) → `FAIL` with note. Impact = "Phase 49 UNBREAK-04/05 planner must repro themselves."
- Never `EXPANDS-SCOPE` — the fix was always scoped to Phase 49; documenting the root cause doesn't expand scope, it informs planning.

Update summary-table row for VERIFY-04.
  </action>
  <verify>
    <automated>grep -q "^## VERIFY-04:" .planning/pre-flight-report.md && grep -q "^### /api/admin/change-log" .planning/pre-flight-report.md && grep -q "^### /api/admin/people" .planning/pre-flight-report.md && grep -q "\*\*Server stack trace:\*\*" .planning/pre-flight-report.md</automated>
  </verify>
  <acceptance_criteria>
    - Report contains `## VERIFY-04:` heading
    - Report contains both `### /api/admin/change-log` and `### /api/admin/people` sub-headings
    - Each sub-section contains `**Static hypothesis:**`, `**Live commands:**`, `**HTTP response:**`, `**Server stack trace:**`, `**Confirmed root cause:**`, and `**Phase 49 handoff:**` labelled blocks
    - Each `**Live commands:**` block contains a `curl` line hitting `http://localhost:3000/api/admin/` (the literal dev URL from UI-RESTRUCTURE-PLAN-v2.md §Wave −1)
    - Summary-table VERIFY-04 row no longer contains `*TBD*`
    - NO source-code change has landed in this task (git diff --stat limited to `.planning/pre-flight-report.md`)
  </acceptance_criteria>
  <done>Both admin API 500 root causes documented with static hypothesis + live stack trace; Phase 49 handoff written for each; no production code modified.</done>
</task>

<task type="auto">
  <name>Task 6: Component reuse — VERIFY-09 (plan-vs-actual cell + timeline-grid snapshot)</name>
  <files>.planning/pre-flight-report.md</files>
  <read_first>
    - .planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md §Wave −1 (implicit — this check was added post-review as J2)
    - .planning/v5.0-USER-JOURNEYS.md §Cross-cutting UX Primitives (items 1 and 2 — timeline grid + plan-vs-actual cell)
    - src/app/(app)/pm/ (PM timeline page — find the cell + grid imports)
    - src/app/(app)/staff/ (Staff schedule page — find the cell + grid imports)
    - src/app/(app)/rd/ (R&D portfolio page — find the cell + grid imports)
    - Any src/components/**/timeline-grid* or src/features/**/timeline-grid* (the shared component)
    - Any src/components/**/plan-vs-actual* or src/features/**/plan-vs-actual* (the shared cell)
  </read_first>
  <action>
Verify the plan-vs-actual cell and timeline-grid components are a single shared implementation used identically by PM, Staff, and R&D — not three forks.

**Command sequence (construct from §Wave −1 J2 intent + D-06 snapshot-compare wording):**

1. Find the timeline-grid component source of truth:
   ```
   grep -rln --include="*.ts" --include="*.tsx" "export.*TimelineGrid\|export default function TimelineGrid" src/
   ```
2. Find the plan-vs-actual cell source:
   ```
   grep -rln --include="*.ts" --include="*.tsx" "PlanVsActualCell\|plan-vs-actual-cell" src/
   ```
3. Find every import of each across the three persona pages:
   ```
   grep -rn "TimelineGrid\|plan-vs-actual" src/app/\(app\)/pm src/app/\(app\)/staff src/app/\(app\)/rd
   ```

**Classification:**
- One component file exported once, imported via the SAME module path by PM + Staff + R&D → `PASS`. Impact = "Shared primitive confirmed; Phase 52 STAFF-01, PM-03, RD-02 can modify the one source and have the change ripple to all three personas."
- Multiple component definitions (separate per-persona forks) → `FAIL`. Impact = "Phase 51 LEAN scope expands informally: one of the three personas' per-journey changes will need to collapse the fork before Phase 52 changes are safe. Record as a planning note; do not auto-add requirement."
- Component does not exist yet (neither grep finds a definition) → `FAIL`. Impact = "Phase 52 per-journey work must first author the shared primitive; planner consumes this note."

Append `## VERIFY-09: Plan-vs-actual cell + timeline-grid reuse` with:
- Requirement wording (from REQUIREMENTS.md VERIFY-09)
- Command 1, 2, 3 verbatim
- Raw output for each (fenced blocks)
- A `Usage matrix` table:

```markdown
| Consumer page | TimelineGrid import path | PlanVsActualCell import path | Identical? |
|---|---|---|---|
| src/app/(app)/pm/... | <path> | <path> | yes/no |
| src/app/(app)/staff/... | <path> | <path> | yes/no |
| src/app/(app)/rd/... | <path> | <path> | yes/no |
```

- Verdict + Impact per rules above.

Update summary.

**Final step — write a closing section.** After VERIFY-09, append:

```markdown
## Scope-Expansion Summary

| Source check | Downstream phase | Expansion text | Recorded in ROADMAP/REQUIREMENTS? |
|---|---|---|---|
<!-- For each VERIFY-0N with verdict = EXPANDS-SCOPE, add one row. Plan 02 checks this column. -->

## Reviewer-Agent Sign-Off

<!-- Reviewer agent (Plan 02, Task 1) writes here: pass/fail and cites evidence. -->
```

This block is intentionally empty for now — Plan 02 populates the scope-expansion rows and the sign-off note.
  </action>
  <verify>
    <automated>grep -q "^## VERIFY-09:" .planning/pre-flight-report.md && grep -q "^## Scope-Expansion Summary" .planning/pre-flight-report.md && grep -q "^## Reviewer-Agent Sign-Off" .planning/pre-flight-report.md && ! grep -q "\*TBD\*" .planning/pre-flight-report.md</automated>
  </verify>
  <acceptance_criteria>
    - Report contains `## VERIFY-09:` heading with a `Usage matrix` table listing PM, Staff, R&D rows
    - Report contains `## Scope-Expansion Summary` and `## Reviewer-Agent Sign-Off` section headings (may have empty body — Plan 02 fills)
    - Summary table has zero `*TBD*` cells remaining (all 9 rows populated)
    - Every `## VERIFY-0N:` section in the file contains a `**Verdict:**` line with exactly one of `PASS` / `FAIL` / `EXPANDS-SCOPE`
    - Nine `## VERIFY-` sections exist (grep `^## VERIFY-` returns 9 lines)
  </acceptance_criteria>
  <done>All 9 VERIFY-0N sections populated with raw command output and verdicts; scope-expansion + reviewer placeholders added for Plan 02 to fill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev shell → Neon dev branch | VERIFY-05 SQL crosses this boundary via `$DATABASE_URL`; read-only query but credentials in env |
| dev shell → local Next.js dev server | VERIFY-04 curl crosses this; server runs with Clerk session cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | I (Information disclosure) | `pre-flight-report.md` VERIFY-05 section | mitigate | Redact DATABASE_URL password to `***` when recording the command; per Task 4 acceptance criteria, grep verifies no `postgres://user:password@` pattern with real value landed in the file |
| T-48-02 | I (Information disclosure) | `pre-flight-report.md` VERIFY-05 raw output | accept | Query returns `organization_id, clerk_user_id, dashboard_id` — these are internal identifiers, not secrets; report lives in `.planning/` (git-tracked, team-visible) which is consistent with prior milestone artifacts |
| T-48-03 | I (Information disclosure) | `pre-flight-report.md` VERIFY-04 stack traces | accept | Stack traces may include local filesystem paths; acceptable since `.planning/` is team-internal and paths are already implicit in repo structure |
| T-48-04 | T (Tampering) | VERIFY-05 accidentally running the UPDATE migration | mitigate | Task 4 explicitly instructs SELECT only; D-06 forbids the migration; reviewer-agent (Plan 02) checks that no `UPDATE dashboard_layouts` command text appears in the VERIFY-05 section |
| T-48-05 | T (Tampering) | Live VERIFY-04 mutating state | accept | Endpoints are GET-equivalent (change-log read, people list read); observed in production to 500 before any state write |
| T-48-06 | D (Denial of service) | `pnpm dev` left running after Task 5 | mitigate | Task 5 explicitly requires stopping the dev server (Ctrl-C / kill PID) before task completion; no acceptance criterion blocks on this but leaving it is harmless in a dev context |
</threat_model>

<verification>
- All 6 tasks complete
- `.planning/pre-flight-report.md` has 9 `## VERIFY-0N:` sections + Scope-Expansion + Reviewer-Agent placeholders
- Every VERIFY-0N verdict is `PASS` / `FAIL` / `EXPANDS-SCOPE`
- Every command text in the report matches its UI-RESTRUCTURE-PLAN-v2.md §Wave −1 counterpart byte-for-byte (no paraphrasing)
- Git diff --stat limited to `.planning/pre-flight-report.md` (no source-code changes)
- No real database password appears in the report
</verification>

<success_criteria>
1. `.planning/pre-flight-report.md` exists and contains a complete summary table + 9 detail sections
2. Every verdict cell cites the verbatim command + raw captured output that justifies it (D-03 self-review checklist)
3. VERIFY-05 records zero-rows → PASS or ≥1-rows → EXPANDS-SCOPE with the row count (Plan 02 consumes this to decide Phase 51 scope)
4. VERIFY-03 records component-exists → PASS or component-missing → EXPANDS-SCOPE (Plan 02 consumes this to decide Phase 49 scope)
5. VERIFY-06 classifies every `e2e/**/*.spec.ts` as keep / update / retire with signal counts in an appendix table
6. No source-code change has landed (phase is verification-only per CONTEXT.md Out-of-scope)
</success_criteria>

<output>
After completion, create `.planning/phases/48-pre-flight-verification/48-01-SUMMARY.md` documenting:
- The 9 verdicts (copy summary table from report)
- Which checks triggered scope expansion (VERIFY-03 / VERIFY-05 if applicable) — lists them for Plan 02 to pick up
- Commit SHA captured in the report header
</output>
