---
phase: 10-platform-admin
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/(platform)/layout.tsx
  - src/app/(platform)/login/page.tsx
  - src/app/(platform)/page.tsx
  - src/app/(platform)/tenants/page.tsx
  - src/app/(platform)/tenants/[orgId]/page.tsx
  - src/app/(platform)/subscriptions/page.tsx
  - src/components/platform/platform-shell.tsx
  - src/components/platform/platform-sidebar.tsx
  - src/features/platform/platform-dashboard.service.ts
  - src/features/platform/platform-tenant.service.ts
  - src/features/platform/platform-tenant.schema.ts
  - src/app/api/platform/tenants/route.ts
  - src/app/api/platform/tenants/[orgId]/route.ts
  - src/app/api/platform/tenants/[orgId]/suspend/route.ts
  - src/app/api/platform/tenants/[orgId]/reactivate/route.ts
  - src/app/api/platform/subscriptions/[orgId]/route.ts
  - src/app/api/platform/dashboard/route.ts
autonomous: true
requirements:
  - PLAT-02
  - PLAT-06
  - PLAT-07

must_haves:
  truths:
    - "Platform admin sees a dashboard with total orgs, total users, recently active orgs, and status breakdown"
    - "Platform admin can view a list of all tenants with name, status, user count, created date"
    - "Platform admin can suspend, reactivate, and delete an organization"
    - "Platform admin can view and edit subscription details (extend trial, override status)"
    - "Platform admin navigates via a sidebar with Dashboard, Tenants, Subscriptions, Users, Audit links"
  artifacts:
    - path: "src/app/(platform)/layout.tsx"
      provides: "Platform route group layout"
    - path: "src/components/platform/platform-shell.tsx"
      provides: "Platform admin shell with sidebar"
    - path: "src/features/platform/platform-dashboard.service.ts"
      provides: "Dashboard metrics aggregation"
      exports: ["getDashboardMetrics"]
    - path: "src/features/platform/platform-tenant.service.ts"
      provides: "Tenant CRUD operations"
      exports: ["listTenants", "getTenantDetail", "suspendTenant", "reactivateTenant", "deleteTenant"]
    - path: "src/app/api/platform/tenants/route.ts"
      provides: "GET tenant list"
      exports: ["GET"]
  key_links:
    - from: "src/app/(platform)/page.tsx"
      to: "/api/platform/dashboard"
      via: "fetch dashboard metrics"
      pattern: "api/platform/dashboard"
    - from: "src/app/(platform)/tenants/page.tsx"
      to: "/api/platform/tenants"
      via: "fetch tenant list"
      pattern: "api/platform/tenants"
    - from: "src/app/api/platform/tenants/[orgId]/suspend/route.ts"
      to: "src/features/platform/platform-tenant.service.ts"
      via: "suspendTenant call"
      pattern: "suspendTenant"
---

<objective>
Platform admin shell, dashboard, tenant management, and subscription management. Complete admin UI with sidebar navigation, dashboard metrics, tenant list/detail with suspend/reactivate/delete, and subscription override controls.

Purpose: Gives platform admins visibility into all tenants and control over org lifecycle (per D-07 through D-11).
Output: Working platform admin dashboard and tenant management pages with API backends.
</objective>

<execution_context>
@.claude/agents/workflows/execute-plan.md
@.claude/agents/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-platform-admin/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/platform-auth.ts:
```typescript
export const PLATFORM_COOKIE = 'platform-token';
export interface PlatformAdmin { adminId: string; email: string; name: string; }
export async function signPlatformToken(payload: PlatformAdmin): Promise<string>;
export async function requirePlatformAdmin(): Promise<PlatformAdmin>;
```

From src/lib/platform-audit.ts:
```typescript
export interface AuditEntry {
  adminId: string;
  action: string;
  targetOrgId?: string;
  targetUserId?: string;
  impersonationSessionId?: string;
  details?: Record<string, unknown>;
}
export async function logPlatformAction(entry: AuditEntry): Promise<void>;
```

From src/db/schema.ts (organizations table):
```typescript
export const organizations = pgTable('organizations', {
  id: uuid('id').defaultRandom().primaryKey(),
  clerkOrgId: text('clerk_org_id').notNull().unique(),
  name: varchar('name', { length: 100 }).notNull(),
  slug: varchar('slug', { length: 50 }).notNull().unique(),
  subscriptionStatus: subscriptionStatusEnum('subscription_status').default('trial').notNull(),
  stripeCustomerId: text('stripe_customer_id').unique(),
  stripeSubscriptionId: text('stripe_subscription_id'),
  suspendedAt: timestamp('suspended_at', { withTimezone: true }),
  suspendedReason: varchar('suspended_reason', { length: 500 }),
  trialEndsAt: timestamp('trial_ends_at', { withTimezone: true }),
  creditBalanceCents: integer('credit_balance_cents').default(0).notNull(),
  platformNotes: varchar('platform_notes', { length: 2000 }),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
});
```

From src/lib/api-utils.ts:
```typescript
export function handleApiError(error: unknown): NextResponse;
```

From src/components/layout/ (existing app shell pattern):
```
app-shell.tsx, breadcrumbs.tsx, side-nav.tsx, top-nav.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Platform shell, dashboard service/API, tenant service/API with subscription management</name>
  <files>
    src/components/platform/platform-sidebar.tsx,
    src/components/platform/platform-shell.tsx,
    src/app/(platform)/layout.tsx,
    src/app/(platform)/login/page.tsx,
    src/features/platform/platform-dashboard.service.ts,
    src/features/platform/platform-tenant.service.ts,
    src/features/platform/platform-tenant.schema.ts,
    src/app/api/platform/dashboard/route.ts,
    src/app/api/platform/tenants/route.ts,
    src/app/api/platform/tenants/[orgId]/route.ts,
    src/app/api/platform/tenants/[orgId]/suspend/route.ts,
    src/app/api/platform/tenants/[orgId]/reactivate/route.ts,
    src/app/api/platform/subscriptions/[orgId]/route.ts
  </files>
  <read_first>
    src/lib/platform-auth.ts,
    src/lib/platform-audit.ts,
    src/lib/api-utils.ts,
    src/lib/errors.ts,
    src/db/schema.ts (lines 50-69 for organizations table),
    src/components/layout/app-shell.tsx,
    src/components/layout/side-nav.tsx,
    src/app/(app)/layout.tsx
  </read_first>
  <action>
    1. Create `src/components/platform/platform-sidebar.tsx` (per D-08):
       - 'use client' component.
       - Navigation links: Dashboard (`/platform`), Tenants (`/platform/tenants`), Subscriptions (`/platform/subscriptions`), Users (`/platform/users`), Audit Log (`/platform/audit`).
       - Use `usePathname()` from `next/navigation` to highlight active link.
       - Icons from lucide-react: LayoutDashboard, Building2, CreditCard, Users, FileText.
       - Styling: fixed left sidebar, 240px wide, dark bg (slate-900), white text, active item highlighted with bg-slate-700.
       - Include admin name/email at bottom, logout button that POSTs to `/api/platform/auth/logout` then redirects to `/platform/login`.

    2. Create `src/components/platform/platform-shell.tsx`:
       - 'use client' component wrapping children with platform sidebar.
       - Layout: sidebar on left, main content area with `ml-60 p-6` on the right.
       - Fetches admin identity from `/api/platform/auth/me` on mount — if 401, redirect to `/platform/login`.

    3. Create `src/app/(platform)/layout.tsx`:
       - Server component. Renders `<PlatformShell>{children}</PlatformShell>`.
       - This is the platform route group layout — all platform pages get the sidebar.
       - Do NOT wrap with ClerkProvider (already in root layout, harmless but platform does not use it).

    4. Create `src/app/(platform)/login/page.tsx` (per D-05):
       - 'use client' component with simple email/password form.
       - On submit, POST to `/api/platform/auth/login` with JSON body.
       - On success, redirect to `/platform` using `router.push('/platform')`.
       - On error, show error message below form.
       - Minimal styling: centered card, max-w-sm, "Platform Admin" title.
       - This page should NOT use the platform shell layout — add a separate layout or check. Actually, the login page IS inside (platform) group so it gets the shell. To avoid this: create `src/app/(platform)/login/layout.tsx` that renders children WITHOUT the shell (just a centered container).

    5. Create `src/features/platform/platform-dashboard.service.ts` (per D-07):
       - Export `getDashboardMetrics()`: queries organizations table for total count, counts by subscriptionStatus (groupBy), recently active orgs (updatedAt >= 7 days ago, limit 10).
       - For total users: count from `people` table (not Clerk users — that would require API calls per org). The "users" metric represents people records across all orgs.
       - Returns `{ totalOrgs, totalUsers, orgsByStatus: Record<string, number>, recentlyActive: Array<{id, name, slug, subscriptionStatus, updatedAt}> }`.

    6. Create `src/features/platform/platform-tenant.service.ts` (per D-09, D-10, D-06):
       - Export `listTenants()`: select all from organizations, order by createdAt desc. For each, count people in that org (subquery or join). Return array of `{ id, clerkOrgId, name, slug, subscriptionStatus, userCount, createdAt }`.
       - Export `getTenantDetail(orgId: string)`: select org by id, count people. Return org details with userCount.
       - Export `suspendTenant(orgId: string, reason: string)`: update organizations set subscriptionStatus='suspended', suspendedAt=new Date(), suspendedReason=reason. Throws NotFoundError if org not found.
       - Export `reactivateTenant(orgId: string)`: update organizations set subscriptionStatus='active', suspendedAt=null, suspendedReason=null. Throws NotFoundError if not found.
       - Export `deleteTenant(orgId: string)`: delete from organizations where id=orgId. This cascades due to FK constraints. Throws NotFoundError if not found.
       - Export `updateSubscription(orgId: string, data: { subscriptionStatus?: string, trialEndsAt?: string, platformNotes?: string })`: update organizations with provided fields. For trialEndsAt, parse ISO string to Date.

    7. Create `src/features/platform/platform-tenant.schema.ts`:
       - Export `suspendSchema = z.object({ reason: z.string().min(1).max(500) })`.
       - Export `subscriptionUpdateSchema = z.object({ subscriptionStatus: z.enum(['trial','active','past_due','cancelled','suspended']).optional(), trialEndsAt: z.string().datetime().optional(), platformNotes: z.string().max(2000).optional() })`.

    8. Create `src/app/api/platform/dashboard/route.ts`:
       - GET: `requirePlatformAdmin()`, call `getDashboardMetrics()`, return JSON.

    9. Create `src/app/api/platform/tenants/route.ts`:
       - GET: `requirePlatformAdmin()`, call `listTenants()`, return JSON.

    10. Create `src/app/api/platform/tenants/[orgId]/route.ts`:
        - GET: `requirePlatformAdmin()`, call `getTenantDetail(orgId)`, return JSON.
        - DELETE: `requirePlatformAdmin()`, call `deleteTenant(orgId)`, call `logPlatformAction({ adminId, action: 'tenant.delete', targetOrgId: orgId })`, return 200.

    11. Create `src/app/api/platform/tenants/[orgId]/suspend/route.ts`:
        - POST: `requirePlatformAdmin()`, parse body with `suspendSchema`, call `suspendTenant(orgId, reason)`, call `logPlatformAction({ adminId, action: 'tenant.suspend', targetOrgId: orgId, details: { reason } })`, return 200.

    12. Create `src/app/api/platform/tenants/[orgId]/reactivate/route.ts`:
        - POST: `requirePlatformAdmin()`, call `reactivateTenant(orgId)`, call `logPlatformAction({ adminId, action: 'tenant.reactivate', targetOrgId: orgId })`, return 200.

    13. Create `src/app/api/platform/subscriptions/[orgId]/route.ts` (per D-11, PLAT-07):
        - PATCH: `requirePlatformAdmin()`, parse body with `subscriptionUpdateSchema`, call `updateSubscription(orgId, data)`, call `logPlatformAction({ adminId, action: 'subscription.update', targetOrgId: orgId, details: data })`, return 200.
  </action>
  <verify>
    <automated>cd "D:\Kod Projekt\Resurs & Projektplanering" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LayoutDashboard\|Building2\|CreditCard\|Users\|FileText" src/components/platform/platform-sidebar.tsx` returns 5
    - `grep "requirePlatformAdmin" src/app/api/platform/dashboard/route.ts` confirms auth check
    - `grep "logPlatformAction" src/app/api/platform/tenants/[orgId]/suspend/route.ts` confirms audit logging
    - `grep -c "listTenants\|getTenantDetail\|suspendTenant\|reactivateTenant\|deleteTenant\|updateSubscription" src/features/platform/platform-tenant.service.ts` returns 6+
    - `grep "getDashboardMetrics" src/features/platform/platform-dashboard.service.ts` confirms dashboard service
    - `ls src/app/\(platform\)/layout.tsx` confirms platform route group exists
    - `ls src/app/\(platform\)/login/page.tsx` confirms login page exists
  </acceptance_criteria>
  <done>
    Platform shell with sidebar navigation renders at /platform/*. Dashboard API returns org metrics. Tenant list/detail/suspend/reactivate/delete APIs work with audit logging. Subscription update API works. Login page at /platform/login with email/password form. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard, tenant list, tenant detail, and subscription management pages</name>
  <files>
    src/app/(platform)/page.tsx,
    src/app/(platform)/tenants/page.tsx,
    src/app/(platform)/tenants/[orgId]/page.tsx,
    src/app/(platform)/subscriptions/page.tsx
  </files>
  <read_first>
    src/components/platform/platform-shell.tsx,
    src/components/platform/platform-sidebar.tsx,
    src/features/platform/platform-dashboard.service.ts,
    src/features/platform/platform-tenant.service.ts,
    src/app/api/platform/dashboard/route.ts,
    src/app/api/platform/tenants/route.ts,
    src/app/api/platform/subscriptions/[orgId]/route.ts
  </read_first>
  <action>
    1. Create `src/app/(platform)/page.tsx` (per D-07 — dashboard):
       - 'use client' component. Fetches from `/api/platform/dashboard` on mount.
       - Renders 4 metric cards in a 2x2 grid (lg:grid-cols-4): Total Organizations, Total Users, Active Orgs, Trial Orgs.
       - Below cards: "Recently Active Organizations" section — a simple table with org name, status, last updated.
       - Status badges: trial=blue, active=green, suspended=red, past_due=amber, cancelled=gray.
       - Loading state: skeleton cards while fetching.

    2. Create `src/app/(platform)/tenants/page.tsx` (per D-09):
       - 'use client' component. Fetches from `/api/platform/tenants`.
       - Renders a table with columns: Name, Status (badge), User Count, Created Date, Actions.
       - Actions column: "View" link to `/platform/tenants/[orgId]`.
       - Status badges same color scheme as dashboard.

    3. Create `src/app/(platform)/tenants/[orgId]/page.tsx` (per D-10):
       - 'use client' component. Fetches from `/api/platform/tenants/[orgId]`.
       - Shows org info card: name, slug, status, created date, user count, platform notes.
       - Action buttons:
         - "Suspend" (if active/trial) — opens confirm dialog with reason input, POSTs to `/api/platform/tenants/[orgId]/suspend`.
         - "Reactivate" (if suspended) — confirm dialog, POSTs to `/api/platform/tenants/[orgId]/reactivate`.
         - "Delete" (always, red button) — confirm dialog with org name typed to confirm, DELETEs `/api/platform/tenants/[orgId]`.
         - "Impersonate" button — placeholder for now (text "Impersonate" disabled with tooltip "Coming in next plan").
       - Below: user list placeholder — "Users in this organization" with note "User list available in Users page".

    4. Create `src/app/(platform)/subscriptions/page.tsx` (per D-11):
       - 'use client' component. Fetches from `/api/platform/tenants` (reuses tenant list data).
       - Table with columns: Organization, Status, Trial Ends, Notes, Actions.
       - Inline edit for each row: click "Edit" to show form fields for status dropdown, trial end date picker, notes textarea.
       - Save button PATCHes to `/api/platform/subscriptions/[orgId]`.
       - Cancel button reverts to display mode.
  </action>
  <verify>
    <automated>cd "D:\Kod Projekt\Resurs & Projektplanering" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "api/platform/dashboard" src/app/\(platform\)/page.tsx` confirms dashboard data fetch
    - `grep "api/platform/tenants" src/app/\(platform\)/tenants/page.tsx` confirms tenant list fetch
    - `grep "suspend\|reactivate\|Delete" src/app/\(platform\)/tenants/\[orgId\]/page.tsx` confirms action buttons
    - `grep "api/platform/subscriptions" src/app/\(platform\)/subscriptions/page.tsx` confirms subscription management
    - All 4 page files exist under src/app/(platform)/
  </acceptance_criteria>
  <done>
    Platform dashboard shows org metrics in cards with recently active list. Tenants page shows all orgs in a table. Tenant detail page shows org info with suspend/reactivate/delete actions. Subscriptions page allows inline edit of status, trial end date, and notes. All pages compile cleanly.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles with zero errors: `npx tsc --noEmit`
2. Platform shell renders sidebar with all 5 nav links
3. Dashboard API returns metrics, page renders cards
4. Tenant CRUD APIs all require platform auth and log actions
5. Subscription update API accepts status, trialEndsAt, platformNotes
</verification>

<success_criteria>
- Platform admin shell with sidebar at /platform/* with 5 navigation links (per D-08)
- Dashboard page shows total orgs, total users, recently active, status breakdown (per D-07)
- Tenants list page with name, status, user count, created date (per D-09)
- Tenant detail page with suspend/reactivate/delete actions (per D-10)
- Subscriptions page with inline edit for status, trial end, notes (per D-11)
- All mutations audit-logged via logPlatformAction
- Login page at /platform/login with email/password form (per D-05)
</success_criteria>

<output>
After completion, create `.planning/phases/10-platform-admin/10-02-SUMMARY.md`
</output>
