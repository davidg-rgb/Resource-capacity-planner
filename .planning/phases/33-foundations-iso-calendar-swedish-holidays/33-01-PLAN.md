---
phase: 33-foundations-iso-calendar-swedish-holidays
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/time/iso-calendar.ts
  - src/lib/time/swedish-holidays.ts
  - src/lib/time/formatters.ts
  - src/lib/time/index.ts
  - src/lib/time/__tests__/iso-calendar.test.ts
  - src/lib/time/__tests__/swedish-holidays.test.ts
  - src/lib/time/__tests__/formatters.test.ts
  - src/lib/errors.ts
  - eslint.config.mjs
  - vitest.config.ts
autonomous: true
requirements: [FOUND-V5-01, FOUND-V5-02]
must_haves:
  truths:
    - "Any caller in src/ can ask 'what ISO week is this date?' and get a correct answer for 2026 (53-week year)"
    - "Any caller can ask 'is this date a Swedish holiday?' for years 2026–2030 and get the correct answer"
    - "Any caller can ask 'how many working days between A and B?' excluding Mon–Fri holidays"
    - "Any caller can format a date as 'v.14' or 'vecka 14, 2026' via the central helper"
    - "CI fails if a non-lib/time/ file imports date-fns week helpers or calls .getDay() for day-of-week"
    - "Calling getSwedishHolidays(2031) throws ValidationError with code ERR_HOLIDAY_YEAR_OUT_OF_RANGE"
  artifacts:
    - path: "src/lib/time/iso-calendar.ts"
      provides: "ISO 8601 week math (getISOWeek, getISOWeekYear, getISOWeeksInYear, isISO53WeekYear, workingDaysInRange, countWorkingDays)"
    - path: "src/lib/time/swedish-holidays.ts"
      provides: "Hardcoded 2026–2030 holiday table + isSwedishHoliday + getSwedishHolidays"
    - path: "src/lib/time/formatters.ts"
      provides: "formatWeekShort / formatWeekLong / formatWeekRange Swedish display helpers"
    - path: "src/lib/time/index.ts"
      provides: "Barrel re-export — the only public entry point"
    - path: "src/lib/time/__tests__/iso-calendar.test.ts"
      provides: "TC-CAL-001, TC-CAL-002, TC-CAL-005 coverage"
    - path: "src/lib/time/__tests__/swedish-holidays.test.ts"
      provides: "TC-CAL-003, TC-CAL-004, TC-CAL-006 coverage"
    - path: "src/lib/time/__tests__/formatters.test.ts"
      provides: "TC-CAL-007 coverage"
    - path: "eslint.config.mjs"
      provides: "no-restricted-syntax / no-restricted-imports rule blocking direct date-fns week helpers and .getDay() outside lib/time/ (TC-CAL-008)"
    - path: "vitest.config.ts"
      provides: "Vitest test runner config (no test runner exists today — must add)"
  key_links:
    - from: "src/lib/time/iso-calendar.ts"
      to: "src/lib/time/swedish-holidays.ts"
      via: "workingDaysInRange imports isSwedishHoliday"
      pattern: "isSwedishHoliday"
    - from: "src/lib/time/swedish-holidays.ts"
      to: "src/lib/errors.ts"
      via: "throws ValidationError on year out of range"
      pattern: "ValidationError.*ERR_HOLIDAY_YEAR_OUT_OF_RANGE"
    - from: "eslint.config.mjs"
      to: "src/lib/time/"
      via: "no-restricted-imports override allowing date-fns inside lib/time/ only"
      pattern: "lib/time"
---

<objective>
Deliver the foundational `lib/time/` module that becomes the sole source of truth for ISO 8601 week math, Swedish holiday detection, working-day counting, and Swedish week display formatting across the v5.0 build. Satisfies FOUND-V5-01 (ISO calendar) and FOUND-V5-02 (Swedish holidays + working-day semantics).

Purpose: Every downstream v5.0 phase (actuals distribution, Excel import, timeline rendering, historic-edit guardrail, proposal period math) calls into this module. Getting it wrong here cascades into every later phase. Getting it right here means everything downstream can trust dates blindly.

Output: A `src/lib/time/` directory with three source files, three test files, a barrel index, a Vitest config, an eslint enforcement rule, one new error code, and a hardcoded Swedish holiday table for 2026–2030.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-foundations-iso-calendar-swedish-holidays/33-CONTEXT.md
@.planning/v5.0-ARCHITECTURE.md
@.planning/REQUIREMENTS.md
@src/lib/errors.ts
@src/lib/date-utils.ts
@eslint.config.mjs
@package.json

<interfaces>
<!-- Existing AppError taxonomy from src/lib/errors.ts (already verified) -->
```ts
// src/lib/errors.ts already exports:
export class AppError extends Error { code: string; status: number; ... }
export class ValidationError extends AppError { /* status 400 */ }
// + AuthError, ForbiddenError, NotFoundError, ConflictError, PayloadTooLargeError, InternalError
```

REUSE existing `ValidationError` — do NOT create a new subclass. Just throw with `code: "ERR_HOLIDAY_YEAR_OUT_OF_RANGE"`.

<!-- Public API target (from 33-CONTEXT.md §specifics) -->
```ts
// src/lib/time/index.ts (barrel)
export {
  getISOWeek, getISOWeekYear, getISOWeeksInYear, isISO53WeekYear,
  workingDaysInRange, countWorkingDays,
} from "./iso-calendar";
export {
  isSwedishHoliday, getSwedishHolidays,
} from "./swedish-holidays";
export {
  formatWeekShort, formatWeekLong, formatWeekRange,
} from "./formatters";
```
</interfaces>

<environment_facts>
- **Package manager:** pnpm 10.33.0
- **`date-fns` is NOT installed.** Decision: implement ISO week math directly using the standard algorithm rather than adding a dependency for ~30 lines of math. Avoids supply chain bloat. (See risk: validate algorithm against TC-CAL-001 / TC-CAL-002.)
- **No test runner installed.** Vitest must be added as a devDependency in this phase. Pick `vitest@^2` + `@vitest/ui` (optional) — no jsdom needed (pure functions).
- **Eslint config:** flat config in `eslint.config.mjs` (eslint 9 + eslint-config-next 16). Use `no-restricted-imports` + `no-restricted-syntax` with file-pattern overrides.
- **Existing date helpers:** `src/lib/date-utils.ts` exists (65 lines). Read it in Task 1; if it contains anything overlapping (week math, formatting), either delete those exports and migrate callers, or leave it alone if it's purely about ISO date strings / month math. Do NOT duplicate.
- **Project convention for tests:** None established yet — this is the first test file in the project. Establish convention here: `__tests__/` colocated next to source.
- **Timezone:** Swedish business operates in CET/CEST. This module operates on **calendar dates**, not instants. All inputs treated as local dates. When constructing Date objects internally for week math, use UTC midnight (`new Date(Date.UTC(y, m, d))`) to avoid DST drift, then operate on UTC fields. Document this in a JSDoc block at top of `iso-calendar.ts`.
</environment_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing src/lib/ and confirm no overlap; add Vitest</name>
  <files>src/lib/date-utils.ts (read-only audit), package.json, vitest.config.ts</files>
  <action>
1. Read `src/lib/date-utils.ts` end-to-end. List its exports. If it contains anything that overlaps the new `lib/time/` API (week math, holiday checks, Swedish week formatting), note it in your task notes — DO NOT delete it in this task; surface for Task 6 cleanup. If it's purely ISO-string parsing / month-bucket math, leave it alone — `lib/time/` is additive.
2. Verify `date-fns` is absent from `package.json` (confirmed in plan: it is). Decision is locked: implement ISO week math directly, no new runtime dep.
3. Add Vitest to devDependencies: `pnpm add -D vitest@^2`. Do NOT add `@vitest/ui`, `jsdom`, or `@testing-library/*` — this module is pure functions, no DOM.
4. Create `vitest.config.ts` at repo root with minimal config:
   ```ts
   import { defineConfig } from "vitest/config";
   export default defineConfig({
     test: {
       include: ["src/**/__tests__/**/*.test.ts"],
       environment: "node",
     },
   });
   ```
5. Add `"test": "vitest run"` and `"test:watch": "vitest"` to `package.json` scripts.
6. Run `pnpm install` then `pnpm test` — should report "no test files found" cleanly (not error).
  </action>
  <verify>
    <automated>pnpm test 2>&1 | grep -E "No test files found|passed" &amp;&amp; node -e "const p=require('./package.json'); if(!p.devDependencies.vitest) process.exit(1); if(!p.scripts.test) process.exit(1)"</automated>
  </verify>
  <done>Vitest installed, vitest.config.ts present, `pnpm test` runs and exits 0 with no test files. Audit notes captured for date-utils.ts overlap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement iso-calendar.ts + swedish-holidays.ts + formatters.ts (TDD)</name>
  <files>
    src/lib/time/iso-calendar.ts,
    src/lib/time/swedish-holidays.ts,
    src/lib/time/formatters.ts,
    src/lib/time/index.ts,
    src/lib/time/__tests__/iso-calendar.test.ts,
    src/lib/time/__tests__/swedish-holidays.test.ts,
    src/lib/time/__tests__/formatters.test.ts,
    src/lib/errors.ts
  </files>
  <behavior>
    Tests MUST be written FIRST (RED), then implementation (GREEN). Each TC-CAL-* assertion gets one named `it()` block. Test names MUST contain the TC-ID for traceability.

    **iso-calendar.test.ts:**
    - `it("TC-CAL-001: 2026 is a 53-week ISO year")` → `expect(getISOWeeksInYear(2026)).toBe(53)` AND `expect(isISO53WeekYear(2026)).toBe(true)`. Also assert 2025=52, 2027=52, 2032=53, 2037=53.
    - `it("TC-CAL-002: Week 1 of 2027 starts Mon Jan 4 (because 2026-12-31 falls in W53)")` → `expect(getISOWeek(new Date(Date.UTC(2026,11,31)))).toBe(53)` AND `expect(getISOWeekYear(new Date(Date.UTC(2026,11,31)))).toBe(2026)` AND `expect(getISOWeek(new Date(Date.UTC(2027,0,4)))).toBe(1)` AND `expect(getISOWeekYear(new Date(Date.UTC(2027,0,4)))).toBe(2027)`. Also: Jan 1–3 2027 should be W53/2026.
    - `it("TC-CAL-005: workingDaysInRange April 2026 excludes Good Fri + Easter Mon")` → `countWorkingDays(2026-04-01, 2026-04-30)` equals (22 weekdays in April 2026) − 2 (Good Fri Apr 3 + Easter Mon Apr 6) = 20. May Day Apr 30? May 1 is May, outside range — boundary test confirms it's NOT subtracted. Also assert `workingDaysInRange` returns the full Date[] and length matches `countWorkingDays`.
    - Edge: `countWorkingDays(sameDay, sameDay)` for a Tuesday non-holiday = 1; for a Saturday = 0; for Good Fri = 0.

    **swedish-holidays.test.ts:**
    - `it("TC-CAL-003: Midsummer Eve 2026-06-19 is a Swedish holiday")` → `expect(isSwedishHoliday(new Date(Date.UTC(2026,5,19)))).toBe(true)`.
    - `it("TC-CAL-004: Jan 1 2026 (Thu) is a holiday and is in ISO week 1")` → both `isSwedishHoliday` true AND `getISOWeek` returns 1.
    - `it("TC-CAL-006: getSwedishHolidays(2031) throws ERR_HOLIDAY_YEAR_OUT_OF_RANGE")` → `expect(() => getSwedishHolidays(2031)).toThrow(ValidationError)` and assert `.code === "ERR_HOLIDAY_YEAR_OUT_OF_RANGE"`. Same for 2025 (below range).
    - Coverage: assert all 13 Swedish holidays for 2026 are in `getSwedishHolidays(2026)` (5 Easter-driven + 8 fixed-date). Repeat spot-check for 2027–2030 (one Easter date per year is enough).
    - Negative: `isSwedishHoliday(new Date(Date.UTC(2026,3,2)))` (Apr 2 = Thu, not a holiday) returns false.

    **formatters.test.ts:**
    - `it("TC-CAL-007: formatWeekShort 2026-06-15 returns 'v.25'")` → exact string match.
    - `it("formatWeekLong 2026-06-15 returns 'vecka 25, 2026'")`.
    - `it("formatWeekLong handles 53-week year boundary: 2026-12-31 returns 'vecka 53, 2026'")`.
    - `it("formatWeekLong handles year boundary: 2027-01-01 returns 'vecka 53, 2026'")` (uses ISO week year, not calendar year — critical).
    - `it("formatWeekRange 2026-04-06 to 2026-04-12 returns 'v.15'")` (single week) and cross-week range returns `"v.15–v.16"` (en-dash, planner's choice — document in JSDoc).
  </behavior>
  <action>
**RED phase — write failing tests first:**

1. Create the three test files under `src/lib/time/__tests__/` with the assertions enumerated above. Import from `../iso-calendar`, `../swedish-holidays`, `../formatters` (these don't exist yet — tests will fail to import).
2. Run `pnpm test` — confirm RED (import errors are acceptable; the point is tests exist and fail).

**GREEN phase — implement:**

3. **`src/lib/errors.ts`**: Add a constant export `export const ERR_HOLIDAY_YEAR_OUT_OF_RANGE = "ERR_HOLIDAY_YEAR_OUT_OF_RANGE";` near the existing exports. Do NOT subclass `ValidationError` — reuse it directly with this code string.

4. **`src/lib/time/iso-calendar.ts`**:
   - Top-of-file JSDoc block explaining: "All inputs are treated as calendar dates. Internally we use UTC fields (`getUTCFullYear`, `getUTCMonth`, `getUTCDate`, `getUTCDay`) to avoid DST drift. Callers passing local-time Date objects on a DST boundary day may see edge cases — document expected usage as 'pass UTC midnight or a date constructed from y/m/d numbers'."
   - Implement `getISOWeek(date)` using the standard algorithm:
     ```
     const target = new Date(Date.UTC(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate()));
     const dayNum = target.getUTCDay() || 7;            // Sun=0 → 7
     target.setUTCDate(target.getUTCDate() + 4 - dayNum); // Thu of this ISO week
     const yearStart = new Date(Date.UTC(target.getUTCFullYear(), 0, 1));
     return Math.ceil((((target.getTime() - yearStart.getTime()) / 86400000) + 1) / 7);
     ```
   - Implement `getISOWeekYear(date)` analogously (the year of the Thursday of this ISO week).
   - Implement `getISOWeeksInYear(year)`: A year has 53 ISO weeks iff Jan 1 is a Thursday OR (it's a leap year AND Jan 1 is a Wednesday). Return `52 | 53`.
   - Implement `isISO53WeekYear(year)`: thin wrapper.
   - Implement `workingDaysInRange(start, end)`: iterate day-by-day from start to end inclusive (UTC), skip Saturdays/Sundays, skip `isSwedishHoliday`. Return `Date[]`.
   - Implement `countWorkingDays(start, end)`: `workingDaysInRange(start, end).length`.
   - Import `isSwedishHoliday` from `./swedish-holidays`.
   - Add a JSDoc note: "Leap year (Gregorian, 366 days) and 53-week ISO year are DISTINCT concepts. 2024 is a leap year but a 52-week ISO year. 2026 is NOT a leap year but IS a 53-week ISO year. Do not conflate."

5. **`src/lib/time/swedish-holidays.ts`**:
   - Hardcoded table object literal `EASTER_TABLE: Record<number, { easter: [m,d], goodFri: [m,d], easterMon: [m,d], ascension: [m,d], midsummerEve: [m,d] }>` for 2026–2030 from the table in 33-CONTEXT.md §decisions.
   - Fixed-date holidays array: New Year (1/1), Epiphany (1/6), May Day (5/1), National Day (6/6), Christmas Eve (12/24), Christmas Day (12/25), Boxing Day (12/26), New Year's Eve (12/31). Each with Swedish name (`Nyårsdagen`, `Trettondedag jul`, `Första maj`, `Sveriges nationaldag`, `Julafton`, `Juldagen`, `Annandag jul`, `Nyårsafton`).
   - `getSwedishHolidays(year)`: if `year < 2026 || year > 2030` throw `new ValidationError("Holiday data unavailable for year " + year, ERR_HOLIDAY_YEAR_OUT_OF_RANGE)`. Otherwise build and return the full array of `{ date, name }` for that year. Sort by date.
   - `isSwedishHoliday(date)`: extract UTC year/month/day; if year is in range, look up; if not, return false (do NOT throw — `isSwedishHoliday` is a query, callers shouldn't have to wrap in try/catch). Document this asymmetry in JSDoc.
   - Easter-driven names in Swedish: `Påskdagen`, `Långfredagen`, `Annandag påsk`, `Kristi himmelsfärdsdag`, `Midsommarafton`.

6. **`src/lib/time/formatters.ts`**:
   - `formatWeekShort(date)` → `\`v.${getISOWeek(date)}\``
   - `formatWeekLong(date)` → `\`vecka ${getISOWeek(date)}, ${getISOWeekYear(date)}\`` (note: ISO week year, not calendar year — TC-CAL test for 2027-01-01 → "vecka 53, 2026" depends on this).
   - `formatWeekRange(start, end)`: if same ISO week+year, return `formatWeekShort(start)`; else return `\`${formatWeekShort(start)}–${formatWeekShort(end)}\`` (en-dash U+2013).

7. **`src/lib/time/index.ts`**: Barrel re-exports everything listed in the `<interfaces>` block above.

8. Run `pnpm test` — all tests must pass (GREEN). Run `pnpm typecheck` — must pass.
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tee /tmp/test.out &amp;&amp; pnpm typecheck &amp;&amp; grep -E "TC-CAL-001|TC-CAL-002|TC-CAL-003|TC-CAL-004|TC-CAL-005|TC-CAL-006|TC-CAL-007" /tmp/test.out</automated>
  </verify>
  <done>All 7 named TC-CAL-* tests pass (TC-CAL-008 covered in Task 3). `pnpm typecheck` clean. Module exports stable per `<interfaces>` block. No new runtime dependencies added (vitest is dev-only).</done>
</task>

<task type="auto">
  <name>Task 3: Add eslint enforcement rule + verify TC-CAL-008</name>
  <files>eslint.config.mjs, src/lib/time/__tests__/lint-rule.test.md (notes only)</files>
  <action>
1. Read current `eslint.config.mjs`. Identify where rule overrides go in the flat config array.
2. Add a new config block targeting all of `src/**/*.{ts,tsx}` EXCEPT `src/lib/time/**`:
   ```js
   {
     files: ["src/**/*.{ts,tsx}"],
     ignores: ["src/lib/time/**"],
     rules: {
       "no-restricted-imports": ["error", {
         paths: [
           { name: "date-fns", message: "Import week/date helpers from '@/lib/time' instead. date-fns week APIs default to US conventions; lib/time enforces ISO 8601." },
           { name: "date-fns/getWeek", message: "Use getISOWeek from '@/lib/time'." },
           { name: "date-fns/getISOWeek", message: "Use getISOWeek from '@/lib/time'." },
         ],
       }],
       "no-restricted-syntax": ["error", {
         selector: "CallExpression[callee.property.name='getDay']",
         message: "Do not use Date#getDay() for day-of-week decisions outside lib/time/. Use helpers from '@/lib/time'. (Sunday=0 trap; ISO weeks start Monday.)",
       }],
     },
   },
   ```
   Note: `date-fns` isn't installed, but the rule is forward-defensive for the day someone tries.
3. **TC-CAL-008 verification (manual but scriptable):** Create a throwaway file `src/lib/__lint-probe.ts` containing a single line: `const d = new Date(); const x = d.getDay();` Run `pnpm lint`. It MUST report the `no-restricted-syntax` error on that line. Then DELETE the file. Run `pnpm lint` again — must pass. Document in commit message.
4. Confirm the rule does NOT fire inside `src/lib/time/` by running `pnpm lint` against the new module — must pass clean.
5. Run full `pnpm lint` — entire repo must pass. If existing files happen to use `.getDay()`, surface them as findings: either fix-forward by routing through `lib/time` or add a narrow per-file `eslint-disable-next-line` with a TODO comment referencing FOUND-V5-01. Do NOT broaden the rule's `ignores` to silence them.
  </action>
  <verify>
    <automated>echo 'const d = new Date(); const x = d.getDay(); export {};' > src/lib/__lint-probe.ts &amp;&amp; (pnpm lint 2>&amp;1 | grep -q "no-restricted-syntax" &amp;&amp; echo TC-CAL-008-PASS || (echo TC-CAL-008-FAIL &amp;&amp; rm src/lib/__lint-probe.ts &amp;&amp; exit 1)) &amp;&amp; rm src/lib/__lint-probe.ts &amp;&amp; pnpm lint</automated>
  </verify>
  <done>
- Eslint rule present in `eslint.config.mjs` with both `no-restricted-imports` (date-fns) and `no-restricted-syntax` (`.getDay()`).
- Probe file proves rule fires (TC-CAL-008 satisfied empirically).
- `pnpm lint` clean across full repo after probe removed.
- Any pre-existing `.getDay()` callers either fixed or explicitly annotated with TODO + FOUND-V5-01.
- Grep confirms zero non-lib/time files import `date-fns` week helpers: `grep -r "date-fns" src/ --exclude-dir=lib/time` returns empty.
  </done>
</task>

</tasks>

<verification>
**Full phase verification (run after all 3 tasks):**

1. `pnpm test` — all TC-CAL-001..007 named tests pass.
2. `pnpm typecheck` — clean.
3. `pnpm lint` — clean.
4. `pnpm build` — clean (smoke test that the new module compiles in the Next.js build pipeline).
5. Grep for forbidden patterns:
   - `grep -rE "from ['\"]date-fns" src/ --exclude-dir=time` → empty
   - `grep -rE "\.getDay\(\)" src/ --exclude-dir=time` → empty (or only TODO-annotated lines)
6. Spot-check exports: `node -e "import('./src/lib/time/index.ts').then(m => console.log(Object.keys(m).sort()))"` (or via a tiny tsx script) — must list all 11 expected exports.
7. **TC-CAL-* coverage table verified:** see table below.
</verification>

<tc_cal_coverage>
## TC-CAL-* → Test Coverage Map

| ID | Assertion | Test File | Test Name |
|----|-----------|-----------|-----------|
| TC-CAL-001 | 2026 has 53 ISO weeks | `iso-calendar.test.ts` | `TC-CAL-001: 2026 is a 53-week ISO year` |
| TC-CAL-002 | Week 1 of 2027 starts Mon Jan 4 (2026-12-31 in W53) | `iso-calendar.test.ts` | `TC-CAL-002: Week 1 of 2027 starts Mon Jan 4` |
| TC-CAL-003 | Midsummer Eve 2026-06-19 is a holiday | `swedish-holidays.test.ts` | `TC-CAL-003: Midsummer Eve 2026-06-19 is a Swedish holiday` |
| TC-CAL-004 | Jan 1 2026 (Thu) is a holiday AND in ISO week 1 | `swedish-holidays.test.ts` | `TC-CAL-004: Jan 1 2026 is a holiday and in ISO week 1` |
| TC-CAL-005 | `workingDaysInRange(2026-04-01..30)` excludes Good Fri + Easter Mon, May Day boundary | `iso-calendar.test.ts` | `TC-CAL-005: workingDaysInRange April 2026 excludes Good Fri and Easter Mon` |
| TC-CAL-006 | `getSwedishHolidays(2031)` throws `ERR_HOLIDAY_YEAR_OUT_OF_RANGE` | `swedish-holidays.test.ts` | `TC-CAL-006: getSwedishHolidays(2031) throws ERR_HOLIDAY_YEAR_OUT_OF_RANGE` |
| TC-CAL-007 | `formatWeekShort(2026-06-15)` → `"v.25"` | `formatters.test.ts` | `TC-CAL-007: formatWeekShort 2026-06-15 returns v.25` |
| TC-CAL-008 | Lint rule flags `.getDay()` outside `lib/time/` | (lint probe in Task 3) | Probe-file empirical verification + automated verify command |

**If ARCHITECTURE.md §15 contains TC-CAL-* IDs beyond 008 not enumerated in 33-CONTEXT.md:** The executor MUST read §15 in full at the start of Task 2 and add a test for every additional TC-CAL-* ID found. This table is the floor, not the ceiling.
</tc_cal_coverage>

<success_criteria>
- [x] FOUND-V5-01 (ISO 8601 calendar with 53-week support) → Tasks 2.4, verified by TC-CAL-001/002/005
- [x] FOUND-V5-02 (Swedish holidays + working-day semantics) → Tasks 2.5, verified by TC-CAL-003/004/006
- [x] All TC-CAL-001..008 tests pass (or are empirically verified for TC-CAL-008)
- [x] `pnpm test` / `pnpm lint` / `pnpm typecheck` / `pnpm build` all green
- [x] Zero non-`lib/time/` files import `date-fns` (grep proof)
- [x] Zero non-`lib/time/` files use `.getDay()` without TODO annotation (grep proof)
- [x] Lint rule empirically verified by probe file
- [x] No new runtime dependencies added (vitest is dev-only)
- [x] Public API in `src/lib/time/index.ts` matches the `<interfaces>` block exactly
- [x] `ERR_HOLIDAY_YEAR_OUT_OF_RANGE` constant exported from `src/lib/errors.ts`
</success_criteria>

<requirement_trace>
- **FOUND-V5-01** (ISO 8601 calendar foundation) → Task 2 (`iso-calendar.ts` + tests TC-CAL-001/002/005), Task 3 (lint enforcement TC-CAL-008)
- **FOUND-V5-02** (Swedish holidays + working-day semantics) → Task 2 (`swedish-holidays.ts` + tests TC-CAL-003/004/006), Task 2 (`workingDaysInRange` in `iso-calendar.ts` integrating with `isSwedishHoliday`)
</requirement_trace>

<risks_and_edge_cases>
- **53-week year vs leap year confusion.** These are independent. 2024 = leap (366 days) but 52 ISO weeks. 2026 = not leap but 53 ISO weeks. Document in `iso-calendar.ts` JSDoc; test both 2024 (52) and 2026 (53) explicitly in TC-CAL-001 expansion.
- **Year boundary in `getISOWeekYear`.** 2026-12-31 is in ISO week 53 of *2026* (week year). 2027-01-01, -02, -03 are *also* in week 53 of *2026*. 2027-01-04 is week 1 of 2027. `formatWeekLong` MUST use `getISOWeekYear`, not `date.getUTCFullYear()`. Tested in `formatters.test.ts` boundary cases.
- **DST.** Sweden observes CET/CEST. This module operates on calendar dates only and uses UTC fields internally. Callers must pass dates constructed from year/month/day numbers (`new Date(Date.UTC(y, m, d))`) or accept that local-time Date objects on a DST transition day may misbehave by ±1 hour. Document this contract in the top-of-file JSDoc and reinforce in `index.ts` barrel comment.
- **`isSwedishHoliday` for years outside 2026–2030.** Returns `false` (does not throw). Asymmetric with `getSwedishHolidays` which throws. Rationale: query helpers should be safe to call defensively; lookup helpers should fail loudly. Documented in JSDoc.
- **Pre-existing `.getDay()` callers** in the current codebase may break the lint rule. Plan: surface them in Task 3, fix-forward where trivial, otherwise annotate with `TODO(FOUND-V5-01)` + narrow disable comment. Do NOT broaden the lint rule's ignore list.
- **`date-utils.ts` overlap.** Existing module — Task 1 audits it. If it duplicates anything in `lib/time/`, leave the duplicate in place (out of scope to migrate callers in this phase) but capture in audit notes for a future cleanup phase.
- **TC-CAL-008 automation.** The probe-file approach is empirical, not a unit test. That's intentional — testing eslint rules properly requires `@typescript-eslint/rule-tester` or similar overhead not justified for one rule. The automated verify command in Task 3 makes the probe reproducible in CI.
</risks_and_edge_cases>

<dependencies_on_other_phases>
- **Upstream:** None. This is the foundation phase for v5.0.
- **Downstream:** Every later v5.0 phase depends on `@/lib/time`. Specifically: Phase 37 (actuals distribution — `workingDaysInRange`), Phase 38 (Excel import — `getISOWeek` validation), Phase 40–42 (timeline rendering — `getISOWeeksInYear` for column layout), Phase 34 (historic-edit guardrail — week math for "current month").
</dependencies_on_other_phases>

<output>
After completion, create `.planning/phases/33-foundations-iso-calendar-swedish-holidays/33-01-SUMMARY.md` documenting:
- Final exports list from `src/lib/time/index.ts`
- TC-CAL-* coverage results (which passed, count of any §15 IDs added beyond 001–008)
- Lint rule verification result (probe test outcome)
- Any pre-existing `.getDay()` callers fixed or annotated
- Confirmation no `date-fns` was added
- Audit notes from Task 1 about `date-utils.ts` overlap
</output>
</content>
</invoke>