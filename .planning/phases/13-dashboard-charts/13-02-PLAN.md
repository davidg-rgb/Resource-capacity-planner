---
phase: 13-dashboard-charts
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/components/charts/kpi-card.tsx
  - src/components/charts/department-bar-chart.tsx
  - src/components/charts/discipline-chart.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, CHRT-01, CHRT-02, CHRT-03]

must_haves:
  truths:
    - "User sees 4 KPI cards on the dashboard: utilization %, headcount, overloaded count, underutilized count"
    - "User can click a KPI card to drill down to the team heat map filtered by status"
    - "User sees a departmental utilization horizontal bar chart"
    - "User sees a discipline breakdown horizontal bar chart"
    - "User can switch time range between 3, 6, and 12 months and all data updates"
    - "Charts render with Nordic Precision design tokens (CHART_COLORS, CHART_FONT)"
  artifacts:
    - path: "src/components/charts/kpi-card.tsx"
      provides: "Reusable KPI card with drill-down link"
      exports: ["KPICard"]
    - path: "src/components/charts/department-bar-chart.tsx"
      provides: "Recharts horizontal BarChart for department utilization"
      exports: ["DepartmentBarChart"]
    - path: "src/components/charts/discipline-chart.tsx"
      provides: "Recharts horizontal BarChart for discipline breakdown"
      exports: ["DisciplineChart"]
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Full dashboard page replacing placeholder"
      contains: "KPICard"
  key_links:
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "src/hooks/use-dashboard.ts"
      via: "useDashboardKPIs, useDepartmentUtilization, useDisciplineBreakdown imports"
      pattern: "useDashboardKPIs|useDepartmentUtilization|useDisciplineBreakdown"
    - from: "src/components/charts/department-bar-chart.tsx"
      to: "src/components/charts/chart-colors.ts"
      via: "CHART_COLORS import"
      pattern: "CHART_COLORS"
    - from: "src/components/charts/kpi-card.tsx"
      to: "/dashboard/team"
      via: "Link href for drill-down"
      pattern: "dashboard/team"
---

<objective>
Dashboard UI: install Recharts, create KPI card and chart components, and replace the dashboard placeholder page with the full management dashboard featuring KPI cards, department utilization bar chart, discipline breakdown chart, and time range selector.

Purpose: Delivers the visual dashboard that managers use to monitor organizational capacity health.
Output: 3 chart components, 1 fully functional dashboard page with time range switching and KPI drill-down.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dashboard-charts/13-01-SUMMARY.md
@.planning/phases/13-dashboard-charts/13-RESEARCH.md

<interfaces>
<!-- Types and hooks created by Plan 01 -->

From src/features/analytics/analytics.types.ts:
```typescript
export interface DashboardKPIs {
  totalPeople: number;
  utilizationPercent: number;
  overloadedCount: number;
  underutilizedCount: number;
}

export interface DepartmentUtilization {
  departmentId: string;
  departmentName: string;
  utilizationPercent: number;
}

export interface DisciplineBreakdown {
  disciplineId: string;
  disciplineName: string;
  totalHours: number;
}
```

From src/hooks/use-dashboard.ts:
```typescript
export function useDashboardKPIs(monthFrom: string, monthTo: string): UseQueryResult<DashboardKPIs>;
export function useDepartmentUtilization(monthFrom: string, monthTo: string): UseQueryResult<DepartmentUtilization[]>;
export function useDisciplineBreakdown(monthFrom: string, monthTo: string): UseQueryResult<DisciplineBreakdown[]>;
```

From src/components/charts/chart-colors.ts:
```typescript
export const CHART_COLORS: { primary: string; grid: string; text: string; textMuted: string; over: string; healthy: string; under: string; idle: string; palette: string[]; ... };
export const CHART_FONT: { family: string; headlineFamily: string; size: number; smallSize: number; };
```

From src/lib/date-utils.ts:
```typescript
export function getCurrentMonth(): string;
export function generateMonthRange(startMonth: string, count: number): string[];
```

From src/app/(app)/dashboard/page.tsx (current placeholder):
```typescript
// Simple page with Breadcrumbs, heading, and a single Link card to /dashboard/team
// This entire page will be REPLACED
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and create KPI card and chart components</name>
  <files>src/components/charts/kpi-card.tsx, src/components/charts/department-bar-chart.tsx, src/components/charts/discipline-chart.tsx</files>
  <read_first>
    - src/components/charts/chart-colors.ts (created in Plan 01)
    - src/features/analytics/analytics.types.ts (for DepartmentUtilization, DisciplineBreakdown types)
    - src/app/globals.css (for Tailwind class names used in the project)
    - .planning/phases/13-dashboard-charts/13-RESEARCH.md (for Recharts patterns and pitfalls)
  </read_first>
  <action>
**0. Install Recharts:**
```bash
pnpm add recharts@^3.8
```

**1. Create `src/components/charts/kpi-card.tsx`** (per DASH-01, DASH-04):
- `'use client'` is NOT needed (this is a server-compatible component using next/link)
- Import `Link` from `next/link`
- Props interface `KPICardProps`: `title: string`, `value: string | number`, `subtitle?: string`, `href?: string`
- Render a card div with: `rounded-lg border border-outline-variant/30 bg-surface-container-low p-6`
- Title: `font-headline text-xs font-semibold uppercase tracking-widest text-outline`
- Value: `font-headline text-3xl font-bold tracking-tight text-on-surface`
- Subtitle: `text-sm text-on-surface-variant`
- If `href` provided, wrap in `<Link href={href}>` with `block transition-shadow hover:shadow-md` (drill-down per DASH-04)
- Export as named export `KPICard`

**2. Create `src/components/charts/department-bar-chart.tsx`** (per DASH-02, CHRT-03):
- `'use client'` directive (REQUIRED -- Recharts uses DOM, see RESEARCH.md pitfall 3)
- Import `BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer` from `recharts`
- Import `CHART_COLORS, CHART_FONT` from `./chart-colors`
- Import `DepartmentUtilization` type from `@/features/analytics/analytics.types`
- Props: `{ data: DepartmentUtilization[] }`
- Render `ResponsiveContainer width="100%" height={300}` (parent must provide height -- document in JSDoc)
- `BarChart` with `layout="vertical"` and `margin={{ left: 120 }}`
- XAxis: type="number", domain=[0, 'auto'], tickFormatter `(v) => \`${v}%\``
- YAxis: type="category", dataKey="departmentName", width=110
- CartesianGrid: strokeDasharray="3 3", stroke=CHART_COLORS.grid
- Tooltip with formatter showing percentage
- Bar: dataKey="utilizationPercent", fill=CHART_COLORS.primary, radius=[0, 4, 4, 0]
- All tick font styles use CHART_FONT.family and CHART_FONT.size

**3. Create `src/components/charts/discipline-chart.tsx`** (per CHRT-01, CHRT-03):
- `'use client'` directive (REQUIRED)
- Same imports pattern as department chart
- Import `DisciplineBreakdown` type
- Props: `{ data: DisciplineBreakdown[] }`
- Horizontal bar chart (layout="vertical") showing totalHours per discipline
- XAxis: type="number", tickFormatter showing hours (no percentage)
- YAxis: type="category", dataKey="disciplineName"
- Bar: dataKey="totalHours", fill=CHART_COLORS.secondary (different from department chart), radius=[0, 4, 4, 0]
- Same grid, tooltip, font styling as department chart
- Use a bar chart, NOT a pie chart (per research recommendation -- bar charts better for 10+ categories)
  </action>
  <verify>
    <automated>cd "D:/Kod Projekt/Resurs & Projektplanering" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "recharts" package.json returns match (installed)
    - grep "KPICard" src/components/charts/kpi-card.tsx returns match
    - grep "DepartmentBarChart" src/components/charts/department-bar-chart.tsx returns match
    - grep "DisciplineChart" src/components/charts/discipline-chart.tsx returns match
    - grep "use client" src/components/charts/department-bar-chart.tsx returns match
    - grep "use client" src/components/charts/discipline-chart.tsx returns match
    - grep "CHART_COLORS" src/components/charts/department-bar-chart.tsx returns match
    - grep "CHART_COLORS" src/components/charts/discipline-chart.tsx returns match
    - tsc --noEmit passes with no errors
  </acceptance_criteria>
  <done>Recharts installed, KPICard component renders metric with optional drill-down link, DepartmentBarChart renders horizontal bar chart of department utilization, DisciplineChart renders horizontal bar chart of hours by discipline. All use Nordic Precision design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Replace dashboard placeholder with full KPI + charts page</name>
  <files>src/app/(app)/dashboard/page.tsx</files>
  <read_first>
    - src/app/(app)/dashboard/page.tsx (current placeholder to replace)
    - src/app/(app)/dashboard/team/page.tsx (to understand URL pattern for drill-down links)
    - src/components/charts/kpi-card.tsx (just created)
    - src/components/charts/department-bar-chart.tsx (just created)
    - src/components/charts/discipline-chart.tsx (just created)
    - src/hooks/use-dashboard.ts (hooks from Plan 01)
    - src/lib/date-utils.ts (for getCurrentMonth, generateMonthRange)
    - src/components/heat-map/heat-map-filters.tsx (for URL filter pattern reference)
  </read_first>
  <action>
**Replace `src/app/(app)/dashboard/page.tsx` entirely** with a full dashboard page:

The page must use a client component wrapper for hooks/searchParams (same Suspense pattern as team overview page).

**Structure:**

1. **Server component shell** (the default export):
   - `Breadcrumbs` component
   - Page heading "Dashboard" with subtitle "Key capacity metrics and departmental overview."
   - Wrap `DashboardContent` in `<Suspense fallback={loading skeleton}>` (needed for useSearchParams)

2. **`DashboardContent` client component** (inline in same file, or separate -- keep in same file for simplicity):
   - `'use client'` section using `useSearchParams` and `useRouter`
   - Read `range` param from URL: `searchParams.get('range') ?? '3'` (per DASH-03)
   - Compute `monthFrom` = `getCurrentMonth()` and `monthTo` = last month of range using `generateMonthRange(monthFrom, parseInt(range))` -- take the last element
   - Call all three hooks: `useDashboardKPIs(monthFrom, monthTo)`, `useDepartmentUtilization(monthFrom, monthTo)`, `useDisciplineBreakdown(monthFrom, monthTo)`

3. **Time Range Selector** (per DASH-03, CHRT-02):
   - Three toggle buttons: "3 months", "6 months", "12 months"
   - Active button: `bg-primary text-on-primary` with rounded-sm
   - Inactive: `text-on-surface-variant hover:bg-surface-container-high`
   - On click: `router.replace(\`/dashboard?range=${value}\`, { scroll: false })`
   - Container: `flex gap-1 rounded-md border border-outline-variant/30 p-1`

4. **KPI Cards row** (per DASH-01, DASH-04):
   - Grid: `grid gap-4 sm:grid-cols-2 lg:grid-cols-4`
   - 4 KPICard components:
     - "Utilization" showing `{kpis.utilizationPercent}%`, href="/dashboard/team" (drill to full team)
     - "Headcount" showing `{kpis.totalPeople}`, href="/dashboard/team"
     - "Overloaded" showing `{kpis.overloadedCount}`, subtitle="Above 100%", href="/dashboard/team?status=over" (filtered heat map, if team page supports it -- link anyway for future)
     - "Underutilized" showing `{kpis.underutilizedCount}`, subtitle="Below 50%", href="/dashboard/team?status=under"

5. **Charts section** (per DASH-02, CHRT-01):
   - Two-column grid on large screens: `grid gap-6 lg:grid-cols-2`
   - Left: Card wrapper with heading "Department Utilization" + `<DepartmentBarChart data={departments} />`
   - Right: Card wrapper with heading "Discipline Breakdown" + `<DisciplineChart data={disciplines} />`
   - Card wrapper: `rounded-lg border border-outline-variant/30 bg-surface-container-low p-6`
   - Each chart wrapped in a div with `h-[300px]` to give ResponsiveContainer a sized parent (RESEARCH.md pitfall 2)

6. **Link to Team Overview** (preserve existing functionality):
   - Below charts, a link card to "/dashboard/team" reading "Team Overview -- Capacity heat map across all team members"

7. **Loading/error states:**
   - While any query is loading: show a skeleton or "Loading..." in each section
   - On error: show "Failed to load" message in the affected section
   - Empty state: if totalPeople is 0, show "No team members found" message

**Key implementation notes:**
- Use `Suspense` around the client component that calls `useSearchParams` to avoid Next.js SSR bailout (same pattern as team/page.tsx)
- All chart components are already `'use client'` so they can be rendered inside the client wrapper
- The page itself can be a Server Component that renders the heading and wraps the client content in Suspense
  </action>
  <verify>
    <automated>cd "D:/Kod Projekt/Resurs & Projektplanering" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "KPICard" src/app/(app)/dashboard/page.tsx returns match
    - grep "DepartmentBarChart" src/app/(app)/dashboard/page.tsx returns match
    - grep "DisciplineChart" src/app/(app)/dashboard/page.tsx returns match
    - grep "useDashboardKPIs" src/app/(app)/dashboard/page.tsx returns match
    - grep "useDepartmentUtilization" src/app/(app)/dashboard/page.tsx returns match
    - grep "useDisciplineBreakdown" src/app/(app)/dashboard/page.tsx returns match
    - grep "range" src/app/(app)/dashboard/page.tsx returns match (time range selector)
    - grep "Suspense" src/app/(app)/dashboard/page.tsx returns match
    - grep "dashboard/team" src/app/(app)/dashboard/page.tsx returns match (drill-down links)
    - tsc --noEmit passes with no errors
  </acceptance_criteria>
  <done>Dashboard page shows 4 KPI cards (utilization, headcount, overloaded, underutilized) with drill-down links to team heat map, department utilization bar chart, discipline breakdown bar chart, and a 3/6/12 month time range selector. All charts use Nordic Precision design tokens via CHART_COLORS. URL-based range state is shareable.</done>
</task>

</tasks>

<verification>
- `pnpm build` compiles without errors
- Dashboard page at /dashboard renders KPI cards, department bar chart, discipline breakdown chart
- Time range selector updates URL param and refetches all data
- KPI cards link to /dashboard/team for drill-down
- Charts use CHART_COLORS from Nordic Precision design tokens
- Recharts components all have 'use client' directive
- Chart containers have explicit height for ResponsiveContainer
</verification>

<success_criteria>
Managers can view the dashboard at /dashboard and see:
1. Four KPI cards showing utilization %, headcount, overloaded count, underutilized count (DASH-01)
2. Clickable KPI cards that drill down to the team heat map (DASH-04)
3. Department utilization horizontal bar chart (DASH-02)
4. Discipline breakdown horizontal bar chart (CHRT-01)
5. Time range selector (3/6/12 months) that updates all metrics and charts (DASH-03, CHRT-02)
6. All charts styled with Nordic Precision design tokens (CHRT-03)
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-charts/13-02-SUMMARY.md`
</output>
