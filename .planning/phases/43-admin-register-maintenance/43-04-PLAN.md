---
phase: 43-admin-register-maintenance
plan: 04
type: execute
wave: 4
depends_on: ["43-01", "43-02", "43-03"]
files_modified:
  - src/app/(app)/admin/page.tsx
  - src/app/(app)/admin/change-log/page.tsx
  - src/components/change-log/change-log-feed.tsx
  - src/features/personas/__tests__/persona-landing.test.ts
  - src/features/admin/__tests__/register.integration.test.ts
  - src/features/change-log/__tests__/mutations-manifest.test.ts
  - src/messages/sv.json
  - src/messages/en.json
  - src/messages/keys.ts
autonomous: true
requirements: [ADM-03, ADM-04]
must_haves:
  truths:
    - "/admin landing route renders the global change_log feed using Phase 41 ChangeLogFeed with initialFilter={} (scope=all)"
    - "ChangeLogFeed exposes an entity filter dropdown in its top-bar covering { all, person, project, department, discipline, program, allocation, proposal, actual, import }"
    - "/admin/change-log redirects (or is a thin wrapper) to /admin so Phase 41 links keep working"
    - "Persona default-landing router sends admin persona to /admin (D-19) — the ACTUAL file is discovered in Task 1 via grep (persona-router.tsx does NOT exist per RESEARCH §7.9)"
    - "TC-REG integration tests: TC-REG-001 create (all 5 entities), TC-REG-002 update, TC-REG-003..007 dependent-row blockers (per-entity, covered in 43-01 — re-referenced here), TC-REG-008 archive happy path, TC-REG-009 un-archive via PATCH, TC-REG-010 audit log written for every mutation"
    - "Mutations manifest regeneration (Phase 35 TC-CL-005) includes the four register.service.ts mutations"
  artifacts:
    - path: "src/app/(app)/admin/page.tsx"
      provides: "Admin landing = global change-log feed"
      contains: "ChangeLogFeed"
    - path: "src/features/admin/__tests__/register.integration.test.ts"
      provides: "TC-REG-001, 002, 008, 009, 010 end-to-end via API routes"
      contains: "TC-REG"
    - path: "src/features/change-log/__tests__/mutations-manifest.test.ts"
      provides: "TC-CL-005 invariant rerun with register mutations included"
      contains: "register.service"
  key_links:
    - from: "src/app/(app)/admin/page.tsx"
      to: "src/components/change-log/change-log-feed.tsx"
      via: "<ChangeLogFeed initialFilter={{}} />"
      pattern: "ChangeLogFeed"
    - from: "PersonaProvider / layout landing switch (file TBD via grep)"
      to: "/admin"
      via: "case 'admin': return '/admin'"
      pattern: "case 'admin'"
---

<objective>
Wave 4: Close the phase. Ship the admin landing change-log feed, extend the feed's entity filter dropdown to include program (and the other register entities), redirect /admin/change-log → /admin, land the one-line persona default-landing change (after grepping for the real location), write the TC-REG-* integration tests that exercise the full API→service→db path, and rerun the TC-CL-005 mutations manifest so it covers the new register mutations.

Purpose: ADM-04 admin landing; ADM-03 closure via TC-REG-* and TC-CL-005 coverage of the new mutations.
Output: /admin page + /admin/change-log redirect + ChangeLogFeed filter extension + persona-landing fix + 2 test files + i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-admin-register-maintenance/43-CONTEXT.md
@.planning/phases/43-admin-register-maintenance/43-RESEARCH.md
@.planning/phases/43-admin-register-maintenance/43-01-PLAN.md
@.planning/phases/43-admin-register-maintenance/43-03-PLAN.md
@src/components/change-log/change-log-feed.tsx
@src/features/change-log/change-log.read.ts
@src/features/change-log/change-log.types.ts
@src/app/api/v5/change-log/route.ts
@src/features/personas/persona.context.tsx
@src/features/personas/persona-route-guard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Grep for persona default-landing site + ChangeLogFeed entity-filter audit</name>
  <files>(read-only — writes happen in Tasks 1–3)</files>
  <read_first>
    - 43-RESEARCH.md §7.9 (persona-router.tsx does NOT exist — landing logic lives elsewhere)
    - src/features/personas/persona.context.tsx
    - src/app/(app)/layout.tsx
  </read_first>
  <action>
    1. Grep the repo for the admin default-landing route to find the file to edit in Task 2:
       ```
       rg -n "case ['\"]admin['\"]" src/
       rg -n "/admin/change-log" src/
       rg -n "persona.*kind.*admin.*=>" src/
       rg -n "defaultLandingFor|personaLanding|landingFor" src/
       ```
       Record the file + line in the Task 2 action. Expected candidates per RESEARCH §7.9: `src/features/personas/persona.context.tsx`, `src/app/(app)/layout.tsx`, or a fresh helper under `src/features/personas/`.
    2. Grep ChangeLogFeed for its existing filter dropdown to know what to extend:
       ```
       rg -n "entity.*filter|FeedFilter" src/components/change-log/ src/features/change-log/
       ```
       Note whether the entity dropdown already exists in ChangeLogFeed (Phase 41) and what entity options it already has. The research says the dropdown exists and supports entity[]; confirm + note which entity codes are already listed.
    3. Write a 5-line note at the top of Task 2 below listing the two file paths discovered.
  </action>
  <verify>
    <automated>echo "grep-only task — no file writes"</automated>
  </verify>
  <done>Task 2 has concrete file paths for (a) the persona landing switch and (b) the ChangeLogFeed entity dropdown. No file writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: /admin landing page + /admin/change-log redirect + ChangeLogFeed entity-filter extension + i18n</name>
  <files>src/app/(app)/admin/page.tsx, src/app/(app)/admin/change-log/page.tsx, src/components/change-log/change-log-feed.tsx, src/messages/sv.json, src/messages/en.json, src/messages/keys.ts</files>
  <read_first>
    - src/components/change-log/change-log-feed.tsx (Phase 41 component — note props: `initialFilter: FeedFilter, projects?, people?`; it already fetches via useInfiniteQuery from /api/v5/change-log)
    - src/app/(app)/admin/change-log/page.tsx (existing Phase 41 page — the pattern we mirror for /admin/page.tsx)
    - src/features/change-log/change-log.types.ts (ChangeLogEntity union — includes person/project/department/discipline/allocation/proposal/actual_entry/import_batch; MUST now also include 'program' after 43-01's enum migration)
  </read_first>
  <behavior>
    - `/admin/page.tsx` is a `'use client'` page wrapped in `<PersonaGate allowed={['admin']}>` + Clerk orgRole gate, rendering `<ChangeLogFeed initialFilter={{}} />` — no entity filter, "all entities" default
    - `/admin/change-log/page.tsx` becomes a thin redirect to `/admin` via `next/navigation#redirect()` on the server side (keeps external Phase 41 links alive)
    - ChangeLogFeed top-bar entity filter includes (new or confirmed): `all, person, project, department, discipline, program, allocation, proposal, actual_entry, import_batch`. The `program` option is the load-bearing addition — without it, register_row events for programs can't be filtered from the UI.
    - i18n keys `v5.admin.landing.title` and, if not already present from Phase 41, `v5.changeLog.entityFilter.{all|person|project|department|discipline|program|allocation|proposal|actual_entry|import_batch}`
  </behavior>
  <action>
    1. Create `src/app/(app)/admin/page.tsx` as a client component mirroring `src/app/(app)/admin/change-log/page.tsx` but passing `initialFilter={{}}` (no entity filter) and rendering a page title "Ändringslogg" (Swedish) / "Change log" (English) from i18n.
    2. Replace the body of `src/app/(app)/admin/change-log/page.tsx` with:
       ```tsx
       import { redirect } from 'next/navigation';
       export default function AdminChangeLogRedirect() {
         redirect('/admin');
       }
       ```
       Keep it a server component so redirect() works cleanly. This preserves the old URL for any deep links.
    3. Extend `src/components/change-log/change-log-feed.tsx` entity dropdown (audited in Task 0) to include `program` — and any missing entity codes from the union. If the dropdown is data-driven (reads from `changeLogEntityEnum.enumValues` at module load), 43-01's schema edit to include 'program' already flows through and this step is a no-op verification. Write a quick assertion test OR confirm by reading the rendered options in an existing feed test.
    4. Add i18n keys in sv.json + en.json + keys.ts: `v5.admin.landing.title`, and ensure `v5.changeLog.entityFilter.program` (+ any missing) exists with Swedish/English labels.
  </action>
  <verify>
    <automated>pnpm vitest run src/components/change-log/__tests__ src/app/(app)/admin && pnpm tsc --noEmit</automated>
  </verify>
  <done>/admin renders ChangeLogFeed with no filter; /admin/change-log redirects to /admin; entity dropdown includes program; i18n keys in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persona default-landing one-liner (D-19) + test</name>
  <files>(file discovered in Task 0 — likely src/features/personas/persona.context.tsx or src/app/(app)/layout.tsx), src/features/personas/__tests__/persona-landing.test.ts</files>
  <read_first>
    - Task 0's recorded file path for the admin landing switch
    - src/features/personas/persona.context.tsx
    - 43-CONTEXT.md D-19 (target is `/admin`, consolidated from `/admin/change-log`)
  </read_first>
  <behavior>
    - Setting persona.kind = 'admin' in the persona context causes the first-load / switch redirect to land on `/admin` (not `/admin/change-log`, not `/`)
    - Behavior is verified by a focused unit test on the landing-resolver function, NOT by a full route test
  </behavior>
  <action>
    1. Open the file identified in Task 0. Change the `admin → /admin/change-log` branch to `admin → /admin`. If the logic is a switch statement, it's literally a one-line change. If the logic is data-driven (a map), edit the map entry.
    2. Create `src/features/personas/__tests__/persona-landing.test.ts`:
       - Import the exported resolver function (export it if it's currently private — minimal refactor)
       - Assert: `resolveLandingFor({ kind: 'admin' }) === '/admin'`
       - Assert: other personas still return their existing paths (to prevent accidental regression)
       - If the landing is a React effect without a testable pure function, extract the mapping into a `PERSONA_LANDING: Record<PersonaKind, string>` constant and test THAT
  </action>
  <verify>
    <automated>pnpm vitest run src/features/personas/__tests__/persona-landing.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>Admin persona lands on /admin on persona switch; regression test in place for all persona landings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TC-REG integration tests (full API path) + TC-CL-005 mutations manifest regen</name>
  <files>src/features/admin/__tests__/register.integration.test.ts, src/features/change-log/__tests__/mutations-manifest.test.ts, tests/invariants/mutations.json</files>
  <read_first>
    - src/features/admin/__tests__/register.service.test.ts (from 43-01 — unit-level)
    - src/features/admin/__tests__/register.dependents.test.ts (from 43-01 — per-entity blockers)
    - src/features/admin/__tests__/register.audit.test.ts (from 43-01 — per-mutation audit)
    - src/app/api/v5/admin/registers/[entity]/__tests__/contract.test.ts (from 43-01 — HTTP-level shapes)
    - src/features/change-log/__tests__/ (Phase 35 mutations manifest invariant test location)
    - scripts/generate-mutations-manifest.ts
    - tests/invariants/mutations.json (existing manifest)
  </read_first>
  <behavior>
    - `register.integration.test.ts` is a single PGlite + Next test-harness file exercising the full HTTP→route→service→db→change_log path for all 5 entities. Tests map 1:1 to TC-REG codes:
      - **TC-REG-001 create**: POST /api/v5/admin/registers/<entity> for each of 5 entities → 201 + row visible in subsequent GET
      - **TC-REG-002 update**: PATCH /api/v5/admin/registers/<entity>/<id> → 200 + updated fields + change_log REGISTER_ROW_UPDATED row with before/after context
      - **TC-REG-003..007** (dependent rows): cross-reference to 43-01's register.dependents.test.ts (unit-level); ADD an HTTP-level smoke test here that hits DELETE with a dependent-row seed and asserts 409 JSON body has `code: 'DEPENDENT_ROWS_EXIST'` and blocker counts
      - **TC-REG-008 archive happy path**: DELETE /<entity>/<id> with no dependents → 200, row has archived_at set, hidden from default GET, visible with ?includeArchived=true
      - **TC-REG-009 un-archive**: PATCH /<entity>/<id> with `{ archivedAt: null }` → row un-archived; change_log REGISTER_ROW_UPDATED captures the transition
      - **TC-REG-010 audit completeness**: After running create+update+archive for every entity, query change_log and assert exactly 15 rows (5 × 3 mutations), each with entity matching, persona='admin', action in the register set, context non-null
    - `mutations-manifest.test.ts` (may already exist from Phase 35 — extend or create a sibling):
      - Runs `scripts/generate-mutations-manifest.ts` in dry-run mode
      - Asserts the output manifest includes the four new register.service.ts mutations (createRegisterRow, updateRegisterRow, archiveRegisterRow, and — if listRegisterRows were mutating, which it isn't — drops it). Only mutations count.
      - Asserts `tests/invariants/mutations.json` is up-to-date (regen and compare; CI-failing if stale per Phase 35 rule)
  </behavior>
  <action>
    1. Create `src/features/admin/__tests__/register.integration.test.ts`. Use the same PGlite bootstrap as the contract test in 43-01. Seed org + admin user + baseline reference data (1 department, 1 discipline, 1 program, 1 person, 1 project) so dependent-row blockers have something to block against.
    2. Structure with `describe.each(['person','project','department','discipline','program'])` for create/update/archive/un-archive + a separate `describe('TC-REG-003..007 HTTP-level blockers')` block using seeded dependents.
    3. For each HTTP call, use the same request helper the contract test in 43-01 uses (or extract one to `src/test/helpers/admin-request.ts` in this task).
    4. After the 15 mutations for TC-REG-010, SELECT change_log WHERE organizationId = test org → assert row count and shape.
    5. Regenerate the mutations manifest:
       ```
       pnpm tsx scripts/generate-mutations-manifest.ts
       ```
       Commit the updated `tests/invariants/mutations.json` (the Phase 35 runtime invariant test will then rerun against the new entries automatically on `pnpm vitest`).
    6. If the existing Phase 35 manifest test is self-verifying (greps the manifest for new entries), confirm it green. If not, add `src/features/change-log/__tests__/mutations-manifest.test.ts` with explicit assertions for the three register.service mutation names.
  </action>
  <verify>
    <automated>pnpm vitest run src/features/admin/__tests__/register.integration.test.ts src/features/change-log/__tests__/mutations-manifest.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>All 10 TC-REG-* codes pass; TC-CL-005 still green with register mutations in manifest; tests/invariants/mutations.json updated and committed.</done>
</task>

</tasks>

<verification>
- pnpm tsc --noEmit clean
- pnpm vitest run (full suite) green — no regressions in Phase 35 TC-CL-005, Phase 41 ChangeLogFeed, Phase 43 Wave 1–3 tests
- pnpm vitest run src/features/admin src/app/api/v5/admin src/components/change-log src/features/personas/__tests__/persona-landing.test.ts green
- Manual: switch persona to admin from the PersonaSwitcher → lands on /admin → sees change-log feed → filter by `program` → create a program in /admin/programs → the new REGISTER_ROW_CREATED row appears in /admin feed under entity=program
- Manual: visit old /admin/change-log URL → redirected to /admin
</verification>

<success_criteria>
- ADM-03 fully closed: every admin-driven register mutation writes change_log and TC-CL-005 invariant is green with the new service included
- ADM-04 satisfied: /admin = global change_log feed; admin persona lands there by default
- TC-REG-001..010 all mapped to passing tests (TC-REG-003..007 at unit level in 43-01 plus HTTP-level smoke here)
- /admin/change-log deep links keep working via redirect
- ChangeLogFeed entity filter includes `program`
</success_criteria>

<out_of_scope>
- Touching /admin/members (Clerk) — D-01
- Rewriting ChangeLogFeed component — only extending its entity filter
- Any new register entities — D-06 non-goals
- Persona assignment UI (person → persona mapping) — Phase 34 non-goal
- Audit-detail-per-row page — D non-goal
- Hardening /api/v5/* AppError taxonomy beyond what 43-01 shipped — that's Phase 44's job
</out_of_scope>

<output>
Create `.planning/phases/43-admin-register-maintenance/43-04-SUMMARY.md`
</output>
</content>
