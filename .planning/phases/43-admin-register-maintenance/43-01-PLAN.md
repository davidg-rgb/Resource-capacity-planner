---
phase: 43-admin-register-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/migrations/0007_register_archive.sql
  - drizzle/migrations/0008_change_log_entity_program.sql
  - src/db/schema.ts
  - src/features/admin/register.service.ts
  - src/features/admin/__tests__/register.service.test.ts
  - src/features/admin/__tests__/register.dependents.test.ts
  - src/features/admin/__tests__/register.audit.test.ts
  - src/app/api/v5/admin/registers/[entity]/route.ts
  - src/app/api/v5/admin/registers/[entity]/[id]/route.ts
  - src/app/api/v5/admin/registers/[entity]/__tests__/contract.test.ts
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03]
must_haves:
  truths:
    - "Migration 0007 adds archived_at + org_archived_idx to departments/disciplines/programs; 0008 extends change_log_entity enum with 'program'"
    - "register.service.ts exposes exactly createRegisterRow, updateRegisterRow, archiveRegisterRow, listRegisterRows — matches ARCHITECTURE §6.11b"
    - "Every mutation opens its own db.transaction(tx => ...) and calls recordChange(..., tx) inside the SAME tx (ADR-003 one tx, one writer)"
    - "archiveRegisterRow throws ConflictError('DEPENDENT_ROWS_EXIST', { entity, id, blockers }) using the corrected blocker rules from RESEARCH §3"
    - "listRegisterRows({ includeArchived: false }) filters WHERE archived_at IS NULL; true returns both ordered archived_at DESC NULLS FIRST, name ASC"
    - "GET/POST /api/v5/admin/registers/[entity] and PATCH/DELETE /api/v5/admin/registers/[entity]/[id] are wrapped with withTenant + admin persona guard + Clerk org:admin/org:owner gate"
    - "Unknown entity → 404 NotFoundError; DEPENDENT_ROWS_EXIST → 409 via handleApiError"
    - "TC-CL-005 mutations manifest regenerates green including the four new register.service mutations"
  artifacts:
    - path: "drizzle/migrations/0007_register_archive.sql"
      provides: "archived_at columns + org_archived indices"
      contains: "ALTER TABLE \"departments\" ADD COLUMN \"archived_at\""
    - path: "drizzle/migrations/0008_change_log_entity_program.sql"
      provides: "ALTER TYPE change_log_entity ADD VALUE 'program' (separate file — cannot run inside a tx)"
      contains: "ALTER TYPE \"change_log_entity\" ADD VALUE"
    - path: "src/features/admin/register.service.ts"
      provides: "Thin dispatcher: createRegisterRow, updateRegisterRow, archiveRegisterRow, listRegisterRows"
      contains: "db.transaction"
    - path: "src/app/api/v5/admin/registers/[entity]/route.ts"
      provides: "GET list + POST create"
      contains: "withTenant"
    - path: "src/app/api/v5/admin/registers/[entity]/[id]/route.ts"
      provides: "PATCH update + DELETE archive"
      contains: "DEPENDENT_ROWS_EXIST"
  key_links:
    - from: "src/features/admin/register.service.ts"
      to: "src/features/change-log/change-log.service.ts"
      via: "recordChange(input, tx) inside db.transaction"
      pattern: "recordChange\\(.*tx"
    - from: "src/app/api/v5/admin/registers/[entity]/route.ts"
      to: "src/features/admin/register.service.ts"
      via: "service dispatch by entity"
      pattern: "registerService\\.(list|create)"
    - from: "src/app/api/v5/admin/registers/[entity]/[id]/route.ts"
      to: "src/features/admin/register.service.ts"
      via: "service dispatch by entity"
      pattern: "registerService\\.(update|archive)"
---

<objective>
Wave 1: Backend foundation for Phase 43. Land the schema migrations, the `register.service.ts` dispatcher, and the v5 API routes that drive every admin register screen in Waves 2–4. Zero UI in this plan.

Purpose: ADM-01 (register CRUD API surface), ADM-02 (archive + dependent-row blocking), ADM-03 (universal change_log coverage for admin-driven mutations, ARCHITECTURE §6.11b).
Output: 2 migration files, schema.ts edits, register.service.ts + 3 test files, 2 API route files + contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-admin-register-maintenance/43-CONTEXT.md
@.planning/phases/43-admin-register-maintenance/43-RESEARCH.md
@.planning/v5.0-ARCHITECTURE.md
@src/db/schema.ts
@src/features/change-log/change-log.service.ts
@src/features/change-log/change-log.types.ts
@src/lib/tenant.ts
@src/lib/errors.ts
@src/lib/api-utils.ts
@src/features/people/person.service.ts
@src/features/projects/project.service.ts
@src/features/departments/department.service.ts
@src/features/disciplines/discipline.service.ts
@src/features/programs/program.service.ts
</context>

<interfaces>
<!-- Key contracts the executor MUST match. Extracted from schema.ts + change-log.service.ts during research. -->

Register entity union (NEW — define in register.service.ts):
```ts
export type RegisterEntity = 'person' | 'project' | 'department' | 'discipline' | 'program';
```

recordChange signature (src/features/change-log/change-log.service.ts:25):
```ts
// Already accepts an optional executor — pass the tx handle from db.transaction()
export async function recordChange(
  input: { organizationId: string; actorUserId: string; persona: PersonaKind; entity: ChangeLogEntity; entityId: string; action: ChangeLogAction; context?: unknown },
  tx?: Pick<typeof db, 'insert'>,
): Promise<void>;
```

ConflictError (src/lib/errors.ts:59):
```ts
new ConflictError('DEPENDENT_ROWS_EXIST', { entity, id, blockers });
// handleApiError maps to HTTP 409
```

Already-existing enum values (schema.ts:85-87) — NO new action codes required:
- 'REGISTER_ROW_CREATED' | 'REGISTER_ROW_UPDATED' | 'REGISTER_ROW_DELETED'

Currently MISSING from changeLogEntityEnum (schema.ts:63-72): 'program' — migration 0008 adds it.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations 0007 + 0008 + schema.ts edits + migration smoke test</name>
  <files>drizzle/migrations/0007_register_archive.sql, drizzle/migrations/0008_change_log_entity_program.sql, src/db/schema.ts, src/features/admin/__tests__/register.service.test.ts</files>
  <read_first>
    - src/db/schema.ts lines 63-90 (changeLogEntityEnum + changeLogActionEnum)
    - src/db/schema.ts lines 129-175 (departments / disciplines / programs tables)
    - src/db/schema.ts lines 194-222 (people.archivedAt exists; projects.archivedAt ALSO already exists)
    - drizzle/migrations/0006_import_status_staged_committed.sql (format/style reference)
    - 43-RESEARCH.md §5 (exact ALTER statements + the split-migration rationale)
  </read_first>
  <behavior>
    - pnpm db:generate produces no drift after the migrations + schema edits land
    - A fresh PGlite test spins up schema and can INSERT into change_log with entity='program'
    - Departments/disciplines/programs rows can be soft-archived by setting archived_at
    - projects.archived_at index is added (column already existed per RESEARCH §5)
  </behavior>
  <action>
    1. Create `drizzle/migrations/0007_register_archive.sql`:
       ```sql
       ALTER TABLE "departments" ADD COLUMN "archived_at" timestamp with time zone;
       ALTER TABLE "disciplines" ADD COLUMN "archived_at" timestamp with time zone;
       ALTER TABLE "programs"    ADD COLUMN "archived_at" timestamp with time zone;

       CREATE INDEX "departments_org_archived_idx" ON "departments" ("organization_id","archived_at");
       CREATE INDEX "disciplines_org_archived_idx" ON "disciplines" ("organization_id","archived_at");
       CREATE INDEX "programs_org_archived_idx"    ON "programs"    ("organization_id","archived_at");
       CREATE INDEX "projects_org_archived_idx"    ON "projects"    ("organization_id","archived_at");
       ```
       Do NOT add archived_at to projects — it already exists at schema.ts:222 (RESEARCH §5).
    2. Create `drizzle/migrations/0008_change_log_entity_program.sql` — SEPARATE file because Postgres ALTER TYPE ADD VALUE cannot run inside a transaction block:
       ```sql
       ALTER TYPE "change_log_entity" ADD VALUE IF NOT EXISTS 'program';
       ```
    3. Edit `src/db/schema.ts`:
       - Extend `changeLogEntityEnum` literal array to include `'program'`.
       - On the `departments` pgTable: add `archivedAt: timestamp('archived_at', { withTimezone: true })` and an index `index('departments_org_archived_idx').on(t.organizationId, t.archivedAt)`.
       - Same for `disciplines`.
       - On `programs` pgTable: add `archivedAt` + index.
       - On `projects` pgTable: add the missing `index('projects_org_archived_idx').on(t.organizationId, t.archivedAt)` (column already exists — do NOT re-declare it).
    4. Create `src/features/admin/__tests__/register.service.test.ts` — scaffold file with a SINGLE smoke test for now: PGlite bootstrap, INSERT a program, INSERT a change_log row with entity='program', assert no error. This locks in the enum migration before Task 2 expands the file.
  </action>
  <verify>
    <automated>pnpm db:generate --dry-run || true && pnpm vitest run src/features/admin/__tests__/register.service.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>Both migration files on disk; schema.ts accepts `entity='program'` at type level; smoke test green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: register.service.ts dispatcher (transactional, inline Drizzle) + dependent-row blocker tests + audit tests</name>
  <files>src/features/admin/register.service.ts, src/features/admin/__tests__/register.service.test.ts, src/features/admin/__tests__/register.dependents.test.ts, src/features/admin/__tests__/register.audit.test.ts</files>
  <read_first>
    - src/features/change-log/change-log.service.ts (recordChange signature + tx param)
    - src/features/people/person.service.ts (field shape reference — do NOT call it)
    - src/features/projects/project.service.ts (status='archived' handling; we MUST also set status='archived' when archiving a project to stay consistent with v4 archiveProject)
    - src/db/schema.ts — people, projects, departments, disciplines, programs, allocations (no status/end_date!), allocationProposals (personId, NOT target_person_id)
    - src/lib/errors.ts (ConflictError, NotFoundError, ValidationError)
    - 43-RESEARCH.md §1 (tx strategy — OPTION A is locked), §3 (corrected blocker queries)
  </read_first>
  <behavior>
    - createRegisterRow({ orgId, actorUserId, entity, data }) → opens db.transaction, inlines `tx.insert(schema.<entity>).values({...organizationId: orgId, ...data}).returning()`, calls `recordChange({ entity, entityId: row.id, action: 'REGISTER_ROW_CREATED', context: { snapshot: row } }, tx)`, returns the row
    - updateRegisterRow({ orgId, actorUserId, entity, id, data }) → SELECT before-row; UPDATE SET ... WHERE id AND organizationId; recordChange action='REGISTER_ROW_UPDATED' context={before, after}; returns after-row. NotFoundError if no row matched. Allows `{ archivedAt: null }` as an un-archive PATCH (D-11).
    - archiveRegisterRow({ orgId, actorUserId, entity, id }) → runs the per-entity blocker queries INSIDE tx; if any counts > 0 throws ConflictError('DEPENDENT_ROWS_EXIST', { entity, id, blockers }); else UPDATE archived_at = now() (and for project ALSO set status='archived'); recordChange action='REGISTER_ROW_DELETED' context={before: row}
    - listRegisterRows({ orgId, entity, includeArchived }) → SELECT with/without `archived_at IS NULL`, ORDER BY `archived_at DESC NULLS FIRST, name ASC`. NO tx needed — read path.
    - Unknown entity → throw NotFoundError('UNKNOWN_ENTITY', { entity })
    - Validation: use each entity's existing Zod schema (people.schema.ts etc.) for create/update payload. ValidationError on fail.

    Dependent-row rules (CORRECTED from RESEARCH §3 — these OVERRIDE D-10):
    - person:
        allocations: count where organizationId=orgId AND personId=id AND month >= current monthKey   (NO status, NO end_date — allocations table doesn't have those cols)
        leadPm:      count where projects.leadPmPersonId=id AND status != 'archived'
        proposals:   count where allocationProposals.personId=id AND status='proposed'               (column is personId, NOT target_person_id)
    - project:
        allocations: count where projectId=id AND month >= current monthKey
        proposals:   count where allocationProposals.projectId=id AND status='proposed'
    - department:
        people:      count where people.departmentId=id AND archivedAt IS NULL
        (DROP the "projects.department_id" blocker — projects table has NO department_id column per RESEARCH §3)
    - discipline:
        people:      count where people.disciplineId=id AND archivedAt IS NULL
    - program:
        projects:    count where projects.programId=id AND status != 'archived' AND archivedAt IS NULL
  </behavior>
  <action>
    1. Create `src/features/admin/register.service.ts`:
       - Export `type RegisterEntity = 'person' | 'project' | 'department' | 'discipline' | 'program'`.
       - Export four functions with the signatures in <behavior>. Internal impl uses `await db.transaction(async (tx) => { ... })` for all mutations.
       - Inline Drizzle calls against `schema.people | schema.projects | schema.departments | schema.disciplines | schema.programs` — do NOT import or call person.service / project.service / etc. (See RESEARCH §1 — Option A.)
       - Pass `tx` as the second arg to every `recordChange(...)` call.
       - Dispatcher uses a switch on `entity`. Extract per-entity helpers (`insertPerson`, `updatePerson`, `archivePersonChecks`, etc.) as internal module functions if the switch gets gnarly.
       - Currently-month helper: import `getServerNowMonthKey` from Phase 34 helper if available (grep `getServerNowMonthKey`), otherwise compute inline `new Date().toISOString().slice(0,7)`.
       - For project archival: set BOTH `status = 'archived'` AND `archivedAt = now()` inside the UPDATE to match v4 `archiveProject` semantics.

    2. Extend `src/features/admin/__tests__/register.service.test.ts` (PGlite) to cover:
       - create: inserts row + writes change_log with action REGISTER_ROW_CREATED and entity=<entity>, persona='admin', inside same tx (rollback test — if recordChange fails, the insert rolls back too)
       - update: before/after context, NotFoundError on wrong id/org
       - archive happy path: sets archived_at, hides from `listRegisterRows({ includeArchived: false })`, appears in `includeArchived: true`
       - un-archive via update({ archivedAt: null })
       - list ordering: archived rows first (DESC NULLS FIRST), then alphabetic name

    3. Create `src/features/admin/__tests__/register.dependents.test.ts` (PGlite) — one describe block per entity, each with:
       - A seed that creates the blocker row, calls `archiveRegisterRow`, asserts ConflictError with code='DEPENDENT_ROWS_EXIST' and the expected `blockers` shape (non-zero counts for exactly the blocker under test).
       - A happy-path test with no blockers — archive succeeds.
       These are the TC-REG dependent-row tests (counted as TC-REG-003..007 in Plan 43-04).

    4. Create `src/features/admin/__tests__/register.audit.test.ts` (PGlite) — calls create/update/archive once per entity and asserts:
       - Exactly one change_log row inserted per mutation
       - entity column matches the RegisterEntity
       - action ∈ { REGISTER_ROW_CREATED, REGISTER_ROW_UPDATED, REGISTER_ROW_DELETED }
       - context JSON contains snapshot (create) / before+after (update) / before (archive)
       This is the input to TC-CL-005 invariant regen in Plan 43-04.
  </action>
  <verify>
    <automated>pnpm vitest run src/features/admin/__tests__ && pnpm tsc --noEmit</automated>
  </verify>
  <done>register.service.ts + all three test files green; every mutation writes change_log inside the same tx; every dependent-row rule blocks with the corrected blocker shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: v5 API routes + contract test + persona/Clerk guards</name>
  <files>src/app/api/v5/admin/registers/[entity]/route.ts, src/app/api/v5/admin/registers/[entity]/[id]/route.ts, src/app/api/v5/admin/registers/[entity]/__tests__/contract.test.ts</files>
  <read_first>
    - src/app/api/v5/planning/allocations/route.ts (withTenant + persona guard + handleApiError pattern — mirror exactly)
    - src/app/api/v5/change-log/route.ts (Phase 41 route shape)
    - src/lib/tenant.ts (withTenant factory)
    - src/lib/api-utils.ts (handleApiError mapping — confirms ConflictError → 409)
    - 43-CONTEXT.md D-12, D-14; 43-RESEARCH.md §4 (persona guard)
  </read_first>
  <behavior>
    - GET  /api/v5/admin/registers/[entity]?includeArchived=true|false   → 200 list
    - POST /api/v5/admin/registers/[entity]                              → 201 created row
    - PATCH /api/v5/admin/registers/[entity]/[id]                        → 200 updated row (handles archivedAt:null un-archive)
    - DELETE /api/v5/admin/registers/[entity]/[id]                       → 200 archived row
    - Validates `entity` against RegisterEntity union → 404 NotFoundError if unknown
    - Wraps handler body in withTenant() + persona guard allowing only 'admin' + Clerk orgRole gate (org:admin or org:owner)
    - Surfaces registerService errors via handleApiError: ValidationError→400, NotFoundError→404, ConflictError→409 (payload includes `code: 'DEPENDENT_ROWS_EXIST'` and `blockers`), else 500
    - Contract test hits every verb × every entity with PGlite seed, asserts status codes + shape
  </behavior>
  <action>
    1. Create `src/app/api/v5/admin/registers/[entity]/route.ts` — Next.js App Router handler exporting `GET` and `POST`. Both pull `params.entity`, validate against RegisterEntity, call `withTenant` to resolve orgId + actorUserId + persona, assert persona === 'admin' (403 otherwise), assert Clerk orgRole ∈ {'org:admin','org:owner'} (403 otherwise), then dispatch to `registerService.listRegisterRows` / `.createRegisterRow`. Wrap in try/catch → handleApiError.
    2. Create `src/app/api/v5/admin/registers/[entity]/[id]/route.ts` with `PATCH` and `DELETE` mirroring the same guard stack, dispatching to `updateRegisterRow` / `archiveRegisterRow`.
    3. Create contract test `src/app/api/v5/admin/registers/[entity]/__tests__/contract.test.ts` (PGlite + Next test harness):
       - Happy path per verb per entity (5 × 4 = 20 micro-assertions, collapse into a `describe.each([...entities])`)
       - Unknown entity → 404
       - Non-admin persona → 403
       - Non-admin Clerk orgRole → 403
       - DELETE with a dependent row → 409 with `{ code: 'DEPENDENT_ROWS_EXIST', blockers: {...} }` (reuse one blocker seed from Task 2's dependents test fixtures)
       - PATCH with `{ archivedAt: null }` on an archived row → 200 and row.archivedAt === null
  </action>
  <verify>
    <automated>pnpm vitest run src/app/api/v5/admin/registers && pnpm tsc --noEmit</automated>
  </verify>
  <done>All four verbs live under /api/v5/admin/registers/[entity]; contract test green; 403/404/409 paths all exercised.</done>
</task>

</tasks>

<verification>
- pnpm tsc --noEmit clean
- pnpm vitest run src/features/admin/__tests__ src/app/api/v5/admin/registers green
- pnpm db:generate produces no drift
- Manual sanity: hit `GET /api/v5/admin/registers/department` as admin and as pm — admin 200, pm 403
</verification>

<success_criteria>
- ADM-01 backend surface in place (CRUD API for all 5 entities)
- ADM-02 archive + DEPENDENT_ROWS_EXIST wired with corrected blocker rules
- ADM-03 every admin mutation writes change_log inside the same tx (one-writer invariant preserved)
- Test codes introduced: TC-REG-003..007 (dependent-row blockers, one per entity), TC-REG-audit (audit row per mutation). Final TC-REG-001/002/008..010 come from Plan 43-04 integration tests.
</success_criteria>

<out_of_scope>
- ANY UI work (pages, components, hooks) — belongs to 43-02 and 43-03
- Touching v4 services (person.service, project.service, department.service, discipline.service, program.service) — treat as read-side helpers only, do NOT thread tx through them (RESEARCH §1)
- Touching v4 hooks in `src/hooks/use-reference-data.ts` (D-13 — leave orphaned)
- /admin/members (Clerk) — D-01 hard no
- New change_log action codes — REGISTER_ROW_* already exist per RESEARCH §0/D-20
- Admin landing `/admin` route and change-log filter — Plan 43-04
- Persona-router default-landing change — Plan 43-04
- Adding departments.parent / disciplines.color columns (D-17 aspirational — dropped per RESEARCH §7.4)
</out_of_scope>

<output>
Create `.planning/phases/43-admin-register-maintenance/43-01-SUMMARY.md`
</output>
</content>
