---
phase: 52-per-journey-friction-fixes
plan: 05
type: execute
wave: 4
depends_on: ["52-01", "52-02", "52-03", "52-04"]
files_modified:
  - src/components/drawer/PlanVsActualDrawer.tsx
  - src/components/drawer/usePlanVsActualDrawer.tsx
  - src/components/drawer/Drawer.tsx
  - src/components/drawer/__tests__/plan-vs-actual-drawer.deeplink.test.tsx
  - src/app/(app)/pm/projects/[projectId]/page.tsx
  - src/app/(app)/rd/page.tsx
  - src/components/admin/AdminRegisterPageShell.tsx
  - src/components/admin/__tests__/admin-archive-toast.test.tsx
  - e2e/pm/1a-monday-checkin.spec.ts
  - e2e/pm/1b-submit-wish.spec.ts
  - e2e/pm/1c-rejected-wish.spec.ts
  - e2e/pm/1d-historic-edit.spec.ts
  - e2e/line-manager/2a-capacity-overview.spec.ts
  - e2e/line-manager/2b-approve-reject.spec.ts
  - e2e/line-manager/2c-direct-edit.spec.ts
  - e2e/staff/3a-check-schedule.spec.ts
  - e2e/rd/4a-portfolio-overview.spec.ts
  - e2e/rd/4b-overcommit-drilldown.spec.ts
  - e2e/admin/5b-archive-dependent.spec.ts
  - e2e/_invariants/flag-off-parity.spec.ts
  - package.json
autonomous: true
requirements: [SHARED-01, ADMIN-01, LM-02]
must_haves:
  truths:
    - "SHARED-01: loading `/pm/projects/<id>?drawer=person-month&personId=<pid>&month=<YYYY-MM>` opens the drawer on mount with the correct payload"
    - "SHARED-01: loading `/rd?drawer=person-month&personId=<pid>&month=<YYYY-MM>` opens the drawer on mount"
    - "SHARED-01: pressing Escape while drawer is open strips `drawer`/`personId`/`month` from the URL via `router.replace`"
    - "SHARED-01: Drawer focus trap verified — Tab cycling while open stays inside the drawer (using existing Drawer primitive or focus-trap-react install per Q5)"
    - "ADMIN-01: archiving a project with active allocations surfaces the error via sonner `toast.error(React-node)` containing an expandable `<details>` block"
    - "ADMIN-01: `<details>` lists kind-counts verbatim (e.g. `Allokeringar: 12`, `Aktuella perioder: 3`) — NOT per-row data (per Q1 resolution; DependentRowsError.blockers stays `Record<string, number>`)"
    - "All 11 Playwright journey specs exist and enforce `expect(getClickCount(page)).toBeLessThanOrEqual(<target>)` at the end"
    - "Flag-off parity spec: every persona landing works identically to Phase 51 when `uiV6PerJourney=false`"
    - "Each persona landing passes `@axe-core/playwright` zero-violations when flag on"
  artifacts:
    - path: "src/components/drawer/PlanVsActualDrawer.tsx"
      provides: "Reads `?drawer=person-month&personId=&month=` on mount via useSearchParams + useEffect; on close, strips those params via router.replace"
      contains: "searchParams.get('drawer')"
    - path: "src/components/drawer/Drawer.tsx"
      provides: "Focus trap active while drawer is open (per Q5: use focus-trap-react if not already present)"
      contains: "focus"
    - path: "src/components/admin/AdminRegisterPageShell.tsx"
      provides: "handleArchive catches DependentRowsError → toast.error with React node containing `<details>` + kind-count list"
      contains: "toast.error"
    - path: "e2e/pm/1a-monday-checkin.spec.ts"
      provides: "Journey 1A — PM Monday check-in. Target: 2 clicks. Exercises SHARED-01 deep-link drawer."
      min_lines: 20
    - path: "e2e/pm/1b-submit-wish.spec.ts"
      provides: "Journey 1B — PM submit wish. Target: 3 clicks."
      min_lines: 15
    - path: "e2e/pm/1c-rejected-wish.spec.ts"
      provides: "Journey 1C — PM rejected wish via top-bar chip. Target: 2 clicks."
      min_lines: 15
    - path: "e2e/pm/1d-historic-edit.spec.ts"
      provides: "Journey 1D — 4-combo matrix (PM past/future × LM past/future). Target: 3 clicks per path."
      min_lines: 40
    - path: "e2e/line-manager/2a-capacity-overview.spec.ts"
      provides: "Journey 2A — capacity overview. Target: 1 click."
    - path: "e2e/line-manager/2b-approve-reject.spec.ts"
      provides: "Journey 2B — approve via badge. Target: 1 click."
    - path: "e2e/line-manager/2c-direct-edit.spec.ts"
      provides: "Journey 2C — project-breakdown cells on LM timeline (LM-02 coverage)."
    - path: "e2e/staff/3a-check-schedule.spec.ts"
      provides: "Journey 3A — staff read-only schedule. Target: 0 clicks."
    - path: "e2e/rd/4a-portfolio-overview.spec.ts"
      provides: "Journey 4A — portfolio zoom matrix (2026 53-week × 2027 × 2028 × {month,quarter,year}). Target: 0 clicks with zoom."
      min_lines: 40
    - path: "e2e/rd/4b-overcommit-drilldown.spec.ts"
      provides: "Journey 4B — red-cell → OvercommitDialog + deep-link drawer coverage. Target: 1 click."
    - path: "e2e/admin/5b-archive-dependent.spec.ts"
      provides: "Journey 5B — archive project → DEPENDENT_ROWS_EXIST toast with <details>. Target: 2 clicks."
    - path: "e2e/_invariants/flag-off-parity.spec.ts"
      provides: "Fully-expanded invariant: with flag OFF, every journey's pre-Phase-52 path still works"
      min_lines: 60
  key_links:
    - from: "src/app/(app)/pm/projects/[projectId]/page.tsx"
      to: "src/components/drawer/usePlanVsActualDrawer.tsx"
      via: "useEffect reading ?drawer=person-month query params → drawer.open(...)"
      pattern: "searchParams.get\\('drawer'\\)"
    - from: "src/app/(app)/rd/page.tsx"
      to: "src/components/drawer/usePlanVsActualDrawer.tsx"
      via: "same useEffect pattern for the R&D route"
      pattern: "searchParams.get\\('drawer'\\)"
    - from: "src/components/admin/AdminRegisterPageShell.tsx"
      to: "sonner toast"
      via: "toast.error(<ErrorContent />) with React node containing <details>"
      pattern: "toast\\.(error|custom)"
    - from: "e2e/*/*.spec.ts"
      to: "e2e/helpers/click-counter.ts"
      via: "resetClickCount + getClickCount + expect(<=target)"
      pattern: "getClickCount"
---

<objective>
Ship the final Phase 52 wave: SHARED-01 drill-down drawer deep-link + focus trap + ESC-strip-params (per D-11 + Q5), ADMIN-01 toast with kind-count `<details>` (per D-12 + Q1 reinterpretation — kind-counts only, no backend extension), and all 11 Playwright journey specs + the flag-off parity invariant (per D-14 + Nyquist invariant #2). This closes every one of the 13 REQs for the phase.

Purpose: The journey specs are the Nyquist enforcement layer — every REQ click-count target must pass CI. The flag-off parity spec locks the Phase 51 safety net. SHARED-01 and ADMIN-01 round out the remaining friction fixes.

Output: 3 code changes (drawer, admin shell, 2 page-level deep-link effects), 2 unit test files, 11 Playwright spec files, 1 expanded flag-off parity spec, up to 1 dependency install (focus-trap-react if Drawer lacks a trap today), 2 i18n keys for ADMIN-01 toast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-per-journey-friction-fixes/52-CONTEXT.md
@.planning/phases/52-per-journey-friction-fixes/52-RESEARCH.md
@.planning/phases/52-per-journey-friction-fixes/52-VALIDATION.md
@.planning/v5.0-USER-JOURNEYS.md
@.planning/ui-reviews/UX-AUDIT-PERSONAS.md
@.planning/ui-reviews/UI-RESTRUCTURE-PLAN-v2.md
@src/components/drawer/Drawer.tsx
@src/components/drawer/PlanVsActualDrawer.tsx
@src/components/drawer/usePlanVsActualDrawer.tsx
@src/app/(app)/pm/projects/[projectId]/page.tsx
@src/app/(app)/rd/page.tsx
@src/components/admin/AdminRegisterPageShell.tsx
@src/hooks/use-admin-registers.ts
@src/lib/errors.ts
@src/app/(app)/layout.tsx
@e2e/helpers/persona-setup.ts
@e2e/helpers/click-counter.ts
@e2e/helpers/a11y.ts
@e2e/lib/seed.ts
@messages/sv.json
@messages/en.json

<interfaces>
<!-- Journey click-count targets (from UX-AUDIT-PERSONAS.md §Click-Count Summary). Enforce verbatim. -->

| Spec | Journey | Click-count target |
|---|---|---|
| `e2e/pm/1a-monday-checkin.spec.ts` | 1A PM Monday check-in | `<= 2` |
| `e2e/pm/1b-submit-wish.spec.ts` | 1B PM submit wish | `<= 3` |
| `e2e/pm/1c-rejected-wish.spec.ts` | 1C PM rejected wish | `<= 2` |
| `e2e/pm/1d-historic-edit.spec.ts` | 1D Historic edit (per 4-combo: past fires dialog = 3; future = 2) | `<= 3` |
| `e2e/line-manager/2a-capacity-overview.spec.ts` | 2A LM capacity overview | `<= 1` |
| `e2e/line-manager/2b-approve-reject.spec.ts` | 2B LM approve via badge | `<= 1` |
| `e2e/line-manager/2c-direct-edit.spec.ts` | 2C LM direct edit / project breakdown | (verify LM-02 cells; target not asserted per AUDIT) |
| `e2e/staff/3a-check-schedule.spec.ts` | 3A Staff schedule | `<= 0` (landing renders, no clicks needed) |
| `e2e/rd/4a-portfolio-overview.spec.ts` | 4A R&D portfolio | `<= 0` (zoom clicks are orthogonal — see note below) |
| `e2e/rd/4b-overcommit-drilldown.spec.ts` | 4B Overcommit drill | `<= 1` (red-cell click) |
| `e2e/admin/5b-archive-dependent.spec.ts` | 5B Admin archive dependent | `<= 2` |

Spec skeleton pattern (replicate for every journey):
```ts
import { test, expect } from '../fixtures/test-base';
import { personaAs } from '../helpers/persona-setup';
import { resetClickCount, getClickCount } from '../helpers/click-counter';
import { checkA11y } from '../helpers/a11y';

test.describe('Journey 1A — PM Monday check-in', () => {
  test('reaches drawer within 2 clicks', async ({ page }) => {
    await personaAs(page, 'pm');
    await page.goto('/pm');
    await resetClickCount(page);

    // click 1: project card (auto-redirected if single project; else click the card)
    // click 2: Erik's cell → opens drawer
    // ... journey-specific content assertions ...

    await expect(page.locator('[role="dialog"]')).toBeVisible();
    expect(await getClickCount(page)).toBeLessThanOrEqual(2);
    await checkA11y(page); // zero axe violations
  });

  test('deep-link opens drawer without extra clicks', async ({ page }) => {
    await personaAs(page, 'pm');
    await page.goto(`/pm/projects/<seed-project-id>?drawer=person-month&personId=<seed-person-id>&month=2026-06`);
    await expect(page.locator('[role="dialog"]')).toBeVisible();
  });
});
```

Drawer deep-link effect (per D-11, RESEARCH §SHARED-01 lines 329-365):
```tsx
// inside /pm/projects/[projectId]/page.tsx and /rd/page.tsx
const searchParams = useSearchParams();
const drawer = usePlanVsActualDrawer();
useEffect(() => {
  if (searchParams.get('drawer') !== 'person-month') return;
  const personId = searchParams.get('personId');
  const month = searchParams.get('month');
  if (!personId || !month) return;
  drawer.open({
    mode: 'daily',
    personId,
    projectId, // from useParams on PM, from rowId/derived on RD
    monthKey: month,
    personName: '', projectName: '', monthLabel: month, // drawer fills labels on fetch
  });
}, [searchParams, drawer, projectId]);
```

ESC strip-params close handler (patch `PlanVsActualDrawer.tsx`):
```ts
function handleClose() {
  store.close();
  const params = new URLSearchParams(searchParams.toString());
  params.delete('drawer');
  params.delete('personId');
  params.delete('month');
  const qs = params.toString();
  router.replace(qs ? `${pathname}?${qs}` : pathname, { scroll: false });
}
```

ADMIN-01 toast body (per D-12 + Q1 — kind-count only, no per-row data):
```tsx
// inside AdminRegisterPageShell.handleArchive catch block
import { toast } from 'sonner';
import { useTranslations } from 'next-intl';
// ...
toast.error(
  <div>
    <p>{t('dependentRows.title', { total })}</p>
    <details>
      <summary>{t('dependentRows.expand')}</summary>
      <ul>
        {Object.entries(blockers).map(([kind, count]) => (
          <li key={kind}>{t(`dependentRows.kind.${kind}`, { count })}</li>
        ))}
      </ul>
    </details>
  </div>,
  { duration: Infinity, dismissible: true }
);
```

DependentRowsError schema (verified, DO NOT extend per Q1):
```ts
// src/lib/errors.ts
export class DependentRowsError extends Error {
  code = 'DEPENDENT_ROWS_EXIST';
  blockers: Record<string, number>; // kind → count aggregation. PHASE 52 DOES NOT CHANGE THIS.
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SHARED-01 — drawer deep-link + ESC strip-params + focus trap (per D-11 + Q5 resolution)</name>
  <files>src/components/drawer/PlanVsActualDrawer.tsx, src/components/drawer/usePlanVsActualDrawer.tsx, src/components/drawer/Drawer.tsx, src/components/drawer/__tests__/plan-vs-actual-drawer.deeplink.test.tsx, src/app/(app)/pm/projects/[projectId]/page.tsx, src/app/(app)/rd/page.tsx, package.json</files>
  <behavior>
    - Test 1 (deep-link open on PM page): mount `<PmProjectTimelinePageInner>` at URL `?drawer=person-month&personId=u1&month=2026-06` → `drawer.open` called with matching payload.
    - Test 2 (deep-link ignored on bad params): `?drawer=person-month&personId=u1` (missing month) → no drawer open.
    - Test 3 (ESC strips params): drawer open at URL with drawer params → Escape keydown → `router.replace(pathname)` called with params removed.
    - Test 4 (focus trap while open): Tab key cycles within the drawer; last focusable element → Tab → wraps to first (using whatever focus-trap primitive is installed).
    - Test 5 (drawer open works on /rd page too): same as test 1 for `/rd`.
  </behavior>
  <read_first>
    - src/components/drawer/Drawer.tsx (lines 33-40: current keydown Escape binding; VERIFY whether focus trap exists — grep for `focus` / `tabindex` / `focus-trap` / `inert`)
    - src/components/drawer/PlanVsActualDrawer.tsx (lines 136-143: existing close handler; line 167: role="dialog" + aria-label)
    - src/components/drawer/usePlanVsActualDrawer.tsx (lines 46-63: store / open / close contract)
    - src/app/(app)/pm/projects/[projectId]/page.tsx (lines 53-107: `PmProjectTimelinePageInner` — mount point for deep-link effect)
    - src/app/(app)/rd/page.tsx (modified by Plan 04 — preserve those changes; add drawer-deep-link effect alongside)
    - .planning/phases/52-per-journey-friction-fixes/52-CONTEXT.md D-11
    - .planning/phases/52-per-journey-friction-fixes/52-RESEARCH.md §SHARED-01 (lines 328-365) + §Environment Availability (focus-trap-react missing) + Pitfall #1 (Suspense)
  </read_first>
  <action>
    Per D-11 and resolved Q5:

    1. **First, verify the focus-trap status of `src/components/drawer/Drawer.tsx`** (Q5 resolution). Grep for focus-related logic. Three outcomes:
       - (a) Drawer already has a working focus trap → no install needed; proceed to deep-link work.
       - (b) Drawer has no focus trap → install `focus-trap-react` as a dependency:
         ```
         pnpm add focus-trap-react
         ```
         Wrap the drawer content in `<FocusTrap active={isOpen}>` inside `Drawer.tsx`.
       - (c) Drawer partially traps (e.g. binds keydown but doesn't manage focus on open) → install `focus-trap-react` (b path).
       Do NOT migrate to native `<dialog>` — out of scope per Q5.

    2. Edit `src/components/drawer/PlanVsActualDrawer.tsx`:
       - Import `useSearchParams`, `usePathname`, `useRouter` from `next/navigation`.
       - Modify the existing close handler (lines ~136-143) to also strip `drawer`, `personId`, `month` query params via `router.replace` — verbatim from the `<interfaces>` block above.

    3. Edit `src/app/(app)/pm/projects/[projectId]/page.tsx`:
       - Inside `PmProjectTimelinePageInner`, add the `useEffect` verbatim from the `<interfaces>` block above. Read `projectId` from `useParams()`.
       - Ensure the page is wrapped in `<Suspense>` if not already (Pitfall #1).

    4. Edit `src/app/(app)/rd/page.tsx` (preserving all Plan 04 changes):
       - Add the same `useEffect` for the drawer deep-link. On RD the `projectId` concept is different — derive it from the clicked row or pass the `rowId` / `scopeId` in its place. If the RD drawer uses a different mode (project-person-breakdown), adapt the `mode:` value accordingly.

    5. Create `src/components/drawer/__tests__/plan-vs-actual-drawer.deeplink.test.tsx` covering tests 1-5. Use @testing-library + a search-params mock. For focus trap (test 4), use `userEvent.tab()` and assert `document.activeElement` cycles within the drawer subtree.
  </action>
  <verify>
    <automated>pnpm test --run src/components/drawer src/app/(app)/pm/projects src/app/(app)/rd</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "searchParams.get\('drawer'\)" src/app/(app)/pm/projects/\[projectId\]/page.tsx` returns ≥ 1 line
    - `grep -nE "searchParams.get\('drawer'\)" src/app/(app)/rd/page.tsx` returns ≥ 1 line
    - `grep -n "params.delete('drawer')" src/components/drawer/PlanVsActualDrawer.tsx` returns ≥ 1 line (ESC strip-params)
    - Drawer focus trap present: either `grep -n "focus-trap-react\|FocusTrap" src/components/drawer/Drawer.tsx` returns ≥ 1 line OR a test captures cycling behavior without the library (pre-existing)
    - `pnpm test --run src/components/drawer/__tests__/plan-vs-actual-drawer.deeplink.test.tsx` exits 0 with ≥ 5 tests
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Drawer opens on mount when URL carries drawer params (both /pm/projects/[id] and /rd); ESC strips params; focus trap verified (library installed if needed); 5+ unit tests green. Implements SHARED-01 / D-11 / Q5.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ADMIN-01 — toast.error with kind-count `<details>` (Q1 resolution, no backend change)</name>
  <files>src/components/admin/AdminRegisterPageShell.tsx, src/components/admin/__tests__/admin-archive-toast.test.tsx, messages/sv.json, messages/en.json</files>
  <behavior>
    - Test 1 (archive success): `handleArchive` resolves without error → no toast shown, banner cleared.
    - Test 2 (archive dependent rows): `handleArchive` throws `DependentRowsError` with `blockers={allocations: 12, activeProposals: 3}` → `toast.error` invoked with a React node; node contains `<details>` + `<summary>` + list of `Allokeringar: 12` + `Aktuella förslag: 3` (localized).
    - Test 3 (other error): `handleArchive` throws non-DependentRowsError → existing error path (NOT the toast), no regression.
    - Test 4 (flag independence): ADMIN-01 is NOT flag-gated per CONTEXT (no decision locks it to `uiV6PerJourney`). Verify toast fires regardless of flag state.
  </behavior>
  <read_first>
    - src/components/admin/AdminRegisterPageShell.tsx (lines 145-164: current `setBanner` path in `handleArchive` catch block; lines 42-60: `useBlockerFormatter`)
    - src/hooks/use-admin-registers.ts (DependentRowsError handling + parseErrorBody)
    - src/lib/errors.ts (`DependentRowsError` — Q1 confirms: `blockers: Record<string, number>` is NOT extended)
    - src/app/(app)/layout.tsx (line 44: Sonner `<Toaster>` mount) + Sonner API
    - .planning/phases/52-per-journey-friction-fixes/52-CONTEXT.md D-12 (reinterpreted per Q1)
    - .planning/phases/52-per-journey-friction-fixes/52-RESEARCH.md §ADMIN-01 (lines 367-395) + Pitfall #5 (use `toast.custom` or React-node `toast.error` if sonner clips `<details>`)
  </read_first>
  <action>
    Per D-12 and resolved Q1 (kind-counts only, no row-level data, no backend change):

    1. Edit `src/components/admin/AdminRegisterPageShell.tsx`:
       - Import `toast` from `sonner`.
       - In `handleArchive`'s `catch` block where `DependentRowsError` is caught:
         - Keep any existing banner path for now (can be removed in a polish PR) OR replace the `setBanner(...)` call with `toast.error(<DependentRowsContent blockers={err.blockers} t={t} />, { duration: Infinity, dismissible: true })`.
         - Create a small inline component `DependentRowsContent` (or helper function returning JSX) that renders:
           - Title: `t('dependentRows.title', { total })`
           - `<details><summary>{t('dependentRows.expand')}</summary><ul>...kinds...</ul></details>`
         - Where `total = Object.values(blockers).reduce((a,b)=>a+b, 0)` and each `<li>` is `t('dependentRows.kind.' + kind, { count })`.
       - Per Pitfall #5: if layout testing shows sonner clips `<details>` content, switch to `toast.custom((id) => <CustomNode />, { duration: Infinity })`. Test 2 should visually verify `<details>` is expandable.

    2. Add i18n keys in both sv.json and en.json under `v5.admin.dependentRows.*`:
       - `title` — sv `"Kan inte arkivera projekt — {total} aktiva beroenden"`, en `"Cannot archive project — {total} active dependencies"`
       - `expand` — sv `"Visa detaljer"`, en `"Show details"`
       - `kind.allocations` — sv `"{count, plural, one{1 allokering} other{# allokeringar}}"`, en `"{count, plural, one{1 allocation} other{# allocations}}"`
       - `kind.activeProposals` (and any other blocker kinds present in `DependentRowsError.blockers` — grep `collectBlockers` in `src/features/admin/register.service.ts` for the full set)

    3. Create `src/components/admin/__tests__/admin-archive-toast.test.tsx` with tests 1-4. Mock sonner's `toast.error` to capture the React node argument and assert its HTML structure.
  </action>
  <verify>
    <automated>pnpm test --run src/components/admin/__tests__/admin-archive-toast.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "toast\.(error|custom)" src/components/admin/AdminRegisterPageShell.tsx` returns ≥ 1 line
    - `grep -n "<details>" src/components/admin/AdminRegisterPageShell.tsx` returns ≥ 1 line
    - `grep -n "v5.admin.dependentRows" messages/sv.json messages/en.json` returns ≥ 3 lines per file (title, expand, at least one kind)
    - No modification to `src/lib/errors.ts` DependentRowsError schema: `git diff src/lib/errors.ts` is empty (Q1 honored)
    - `pnpm test --run src/components/admin/__tests__/admin-archive-toast.test.tsx` exits 0 with ≥ 4 tests
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>toast.error replaces banner for DependentRowsError; `<details>` lists kind-counts from `Record<string, number>` (Q1); no backend change; tests verify kind-count render path. Implements ADMIN-01 / D-12 / Q1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Ship 11 Playwright journey specs with click-count + axe assertions (D-14)</name>
  <files>e2e/pm/1a-monday-checkin.spec.ts, e2e/pm/1b-submit-wish.spec.ts, e2e/pm/1c-rejected-wish.spec.ts, e2e/pm/1d-historic-edit.spec.ts, e2e/line-manager/2a-capacity-overview.spec.ts, e2e/line-manager/2b-approve-reject.spec.ts, e2e/line-manager/2c-direct-edit.spec.ts, e2e/staff/3a-check-schedule.spec.ts, e2e/rd/4a-portfolio-overview.spec.ts, e2e/rd/4b-overcommit-drilldown.spec.ts, e2e/admin/5b-archive-dependent.spec.ts</files>
  <read_first>
    - .planning/v5.0-USER-JOURNEYS.md (full journey narratives §Persona 1-5)
    - .planning/ui-reviews/UX-AUDIT-PERSONAS.md §Click-Count Summary (target targets per journey)
    - e2e/helpers/click-counter.ts (from Plan 01)
    - e2e/helpers/a11y.ts (from Plan 01)
    - e2e/helpers/persona-setup.ts (LM needs `departmentId` — Pitfall #11)
    - e2e/fixtures/test-base.ts + e2e/fixtures/persona.ts (seed contract — confirms which seed IDs are deterministic)
    - e2e/lib/seed.ts (confirm fixture IDs for: a project with 1-1 PM assignment for journey 1A; LM dept with ≥3 pending proposals for 2B; overcommit scenario for 4B; archivable project WITH blocking allocations for 5B)
    - .planning/phases/52-per-journey-friction-fixes/52-CONTEXT.md D-14
    - .planning/phases/52-per-journey-friction-fixes/52-RESEARCH.md §Playwright spec organization + §Click-tracker wiring §Attribute annotation targets
  </read_first>
  <action>
    Create 11 Playwright spec files, each following the spec skeleton in the `<interfaces>` block above. Every spec:
    - Uses `personaAs(page, kind, extras?)` — LM specs pass `{ departmentId }` (Pitfall #11).
    - Calls `await resetClickCount(page);` after navigating to the journey start.
    - Performs journey actions ONLY via user interaction (click / keyboard), NOT via `page.evaluate`.
    - Ends with:
      ```ts
      expect(await getClickCount(page)).toBeLessThanOrEqual(<target>);
      await checkA11y(page);
      ```
    - Adds journey-specific content assertions (drawer visible, correct data, etc.).

    Per-spec requirements:

    **`e2e/pm/1a-monday-checkin.spec.ts`** (target: 2):
    - Test 1: seed 1 PM project → visit `/pm` → auto-redirects to `/pm/projects/<id>` → click Erik's March cell → drawer opens. Assert click count ≤ 2.
    - Test 2 (SHARED-01): visit `/pm/projects/<id>?drawer=person-month&personId=<pid>&month=2026-06` → drawer open without any click.

    **`e2e/pm/1b-submit-wish.spec.ts`** (target: 3):
    - Navigate to a PM project page → click Sara's cell → type value → click Submit → wish created. Assert click count ≤ 3.

    **`e2e/pm/1c-rejected-wish.spec.ts`** (target: 2):
    - Seed PM with 1 rejected proposal → visit `/pm` (or app shell) → click chip (href `/pm/wishes?tab=rejected`) → click rejected wish card. Assert click count ≤ 2.

    **`e2e/pm/1d-historic-edit.spec.ts`** (target: 3 — 4-combo matrix):
    - 4 tests: PM past (dialog fires, 3 clicks incl. confirm) / PM future (2 clicks, no dialog) / LM past (dialog fires) / LM future (no dialog). Use `NC_TEST_NOW` env override to force server month (per PM-03 infra).

    **`e2e/line-manager/2a-capacity-overview.spec.ts`** (target: 1):
    - Navigate to `/line-manager` (dept auto-scoped via seed departmentId) → click a person row → heatmap drill. Assert click count ≤ 1.

    **`e2e/line-manager/2b-approve-reject.spec.ts`** (target: 1):
    - Seed 3 pending proposals in the LM dept → visit `/line-manager` → badge visible with "3" → click badge (1 click) → approval-queue page loads → click Approve on first row. Note: approval click consumed; assert the badge-click alone ≤ 1 if target is landing-to-queue, or extend to 2 if Approve is counted.
    - Follow UX-AUDIT column (target 1) — verify by asserting the badge-click lands on `/line-manager/approval-queue` within 1 click.

    **`e2e/line-manager/2c-direct-edit.spec.ts`** (LM-02 coverage):
    - Visit `/line-manager/timeline` → click first person-row disclosure triangle → assert N project rows appear via `getByTestId(/lm-project-label-/)`. Assert per-project hours visible.

    **`e2e/staff/3a-check-schedule.spec.ts`** (target: 0):
    - Visit `/` or `/staff` → grid renders. Assert click count === 0.
    - Assert no editable cells: `await expect(page.locator('[data-testid="plan-vs-actual-cell"][data-editable="true"]')).toHaveCount(0);`

    **`e2e/rd/4a-portfolio-overview.spec.ts`** (target: 0 with zoom — matrix):
    - 9 sub-tests: for year in [2026, 2027, 2028] × zoom in [month, quarter, year]:
      - Force clock via `NC_TEST_NOW=<year>-01-15`.
      - Visit `/rd` → click `[data-testid="zoom-<level>"]` (zoom clicks are instrumental, not counted as journey clicks — reset after zoom).
      - Assert column count: month → 12 per year, quarter → 4 per year, year → 1 per year (2026 must render ONLY 1 column).
      - Reset click count AFTER zoom → assert remaining click count === 0.

    **`e2e/rd/4b-overcommit-drilldown.spec.ts`** (target: 1):
    - Seed overcommit scenario → visit `/rd` → click red cell (1 click) → OvercommitDialog renders with both sections. Assert click count ≤ 1.
    - Additional test: deep-link via `?drawer=person-month&...` opens drawer on `/rd` (SHARED-01 coverage).

    **`e2e/admin/5b-archive-dependent.spec.ts`** (target: 2):
    - Seed a project with active allocations → visit `/admin/projects` → click project row (1) → click Archive (2) → toast.error appears with `<details>`; expand details → assert list entries match fixture blockers. Assert click count ≤ 2.
    - NOTE: `e2e/admin/` directory does not exist today per Plan 01 Wave 0 requirements — this spec creates the directory.

    All specs must run independently (no cross-test state) and must not mutate shared seed data beyond their own tenant/test isolation.
  </action>
  <verify>
    <automated>pnpm test:e2e e2e/pm e2e/line-manager e2e/staff e2e/rd e2e/admin</automated>
  </verify>
  <acceptance_criteria>
    - All 11 spec files exist at the exact paths listed in `files_modified`
    - `grep -c "getClickCount" e2e/pm e2e/line-manager e2e/staff e2e/rd e2e/admin -r` returns ≥ 11 (one assertion per spec minimum)
    - `grep -c "checkA11y" e2e/pm e2e/line-manager e2e/staff e2e/rd e2e/admin -r` returns ≥ 11 (axe gate per spec)
    - `grep -cE "toBeLessThanOrEqual\((0|1|2|3)\)" e2e/pm e2e/line-manager e2e/staff e2e/rd e2e/admin -r` returns ≥ 10 (click-count target per spec; 2c may not assert)
    - `pnpm test:e2e --list | grep -cE "1[abcd]-|2[abc]-|3a-|4[ab]-|5b-"` returns 11
    - `pnpm test:e2e e2e/pm e2e/line-manager e2e/staff e2e/rd e2e/admin` exits 0 (all 11 journey specs + sub-tests green)
  </acceptance_criteria>
  <done>11 Playwright specs shipped; each enforces click-count target and axe zero-violations; LM specs pass `departmentId` fixture; 1D runs 4-combo matrix; 4A runs 9-cell zoom × year matrix; 5B verifies toast `<details>`. Implements D-14.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Expand `e2e/_invariants/flag-off-parity.spec.ts` + LM-02 and other cross-journey invariants (D-15 + Nyquist invariant #2)</name>
  <files>e2e/_invariants/flag-off-parity.spec.ts, e2e/lib/seed.ts</files>
  <read_first>
    - e2e/_invariants/flag-off-parity.spec.ts (scaffold from Plan 01 — 5 landing tests)
    - e2e/lib/seed.ts (flag seed insertion — needs a helper to toggle flag per-test)
    - .planning/phases/52-per-journey-friction-fixes/52-VALIDATION.md §Cross-Journey Invariants (6 invariants)
    - .planning/phases/52-per-journey-friction-fixes/52-CONTEXT.md D-15
  </read_first>
  <action>
    Expand the flag-off parity spec from Plan 01 into the full Nyquist invariant enforcement:

    1. Add a `setFlag(enabled: boolean)` helper in `e2e/lib/seed.ts` or a new `e2e/helpers/flag-toggle.ts` that UPDATEs the `featureFlags` row for `uiV6PerJourney` in the test tenant. Every invariant test resets the flag to `false` in its `beforeEach` and to `true` (or default) in `afterEach`.

    2. Flesh out `e2e/_invariants/flag-off-parity.spec.ts` — one `describe` block per persona, each running:
       - **Scenario A (PM)**: flag OFF → visit `/pm` → no chip in top-bar, no auto-redirect from `/pm` even with 1 project (Phase 51 behavior: cards grid always renders).
       - **Scenario B (LM)**: flag OFF → visit `/line-manager` → no approval-queue badge; switcher optgroup has no `(N)` suffix.
       - **Scenario C (Staff)**: flag OFF → visit `/staff` → cells render identical to Phase 51. `data-editable="false"` still present (because STAFF-01 attribute is always-on; the staff page is read-only regardless of flag — this is fine, just document in a comment).
       - **Scenario D (R&D)**: flag OFF → visit `/rd` → zoom control still renders, but aggregation stays on month (Phase 51 baseline). Click a red cell → opens existing drawer (not OvercommitDialog).
       - **Scenario E (Admin)**: flag OFF → archive flow → ADMIN-01 toast still shows (ADMIN-01 is not flag-gated per CONTEXT; explicit non-regression).

    3. Add invariant sub-tests for Nyquist invariants #3 and #5 (VALIDATION.md):
       - Invariant #3 (ISO 53-week correctness): unit-level test already green (per RESEARCH line 535). Add a thin Playwright smoke: 2026 year mode column header text contains exactly "2026" (not "2026 / 2027").
       - Invariant #5 (tenant isolation): call `/api/v5/proposals/queue/count` via `page.request` with no auth → 401. With auth for org A and `departmentId` from org B's seed → 200 with `{ count: 0 }`.

    4. Ensure the axe check (invariant #6) runs on each of the 5 persona landings in the flag-ON state (inside journey specs from Task 3, not this invariant file — this file is flag-OFF only).
  </action>
  <verify>
    <automated>pnpm test:e2e e2e/_invariants/flag-off-parity.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test\(" e2e/_invariants/flag-off-parity.spec.ts` returns ≥ 10 (5 persona scenarios + invariants #3 + #5 sub-tests)
    - `grep -n "uiV6PerJourney" e2e/_invariants/flag-off-parity.spec.ts` returns ≥ 2 lines (flag toggle in beforeEach + afterEach)
    - `grep -n "setFlag\|toggleFlag" e2e/_invariants/flag-off-parity.spec.ts e2e/lib/seed.ts e2e/helpers/flag-toggle.ts 2>/dev/null` returns ≥ 2 lines
    - `pnpm test:e2e e2e/_invariants/flag-off-parity.spec.ts` exits 0
  </acceptance_criteria>
  <done>Flag-off parity spec covers all 5 personas' invariants + ISO 53-week smoke + tenant isolation; a helper toggles the flag per test. Implements D-15 parity + Nyquist invariants #2, #3, #5.</done>
</task>

</tasks>

<verification>
Phase 52 Wave 4 full checks:
1. `pnpm typecheck` exits 0
2. `pnpm test --run src/components/drawer src/components/admin` exits 0
3. `pnpm test:e2e` exits 0 across all 11 journey specs + flag-off parity spec
4. Every journey spec asserts `getClickCount(page) <= target` per the table in the `<interfaces>` block
5. axe-core runs on each persona landing with zero violations under flag-ON
6. Flag-OFF parity holds for all 5 personas
7. No regression to Phase 51 (flag-off sub-tests green)
</verification>

<success_criteria>
- SHARED-01 D-11 + Q5 fully implemented: deep-link opens drawer on /pm/projects/[id] and /rd; ESC strips params; focus trap verified.
- ADMIN-01 D-12 + Q1 fully implemented: toast.error with kind-count `<details>` block (no backend change, `DependentRowsError.blockers` schema preserved).
- All 11 Playwright specs exist and green under flag-ON; each enforces its click-count target from UX-AUDIT.
- Flag-off parity invariant spec guarantees Phase 51 behavior is preserved when `uiV6PerJourney=false`.
- All 13 REQ IDs (PM-01..PJ-FLAG) now have at least one passing automated test across Plans 02-05.
</success_criteria>

<output>
After completion, create `.planning/phases/52-per-journey-friction-fixes/52-05-SUMMARY.md` documenting: SHARED-01 deep-link effect mount sites, focus-trap status (installed or pre-existing), ADMIN-01 toast-node structure, 11 spec file list with per-spec click-count targets and axe assertions, flag-off parity coverage matrix (each persona × each flag-gated behavior). Also report the final Nyquist coverage status: 13 REQs × test types from VALIDATION.md.
</output>
