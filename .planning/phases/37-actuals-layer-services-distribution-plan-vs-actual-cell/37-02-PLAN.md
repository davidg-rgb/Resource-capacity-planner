---
phase: 37-actuals-layer-services-distribution-plan-vs-actual-cell
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - src/components/timeline/PlanVsActualCell.tsx
  - src/components/timeline/PlanVsActualCell.module.css
  - src/components/timeline/__tests__/PlanVsActualCell.test.tsx
  - src/components/drawer/PlanVsActualDrawer.tsx
  - src/components/drawer/usePlanVsActualDrawer.ts
  - src/components/drawer/__tests__/PlanVsActualDrawer.test.tsx
  - src/features/actuals/use-actuals-cell.ts
  - src/messages/keys.ts
  - src/messages/sv.json
  - src/messages/en.json
  - drizzle/seed.ts
autonomous: true
requirements: [ACT-03, ACT-04]
must_haves:
  truths:
    - "PlanVsActualCell renders three numbers: planned, actual, delta"
    - "Cell color is green when actual ≤ planned, red when actual > planned, neutral when actual === planned (within 0.01h)"
    - "Cell color is grey/neutral when actual is null (no actual logged yet)"
    - "Cell is a single reusable component imported by PM, Line Mgr, Staff, R&D timelines (verified by component test, not by usage scan)"
    - "Clicking a cell opens the PlanVsActualDrawer showing daily plan vs actual rows for that person/project/period"
    - "Drawer reads from actuals.read.getDailyRows + the existing allocations slice — no new API surface"
    - "Every user-facing string in the cell + drawer comes from useTranslations('v5.*'), not a literal — eslint no-restricted-syntax JSXText guard passes"
    - "Seed fixtures include enough actual_entries rows that the drill-down drawer renders a meaningful daily breakdown for at least one (person, project, month) tuple in the demo org"
  artifacts:
    - path: "src/components/timeline/PlanVsActualCell.tsx"
      provides: "Reusable plan-vs-actual cell"
      exports: ["PlanVsActualCell", "PlanVsActualCellProps"]
    - path: "src/components/drawer/PlanVsActualDrawer.tsx"
      provides: "Drill-down drawer with daily breakdown"
      exports: ["PlanVsActualDrawer"]
    - path: "src/components/drawer/usePlanVsActualDrawer.ts"
      provides: "Hook for opening/closing the drawer with cell context"
      exports: ["usePlanVsActualDrawer"]
    - path: "src/features/actuals/use-actuals-cell.ts"
      provides: "TanStack Query hook returning {planned, actual, delta} for a (person,project,monthKey) tuple"
      exports: ["useActualsCell"]
  key_links:
    - from: "src/components/timeline/PlanVsActualCell.tsx"
      to: "src/messages/keys.ts"
      via: "useTranslations('v5.cell.*')"
      pattern: "v5\\.cell"
    - from: "src/components/drawer/PlanVsActualDrawer.tsx"
      to: "src/features/actuals/actuals.read.ts"
      via: "getDailyRows fetch"
      pattern: "getDailyRows"
    - from: "src/features/actuals/use-actuals-cell.ts"
      to: "src/features/actuals/actuals.read.ts"
      via: "aggregateByMonth"
      pattern: "aggregateByMonth"
---

<objective>
Ship the reusable plan-vs-actual cell + drill-down drawer that every v5.0 persona timeline depends on. Wires the cell to the read model from 37-01, adds the i18n keys for cell + drawer strings under the v5 namespace, and seeds enough actual_entries rows that the drawer has real data to render in the demo org.

Purpose: ACT-03 and ACT-04 are the visible payoff of the actuals layer. Persona phases 40/41/42 will import this cell verbatim.

Output: Cell component + drawer + hook + i18n keys + seeded fixtures + UI tests covering TC-UI-001..002.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v5.0-ARCHITECTURE.md
@.planning/v5.0-USER-JOURNEYS.md
@.planning/phases/34-foundations-personas-i18n-catalog-historic-edit-helper/34-01-PLAN.md
@src/messages/keys.ts
@src/messages/sv.json
@src/messages/en.json
@drizzle/seed.ts
@eslint.config.mjs

<interfaces>
<!-- From 37-01 (consume directly) -->
```ts
// src/features/actuals/actuals.read.ts
export function aggregateByMonth(orgId, filter): Promise<Array<{
  personId: string; projectId: string; monthKey: string; hours: number;
}>>;
export function getDailyRows(orgId, args: {
  personId: string; projectId: string; monthKey: string;
}): Promise<Array<{ date: string; hours: number; source: 'manual'|'import' }>>;
```

<!-- The existing allocations slice (v4.0) is the source of "planned" -->
<!-- See src/features/allocations or wherever the v4 grid reads from. -->
<!-- The plan side is monthly-grain (one row per person/project/month). -->
```ts
// expected (verify in code; if the function name differs, use what's there):
export function getMonthlyAllocations(orgId, filter): Promise<Array<{
  personId: string; projectId: string; monthKey: string; plannedHours: number;
}>>;
```

<!-- i18n contract (FOUND-V5-05) -->
// Every v5 component path globbed in eslint.config.mjs is forbidden from
// containing JSX text literals. Add new keys to:
//   src/messages/keys.ts (typed catalog under K.v5.cell.* and K.v5.drawer.*)
//   src/messages/sv.json (Swedish, primary)
//   src/messages/en.json (English, fallback)
// keys.test.ts enforces parity across all three.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useActualsCell hook + i18n keys for cell &amp; drawer</name>
  <files>
    src/features/actuals/use-actuals-cell.ts,
    src/messages/keys.ts,
    src/messages/sv.json,
    src/messages/en.json
  </files>
  <behavior>
    - useActualsCell({ orgId, personId, projectId, monthKey }) returns { planned, actual, delta, isLoading, error }
    - planned reads from the v4.0 allocations slice for that (person, project, monthKey)
    - actual reads from aggregateByMonth() (37-01) filtered to the same tuple
    - delta = actual - planned (signed)
    - When no actual rows exist for the tuple, actual is null and delta is null
    - i18n keys exist under v5.cell.* and v5.drawer.* in keys.ts AND both sv.json + en.json with parity (keys.test.ts passes)
  </behavior>
  <action>
    1. **i18n keys** — extend `src/messages/keys.ts`:
       Under `K.v5`, add nested objects:
       ```ts
       cell: {
         planned: 'cell.planned',          // "Planerat" / "Planned"
         actual: 'cell.actual',            // "Utfall" / "Actual"
         delta: 'cell.delta',              // "Avvikelse" / "Delta"
         noActual: 'cell.noActual',        // "—" / "—" (placeholder dash, but routed through i18n so designers can change it)
         overBy: 'cell.overBy',            // "+{hours}h" / "+{hours}h"
         underBy: 'cell.underBy',          // "-{hours}h" / "-{hours}h"
         onPlan: 'cell.onPlan',            // "På plan" / "On plan"
       },
       drawer: {
         title: 'drawer.title',            // "Plan vs utfall — {person}, {project}, {month}" / "Plan vs actual — {person}, {project}, {month}"
         dateColumn: 'drawer.dateColumn',  // "Datum" / "Date"
         plannedColumn: 'drawer.plannedColumn',
         actualColumn: 'drawer.actualColumn',
         deltaColumn: 'drawer.deltaColumn',
         empty: 'drawer.empty',            // "Inga utfallsrader för denna period." / "No actual rows for this period."
         close: 'drawer.close',            // "Stäng" / "Close"
       },
       ```
       Add the matching string values under `v5.cell.*` and `v5.drawer.*` namespaces in BOTH `src/messages/sv.json` and `src/messages/en.json`. Run the existing keys parity test (`pnpm test src/messages/__tests__/keys.test.ts` or whatever Phase 34 named it) to confirm.

    2. **Hook** (`src/features/actuals/use-actuals-cell.ts`):
       Use TanStack Query (`useQuery`). Two queries: one for plannedHours from the v4 allocations slice, one for actual from `aggregateByMonth`. Combine with `useMemo`. Return `{ planned: number, actual: number | null, delta: number | null, isLoading, error }`. queryKey: `['actuals-cell', orgId, personId, projectId, monthKey]`.

       If the v4.0 allocations read is server-only / RSC, expose it via a thin `/api/v5/actuals/cell` endpoint OR (preferred for this plan) add a server action `getCellData(orgId, personId, projectId, monthKey)` in `actuals.read.ts` that does both queries server-side and returns `{planned, actual}`. Pick whichever matches the existing v4 pattern — DO NOT introduce a new transport convention.

    3. Test the hook in `src/features/actuals/__tests__/use-actuals-cell.test.tsx` using @testing-library/react with a QueryClientProvider wrapper and a mocked server action / fetch. Three cases: (a) actual > planned → positive delta, (b) actual < planned → negative delta, (c) no actual rows → actual=null, delta=null.
  </action>
  <verify>
    <automated>pnpm test src/features/actuals/__tests__/use-actuals-cell.test.tsx &amp;&amp; pnpm test src/messages/</automated>
  </verify>
  <done>
    Hook returns the documented shape; i18n parity tests green; pnpm typecheck green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlanVsActualCell component + tests (TC-UI-001..002)</name>
  <files>
    src/components/timeline/PlanVsActualCell.tsx,
    src/components/timeline/PlanVsActualCell.module.css,
    src/components/timeline/__tests__/PlanVsActualCell.test.tsx
  </files>
  <behavior>
    - Renders three slots: planned, actual, delta
    - Color states (driven by data attribute, styled in CSS module):
        - data-state="under" when actual !== null && actual < planned → green tint
        - data-state="over" when actual !== null && actual > planned → red tint
        - data-state="on-plan" when actual !== null && |actual - planned| < 0.005 → neutral/blue
        - data-state="no-actual" when actual === null → grey/muted (just shows planned)
    - In editable mode (onCellEdit prop set), the planned number is an inline-editable input with 600ms debounce before firing onCellEdit (TC-UI-002)
    - In read-only mode (onCellEdit omitted), clicking the cell fires onCellClick({personId, projectId, monthKey}) (TC-UI-003 — wired even though out of scope for Phase 37 success criteria, costs nothing)
    - Zero hardcoded user-facing text — every label uses useTranslations('v5.cell')
    - Component is the SAME source file imported by PM/LineMgr/Staff/R&D timelines (verified by a component test that imports it and renders it)
  </behavior>
  <action>
    Build `PlanVsActualCell.tsx` as a presentational React component:

    ```tsx
    export interface PlanVsActualCellProps {
      planned: number;
      actual: number | null;
      delta: number | null;
      personId: string;
      projectId: string;
      monthKey: string;
      editable?: boolean;
      onCellEdit?: (next: number) => void;       // fires after 600ms debounce
      onCellClick?: (ctx: { personId: string; projectId: string; monthKey: string }) => void;
    }
    ```

    - Use `useTranslations('v5.cell')` from next-intl (or whatever Phase 34 wired) for ALL user-facing strings.
    - Compute `state` ('under' | 'over' | 'on-plan' | 'no-actual') with the rules in <behavior>.
    - Set `data-state={state}` on the root element. Style four color variants in `PlanVsActualCell.module.css` using CSS custom properties so dark mode / Stitch tokens stay overridable.
    - Use `tabular-nums` font-feature for the numbers (carry over the v4.0 dashboard convention).
    - Format hours with `{value.toFixed(1)}h` — single decimal is enough for cell density; the drawer shows 2-decimal precision.
    - Editable mode: render an `<input type="number" step="0.5">` initialized to `planned`. Wrap onCellEdit in a 600ms debounce (`setTimeout` with cleanup, or `useDebouncedCallback` from a small inline impl — do not pull a new dep). The debounced fire is what TC-UI-002 asserts.
    - Read-only mode: root element is a `<button type="button">` so it's keyboard-accessible; onClick fires onCellClick.

    **Tests** (`__tests__/PlanVsActualCell.test.tsx`):
    - Render with planned=40, actual=32 → expect data-state='under' and "8.0h" delta indicator
    - Render with planned=40, actual=48 → data-state='over'
    - Render with planned=40, actual=40 → data-state='on-plan'
    - Render with planned=40, actual=null → data-state='no-actual'
    - Editable: type a new value, advance fake timers 600ms, assert onCellEdit called once with the new value (TC-UI-002)
    - Read-only: omit onCellEdit, click root button, assert onCellClick called with the right context (TC-UI-001 / TC-UI-003)
    - i18n: assert that the label "Planerat" comes from useTranslations and the test mounts with the sv locale (use the existing test i18n provider from Phase 34)

    Run `pnpm lint src/components/timeline/PlanVsActualCell.tsx` to confirm the no-restricted-syntax JSXText guard passes (no hardcoded text).
  </action>
  <verify>
    <automated>pnpm test src/components/timeline/__tests__/PlanVsActualCell.test.tsx &amp;&amp; pnpm lint src/components/timeline/</automated>
  </verify>
  <done>
    All four data-state cases tested; debounced edit fires once; lint clean (no hardcoded JSX text); component exports the documented Props interface.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PlanVsActualDrawer + usePlanVsActualDrawer hook + seed fixtures</name>
  <files>
    src/components/drawer/PlanVsActualDrawer.tsx,
    src/components/drawer/usePlanVsActualDrawer.ts,
    src/components/drawer/__tests__/PlanVsActualDrawer.test.tsx,
    drizzle/seed.ts
  </files>
  <behavior>
    - usePlanVsActualDrawer() returns { open, close, isOpen, context }
    - context is { personId, projectId, monthKey } | null
    - Calling open(ctx) sets isOpen=true and stores context; close() resets both
    - PlanVsActualDrawer is a controlled component reading from the hook (or passed props directly) — accepts both patterns to keep persona screens flexible
    - When open, drawer fetches getDailyRows(orgId, context) via TanStack Query and renders a table: Date | Planned (per-day plan, evenly distributed from the month plan) | Actual | Delta
    - Empty state: "Inga utfallsrader för denna period." (sv) when getDailyRows returns []
    - Closes via Esc, Close button, or backdrop click
    - All strings via useTranslations('v5.drawer')
    - Seed: drizzle/seed.ts inserts ≥10 actual_entries rows for at least one (person, project, month) in the demo org so the drawer has real data when a developer runs `pnpm db:seed` and clicks a cell
  </behavior>
  <action>
    1. **Hook** (`usePlanVsActualDrawer.ts`):
       Tiny zustand store OR a React context provider — match whatever Phase 34's persona switcher used. Export `usePlanVsActualDrawer()` returning `{ open, close, isOpen, context }`.

    2. **Drawer** (`PlanVsActualDrawer.tsx`):
       - Reuse the existing v4.0 Drawer/Sheet primitive if one exists (check `src/components/ui/sheet.tsx` or similar — most shadcn-derived projects have one). If none exists, build a simple right-side fixed-position div with backdrop. **Do not introduce a new modal lib.**
       - Inside, fetch via TanStack Query keyed `['drawer-daily', orgId, personId, projectId, monthKey]` calling a thin server action `getDailyCellBreakdown(orgId, args)` that wraps `getDailyRows` from 37-01 and also returns the per-day "planned" value. The per-day planned is computed by calling `distribute(monthlyPlanned, workDaysInMonth(year, monthIndex).length)` and zipping by date — pure derived data, no new schema.
       - Render a `<table>` with `tabular-nums`. Empty state uses the i18n key.
       - Title format: `t('title', { person: personName, project: projectName, month: monthLabel })`. The drawer must accept `personName` / `projectName` / `monthLabel` as props OR as part of the context object (extend the hook's context type) — pick one and stick to it.
       - Closes on Esc (window keydown listener inside useEffect) and on Close button click.

    3. **Tests** (`__tests__/PlanVsActualDrawer.test.tsx`):
       - Mock the server action to return 5 day rows. Render the drawer open. Assert the table has 5 rows with the right dates and the right delta column values.
       - Mock empty array. Assert the empty-state i18n string renders.
       - Click Close button. Assert isOpen flips to false.
       - i18n: render under sv locale, assert title interpolation works.

    4. **Seed extension** (`drizzle/seed.ts`):
       Find the existing demo seed block (Phase 36 added `leadPmPersonId`). Add an idempotent insert of ~15 `actual_entries` rows: pick one person (Anna or whoever already exists), one project (Atlas), span the working days of the current month (use a hardcoded month for determinism — say `2026-04` since today's date is 2026-04-07), with hours roughly matching but not perfectly equal to the planned hours so the drawer renders a non-trivial delta. Use `onConflictDoNothing()` keyed on the unique constraint to stay idempotent. Source = 'manual'. importBatchId = null. Wrap in a try/catch that logs a warning but does NOT fail the seed if the actual_entries table is somehow missing (defensive — the seed should still work on a v4.0-only DB).

    Run `pnpm db:seed` against the dev DB to confirm the rows land.
  </action>
  <verify>
    <automated>pnpm test src/components/drawer/__tests__/PlanVsActualDrawer.test.tsx &amp;&amp; pnpm lint src/components/drawer/ &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    Drawer opens, fetches, renders table, handles empty state, closes on Esc/button; seed contains ≥10 actual_entries for the demo org for the chosen month; lint clean (no hardcoded JSX text); typecheck green.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` green
- `pnpm lint` green (no hardcoded JSX text in cell or drawer; nordic/require-change-log still happy)
- `pnpm test src/components/timeline/ src/components/drawer/ src/features/actuals/__tests__/use-actuals-cell.test.tsx src/messages/` all pass
- `pnpm db:seed` runs cleanly and the demo org has ≥10 actual_entries rows for at least one (person, project, month)
- Manual smoke: import `PlanVsActualCell` into a temporary test page at `/dev/cell-preview` (or the existing dev sandbox if one exists) and confirm the four color states render visually — this is optional and can be deleted before commit; the automated tests are the binding verification.
</verification>

<success_criteria>
- ACT-03: PlanVsActualCell renders planned/actual/delta with green-under / red-over / neutral-on-plan color coding; reusable from a single source file
- ACT-04: PlanVsActualDrawer shows daily plan vs actual breakdown for any (person, project, month) tuple from a seeded demo dataset
- TC-UI-001 (renders cells from a callback) and TC-UI-002 (600ms debounce on edit) covered by tests
- All v5.* string keys present in keys.ts + sv.json + en.json with parity
</success_criteria>

<output>
After completion, create `.planning/phases/37-actuals-layer-services-distribution-plan-vs-actual-cell/37-02-SUMMARY.md`. Include a hand-off note for Phases 40/41/42 listing the exact import path and prop signature of `PlanVsActualCell` and `PlanVsActualDrawer` so persona screens can wire them in without re-deriving the contract.
</output>
