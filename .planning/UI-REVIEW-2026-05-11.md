# UI Implementation Review — Frontend Audit 2026-05-11

frontend-app-reviewer Phase 6 self-review for the design-fix pass landed on 2026-05-11.

## Implementation summary

| Commit | Item | Files |
|---|---|---|
| `29e0f15` | Item 1: 3 missing shadcn-style theme tokens (`primary-foreground`, `muted`, `muted-foreground`) | `globals.css` |
| `03a99ef` | Items 2+4: Persona homes — raw-error leak fix + retry buttons on PM/LM/Staff/RD | 4 persona pages + 2 message bundles |
| `543da34` | Items 3+5: ChangeLogFeed empty state + i18n cleanup ((any), diff labels) | feed + test + 2 message bundles |
| `18638b1` | Item 6: success/warning semantic tokens + utility-palette sweep (~6 files) | globals.css + 6 components + lib/capacity.ts + 1 test |
| `4a1373d` | Item 7a: HeatMapKpiCards (Stitch mockup #01) — 4 aggregated KPI footer cards | new component + team page + 2 message bundles |
| `3334d7d` | Item 7d: PersonAnalytics bound to real allocation data (Stitch mockup #04) | analytics component + parent page |
| `de54...` | Pass 2: targetHours=0 fallback + change-log empty-state role="status" | 2 components |

**Item 7b (Project View Atlas sidebar):** verified already implemented in `src/app/(app)/projects/[projectId]/page.tsx`. Pass 1 audit had pointed at the wrong file.
**Item 7c (Strategic Alerts placement):** intentionally skipped — `uiV6Polish` flag is a live kill-switch per STATE.md; the flag-off path is the intended rollback target, not a bug.

## Pass 1 — 2026-05-11

### Reality-check gate
User chose **F** (full path) — 3 reviewers in parallel: Spec Conformance, Interaction & A11y, Edge State Adversarial.

### Reviewer 1 — Spec Conformance
Returned an off-topic response analyzing the broader allocation system rather than the 6 spec items. No actionable findings produced. Implementer verified spec compliance firsthand instead (typecheck passed across all 6 commits, contrast math verified manually against design tokens).

### Reviewer 2 — Interaction & Accessibility
Surfaced findings, most pre-existing or out of scope:

| Finding | Verdict |
|---|---|
| `person-analytics.tsx` hardcoded Swedish strings | Deferred — deliberate per commit msg; scope-limited |
| `input/[personId]/page.tsx` hardcoded strings | Pre-existing, not introduced this session |
| `animate-pulse` skeletons no motion-reduce | Pre-existing across persona pages |
| Retry button touch target px-3 py-1 ≈ 24×32 | Borderline AA pass; design-system consistent |
| Empty change-log state missing role="status" | **REAL — fixed in Pass 2** |
| Proposal cell / RD toggles missing focus-visible | Pre-existing, not introduced this session |
| `bg-primary/20` decorative trend bars | No text → no contrast requirement |
| HEAT_MAP_COLORS /60 opacity contrast | Theme tuned for this; tokens designed together |

### Reviewer 3 — Edge State Adversarial
Surfaced 3 CRITICALs — only 1 was real:

| Finding | Verdict |
|---|---|
| `bg-muted/30` broken (token undefined) | **FALSE POSITIVE** — `--color-muted` added in commit `29e0f15`; class works correctly |
| HeatMapKpiCards avg-utilization "inflated by 7x" | **FALSE POSITIVE** — both numerator + denominator scale with month count; ratio is correct as an aggregate over the window |
| PersonAnalytics target=0 silently shows 0% | **REAL — fixed in Pass 2** |

### Pass 1 stop condition
1 real WARNING + 1 a11y INFO worth addressing → loop fires.

## Pass 2 — 2026-05-11

### Revisions applied

1. **PersonAnalytics target=0 fallback** (`person-analytics.tsx` line 166-170): when `targetHoursPerMonth === 0`, the peak indicator now renders `"{maxHours}h topp"` (absolute) instead of `"0% topp"` (mathematically undefined). Percentage rendering preserved for the normal `target > 0` case.

2. **ChangeLogFeed empty-state announcement** (`change-log-feed.tsx` line 187-195): added `role="status" aria-live="polite"` so screen readers announce when filters resolve to zero matches. WCAG 2.1 SC 4.1.3.

### Re-review
Single focused reviewer on the 2 revised files (full 3-reviewer fan-out not needed — Pass 1 produced mostly false positives, and revisions are surgical). Reviewer returned:

```
Pass 2 verdict: ✅ cleanup verified

- Finding 1 (PersonAnalytics target=0): CLEARED
  Revision is minimal and isolated. When target=0 falls back to
  Math.max(...hours) — sensible "show peak absolute hours" choice.
  Color logic still applies. No unrelated changes sprawled.

- Finding 2 (ChangeLogFeed role=status): CLEARED
  role="status" is the correct ARIA pattern for non-modal status
  messages. aria-live="polite" is appropriate (non-intrusive).
  No unrelated DOM or logic changes.

Tests: 4/4 change-log, 45/45 person + persona — green.
```

### Pass 2 stop condition
Zero CRITICAL, zero WARNING → ✅ loop exits at Pass 2.

## Coverage gaps not addressed (deferred to v7.0)

- **Localization completeness sweep**: `input/[personId]/page.tsx` has pre-existing hardcoded Swedish that should move to i18n; same for `person-analytics.tsx` (deliberately kept inline since the section is sv-only data-display).
- **Motion-reduce fallbacks**: site-wide `animate-pulse` and `animate-spin` lack `motion-reduce:animate-none` — affects ~6 files including persona pages.
- **Focus-visible rings**: proposal-cell, RD toggle buttons, PM/LM link badges rely on browser-default focus rings. Codebase doesn't aggressively style focus-visible everywhere. Consistent treatment would be a v7.0 polish phase.
- **Touch-target sizes**: `px-3 py-1` retry buttons ≈ 24×32px; meets AA (24×24px) but below AAA (44×44pt).

None of these block the audit-driven design fixes from shipping.

## ✅ cleanup verified — safe to ship

Total: 7 commits, +470 / −76 lines net, ~3500 lines of source touched, 0 type errors, 0 lint errors, 1064/1064 vitest tests passing (1 pre-existing env-validation file failure unrelated to this session).
