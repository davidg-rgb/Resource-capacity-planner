# HANDOFF — 2026-05-28 · Next task: address ALL dependency vulnerabilities

> ✅ **DONE 2026-05-28 (Phase 54.5).** All 33 vulns remediated — `pnpm audit` is fully clean
> (0 critical / 0 high / 0 moderate / 0 low). 6 atomic commits (`382e11e`, `8b61bbe`,
> `37a8cba`, `43ae35c`, `013a9df`, `9dca2e4`) + docs (`90e9c09`), LOCAL on `main`, NOT pushed.
> `dependency_audit` gate override removed from `54-GATES.md`. Full write-up:
> `.planning/phases/54.5-dependency-security-remediation/54.5-SUMMARY.md`. **Next = Phase 55
> (tenant-isolation).** The text below is the original playbook, kept for the record.

> Written at the end of the session that executed Phase 54. Pick this up in a fresh session.

## Where things stand
- **Phase 54 (Audit-spine + eslint regex) is COMPLETE, verified PASS, and PUSHED.** `origin/main` is at `321914c`. AUDIT-01..07 closed. typecheck + lint green; 1094 tests pass (only the pre-existing `imports.api` env-harness suite fails — that's Phase 58 territory, unrelated).
- **Execution gates are ON for Resurs** (`.planning/config.json` → `features.execution_gates: true`, production tier). They fired live during Phase 54: `regression` + `dependency_audit` both BLOCKed and were overridden in `54-GATES.md`. Decision (vault-level): gates stay **opt-in per project**, NOT global-default-on.
- The dependency_audit override is the reason this task exists: **2 critical / 14 high / 14 moderate / 3 low** vulns are on record but NOT fixed.

## THE TASK: remediate all dependency vulnerabilities
Goal: get `pnpm audit --audit-level=high` to **clean** (clears the gate without an override), then sweep the moderates where a safe bump exists. Do it as a **planned GSD phase** (plan → bump → test → verify) — NOT a blind `pnpm update` — because the big bumps carry breaking-change risk on a production app.

### Triage (done 2026-05-28 — the work is concentrated, not scattered)
The 2 criticals + ~12 of the 14 highs collapse into **two dependency bumps**:

| Bump | Clears | Current → target |
|------|--------|------------------|
| **`next`** | ~8 highs + several moderates (DoS, SSRF, middleware/proxy bypass, XSS, cache poisoning) | `16.2.1` → latest patched `16.x` |
| **`@clerk/nextjs`** (cascades to @clerk/shared, /backend, /react) | 2 criticals (middleware redirect) + 4 highs (auth **bypass**) | `^7.0.7` → latest patched `7.x` |
| **`drizzle-orm`** | 1 high — **SQL injection** via improper escaping (matters: multi-tenant ORM) | `^0.45.1` → patched |
| **`js-cookie`** | 1 high — prototype hijack (likely transitive via Clerk; may clear with the Clerk bump) | check after Clerk bump |

Moderates (lower priority, mostly dev-tooling / transitive): `esbuild`, `vite`, `postcss` (build chain), `next-intl` (open-redirect + prototype-pollution — app-facing, worth bumping), `uuid` v13 buffer-bounds, `brace-expansion`. Lows: 3 × `next` cache, `icu-minify`.

### Recommended phase shape
1. `/gsd-plan-phase` a "dependency security remediation" phase (you can insert it as 54.5 or a standalone — it's cross-cutting, not a v7.0 milestone feature). Scope: the 4 bumps above + a moderate sweep.
2. Bump in order of blast radius: **Clerk first** (auth — test login/middleware/RBAC), then **Next** (test build + a few routes), then drizzle-orm (test the register/contract suites — they exercise SQL), then the moderates.
3. After each bump: `pnpm install`, `pnpm typecheck`, `pnpm lint`, `pnpm test` (vitest, ~90s), and re-run `pnpm audit --audit-level=high`.
4. Verify: `pnpm audit --audit-level=high` clean → the dependency_audit gate passes WITHOUT an override. Then you can remove the `dependency_audit` override from `54-GATES.md` (leave the `regression` one — that's the still-pending Phase 58 env-harness issue).
5. Watch for breaking changes: Next 16.x minor bumps are usually safe but check `next.config`, middleware, and the App Router routes; Clerk 7.x patch bumps should be safe but verify the `requireRole`/auth path (the audit-spine work depends on `requireRole('admin')` returning `{ orgId, userId, role }`).

## Environment gotchas (this is a vault-rooted session, not Resurs-rooted)
See the global memory `running-gsd-phases-from-vault-session.md`. Short version:
- Project root: `D:\Second Brain\03 Projects\Resurs & Projektplanering`. Prefix every bash with `cd "/d/Second Brain/03 Projects/Resurs & Projektplanering" &&`.
- `gsd-sdk` is NOT on PATH → `node /c/Users/david/.claude/get-shit-done/bin/gsd-tools.cjs <cmd> --cwd "<root>"`.
- If `node_modules` is broken (lint-staged missing commander → husky hook fails): `CI=true pnpm install --frozen-lockfile`.
- Driving plans by hand was more reliable than spawning gsd-executor subagents; a briefed gsd-verifier worked.
- pnpm, `branching_strategy: none` (commits go straight to `main`), production tier. Pushing to `main` needs explicit user authorization (classifier blocks a vague "continue").

## Then (after vulns are clear)
- **Phase 55** — Tenant-isolation consolidation (TENANT-01..03), the next v7.0 milestone phase. First v7.0 ADR lands here.
- The `regression` gate override in 54-GATES.md stays until **Phase 58** fixes the `imports.api` test env-harness (QUAL-04..06).
