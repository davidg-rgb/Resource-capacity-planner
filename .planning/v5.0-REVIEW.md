# v5.0 Architecture Conformance Review — Iteration 1

**Date:** 2026-04-10
**Method:** 8 parallel review agents, each owning one architecture domain.
**Total findings:** 77 across 8 areas.

## Triage — Real Blockers (fix now)

| # | ID | Area | Severity | Evidence |
|---|----|------|----------|----------|
| 1 | **B1** | Data model + Proposal | **Bug — Missing impl** | `register.service.updateRegisterRow` does NOT sync `allocation_proposals.target_department_id` when person.departmentId changes. Flagged by BOTH data-model agent (GAP-001) and proposal agent (GAP-003). Arch §7.1 line 1150 requires it. TC-REG-010 will fail. Will orphan proposals in wrong dept queue. |
| 2 | **B2** | Proposal | **Missing — PROP-06** | `editProposal` service function does not exist. Arch §6 line 661. |
| 3 | **B3** | Proposal | **Missing — TC-API-013** | `PATCH /api/v5/proposals/[id]` endpoint does not exist. Arch §8 line 1338. |
| 4 | **B4** | Admin/API | **Bug — enum** | `change_log.action` enum may be missing `REGISTER_ROW_CREATED/UPDATED/DELETED`. register.service writes them, but enum membership unverified. |
| 5 | **B5** | Foundations | **Bug — eslint glob** | `nordic/require-change-log` rule glob is `src/features/change-log/**/*.service.ts` — too narrow. Won't catch missing recordChange in admin/proposal/actuals services. ADR-003 enforcement gap. |
| 6 | **B6** | Excel import | **Bug — TC-MOBILE-001** | `ImportWizard` has no `<DesktopOnlyScreen>` wrapper. Test will fail. |

## Yellow — Documentation/Drift (defer or batch-fix)

- CSV parser missing (committed in arch §382, only Excel shipped) — confirm deferred or remove from arch
- Admin register E2E spec missing (`e2e/admin/register-crud.spec.ts`)
- ARCH §7.1:1142 says `proposed_hours integer`, code is `numeric(5,2)` — update arch
- Route name `/line-manager/approval-queue` vs arch `/line-manager/approvals` — pick one
- ARCH-specified missing component dirs: `components/approval/`, `components/dialogs/` (logic exists, just not extracted)
- Many naming/file-location drifts in persona views, ISO calendar API surface (function names) — cosmetic
- Test contract manifest dirty (`tc-manifest.json`) — regenerate

## Green — Intentional/Acceptable

- No RLS policies (per ADR-004, application-layer enforcement only)
- No notification subscriber (deferred per arch §1762)
- 3 deferred API endpoints (`/api/v5/actuals`, `/api/v5/actuals/daily`, bulk-copy) — Phase 44+
- Persona view file consolidation (e.g., `allocation.service` vs `planning.service`) — refactoring decisions

## Plan
1. Fix B1-B6 in order (atomic commits per fix).
2. Re-run only the affected review agents (proposal, data model, admin/api, excel import, foundations).
3. Sweep yellow findings in second pass if iteration budget remains.
