# v6.0 Session Handoff

**Written:** 2026-04-15 (late evening)
**Last session:** Phase 48 shipped + Phase 49 planned (execution pending)
**Resume command:** `/gsd-discuss-phase 49` is already done — go straight to the blocker fix then `/gsd-execute-phase 49`

---

## First 60 Seconds

1. Read this file (you're doing it)
2. Read `.planning/STATE.md` for latest position
3. Fix the ONE blocker in `.planning/phases/49-unbreak-broken-persona-surfaces/49-RESEARCH.md:781` (see §Resume Point below — ~2 min fix)
4. Address 2 warnings (~5 min), then: `/gsd-execute-phase 49`

---

## Where We Are

### ✅ Phase 48 — Pre-flight verification — COMPLETE (shipped this session)

- 9 VERIFY-0N checks captured in `.planning/pre-flight-report.md` with raw command output
- Reviewer-agent APPROVED
- 4 scope expansions propagated atomically to ROADMAP + REQUIREMENTS (UNBREAK-08/09, LEAN-11, LM-03)
- VERIFICATION.md: 4/4 must-haves passed
- Zero source-code changes (scope guard held)

### 🟡 Phase 49 — Unbreak broken persona surfaces — PLANNED, BLOCKED ON ONE DOC FIX

All pre-execution artifacts exist:
- `49-CONTEXT.md` — 6 decisions + guiding principle (personas = testing/walk-through lens)
- `49-RESEARCH.md` — 5 findings, 2 of which reshaped the plan (see "Critical Research Insights" below)
- `49-VALIDATION.md` — Nyquist test map, `wave_0_complete: false`
- `49-01-PLAN.md` through `49-04-PLAN.md` — 4 plans, 2 waves, all committed

Plan-checker ran: **1 BLOCKER + 3 warnings**. Execution cannot start until the blocker is fixed.

---

## Resume Point (exact actions for next session)

### STEP 1 — Fix the blocker (~2 min)

**File:** `.planning/phases/49-unbreak-broken-persona-surfaces/49-RESEARCH.md`
**Problem:** Section at line 781 `## Open Questions` is missing the `(RESOLVED)` suffix required by the plan-checker's Dimension 11. The three questions were answered by the plans themselves, but the heading didn't get updated.

**Fix:** Rename the heading to `## Open Questions (RESOLVED)` and append a `**RESOLVED:**` line under each question:

- Q1 (`pnpm db:migrate` script name) → **RESOLVED** in `49-03-PLAN.md` — `pnpm db:migrate` confirmed in `package.json:23`
- Q2 (production Neon branch migration scope) → **RESOLVED** in `49-03-PLAN.md` Task 5 — deploy checklist (`PROD-NEON-MIGRATION-CHECKLIST.md`), not a Phase 49 task
- Q3 (UNBREAK-03 UX for "no PM selected" vs "no projects") → **RESOLVED** in `49-02-PLAN.md` — collapsed to the same empty-state branch, per research recommendation

Commit: `docs(49): resolve research open questions`

### STEP 2 — Address 2 warnings (~5 min)

**Warning A (context_compliance) — CONTEXT D-02 partial implementation in `49-01-PLAN.md` Task 2:**

Plan renders a hint `<span>` when 0 departments, but D-02 also requires disabling the LM `<option>` in the kind `<select>`. Add a 2-line step: set `disabled={departments.length === 0}` on the `v5.persona.kind.line-manager` `<option>`, and update Task 2 Test 3 to assert `disabled` on that option. Edit `49-01-PLAN.md` Task 2 `<action>` and `<acceptance_criteria>`.

**Warning B (Nyquist Wave 0) — VALIDATION.md Wave 0 gaps unresolved:**

Two test files are assumed to exist but never verified:
- `src/features/personas/__tests__/persona-route-guard.test.tsx`
- `src/components/persona/__tests__/persona-switcher.test.tsx`

Easiest fix: run `ls` on both and, if present, update `49-VALIDATION.md` to `wave_0_complete: true` + `nyquist_compliant: true` and check the three Wave 0 boxes. If either file is missing, add a "verify file exists or create stub" step at the top of the relevant plan task.

**Warning C (Playwright latency) — INFO only, no action required.** Plan 49-04 runs the full Playwright suite per task; acceptable for an E2E-sweep plan.

Commit: `docs(49): address plan-checker warnings — D-02 disable LM option + Wave 0 gap closure`

### STEP 3 — Execute

```
/gsd-execute-phase 49
```

Expected shape:
- **Wave 1** runs 49-01, 49-02, 49-03 in parallel (disjoint `files_modified`)
- **Checkpoint in 49-03 Task 4** — live browser smoke test via `claude-in-chrome` MCP against a signed-in admin session. User inspects the 5 admin pages, types "approved" to continue.
- **Wave 2** runs 49-04 (Playwright spec sweep of 12 specs)

Estimated duration: 45–90 min wall-clock (checkpoint adds human time)

---

## Critical Research Insights (memorize before executing)

Two findings in `49-RESEARCH.md` CHANGED the plan shape. Don't revert to the CONTEXT.md framing when reading plans:

1. **UNBREAK-04/05 admin 500 is ENVIRONMENTAL, not a code bug.** Root cause: Neon dev branch is missing migrations 0005–0008. Error `NeonDbError: column "archived_at" does not exist (42703)`. Fix: `pnpm db:migrate` — NO application code change. Blast radius is wider than VERIFY-04 found: `/admin`, `/admin/people`, `/admin/departments`, `/admin/disciplines`, `/admin/programs` all break. 62+ errors captured in `.next/dev/logs/next-development.log`.

2. **UNBREAK-03 is a guard-ORDER bug**, not a missing empty-state branch. `pm/page.tsx:60-66` already renders `{tScreens('empty')}`. The real bug: `!personaId` keeps `useQuery` disabled, so `isLoading` stays `true` forever. Fix: reorder guards so `!personaId` falls through to the existing empty-state branch.

3. **Department picker is 90% wired already.** `/api/departments` exists; `usePersona().departments` already exposes the list. The picker is a ~30-line inline addition to `persona-switcher.tsx` (scaffold at lines 55-58). Do NOT build a standalone `DepartmentPicker` component.

4. **PersonaGate transform is a no-op.** `PersonaKind` is already hyphenated (`'line-manager'`) in `persona.types.ts:8`. CONTEXT D-01's `lineManager → line-manager` transform is unnecessary. The real bug: wrong namespace (`v5.lineManager` instead of `v5.persona`) + hardcoded "linjechefs-personan" in `sv.json:437`. ~10-line diff.

5. **`change_log` table possibly missing from Neon despite migration 0003** — research Pitfall 5. Verify migration 0003 uses `CREATE TABLE IF NOT EXISTS` before running `pnpm db:migrate`; if not, a psql-only patch is needed first. Plan 49-03 Task 2 handles this.

---

## Plan 49 Summary (for quick scan)

| Plan | Wave | Tasks | UNBREAK IDs | Files Touched |
|------|:---:|:---:|---|---|
| 49-01 | 1 | 3 | 01, 02, 06, 08, 09 | `persona-switcher.tsx`, `persona-route-guard.ts`, 2 LM pages, `sv.json`, `en.json`, 2 test files |
| 49-02 | 1 | 1 | 03 | `pm/page.tsx`, `pm-home.test.tsx` |
| 49-03 | 1 | 5 (incl. 1 checkpoint) | 04, 05 | Evidence + checklist markdown only (environmental fix) |
| 49-04 | 2 | 3 | 07 | 12 Playwright specs |

Wave 1 is parallel-safe (disjoint `files_modified`). Wave 2 depends on Wave 1.

---

## Session Housekeeping

- **22 commits ahead of origin/main** — user may want to `git push` before next session. NOT pushed automatically.
- **No uncommitted changes in working tree.** Clean.
- **Worktrees cleaned up** after Phase 48 waves merged.
- **Chrome extension required for Phase 49 Task 49-03/Task 4** — ensure `claude-in-chrome` MCP is connected to a signed-in Clerk admin session in Chrome before resuming.
- **Dev database state:** Neon dev branch is missing migrations 0005–0008. This is EXPECTED — fixing it is the job of Phase 49 Task 49-03. Do not pre-run `pnpm db:migrate` manually.

---

## What NOT to do next session

- Don't re-plan Phase 49 — plans exist and are sound modulo the blocker fix
- Don't run `pnpm db:migrate` manually before starting the phase — Task 49-03/2 captures the pre-migration Neon snapshot first (rollback path)
- Don't edit source code until the blocker is resolved and `/gsd-execute-phase 49` spawns executors
- Don't skip the 49-03 Task 4 checkpoint — it's a human-verify gate for live admin page smoke testing. Approving without looking risks shipping regressed admin pages.
- Don't push without reviewing the 22 commits — they cover Phase 48 end-to-end plus Phase 49 planning; the user may want to tag `v5.0-pre-ui-restructure` before pushing (per `UI-RESTRUCTURE-PLAN-v2.md §8 Rollback Matrix`).

---

## Resume Command

```
# 1. Fix the blocker (~2 min) — edit 49-RESEARCH.md line 781 per STEP 1 above
# 2. Fix 2 warnings (~5 min) — per STEP 2 above
# 3. Commit the docs fixes
# 4. Execute:
/gsd-execute-phase 49
```

Or if you want the full fix-and-execute chain autonomous: after docs fixes are committed, `/gsd-execute-phase 49 --auto` will run waves 1+2 without prompting except at the one human-verify checkpoint in 49-03/4.
