# Next-session handoff — 2026-05-01

**Created:** end of 2026-04-30 session
**For:** next session (probably 2026-05-01)
**Status:** Dry-run on Neon branch is GREEN. Prod migrations NOT applied yet. Resume at Step 8.

This supersedes `HANDOFF-NEON-MCP-SETUP.md` Steps 1–7 (done) and `HANDOFF-2026-04-29.md` Neon-MCP block.

---

## TL;DR for the next session opener

> "Read `.planning/HANDOFF-2026-05-01.md`. Dry-run is green on `audit-r4-dry-run-2026-04-29` (`br-spring-dawn-al9oyp0x`). Apply the same 4 SQL blocks to the prod branch `production` (`br-gentle-poetry-alfgjqb1`), verify, then close out the audit trail."

---

## What is done (don't redo)

- Neon MCP wired up and verified (`mcp__neon__*` tools available, project `round-block-03109810`).
- Dry-run branch `audit-r4-dry-run-2026-04-29` (id `br-spring-dawn-al9oyp0x`) created from prod.
- All 4 SQL blocks from `runbooks/SESSION-2026-04-28-prod-migrations.md` ran cleanly on the dry-run branch.
- Idempotence proven (second pass = 0 changes; audit counts still 0; CHECK still present).
- Prod branch (`br-gentle-poetry-alfgjqb1`, named **`production`**) was NOT touched.

### Dry-run result table

| # | Migration | Pre | Post | Re-run | Status |
|---|---|---:|---:|---:|---|
| 1 | discipline rename | 1 | 0 | 0 | ✅ |
| 2 | strip widgets (`bench-report`/`strategic-alerts`) | 1 | 0 | 0 | ✅ |
| 3 | strip `resource-conflicts` | 0 | 0 | 0 | ✅ (no-op, already clean) |
| 4 | hours CHECK constraint | 0 violations | constraint present | guard short-circuits | ✅ |

Constraint definition verified on dry-run: `CHECK (((hours >= 0) AND (hours <= 744)))`.

---

## What is left

### Step 8 — Apply to prod branch `br-gentle-poetry-alfgjqb1`

Run the same 4 SQL blocks from `.planning/runbooks/SESSION-2026-04-28-prod-migrations.md` against prod via Neon MCP. Same verification queries. Capture result table. Expected behavior on prod is identical to the dry-run table above (since the dry-run was a copy-on-write of prod taken on 2026-04-29, and prod has had no writes since — confirm before assuming).

**Pre-flight reminder before applying migration 4:** re-check `SELECT COUNT(*) FROM allocations WHERE hours < 0 OR hours > 744;` on prod must be 0. If non-zero (someone wrote violating data between 2026-04-29 and the prod run), STOP and investigate.

**MCP tool call shape (already loaded if you're continuing this session, otherwise `ToolSearch` for `select:mcp__neon__run_sql,mcp__neon__run_sql_transaction`):**

```
mcp__neon__run_sql({
  projectId: "round-block-03109810",
  branchId:  "br-gentle-poetry-alfgjqb1",  // prod
  sql: "<one of the 4 SQL blocks>"
})
```

### Step 9 — Close out the audit trail

After all 4 prod migrations apply cleanly:

1. `.planning/phases/53-chrome-polish/53-HUMAN-UAT.md` Test 3 → status `pass`, add operator + date.
2. `.planning/STATE.md` Deferred Items table → drop the prod-DB-rowcount / Test 3 row.
3. `.planning/MILESTONES.md` v6.0 entry → add one-line note: "Test 3 closure 2026-05-01" (or whatever the actual date is).
4. Delete the dry-run branch: `mcp__neon__delete_branch({ projectId: "round-block-03109810", branchId: "br-spring-dawn-al9oyp0x" })`. Don't delete it before prod is green — it's free-tier cheap and useful as a comparison point if prod misbehaves.
5. Commit the doc updates with a single message like `docs: close out v6.0 Test 3 — prod migrations applied 2026-05-01`.

### Step 10 — Recurring audit (option 3 from `HANDOFF-2026-04-28.md`)

Once Test 3 is closed, the recurring 2-week audit can be set up. The schema-drift Round-0 check the original handoff describes is now feasible — Neon MCP supports `mcp__neon__describe_branch` (full schema tree) and `mcp__neon__describe_table_schema` (per-table). Don't auto-trigger this; offer it to the user when closing out Step 9.

---

## Key IDs (so the next session doesn't re-discover them)

| | |
|---|---|
| Neon project | `round-block-03109810` (name: `Resource-planner`) |
| Neon org | `org-proud-cloud-19622208` |
| Prod branch ID | `br-gentle-poetry-alfgjqb1` |
| Prod branch name | **`production`** (NOT `main` — handoff doc nit, see below) |
| Dry-run branch ID | `br-spring-dawn-al9oyp0x` |
| Dry-run branch name | `audit-r4-dry-run-2026-04-29` |
| Default DB | `neondb` |
| Postgres version | 17.8 |

---

## Doc nits to fix when closing out

- `HANDOFF-NEON-MCP-SETUP.md` Step 7 says `parent: "main"` — actual prod branch is named `production`. The MCP `create_branch` tool also doesn't expose a parent param (it always forks the project default), so the example is doubly wrong. Either delete the example or change to `parent: "production"` and note that the MCP picks the default automatically.
- After Step 9, the SETUP handoff plus this one are both complete. Move both into `.planning/archive/` (or whatever convention the project uses) so the top-level handoff list stays current.

---

## What I still won't touch automatically next session

(Carried forward from `HANDOFF-NEON-MCP-SETUP.md` — these are self-imposed limits, push back if I drift.)

- Drop tables / drop columns / destructive ALTER without per-command confirmation
- Change prod branch's connection string
- Create or delete Neon projects (only branches)
- Delete the dry-run branch before prod is verified green

---

## Other open items (not blocking the prod migration session)

- **E2E rehab still pending.** CI e2e job disabled 2026-04-28 (`a60b493`). Latent bug stack: drizzle driver fixed, Clerk key + others remaining. Separate session.
- **Memory record** `reference_neon_project.md` already has project ID + org. After Step 9, consider adding the branch IDs there too — they're stable and useful for the recurring audit.

---

## Recommended opener for the next session

> "Read `.planning/HANDOFF-2026-05-01.md` and execute Step 8 (apply 4 migrations to prod branch `br-gentle-poetry-alfgjqb1`), then Step 9 close-out. Dry-run is green; idempotence proven. Confirm hours_violations on prod is 0 before migration 4."
